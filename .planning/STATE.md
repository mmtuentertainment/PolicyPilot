---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: phase_6_shipped
last_updated: "2026-06-05T17:42:00Z"
progress:
  total_phases: 8
  completed_phases: 6
  total_plans: 57
  completed_plans: 57
  percent: 75
---

# STATE — PolicyPilot

GSD session state. Updated each time a phase or plan transitions. Source of truth for "where are we right now".

---

## Project Reference

- **Project**: PolicyPilot — AI-powered policy & procedure management SaaS
- **Operator**: Matthew (MMTU Entertainment LLC) — `mmtuentertainment@gmail.com`
- **Core value**: Replaces Google Drive / SharePoint for SMB policy management with AI drafting, append-only acknowledgment tracking, and audit-ready compliance trails — at a price an SMB can afford.
- **Beat-manual gate**: Product must be demonstrably faster and more reliable than a Google Drive folder.
- **Current focus**: `main` is at `1122da5` after PR #42 (`feat(phase-9): Reviewer / approval-workflow MVP — close the publish-leak + R-017 (D-09-01)`) shipped on 2026-06-05. Phase 6 Billing remains the last shipped locked assembly phase via PR #32 squash `243067e9f259561a595230e5e7d3e97634040157`; Phases 7 and 8 remain pending. Phase 9 Reviewer / approval-workflow MVP is an out-of-band R-017 mitigation now live on `main`: Growth+ `approvalWorkflows` is enforced at the true publish boundary inside `publish()` (covering `approve()`), Starter remains direct-publish by design, ADR-030 ratifies `(reviewer)` as a sanctioned route group, and the shared org reviewer queue is the ratified MVP. PR #41 was closed as superseded by PR #42. Phase 7 has not started and requires Matthew's explicit next-phase authorization.
- **Granularity**: standard (8 phases)

---

## Current Position

Phase: Phase 6 Billing remains SHIPPED on `main` via PR #32 squash commit `243067e9f259561a595230e5e7d3e97634040157`; out-of-band Phase 9 Reviewer / approval-workflow MVP shipped via PR #42 at `1122da5` and mitigates R-017 live on `main`.
Plan: 6 / 6 plans executed + committed; Phase 7 has not started and requires Matthew's explicit next-phase authorization
Branch: `main` — last runtime/feature commit `1122da5` (PR #42 merge); the literal current `git` HEAD is the docs state-reconcile commit on top. Local `main` and `origin/main` match. The remote `gsd/phase-9-reviewer` branch was deleted after merge; local `gsd/phase-6-billing` has already been deleted.
Main HEAD: last runtime/feature commit `1122da5` (the literal current `git` HEAD is this docs state-reconcile commit on top). Phase 6 remains the last shipped locked assembly phase at PR #32 squash `243067e` (`Phase 6: Billing`). Shipped since Phase 6: PRs #33–#40 operating/tooling/prep commits, then PR #42 Phase 9 Reviewer / approval-workflow MVP at `1122da5`; PR #41 was closed as superseded by #42.

- **Phase 1** — Foundation **complete** (2026-05-16; 5/5 plans; PR #1 merged)
- **Phase 2** — Data Layer **complete** (2026-05-18; 7/7 plans = 6 main + 02-07 hotfix; verify:phase-2 8/8 OK; PR #2 squash-merged to `main` @ `130b8ab` on 2026-05-19)
- **Phase 3** — Admin UI **SHIPPED** (2026-05-20; 15/15 plans; PR #3 squash-merged to `main` @ `edebab7`; 3 fast-follows shipped PR #5/#7/#13; 3-audit cascade complete PR #8/#10/#11; closure test PR #9; state bookkeeping PR #12)
- **Phase 4** — AI Layer **SHIPPED** (2026-05-22; 14/14 plans; PR #15 squash-merged to `main` @ `f8207f4`; post-merge cleanup PR #17 @ `eda7bb4`; 60/60 STRIDE threats CLOSED; UAT 5/5 PASS with real-key Anthropic smoke verifying SPEC R4 cache mechanics)
- **Phase 5** — Employee Portal **SHIPPED** (2026-05-27; PR #27 "Phase 5: Employee Portal" merged to `main` at `3344847`, parent `c50b317`; `gsd/phase-5-employee-portal` branch is gone).
- **Phase 6** — Billing **SHIPPED** (2026-05-31; PR #32 squash-merged to `main` at `243067e`; 06-01..06-06 committed; local `pnpm db:verify` and pre-merge `pnpm verify:phase-6` green; live Stripe test-mode UAT 11/11 rows PASS; hosted pre-merge PR #32 checks green/acceptable at `1abca44`; post-merge targeted checks PASS).
- **Phase 9** — Reviewer / approval-workflow MVP **SHIPPED out-of-band** (2026-06-05; PR #42 squash-merged to `main` at `1122da5`; closes R-017 with Growth+ approval-completeness enforcement in `publish()`, ADR-030 `(reviewer)` route-group ratification, and shared org reviewer queue MVP).
- **Progress**: 6 / 8 phases shipped (75%); Phases 7-8 not started.

```text
[######--] 6/8 phases shipped - Foundation done / Data Layer done / Admin UI done / AI Layer done / Employee Portal shipped / Billing shipped
```

**Next action**: Matthew may authorize Phase 7 planning next. Do not start Phase 7 until that explicit authorization. Phase 9 does not change the locked Phase 7/8 sequence. Future live Stripe UAT must use the app test-account CLI override or a CLI profile logged into the intended test account; the default CLI profile remains mismatched. Migration note: `0012_billing_state` is applied and verified on the approved TEST/dev target only; staging/prod remain operator-gated by the migration discipline. CI Phase 6 verification mutates only the approved dev/test target through TRUNCATE/seed. SF-WHSEC-1 remains an operator follow-up before any future live webhook smoke if the current `CLERK_WEBHOOK_SECRET` was used before rotation.

**Fast-follow PR backlog** (3 sequential PRs, originally scoped from PR #3 carry-forward):

- **PR 3.1 — ADR-026 typed error classes** — **SHIPPED 2026-05-20 @ `bf65712`** (PR #5). `BootstrapError` abstract base + 5 concrete subclasses + `ProvisioningRaceError` abstract base + `ClerkAuthFailedError` (intentionally NOT a `BootstrapError`). Consumers narrow via `err instanceof Class` with class-constructor allow-lists; v0 substring matcher removed. `BootstrapError.code` is a literal union (`BootstrapErrorCode`) catching typos at compile time. New `scripts/check-error-discipline.ts` ts-morph gate forbids `throw new Error(...)` (and 7 other built-in Error subclasses, with-or-without `new`) in `lib/auth/**.ts(x)` — wired into `verify:phase-3`. 21 vitest cases in `bootstrap-errors.test.ts` lock the hierarchy contracts (including positive `instanceof BootstrapError` × 5, empty-array fail-CLOSED, code-uniqueness, the divergence-lock between dashboard race-recovery + trampoline hard-fail). Review loop: 1 CR cycle (APPROVED + 1 outside-diff finding deferred to PR 3.2) + 1 5-agent pr-review-toolkit cycle (3 v2 polish commits closing ~10 findings) + 1 CR inline finding (closed in 1 commit).

- **PR 3.2 — ADR-027 lookup-scoping defense-in-depth** — **SHIPPED 2026-05-21 @ `c6dca6a`** (PR #7). Sequential lookup with `eq(users.orgId, orgRow.id)` added to `lib/auth/context.ts:126-145`; `and(eq(users.clerkUserId, ...), eq(users.orgId, orgRow.id))` combinator + drizzle-orm `and` import + 3 ts-morph-grade greppable assertions in `scripts/check-artifacts.ts`. ADR-027 ratified — multi-org Clerk lockout documented as product behavior (not bug) per webhook handler `organizationMembership.created` unconditional `users.org_id` overwrite at `app/api/webhooks/clerk/route.ts:296-303`. Review loop: 1 CR APPROVED on `188f6b7`; v2 polish `e32173b` drew CR re-review (`CHANGES_REQUESTED` for comment-only false-positive vulnerability in assertion gate); closure commit `fb2b512` routes the 3 ADR-027 assertions through `ctxNoComments` (CR's exact suggested diff); CR final APPROVED → squash-merged. 2 deferred MEDIUM findings from PR #7's 4-agent pre-merge review carry into PR 3.3 scope decision: `UserNotProvisionedError` subCode discriminant + `orgRow.id` exposure in error message — operator to decide bundle-vs-split-as-ADR-029.

- **PR 3.3 — ADR-028 PolicyId branded type** — pending. Brand `PolicyIdSchema = z.string().uuid().brand<'PolicyId'>()`; thread `PolicyId` type through `policyIdFrom` return, `Policies` repository method signatures, `lib/policies/transitions.ts` orchestrators. ts-morph verify gate forbidding bare `string` in repository policyId params. Slippery-slope policy in ADR: do NOT brand `UserId`/`OrgId` yet; opportunistic adoption.

**Carry-forward queue** (deferred to later phases; all gaps surfaced in Phase 3 UAT are CLOSED in this PR — see 03-G3 SUMMARY):

- **SF-CASCADE-AUDIT** → Phase 6+ obligation when org-delete code path lands. The 0003_fk_hardening cascade currently wipes acknowledgments + ai_generations on tenant offboarding with no app-level audit-event emission. When tenant lifecycle UI ships, the delete handler must log row counts + emit a structured audit event BEFORE the cascade fires (per ADR-018 append-only).
- **Tenant-lifecycle cleanup** → Phase 6+: delete the orphan `MMTU Entertainment` (Title Case) org from the days-old smoke retry; consolidate the case-only duplicate-name org pair. Diagnosed at `.planning/debug/org-topology-uat5.md`.
- **Phase 7+ webhook hardening** → invert idempotency-before-dispatch ordering OR add explicit alerting on stuck `clerk_events` rows. 03-G3 T7 ships the application-layer interim fix (delete `clerk_events` row before non-2xx return).
- **Phase 7+ webhook test coverage** → vitest scaffold for the webhook handler 409/catch paths. T8 deferred from 03-G3; production code verified live via 2 independent paths during UAT-4 + UAT-6.
- **Phase 5** replaces the 03-G3 T9 `/my-policies` stub with the real employee-acknowledgment portal.
- **Nyquist G-08a / G-09a / G-03a** → Phase 2.1 hardening orthogonal to admin UI. Pick up when convenient.

---

## Performance Metrics

| Metric | Value |
|--------|-------|
| Phases complete | 5 / 8 |
| Phase 1 plans drafted | 5 / 5 |
| Phase 1 plans executed | 5 / 5 |
| Phase 2 context | drafted 2026-05-17 |
| Phase 2 plans drafted | 6 / 6 |
| Phase 2 merged to main | 2026-05-19 (PR #2 squash → `130b8ab`) |
| Phase 3 context | drafted 2026-05-19 (`--all` autonomous; 13 HOW decisions D-01..D-13 + 5 USER-LOCKED constraints L-01..L-05) |
| Phase 4 spec | drafted 2026-05-21 (`/gsd-spec-phase 4`; 7 requirements; ambiguity score 0.109) |
| Phase 4 context | drafted 2026-05-21 (`/gsd-discuss-phase 4 --power`; 27 HOW decisions D-01..D-27 via offline HTML editor) |
| Phase 2 plans executed | 6 / 6 code-complete (02-01 ~7min/3 commits/4 files; 02-02 partial ~unknown/0 source commits + SF-DB-1 closed by operator pre-02-06; 02-03 14min/3 commits/9 files + post-commit Task 4; 02-04 ~4min15s/2 commits/9 files — tsc baseline closed; 02-05 ~9m46s/3 commits/1 created + 3 modified files — SF-M4 fully closed; 02-06 Tasks 1-5 ~17m33s/5 commits/4 created + 4 modified files — verify:phase-2 7/7 OK against live TEST DB; Task 6 operator checkpoint OPEN) |
| Requirements mapped | 17 / 17 |
| Locked decisions | 29 (ADRs 001–029) |
| Phase implementation decisions | Phase 1: 15 (D-01..D-15); Phase 2: 9 (D-01..D-09) + 6 USER-LOCKED (L-01..L-06) |
| Constraints (SPEC) extracted | 28 |
| Acceptance criteria pending | 8 + 1 meta |

---
| Phase 05 P01 | 16min | 4 tasks | 7 files |
| Phase 05 P02 | 4min | 1 task | 2 files (1 created + 1 SUMMARY) |
| Phase 05 P07 | 4min | 1 task | 2 files (1 created + 1 SUMMARY) |
| Phase 05 P03 | 25min | 2 tasks | 5 files (3 modified + 1 created + 1 SUMMARY) |
| Phase 05 P04 | 28min | 3 tasks | 7 files (2 created + 4 modified + 1 SUMMARY) |
| Phase 05 P05 | ~45min | 3 tasks | 9 files (7 created + 1 modified-wholesale + 1 SUMMARY) |
| Phase 05 P06 | ~15min | 2 tasks | 6 files (2 created + 3 modified + 1 SUMMARY) |
| Phase 05 P08 | 15min | 2 tasks | 7 files (2 created + 5 modified + 1 SUMMARY) — H-1 closed, gaps 2/4 closed, +58 artifact assertions |
| Phase 05 P09 | ~12min | 2 tasks | 12 files (9 created + 2 modified + 1 SUMMARY) — H-5/H-6 closed at runtime; 9/9 integration tests + 56 new co-located unit tests pass; verify:phase-4 still 0 |
| Phase 05 P10 | ~3min Task 1 (Task 2 pending operator UAT) | 2 tasks (1 ✓ + 1 human-verify checkpoint) | 2 files (1 modified + 1 SUMMARY) — verify:phase-5 chain D-23 verbatim wired; end-to-end runtime 92s; 4/4 chain steps green |

## Accumulated Context

### Locked decisions (do not re-litigate)

All 21 decisions in `PROJECT.md` `<decisions>` block are LOCKED via ADR-001 through ADR-021. Sourced from `BLUEPRINT.md` (precedence 0) and `reference/STACK.md` (precedence 1). Re-opening any requires a new ADR and operator approval.

Phase 1 added 15 implementation decisions (D-01 to D-15) at `.planning/phases/01-foundation/01-CONTEXT.md`.

Phase 2 added 9 implementation decisions (D-01 to D-09) at `.planning/phases/02-data-layer/02-CONTEXT.md` on top of 6 USER-LOCKED deliverables (L-01..L-06) reflecting ADR-023 + ADR-025. Highlights: two-migration split (schema generate + hand-written RLS+GRANT); `org_id` denormalization onto 5 child tables; 4-event Clerk webhook scope + new `clerk_events` idempotency table; `DIRECT_URL` env-var split for migrations; `@ts-expect-error` type-tests locking the ADR-018/005 invariants.

Plan 02-01 (2026-05-17) execution decisions:

- Kept `PgTransaction<any, any, any>` typing in `lib/db/scoped.ts` (default per CONTEXT specifics; RESEARCH Open Question 2 resolution). `Parameters<typeof db.transaction>[0]` tightening deferred to Phase 8 perf pass.
- Alphabetical table order in `lib/db/schema.ts` (Drizzle thunked references defer evaluation; alphabetical wins on review diffs).
- Stricter `{ role?: unknown }` cast in `lib/auth/context.ts` vs middleware's `{ role?: string }` — Phase 2 carries forward stricter form; middleware companion left to Plan 02-05.
- SF-M4 (try/catch around `await auth()`) closed in `lib/auth/context.ts:25-32`. Middleware companion (`middleware.ts:51, 61`) DEFERRED to Plan 02-05's middleware fold task.

### Todos

- [x] Plan 01-01: Scaffold Next.js 15 + install Phase 1 deps + shadcn + DATABASE_URL — **completed 2026-05-15** (commits 5d2057d, 3b74de5, f58aea7)
- [x] Plan 01-02: Operator creates Clerk dev app + Supabase project + populates .env.local — **completed 2026-05-16** (no source commits — `.env.local` is gitignored; only SUMMARY committed in 72ea3b8)
- [x] Plan 01-03: App shell (ClerkProvider + marketing landing + pricing stub + Clerk auth pages) — **completed 2026-05-16** (commits bd12768, 479b06c, b20a6ff)
- [x] Plan 01-04: middleware.ts + Drizzle skeleton + scripts/check-db.ts — **completed 2026-05-16** (commits 49e2826, 6dcd38a, ca568ce)
- [x] Plan 01-05: scripts/check-foundation.ts + verify:phase-1 implementation + operator human-verify — **completed 2026-05-16** (commit b43ed8c; operator approved 6/6 + 5 visual checks)
- [x] Verify `.env.local.example` is complete before Phase 1 plan execution — **folded D-11**: keys complete except `DATABASE_URL` (added by Plan 01-01)
- [x] Confirm pnpm vs npm package manager preference before Phase 1 init — **folded D-01**: pnpm
- [x] Plan 02-01: Drizzle schema (12 tables) + OrgScope + getOrgContext + D-07 type tests — **completed 2026-05-17** (commits 75b397e, e7c6b43, 2fff189); SF-M4 (try/catch around `await auth()`) closed in `lib/auth/context.ts:25-32`; `tsc --noEmit` intentionally failing on `tests/types.ts` until Plan 02-04 ships repository skeletons
- [~] Plan 02-02: Operator manual config — **PARTIAL completed 2026-05-17** (SUMMARY: `.planning/phases/02-data-layer/02-02-SUMMARY.md`). Done: Clerk Org Roles (3 effective; `org:` prefix to strip in webhook handler), Session Token publicMetadata claim, Svix webhook endpoint with 4 events, `DIRECT_URL` populated (BUT: legacy IPv6-only hostname — see SF-DB-2). Deferred (blocker SF-DB-1): `DATABASE_URL_TEST` + `DIRECT_URL_TEST` until Supabase test project resolved.
- [x] Plan 02-03: Drizzle migrations + drizzle.config DIRECT_URL split + schema push — **FULL completed 2026-05-17** (SUMMARY: `.planning/phases/02-data-layer/02-03-SUMMARY.md`). Tasks 1-3 shipped via commits `c1dcf6f`, `0bbf321`, `f443cd0`. Task 4 closed post-commit after SF-DB-2 1-line fix: `pnpm db:migrate` applied both migrations cleanly; live dev DB verified 12/12 tables + 10/10 RLS-enabled tenant tables + 10/10 org_isolation policies + 40 GRANTs + D-03a CHECK.
- [x] Plan 02-04: 9 repository skeletons under `lib/db/repositories/*.ts` — **FULL completed 2026-05-17** (SUMMARY: `.planning/phases/02-data-layer/02-04-SUMMARY.md`). Commits `2973555` (4 critical: Policies + Acknowledgments + Users + PolicyVersions) and `e71000a` (5 remaining: PolicyAssignments + Departments + AiGenerations + Notifications + WorkflowStages). 351 lines across 9 files. `pnpm tsc --noEmit` exits 0 (~2.7s) — closes Plan 02-01 Task 3 baseline failure; D-07 type tests in `tests/types.ts` now actively enforce ADR-018 + ADR-005. 1 Rule-1 deviation: Acknowledgments header switched from `//` to `/** */` block-comment (TS directive-scanner collision); acceptance substrings preserved.
- [x] Plan 02-05: svix install + Clerk webhook handler + middleware SF-M4 fold — **FULL completed 2026-05-17** (SUMMARY: `.planning/phases/02-data-layer/02-05-SUMMARY.md`). Commits `a9301b2` (svix@1.93.0 install + audit), `6ae44f5` (`app/api/webhooks/clerk/route.ts` — 264 lines: svix verify + req.text()-before-parse Pitfall 4 / clerk_events idempotency D-03b / 4 active events D-03 / 3 delete events log-only D-03c / asAppRole with `org:` prefix stripping / ADR-023 allow-list entry #1), `c39ea98` (middleware.ts SF-M4 fold — 2 try blocks / 2 catch blocks / admin-gate fail-closed to 404 / chokepoint fail-closed to /sign-in redirect). 309 lines / 1 new file + 3 modified. `pnpm tsc --noEmit` exits 0. SF-M4 FULLY CLOSED (both halves landed). 2 Rule-3 deviations (pre-existing esbuild audit from drizzle-kit transitive; plan verify regex window cosmetically tight). End-to-end Clerk webhook smoke deferred to Plan 02-06 operator human-verify.
- [x] Plan 02-06: ts-morph + L-05 check-db-imports + L-06 check-rls + D-08 check-schema + verify:phase-2 wiring — **FULL completed 2026-05-18** (SUMMARY: `.planning/phases/02-data-layer/02-06-SUMMARY.md`). Commits `e160728` (ts-morph + env example), `c31d1c8` (check-db-imports.ts L-05), `a156dc5` (check-rls.ts L-06), `ff82746` (check-schema.ts D-08), `9888cf5` (orchestrator + artifacts + verify:phase-2). `pnpm verify:phase-2` exits 0 with 7/7 OK against live TEST DB (runtime ~22s); all 214 check-artifacts assertions pass. Task 6 (operator human-verify) APPROVED 2026-05-18 — `pnpm verify:phase-2` re-run by operator showed clean 7/7 OK after a transient pooler password-lookup lag on the first run cleared on retry. **End-to-end Clerk webhook live-smoke explicitly deferred to Phase 3** by operator decision (rationale: Phase 3 ships the `<CreateOrganization />` UI; live smoke is higher-fidelity then). Phase 2 ALL 6 PLANS COMPLETE.
- [x] Plan 02-07: Code-review hotfix — CR-01 + HI-01 — **FULL completed 2026-05-18** (SUMMARY: `.planning/phases/02-data-layer/02-07-SUMMARY.md`). Commits `5bdcbf9` (CR-01: webhook handler mirrors `users.role` into Clerk `publicMetadata.role` via `clerkClient().users.updateUserMetadata` for the 3 role-affecting events — `user.created` writes default `employee`, `organizationMembership.created` writes narrowed `roleStr`, `organizationMembership.updated` writes narrowed `roleStr`; wrapped in best-effort try/catch so a Backend-API blip doesn't crash dispatch — D-04 dual-write now satisfied end-to-end) and `13a9a30` (HI-01: `middleware.ts:66` narrowing tightened from `{ role?: string }` to `{ role?: unknown }` + `typeof === 'string'` guard, matching the stricter contract in `lib/auth/context.ts:42` — both auth-read sites now share one shape). `pnpm tsc --noEmit` exits 0; `pnpm verify:phase-2` exits 0 with 7/7 OK. No deviations, no new packages, no `any` types introduced. Closes CR-01 + HI-01 from `.planning/phases/02-data-layer/02-REVIEW.md`.
- [x] Plan 05-03: Repository surfaces (Acknowledgments.record + PolicyAssignments.create + Policies.listAssignedAndPublishedForUser + new QaCitationGrants) — **FULL completed 2026-05-24T01:55Z** (SUMMARY: `.planning/phases/05-employee-portal/05-03-repositories-SUMMARY.md`). Commits `e23a4a4` (Task 1: filled 3 throw-stub bodies — Acknowledgments.record with INSERT + ON CONFLICT DO NOTHING per D-06 + D-10 silent-success + console.log ops signal `[ack] no-op (already acked)` for monitoring unusual duplicate-ack rates; PolicyAssignments.create with same pattern for bulk dept-assign idempotency per D-15; added Policies.listAssignedAndPublishedForUser per D-01..D-04 — single SQL with inline user-dept sub-select per D-03 + INNER JOIN policy_assignments (user OR dept assigneeType) + INNER JOIN policy_versions on currentVersion + 2x LEFT JOIN acknowledgments aliases — current_ack + prior_ack — with CASE projecting 3-state ackState enum + SELECT DISTINCT for user+dept double-assignment dedupe per D-15-permits-both / D-01-collapses-at-query-time; new imports `acknowledgments`/`policyAssignments`/`policyVersions`/`users` schema exports + `alias` from drizzle-orm/pg-core), `b8de7f1` (Task 2: created `lib/db/repositories/qa_citation_grants.ts` 132 lines per D-29 — exports `QaCitationGrants` object with `listForUser`/`upsert`/`hasGrant`; UPSERT idempotent via ON CONFLICT DO NOTHING on UNIQUE(org_id, user_id, policy_id) from migration 0011; `hasGrant` returns Promise<boolean> via `SELECT COUNT(*) ... LIMIT 1` + `Number()` coercion + `> 0` boolean for portable driver behavior, leveraging composite btree `qa_citation_grants_user_policy_idx` for fast D-27 page-handler access decisions; PolicyId branded type preserved on explicit hasGrant signature per ADR-028 while QaCitationGrantUpsertInput keeps `policyId: string` per ADR-028 schema-inferred-insert-out-of-brand-scope slippery-slope policy at lib/policies/types.ts:23-31; module exports NO update/delete keys per D-26 write-once / ADR-018-spirit). ADR-018 type-test (tests/types.ts D-07 `@ts-expect-error void Acknowledgments.update/delete`) still flags — invariant locked. ADR-019/023/028 all preserved. `pnpm tsc --noEmit` exits 0 on each commit boundary; runtime probe `pnpm exec tsx --conditions=react-server -e "import { QaCitationGrants } ..."` returns `OK QaCitationGrants surface` (plain tsx blocks via `'server-only'` directive — Phase 2 convention preserved). All 18 plan-level greppable acceptance criteria pass (10 Task 1 + 8 Task 2). Zero deviations, zero new packages, zero `any` types. **Pre-existing baseline failure carry-forward**: `pnpm verify:phase-4` exits 1 with `Total: 393 | Passed: 392 | Failed: 1` — sole failure is `reference/API-SPEC.md amended with 'citations: { title: string, id: string }[]' (D-27)`. Verified failing on baseline commit `8156d94` (the commit immediately before Plan 05-03's first task) by `git checkout 8156d94 -- lib/db/repositories/ ; pnpm verify:phase-4` — same `1/393` failure. Per SCOPE BOUNDARY rule in execute-plan.md, this is logged as DEFERRED-PHASE4-API-SPEC and tracked in SUMMARY § Deferred Issues — natural home is Plan 05-04 (Wave 2) which ships the askQuestion(ctx, q) extraction per D-25 (response shape is documented at the same time the function lands), or a tiny doc-only follow-up commit. Wave 2 sibling Plan 05-04 (orchestrators) is now unblocked — can consume all 4 repository surfaces (Acknowledgments.record from acknowledgment.ts, QaCitationGrants.upsert from askQuestion, Policies.listAssignedAndPublishedForUser from /my-policies Server Component, PolicyAssignments.create from admin bulkAssignToDepartmentAction). Wave 2 progress: 1/2 COMPLETE.
- [x] Plan 05-07: AckStatusBadge per D-11 — **FULL completed 2026-05-24T01:39Z** (SUMMARY: `.planning/phases/05-employee-portal/05-07-ack-status-badge-SUMMARY.md`). Commit `a685d69` (components/policy/AckStatusBadge.tsx new file, 58 lines, Server Component with exhaustive switch on D-04 `ackState` enum `'none' | 'current' | 'stale'`; three render branches mirror PolicyStatusBadge.tsx:18-46 structure verbatim: `'none'` → null (plain Acknowledge CTA renders separately in Plan 05-05 AcknowledgeButton), `'stale'` → `<Badge variant="outline" className="border-amber-500 bg-amber-50 text-amber-700">Requires re-acknowledgment</Badge>` (D-11 className override — mirrors PolicyStatusBadge `archived` branch's outline+override pattern), `'current'` → `<span className="inline-flex items-center gap-1 text-sm text-green-700">✓ Acknowledged on {ackedAt && new Date(ackedAt).toLocaleDateString('en-US')}</span>` (D-11 explicitly NOT a Badge — different visual treatment per CONTEXT.md). D-11 invariant enforced: `git diff components/ui/badge.tsx` returns empty — no new CVA variant added (per CONTEXT discretion bullet, className override is the deliberate scope choice for one-off color tweaks). Literal `✓` character used per D-11 verbatim — no lucide-react Check import. Server Component (no `'use client'` directive — pure render based on props, no state, no effects). `pnpm tsc --noEmit` exits 0; all 11 PLAN.md acceptance criteria pass. Zero deviations, zero new packages, zero `any` types. **Wave 1 COMPLETE 3/3**: 05-01 (schema migrations) ✓ + 05-02 (PolicyDomainError hierarchy) ✓ + 05-07 (AckStatusBadge) ✓. Wave 2 (05-03 repositories + 05-04 orchestrators, both parallel) ready to start.
- [x] Plan 05-02: PolicyDomainError hierarchy per D-30 — **FULL completed 2026-05-24T01:30Z** (SUMMARY: `.planning/phases/05-employee-portal/05-02-errors-SUMMARY.md`). Commit `2456b75` (lib/policies/errors.ts new file, 118 lines, abstract `PolicyDomainError` base + `PolicyDomainErrorCode` literal union ('POLICY_NOT_FOUND' | 'POLICY_ARCHIVED' | 'POLICY_NOT_ASSIGNED') + 3 concrete subclasses each with `public readonly policyId: string` constructor param + explicit `this.name`). Mirrors ADR-026 BootstrapError shape verbatim — no intermediate abstract layer like ProvisioningRaceError (D-07/D-08 share no catch semantics, only distinct UI copy). Pre-emptively satisfies Plan 05-08's widened scripts/check-error-discipline.ts gate (no built-in `throw new Error(` calls). `pnpm tsc --noEmit` exits 0; inline tsx probe (instanceof + code + name assertions on all 3 subclasses) prints `OK`; 5 greppable acceptance criteria pass (`abstract readonly code: PolicyDomainErrorCode` × 1, `readonly code = 'POLICY_*'` × 3, `this.name = 'PolicyXxxError'` × 3, `public readonly policyId: string` × 3, banned-pattern count = 0). 1 Rule-1 documentation deviation: initial file docstring contained literal `throw new Error(` inside a `//` comment describing the future Plan 05-08 gate — sanitized to plain-prose without the literal pattern so the planning-shortcut grep gate stays at 0 (ts-morph CI gate ignores comments per Phase 3 PR #5 precedent; no functional change).
- [x] Plan 05-05: Employee routes — layout + my-policies list (replaces 03-G3 T9 stub) + [id] D-27 3-branch access + acknowledgePolicyAction + ask page/action + AcknowledgeButton + AskQuestionForm — **FULL completed 2026-05-24T02:17Z** (SUMMARY: `.planning/phases/05-employee-portal/05-05-SUMMARY.md`). Commits `6883e84` (Task 2: app/(employee)/my-policies/[id]/actions.ts acknowledgePolicyAction Server Action with Zod policyId brand parse + x-forwarded-for first-hop IP capture per D-05 + typed-error mapping for PolicyArchivedError/PolicyNotAssignedError/PolicyNotFoundError + revalidatePath OUTSIDE try/catch per D-10c; components/employee/AcknowledgeButton.tsx Client wrapper via React 19 useActionState rendering success from state.ackedAt formState per RESEARCH Pitfall 5), `0f9b6af` (Task 1: app/(employee)/layout.tsx force-dynamic minimal shell with getOrgContext gate + UserButton header + container layout — NO admin role narrowing per SPEC In-Scope; app/(employee)/my-policies/page.tsx wholesale replacement of 03-G3 T9 stub Card — calls Policies.listAssignedAndPublishedForUser inside withOrgScope + renders AckStatusBadge per row + "Ask the AI" Link to /my-policies/ask + D-04a empty-state copy "No policies assigned yet — contact your administrator." verbatim with U+2014 em-dash; app/(employee)/my-policies/[id]/page.tsx D-27 access-aware Server Component — PolicyIdSchema.safeParse + notFound() at URL boundary per CR-PR3-#23 spirit + 3-branch access logic returning discriminated AccessResult union from ONE withOrgScope closure: Branch A assigned-and-published → full PolicyView + AckStatusBadge + AcknowledgeButton gated on ackState !== 'current', Branch B has-grant + published → amber-banner TL;DR-only view with D-27 banner copy verbatim, Branch C → notFound(); contentJson cast `as JSONContent` at PolicyView boundary — safe because column is server-controlled per Phase 3 ADR-005), `c4ddb01` (Task 3: app/(employee)/my-policies/ask/page.tsx R-6 RSC shell rendering AskQuestionForm + back-nav Link; app/(employee)/my-policies/ask/actions.ts askQuestionAction Server Action with Zod max(2000) char cap per Phase 4 D-42 attack-surface limit + Anthropic.APIError → semantic 503 inline error per HTTP route /api/ai/qa contract + NO revalidatePath since Q&A is non-mutating; components/employee/AskQuestionForm.tsx Client wrapper via React 19 useActionState rendering answer + clickable citation Links with D-27a visual hint — italic+muted-foreground for tldr-only, plain underline for full). `pnpm tsc --noEmit` exits 0 on every commit boundary; all 8 plan-level grep-asserted acceptance criteria pass. AcknowledgeButton ackState prop union narrowed to `'none' | 'stale'` (excludes 'current') — parent gates render via assignedRow.ackState !== 'current', so structurally unreachable. Discretion: components/employee/ directory chosen over components/policy/ for employee-only Client Components — keeps policy/ namespace for shared admin+employee components (PolicyView, AckStatusBadge, PolicyStatusBadge); CONTEXT.md "Claude's Discretion" permits either. Task-commit ordering: Task 2 committed FIRST (before Task 1) because Task 1's [id]/page.tsx imports AcknowledgeButton from Task 2 — committing Task 1 first would have failed pnpm tsc --noEmit on the missing-import error. Reorder respects per-task atomicity intent while keeping every commit tsc-green per CLAUDE.md ALWAYS rule #1; plan logical task IDs preserved in commit messages so SUMMARY traceability is intact. Zero deviations, zero new packages, zero `any` types. **Wave 3 PARTIAL 1/2**: 05-05 ✓; 05-06 (admin bulk-assign) pending. Branch now 18 ahead of `main` @ `c50b317` (15 prior + 3 task commits + 1 SUMMARY commit pending; this STATE update + ROADMAP update bundle with SUMMARY commit). Next session: Plan 05-06 (admin bulk-assign + PolicyAssignmentsPanel + Departments.listAll listAll method); zero file overlap with 05-05 (app/(admin)/** vs app/(employee)/**) but parallelization=false in plan frontmatter serializes the wave. Downstream consumers ready: scripts/check-acknowledgment-immutability.ts (Plan 05-08) will scan app/(employee)/my-policies/[id]/actions.ts — file contains zero .update(acknowledgments) / .delete(acknowledgments) calls (acknowledgments writes confined to lib/policies/acknowledgment.ts orchestrator via Acknowledgments.record); will pass. scripts/check-error-discipline.ts widening to lib/policies/** + app/(employee)/** — both Server Actions catch typed PolicyDomainError subclasses + Anthropic.APIError, never raw Error; will pass. Plan 05-09 integration test will create app/(employee)/my-policies/[id]/actions.test.ts + ask/actions.test.ts test files; both Server Actions export their AcknowledgeActionState / AskActionState typed unions for test fixtures.
- [x] Plan 05-01: Schema migrations + check-schema.ts extension — **FULL completed 2026-05-24T01:05Z** (SUMMARY: `.planning/phases/05-employee-portal/05-01-schema-migrations-SUMMARY.md`). Tasks 1+2+4 commits `0552341` (schema.ts: 2 UNIQUE adds on acknowledgments + policy_assignments per D-06+D-15, brand-new qaCitationGrants pgTable export per D-29), `b54ff02` (drizzle/0010_phase5_uniques.sql + 0011_qa_citation_grants.sql + meta/0010+0011_snapshot.json + _journal.json — operator-approval headers cite Q-22(a)+Q-23(a) for 0010 and T-2(4c) for 0011; 0011 RLS uses post-0008 wrapped (SELECT auth.jwt()->>'org_id') form per RESEARCH gap-1; two-step Drizzle generation pattern collapsed adjective_noun intermediates into descriptive Phase-suffix journal tags), `298cb59` (check-schema.ts: 'qa_citation_grants' added to TENANT_TABLES + 3-UNIQUE assertion block + 5-column shape assertion + wrapped-RLS predicate assertion + 2-index assertion). DEV migration applied successfully — direct postgres-js probe confirms 12 migrations recorded + qa_citation_grants table + all 3 new constraints present. **TEST DB live verification completed 2026-05-24T01:05Z**: operator rotated postgres-role password (project qwtbbbjbxffioeeazxrw) via Dashboard custom-password reset (3 attempts — first 2 didn't actually click Reset, 3rd succeeded); password landed in .env.local via .tmp/swap-test-password.cjs (file-to-file from blue.txt, 16-char alphanumeric, 2 substitutions validated via URL.parse + port + host + project-ref + length); pooler-auth propagation completed at T+306s (matches STATE.md staging precedent of 3-5min on aws-1-us-east-1); `pnpm exec tsx --env-file=.env.local .tmp/migrate-test-from-env-local.cjs` applied 0010+0011 to TEST DB (DEBUG=drizzle-kit:* surfaced NOTICEs schema "drizzle" already exists + relation __drizzle_migrations already exists, both safely skipped); TEST DB now at 12/12 migrations + 14 public tables + qa_citation_grants present + 3 new UNIQUE constraints present. **Plan 05-01 follow-up**: check-schema.ts:239 assertion `qual?.includes('(SELECT auth.jwt(')` was too strict against PG's normalized qual form `( SELECT (auth.jwt() ->> 'org_id'::text))` (whitespace inside parens); relaxed to `qual.includes('SELECT') && qual.includes('auth.jwt(')` which still rejects unwrapped form (no SELECT keyword) — committed alongside this STATE update. **CREDENTIAL HYGIENE**: blue.txt (operator-supplied custom password file, 16 bytes) + .tmp/env.local.bak (old stale password backup) deleted in cleanup commit. Staging+prod migration follow-up per CLAUDE.md Database Migration Discipline tracked in SUMMARY § User Setup Required.

### Blockers

- **SF-DB-1 (CLOSED 2026-05-18)**: Operator populated `DATABASE_URL_TEST` + `DIRECT_URL_TEST` in `.env.local` before Plan 02-06 execution. Plan 02-06 orchestrator step 2 (`drizzle-kit migrate against TEST DB`) and steps 4-5 (`check-rls.ts` + `check-schema.ts`) all pass against the live TEST DB. The `.env.local.test` file was left as comments-only (placeholder workaround from Plan 02-03 SF-DB-1 deferral); the orchestrator's `checkMigrateTest` overrides `DATABASE_URL` + `DIRECT_URL` from the `_TEST` env vars via spawnSync's `env` field rather than relying on a second env file.
- **SF-DB-2 (RESOLVED 2026-05-17)**: `DIRECT_URL` in `.env.local` updated from legacy IPv6-only `db.kdoahaxhmaftxaiwbtdw.supabase.co:5432` to Session-pooler form `aws-1-us-east-1.pooler.supabase.com:5432` (same hostname as DATABASE_URL, port 5432 instead of 6543, user pattern `postgres.<project_ref>`). `pnpm db:migrate` ran clean; live dev DB has all 12 tables + 10 RLS policies + 40 GRANTs + D-03a CHECK. Verification artifact at `.tmp/verify-dev-db.ts` (gitignored).
- **SF-WHSEC-1**: The Clerk webhook signing secret (`whsec_...`) was pasted into the chat transcript during Plan 02-02 checkpoint resolution. Recommend rotating it via Svix Dashboard before Plan 02-05 testing reaches a real dev tunnel. One-click rotation; no code change required. Closed once rotation done.
- **TEST DB pooler auth gate (CLOSED 2026-05-24T01:05Z)**: Operator rotated TEST DB credential (project qwtbbbjbxffioeeazxrw); pooler-auth propagated at T+306s post-rotation; `.tmp/migrate-test-from-env-local.cjs` applied Phase 5 migrations 0010+0011 to TEST DB successfully; check-schema.ts (with PG-normalization fix) reports clean: `12 tenant-scoped tables verified (exists + RLS + policy + 4 GRANTs); 2 service-role tables verified (NO RLS); policy_versions UNIQUE + Phase 5 acknowledgments/policy_assignments UNIQUE + qa_citation_grants UNIQUE + columns + indexes + wrapped-RLS all present.` Credential leak from previous session is moot — rotated. **NEW carry-forward**: `db:migrate:test` script in package.json still uses `--env-file=.env.local.test` (which is a populated copy of .env.local _TEST vars from the previous executor's diagnostic). Recommended permanent fix per SF-DB-1 closure pattern: change `db:migrate:test` to use the spawnSync env-override approach (read .env.local, override DATABASE_URL/DIRECT_URL from _TEST vars). Track as Phase 5+ opportunistic cleanup.
- **DEV DB pooler auth gate (CLOSED 2026-05-24T05:40Z)**: Operator also rotated DEV DB credential (project kdoahaxhmaftxaiwbtdw) mid-session after `pnpm exec tsx --env-file=.env.local scripts/check-schema.ts` returned 28P01 confirming DEV creds were also stale. New password applied via `.tmp/swap-dev-password.cjs` (file-to-file from blue2.txt — same pattern as TEST DB swap; 16-char alphanumeric; 2 substitutions validated via URL.parse + project ref + port). Pooler-auth propagated at T+124s (faster than TEST DB's T+306s — both on aws-1-us-east-1.pooler.supabase.com). Burst probe confirms 4/5 OK stability; transient 28P01 on probe 4/5 is consistent with the post-rotation pooler-cache settle window per STATE.md staging precedent line 217. `.tmp/probe-dev-db.cjs` confirms DEV at HEAD: PostgreSQL 17.6 + 12 migrations + qa_citation_grants table + 4 orgs / 3 admin users. **Other devs pulling `gsd/phase-5-employee-portal`**: refresh `.env.local` `DATABASE_URL` + `DIRECT_URL` (DEV) AND `DATABASE_URL_TEST` + `DIRECT_URL_TEST` (TEST) from your team secret manager / 1Password / Vault. Both Supabase projects had their postgres-role passwords rotated on 2026-05-24 — pre-rotation `.env.local` values will 28P01. blue.txt + blue2.txt + .tmp/env.local.bak deleted post-rotation (gitignored anyway; explicit cleanup commits afb7693 + 6ac3e4e + 891dc8d). No source-controlled file references either password — only `.env.local.example` (placeholders only, gitignored values).

### Phase 2 follow-ups (deferred — opportunistic cleanup)

Items surfaced during Phase 2 planning + plan-checker review (2026-05-17) that are intentionally deferred. None block Phase 2 success criteria.

- **SF-W5** Plan 02-05 webhook handler writes `clerk_events` BEFORE dispatch. If dispatch silently fails, Clerk receives 200 and does not retry. Phase 7 should invert ordering OR add structured logging + alerts. (Plan-checker WARNING-05, 2026-05-17.)
- **SF-CASCADE-AUDIT** Plan 02-03 + 02-07's `0003_fk_hardening.sql` added `ON DELETE CASCADE` to every org_id FK across 10 tenant tables. Tenant-offboarding now wipes acknowledgments, ai_generations, etc. in one transaction with no app-level signal. When an org-delete route lands (Phase 6+ Billing / tenant lifecycle), it MUST log row counts pre-delete AND emit a structured audit event BEFORE the cascade fires — otherwise the ADR-018 append-only audit trail is silently destroyed on offboarding. No app code path deletes orgs today, so this is a future-phase obligation, not a current bug. (Surfaced by silent-failure-hunter during multi-agent PR review, 2026-05-19.)

### Phase 1 PR-review follow-ups (deferred — opportunistic cleanup)

Surfaced by `/pr-review-toolkit:review-pr` against PR #1 head `e3689d3` (silent-failure-hunter + comment-analyzer + code-reviewer). SF-H4, SF-M3, and the phase-reference comment rot were addressed in `2438f42` and `723ca58`. The items below are real findings that were intentionally deferred — none block Phase 1, none touch production code paths in ways that affect ROADMAP success criteria. Pick up when convenient (e.g. on a `/gsd-quick` between phases) or fold into the relevant phase plan if you're already touching the file.

**Silent-failure hardening** (verify scripts — operator-UX, not Phase-1-criterion gaps):

- **SF-H1** `scripts/check-foundation.ts:62-73, 178-192` — when `spawnSync` sets `result.error` (ENOENT, EACCES), both stdout/stderr are empty; current code reports the generic literal `"tsc failed"`. Branch on `result.error` first, surface `code` + `message`.
- **SF-H2** `scripts/check-foundation.ts:175, 191` — `result.status === null` (signal-killed, e.g. OOM/SIGTERM) is currently masked as `"unknown"`. Surface `result.signal` explicitly.
- **SF-H3** `scripts/check-artifacts.ts:776-784` — server-only walker doesn't try/catch `readdirSync`/`readFileSync` and doesn't skip symlinks. A permission flip mid-walk crashes the whole gate; a symlink loop hangs. Wrap each fs call + use `entry.isSymbolicLink()`; print files-walked count so near-zero is obvious.
- ~~**SF-M1** `scripts/check-db.ts:30-33` — surface `err.constructor.name` (e.g. `PostgresError` / `AbortError`) on the catch path.~~ **CLOSED by C-1 in `764df7a`** — bound catch now surfaces `${err.name}: ${err.message}` with the gate-context prefix `"Drizzle smoke check failed: …"`. Strictly more informative than `err.constructor.name`.
- ~~**SF-M4** `middleware.ts:74, 85` — no try/catch around `await auth()`.~~ **CLOSED** by Plan 02-01 Task 2 (`e7c6b43` — `lib/auth/context.ts:25-32` wraps the getOrgContext `await auth()` call) AND Plan 02-05 Task 3 (`c39ea98` — `middleware.ts:52, 76` both `await auth()` call sites now wrapped in try/catch, admin-gate fails to 404 keeping D-10 advertise-nothing, chokepoint fails to /sign-in redirect). Both halves of SF-M4 are now closed; `[middleware] auth() failed` log lines provide the observability hook for Phase 7+ structured logging swap.
- **SF-M5** `scripts/check-artifacts.ts:28-30` — `read()` has no try/catch; TOCTOU between `exists()` and `read()` could nuke all 114 assertions on one transient FS hiccup. Wrap in try/catch, push a `fail()` Check, continue.
- **SF-L1** `scripts/check-foundation.ts:127` — `res.headers.get("location") ?? ""` ambiguates missing-header vs empty-header. Surface "Location header absent" explicitly.
- **SF-L2** `package.json:15` — `verify:phase-1` chain has `pnpm` shell invocation on the second half (`&& pnpm check:artifacts`), inconsistent with the IN-02 / `process.execPath` hardening on the first half. Cosmetic.

**Comment cleanup** — ~~all closed by `a58469d` (W1 refactor stripped the "Plan 01-05 strict regex, relaxed to substring" + "this file contains the needle" + "Plan 01-04 verify-block" comments) and `4582225` (N-3 trimmed the "Phase 1" coupling)~~. The remaining `Plan 01-NN` mentions in `scripts/check-artifacts.ts` are stable section dividers + frozen plan-doc anchors per the `.coderabbit.yaml` profile.

**Code-reviewer nits** (low-confidence, can defer indefinitely):

- `middleware.ts:75` — `as { role?: string } | undefined` cast on `sessionClaims?.publicMetadata`. Mirroring the `in/typeof` guard pattern from `scripts/check-db.ts` would be cleaner but the branch is dead in Phase 1. Defer to Phase 3 admin-matcher rewrite.
- `app/(marketing)/layout.tsx:28` — hardcoded `© 2026` footer. Bump annually or wire `new Date().getFullYear()`. Trivial.
- `app/(marketing)/pricing/page.tsx:73` — "Annual save 20%" reads ambiguously ("Save 20% annually"). Visible-to-users grammar nit.

---

### Phase 1 plan-checker findings (informational — not blocking)

1. **WARNING** — Plan 01-04 admin matcher pattern `/(admin)/(.*)` is dead code (route groups never appear in URLs). Acknowledged in code comment; Phase 3 will rewrite when real admin routes (`/dashboard`, `/policies`) land.
2. **WARNING** — Plan 01-01 Task 1 step 2 (`create-next-app` in non-empty repo root) carries a directory-overwrite risk. Fallback to temp + `Move-Item` is documented in the plan; execute-phase should treat the YES-to-overwrite path as fallback if the operator confirms collision risk.
3. **INFO** — `any`-detection regex in Plan 01-04 verify catches `: any,` / `: any)` but not `as any` / `<any>`. `tsc --noEmit` with strict mode would catch any genuine misuse, so the gap is theoretical.

### Parking lot (operator-tracked, do not address in MVP)

- DocTract pricing — verify before launch (closest real competitor)
- SAM.gov registration — post milestone 2 ($10K MRR)
- Slack integration — v1.1, not MVP

---

## Session Continuity

- **Ingest**: complete — FOUNDRY 9-document set ingested with 0 BLOCKERs / 0 WARNINGs (see `.planning/intel/SYNTHESIS.md`)
- **Roadmap**: created `2026-05-15` — derived directly from ADR-007's 8-phase locked build sequence
- **Phase 1 context**: captured `2026-05-15` via `/gsd-discuss-phase --all` — 15 implementation decisions (D-01 to D-15) at `.planning/phases/01-foundation/01-CONTEXT.md`
- **Phase 1 plans**: drafted `2026-05-15` via `/gsd-plan-phase 1 --auto` — 5 plans in 4 waves at `.planning/phases/01-foundation/01-0{1..5}-PLAN.md`; passed gsd-plan-checker verification
- **Phase 2 context**: gathered `2026-05-17` via `/gsd-discuss-phase 2 --all` under the operator's no-clarifying-questions directive. Absorbed 6 USER-LOCKED deliverables from ADR-023 + ADR-025 (L-01..L-06). Added 9 HOW decisions (D-01..D-09): hand-written `0001_rls_policies.sql` over inline `sql.raw()`; `org_id` denormalization onto five child tables; four-event Clerk webhook scope + new `clerk_events` idempotency table; `getOrgContext()` reads `publicMetadata.role` via the session-claim template; `DIRECT_URL` + `DATABASE_URL_TEST` env-var split; skeleton repository surface with type-system enforcement of ADR-018/005 invariants via `@ts-expect-error`; six-check `pnpm verify:phase-2` adding a schema audit; Clerk Dashboard role definitions (operator manual step). Folded Phase-1 PR-review todo SF-M4 (try/catch around `auth()`).
- **Last session**: Phase 2 execute-phase Plan 02-03 (2026-05-17 13:58:55Z–14:12:53Z UTC, 14min) — 3 task commits `c1dcf6f`, `0bbf321`, `f443cd0` + SUMMARY.md at `.planning/phases/02-data-layer/02-03-SUMMARY.md`. On-disk migration artifacts shipped: drizzle.config DIRECT_URL split (D-05), `drizzle/0000_initial.sql` (12 tables auto-DDL), `drizzle/0001_rls_policies.sql` (10×RLS + 10×POLICY + 10×GRANT + 1×CHECK on users for D-03a), `_journal.json` with both entries (RESEARCH Pitfall 3 mitigated). Deviations: 2 Rule-3 auto-handled (`.env.local.test` placeholder due to SF-DB-1; `drizzle/` `.gitignore` line removed) + 1 Rule-4 stopped-and-surfaced (Task 4 live DB push deferred — SF-DB-2 surfaced; resolved post-commit).
- **Latest session**: Phase 2 execute-phase Plan 02-04 (2026-05-17 14:26:23Z–14:30:38Z UTC, ~4m15s) — 2 task commits `2973555` (Task 1: Policies + Acknowledgments + Users + PolicyVersions) and `e71000a` (Task 2: PolicyAssignments + Departments + AiGenerations + Notifications + WorkflowStages) + SUMMARY.md at `.planning/phases/02-data-layer/02-04-SUMMARY.md`. 9 repository files / 351 lines under `lib/db/repositories/`. Each: `'server-only'` + OrgScope-first methods + ADR-019 `where(eq(orgId))` + no raw `@/lib/db` import + RESEARCH Pitfall 6 cited. `pnpm tsc --noEmit` exits 0 (~2.7s); D-07 type tests now active. 1 Rule-1 deviation (Acknowledgments header `//` → `/** */` because TypeScript scans line-comments for `@ts-expect-error` directives — TS2578 collision; acceptance substrings ADR-018 / append-only / Pitfall 6 preserved).
- **Current session**: Phase 2 execute-phase Plan 02-05 (2026-05-17 14:36:42Z–14:46:28Z UTC, ~9m46s) — 3 task commits `a9301b2` (Task 1: svix@1.93.0 exact-pinned install + pre/post-install pnpm audit + confirmed no postinstall + SLSA v1 provenance) + `6ae44f5` (Task 2: `app/api/webhooks/clerk/route.ts` 264 lines — POST handler with svix.Webhook.verify after raw req.text() per RESEARCH Pitfall 4, ON CONFLICT DO NOTHING idempotency on clerk_events per D-03b, 4 active event handlers per D-03, 3 delete events log-only per D-03c, asAppRole strips `org:` prefix before narrowing per D-09 fallback, ADR-023 allow-list entry #1 cited inline + Phase 7+ SF-W5 gap documented inline) + `c39ea98` (Task 3: middleware.ts SF-M4 fold — both `await auth()` call sites wrapped in try/catch, admin-gate fail-closed to 404 keeping D-10 advertise-nothing, chokepoint fail-closed to /sign-in redirect with no redirect_url to avoid loops) + SUMMARY.md at `.planning/phases/02-data-layer/02-05-SUMMARY.md`. `pnpm tsc --noEmit` exits 0 on every commit boundary. 2 Rule-3 deviations both pre-existing / out-of-scope: 1) esbuild advisory from `drizzle-kit > @esbuild-kit/esm-loader > @esbuild-kit/core-utils` (pre-dates this plan, baseline finding, no IOC introduced by svix); 2) plan's verify regex 200-char preceding-context window for `try{` before `await auth()` is too tight for the 4-line fold-rationale comments the plan body specified — structural placement is correct (both auth calls visibly inside try blocks at lines 52-72 and 76-93), only the regex distance bound is cosmetically off.
- **Latest session**: Phase 2 execute-phase Plan 02-06 (2026-05-17T23:43:04Z → 2026-05-18T00:00:37Z, ~17m33s) — 5 task commits `e160728` (Task 1: ts-morph@28.0.0 exact-pin install + pre/post audit no new vulns + no postinstall + .env.local.example +4 placeholders), `c31d1c8` (Task 2: scripts/check-db-imports.ts L-05 AST allow-list 126 lines, 8-entry ALLOWLIST regex over POSIX paths, positive control `allowListedHits >= 2`), `a156dc5` (Task 3: scripts/check-rls.ts L-06 cross-org property test 187 lines, seeds 2 orgs/users/policies in BYPASSRLS user then SET LOCAL ROLE authenticated + set_config jwt claims with is_local=true, POSITIVE CONTROL + 10-table negative, intentional ROLLBACK + final TRUNCATE for idempotency), `ff82746` (Task 4: scripts/check-schema.ts D-08 step 5 schema audit 136 lines, pg_catalog + information_schema, 4 checks × 10 tables + 2 service-role no-RLS asserts, 1 Rule-1 fix polname → policyname), `9888cf5` (Task 5: scripts/check-data-layer.ts 7-check orchestrator 207 lines, spawnSync via process.execPath CVE-2024-27980 hardened, env-override DATABASE_URL/DIRECT_URL for migrate-against-TEST DB, scripts/check-artifacts.ts extended +271 lines / 8 Phase 2 functions + comment-strip walker hardening + Phase-1 schema-stale check dropped, package.json verify:phase-2 wired) + SUMMARY.md at `.planning/phases/02-data-layer/02-06-SUMMARY.md`. `pnpm verify:phase-2` exits 0 with 7/7 OK against live TEST DB (runtime ~22s); all 214 check-artifacts assertions pass; `pnpm tsc --noEmit` exits 0 on every commit boundary. 6 deviations (1 Rule-1 bug + 4 Rule-3 blocking + 1 false-positive accommodation; all documented in SUMMARY). Task 6 (operator human-verify of `pnpm verify:phase-2` self-run + end-to-end Clerk webhook smoke) is a `checkpoint:human-verify` gate — awaiting operator's resume signal.
- **Current session**: Phase 2 execute-phase Plan 02-07 hotfix (2026-05-18T23:21:00Z → 2026-05-18T23:24:00Z, ~3min) — 2 task commits `5bdcbf9` (CR-01: `app/api/webhooks/clerk/route.ts` +64/-1 lines — `clerkClient` added to named imports, new `mirrorRoleToClerk(clerkUserId, role, source)` helper calls `(await clerkClient()).users.updateUserMetadata(clerkUserId, { publicMetadata: { role } })` in try/catch with structured logging; wired into `user.created` (default `employee`), `organizationMembership.created` (narrowed `roleStr` after DB returning rows confirms user found), `organizationMembership.updated` (narrowed `roleStr` for role demotions/promotions); D-04 dual-write contract now satisfied end-to-end) and `13a9a30` (HI-01: `middleware.ts` +10/-1 — narrowing tightened from `{ role?: string }` to `{ role?: unknown }` + `typeof === 'string'` guard with comment block citing context.ts:42 parity rationale; no asRole import — middleware only needs literal `=== 'admin'`, asRole stays single source of truth in context.ts) + SUMMARY.md at `.planning/phases/02-data-layer/02-07-SUMMARY.md`. `pnpm tsc --noEmit` exits 0 on each commit boundary; `pnpm verify:phase-2` exits 0 with 7/7 OK against live TEST DB. Zero deviations, zero new packages, zero `any` types. Closes CR-01 + HI-01 from `.planning/phases/02-data-layer/02-REVIEW.md`.
- **Next session entry point**: Phase 2 fully complete (7/7 plans). Next plan path is `/gsd-verify-work` for Phase 2 closeout → Phase 3 (Admin UI) context gathering. Phase-3 carry-forwards remain as documented above (SF-WHSEC-1, webhook live-smoke, REG-P1-01) plus CR-02 (`isAdminRoute` matcher dead code — Phase 3 will replace `/(admin)/(.*)` with concrete admin URLs once `app/(admin)/<route>/page.tsx` files land).

- **Resume 2026-05-20T17:42Z**: Session resumed via `/gsd-resume-work`. PR #3 state advanced since HANDOFF.json (17:29Z): CR's 3rd re-review fired at 17:32:08Z with 1 finding (policyId rejection log was exposing JSON.stringify(id.slice(0,8))). Commit `0287bb6` (17:38:41-04:00Z) responded by dropping the `type` + `sample` log fields and keeping `length` only. CR's 4th review is now PENDING at new HEAD `0287bb6` (status check started 17:38:49Z). Monitor `bjcxzydia` (persistent) re-armed against `.tmp/monitor-pr3.mjs`. PR state on resume: `state=OPEN`, `reviewDecision=CHANGES_REQUESTED` (carryover from CR review on `bd25409`), `mergeStateStatus=UNSTABLE`. Wait state — next signal is CR's 4th review verdict.

- **Phase 3 shipped 2026-05-20T20:35Z**: PR #3 squash-merged to `main` @ `edebab7` after the full review loop completed. Loop summary: CR re-review #4 surfaced 3 findings on `0287bb6` (reviewerId UUID validation, empty-patch rejection, absolute Windows path leak in HANDOFF.json + .continue-here.md) → fix commit `a9ba7f5` (`cr-pr3-postreview-v3` — also deleted HANDOFF.json per resume workflow one-shot rule). Operator-triggered pr-review-toolkit re-review on `bd25409..a9ba7f5` (4 agents — code-reviewer/pr-test-analyzer/silent-failure-hunter/comment-analyzer) returned 0 BLOCK / 0 FLAG from code-reviewer but surfaced 1 Critical (inaccurate empty-patch comment — verified wrong against `lib/db/repositories/policies.ts:113-117`) + 2 MEDIUM (collapsed error strings + missing reviewerId log breadcrumb) + 2 crit-5 (no uppercase-UUID test, no whitespace-trim test) → fix commit `4ea8aab` (`cr-pr3-postreview-v4` — fixed all 5, deferred 3 cosmetic improvements). CR APPROVED `4ea8aab`, `gh pr merge 3 --squash --delete-branch` executed, `git pull --ff-only` to `edebab7`. Branch `gsd/phase-3-admin-ui` deleted on remote + local. Monitor `bjcxzydia` already self-exited before the merge (transient gh failure earlier).

- **Resume 2026-05-21T04:33Z**: Session resumed via `/gsd-resume-work` in fresh context. Phase 3 audit cascade mid-flight (1 of 3 audits done: gsd-secure-phase 3 SHIPPED via PR #8 OPEN @ `333911d`). On-resume PR state diverged from HANDOFF.json snapshot: **PR #7** (ADR-027 lookup-scoping @ `e32173b`) was APPROVED by CR on prior HEAD `188f6b7` but v2 polish `e32173b` drew a fresh CR re-review at 2026-05-21T03:49:33Z → CHANGES_REQUESTED with 1 finding (`scripts/check-artifacts.ts:1268-1293` — three ADR-027 assertions read raw `ctx` so the gate can pass on comment-only false positives; CR's suggested diff: add `const ctxNoComments = ctx.replace(/\/\/[^\n]*/g, "").replace(/\/\*[\s\S]*?\*\//g, "")` and route the 3 assertions through `ctxNoComments`). **PR #8** (Phase 3 security audit @ `333911d`) was APPROVED by CR on `4a444f8` then re-reviewed at 2026-05-21T04:24:28Z after the wip checkpoint commit landed → CHANGES_REQUESTED with 1 finding (`.planning/HANDOFF.json:148` leaks absolute local path with username into repo history). PR #8 finding resolved this commit by deleting both one-shot resume artifacts (`.planning/HANDOFF.json` + `.planning/phases/03-admin-ui/.continue-here.md`) per resume-project workflow line 94 (one-shot rule) + per the `.continue-here.md` self-documented PR-#3-era anti-pattern note about absolute-path leaks. Next: switch to `refactor/lookup-scoping`, apply CR's exact suggested comment-stripping diff (closes PR #7); then create `chore/phase-3-validation-trail` off `main`, spawn `gsd-nyquist-auditor` background agent with stage-gated prompt (anti-pre-load rules forbidding 03-CONTEXT.md / 03-PATTERNS.md / 03-RESEARCH.md / 03-DISCUSSION-LOG.md; pre-loaded State A facts: VALIDATION.md exists, 15 PLAN files + 15 SUMMARY files in `.planning/phases/03-admin-ui/`; 4-stage work plan: requirement→task map → test-infra detect → COVERED/PARTIAL/MISSING classify → update VALIDATION.md). Carried operator-decision question: PR 3.3 (ADR-028 PolicyId brand) scope — bundle PR #7's 2 deferred MEDIUM findings (UserNotProvisionedError subCode discriminant + orgRow.id in error message) or give them their own ADR-029? Decision deferred until after audit cascade ships.

- **Resume 2026-05-21T~14:30Z (post phase-3-complete pause)**: Session resumed via `/gsd-resume-work` in fresh context after `/clear`. HANDOFF.json (one-shot, written 13:20:49Z by prior pause commit `68128ba`) consumed + deleted; companion `.planning/.continue-here.md` also deleted per resume-project workflow §94 one-shot rule. **Phase 3 status on resume**: FULLY CLOSED — shipped (PR #3 @ `edebab7`) + 3 fast-followed (PR #5 ADR-026 typed errors @ `bf65712`; PR #7 ADR-027 lookup-scoping @ `c6dca6a`; PR #13 ADR-028 PolicyId brand + UserNotProvisionedError subCode + tenant-isolation @ `bd2257a`) + 3-audit cascade (PR #8 security @ `5a57000`; PR #10 validation @ `85aebed`; PR #11 verification @ `28f646e`) + closure test (PR #9 VALIDATION-2.7 @ `d185efc`) + state bookkept (PR #12 @ `6b95eaa`). **main HEAD: `bd2257a`** (PR #13); **local HEAD: `68128ba`** (WIP pause commit, not pushed — contains only handoff artifacts which are now deleted in workspace). **No uncommitted changes, no blockers, no in-progress agents.** Untracked workspace items (`.agents/`, `.claude/`, `.mcp.json`, `.wiki/`, `audit-cache/`, `localhost-1779237331998.log`) all pre-existing per HANDOFF.json snapshot. **Wiki PENDING-INGEST queue** carries 8 unprocessed blocks dating back to 2026-05-17; most recent block 09:27:04 lists only `.planning/intel/decisions.md` (decisions ADR-027 + ADR-028 ratifications). `.wiki/` directory is untracked so this is an aspirational system — drain deferred until operator confirms wiki should be a tracked artifact. **Locked decisions count**: needs bump 27 → 28 (ADR-028 ratified in PR #13) — deferred to bundle with first Phase 4 state update per HANDOFF.json `state-bookkeeping-bundling`. **Next: operator picks Phase 4 entry workflow** — three options: `/gsd-discuss-phase 4` (standard HOW-decision gathering), `/gsd-spec-phase 4` (clarify WHAT first if Phase-4 deliverables feel ambiguous), `/gsd-ai-integration-phase 4` (formal AI-SPEC.md with eval planning + framework selection). Default recommendation: `/gsd-ai-integration-phase 4` — Phase 4 is the AI Layer (4 Claude-powered surfaces: Draft, TL;DR, Q&A, Consistency Check); the formal AI-SPEC rigor (eval rubrics, guardrails, production monitoring) fits the project's existing ADR/security/validation discipline pattern.

- **2026-05-21 cascade-and-fast-follows session (04:33Z → 05:35Z, ~1h with deep ultrathink + background-agent passes)**: Single dense session shipped 2 fast-follow merges + opened 4 audit/state PRs. **Closed**: (1) PR #7 CR re-review via `fb2b512` applying CR's exact suggested comment-stripping diff to `scripts/check-artifacts.ts` ADR-027 assertion block; CR APPROVED + squash-merged to main @ `c6dca6a`; (2) PR #8 CR re-review via `17e4397` deleting one-shot handoff artifacts (HANDOFF.json + .continue-here.md) per resume-project workflow §94 — closing path-leak finding. **CR auto-fix incident on PR #8**: CR's `687b6d9` "Fixed 3 file(s) based on 1 unresolved review comment" went out-of-scope on 2/3 files (resurrected the Plan-01-IN-04 deleted `CardAction` shadcn component + 561-line `pnpm-lock.yaml` churn bumping `@unrs/resolver-binding-*` 1.11.1 → 1.12.2 + ~10 new platform bindings + `postcss` 8.5.14 → 8.5.15). Investigation per `audit-before-security-changes` memory + CLAUDE.md "ASK FIRST" for unauthorized package bumps → operator chose **Option B surgical revert**: additive commit `062f341` restored `card.tsx` + `pnpm-lock.yaml` to their `687b6d9^` state while keeping CR's legitimate STATE.md path-redaction. PR #8 then squash-merged cleanly to main @ `5a57000`. **Opened (cascade artifacts)**: PR #9 `test/policies-list-with-filters` @ `58dfce8` (`scripts/check-policies-list-filters.ts` — VALIDATION-2.7 closure with 10 integration assertions: no-filter LIMIT-100-cap-on-105-rows / status=published-2-rows / status=archived-1-row / q=Onboarding-title-ILIKE-2-rows / q=Legal-category-ILIKE-1-row / q+status-compound-1-row / no-match-empty-array / status=draft-LIMIT-100-cap-on-101-rows / cross-org-Org-B-published-count / cross-org-Org-B-q-Onboarding-not-Org-A; wires into `verify:phase-3` chain — now 9 gates; 2 new check-artifacts assertions pinning script + package.json declaration); PR #10 `chore/phase-3-validation-trail` @ `8d906f6` (Phase 3 validation audit refresh — `03-VALIDATION.md` PARTIALLY VALIDATED verdict, 28-COVERED / 18-PARTIAL / 2-MISSING; produced by `gsd-nyquist-auditor` background agent in ~5.7min/208K-tokens with stage-gated prompt + anti-pre-load rules forbidding 4 bird's-eye docs); PR #11 `chore/phase-3-verify-trail` @ `ae9bfcc` (Phase 3 UAT second-pass — VERIFIED 6/6 PASS at main @ `5a57000` via delta-analysis + `claude-in-chrome` MCP live spot-check on `/dashboard` confirming `getOrgContext` returns valid context without `UserNotProvisionedError` despite ADR-027's new `eq(users.orgId, orgRow.id)` lookup-scope clause; also flips `03-VERIFICATION.md` frontmatter `status: human_needed → closed` with full audit trail). **Branch cleanup**: 5 stale local branches deleted (`chore/state-adr-026-shipped`, `chore/state-phase3-shipped`, `gsd/phase-3-admin-ui`, `refactor/typed-errors`, `refactor/lookup-scoping`) + lingering `origin/refactor/lookup-scoping` remote branch deleted (PR #7 was merged externally without `--delete-branch`). **Pace discipline**: Pre-`/clear` HANDOFF.json was correctly deleted post-resume per the one-shot rule; no context-warning panic-batching this session even with 5 PRs of output. **Stage-gated background-auditor pattern proven for two passes** (gsd-security-auditor in prior session + gsd-nyquist-auditor in this session). **Comment-discipline holding**: zero "confidently-wrong behavioral comment about other-file behavior" re-fires this session; grep-before-claim discipline applied throughout (e.g. read `scripts/check-auth-context.ts` for seed-and-rollback pattern + `lib/db/repositories/policies.ts` for `listWithFilters` signature before writing PR #9's integration test). **CR over-scope lesson banked**: CR's auto-fix feature can run `pnpm install` as a side-effect and introduce unauthorized dep bumps — always inspect the diff before merging an auto-fix commit; surgical revert pattern preferred over `git reset --hard` + force-push.

- **2026-05-21 ADR-029 ratification session (post-`/gsd-manager --analyze-deps`)**: Operator invoked `/gsd-manager --analyze-deps` post-Phase-3-ship and pre-Phase-4-entry. Dependency analysis surfaced that ADR-007's strict-sequential gating over-specified vs. true semantic minimums — Phase 5 truly depends on Phase 3 (not Phase 4); Phase 6 on Phase 4 (not Phase 5); Phase 7 on Phase 5 (not Phase 6). Operator directive: soften ADR-007 to "phase boundaries must remain green on main, but in-flight phases may run on parallel branches." Ratified as **ADR-029** — amends ADR-007 (does NOT supersede; phase ORDER preserved Foundation→…→Validation; gating relaxed under green-on-`main` constraint). Files updated this session: `.planning/intel/decisions.md` (long-form ADR-029 appended), `.planning/PROJECT.md` (short-form ADR-028 + ADR-029 added to `<decisions>` block — ADR-028 mirror caught up per HANDOFF.json `state-bookkeeping-bundling` deferred plan), `.planning/ROADMAP.md` (line-3 header + `Depends on` entries for Phases 5/6/7 amended), `CLAUDE.md` root (Build Sequence section reflects new gating + wave grouping), `.planning/STATE.md` (this entry + locked-decision count). Enables **Wave 1**: Phase 4 (AI Layer) ‖ Phase 5 (Employee Portal); **Wave 2**: Phase 6 (Billing) ‖ Phase 7 (Crons + Email); **Wave 3**: Phase 8. Theoretical wall-clock compression 30-40% if Wave 1+2 paired; real cost is `scripts/check-artifacts.ts` + `pnpm-lock.yaml` rebase discipline on the later-merged branch. Parallelism is opportunity, not mandate — operator chooses per-phase. **No code/test/schema changes** — pure planning-artifact amendment. Locked decision count: 27 → 29 (also catches ADR-028 ratification deferred from PR #13 per the bundled-state-update pattern). **Next**: enter Phase 4 — recommended `/gsd-ai-integration-phase 4` (formal AI-SPEC with eval rubrics + framework selection); Wave 1 with Phase 5 is available if operator bandwidth allows.

- **2026-05-21 Phase 4 discuss-phase session (`/gsd-discuss-phase 4 --power`)**: Operator ran power-mode discuss-phase against the SPEC.md-locked Phase 4. Scout pass mapped `lib/ai/` (empty), `lib/db/repositories/ai_generations.ts` (Phase 2 skeleton), `lib/policies/transitions.ts` (Phase 3 orchestrators incl. `publish`), `lib/auth/errors.ts` (ADR-026 typed-error precedent), `app/(admin)/policies/[id]/actions.test.ts` (Phase 3 vitest `vi.mock` pattern), and the 4 reference contracts (API-SPEC.md, PROMPTS.md, TIER-LIMITS.md, SCHEMA.md). Generated `04-QUESTIONS.json` + `04-QUESTIONS.html` self-contained offline editor with 27 questions across 7 sections (AI Foundation, Schema & Persistence, Q&A Implementation, Tier-Limit Module Layout, Error Contract + publishPolicy, Admin UI Hooks, Verify Chain + Tests). Operator answered all 27 — 26 picked option (a) recommended-first; Q-15 initially (a) inline-guard but flipped to (b) throw-based on refresh after the workflow flagged the Q-15/Q-16 contradiction (Q-16=a creates `lib/stripe/errors.ts::TierLimitExceededError` which would be unused dead code without throws). Resolution captured in Q-15+Q-16 `chat_more` fields. `04-CONTEXT.md` written with 27 decisions D-01..D-27, locked SPEC requirements referenced via `<spec_lock>` block (not duplicated — agents read SPEC.md directly), canonical refs + code context + exact code-snippet specifics + deferred ideas. `04-DISCUSSION-LOG.md` written with per-question options table + selections + Q-15 resolution thread for human-audit purposes. All 4 artifacts committed `e7447f7`: `docs(phase-04): capture phase context — 27 implementation decisions`. **No new ADRs** — Q-27=a chose doc-only `reference/API-SPEC.md` amendment for the citation-shape change (`string[]` → `{ title, id }[]`) per SPEC.md R4. Locked decision count stays at 29. **Schema delta locked for Phase 4**: 1 new migration (`0004_batch_jobs` + companion `0005_rls_batch_jobs`) per D-06; NO `extracted_text` column on `policies` per D-07 (Q&A uses `@tiptap/html` `generateHTML`+strip at query time). **Phase 4 net-new files locked**: `lib/ai/{client,models,prompts,cache,qa-extract,qa-parser,summary}.ts` (7), `lib/stripe/{products,errors}.ts` (2), `lib/db/repositories/batch_jobs.ts` (1), `app/api/ai/{draft,summary,qa,consistency}/route.ts` (4) + `app/api/ai/consistency/[batchId]/route.ts` (1), `app/(admin)/dashboard/consistency/page.tsx` + actions (2), `components/policy/PolicyAiDraftDialog.tsx` + `PolicyRegenerateTldrButton.tsx` (2), `components/admin/ConsistencyCheckRunner.tsx` + `ConsistencyFindingsList.tsx` (2), `scripts/check-ai-layer.ts` + `check-ai-prompts.ts` (2), `drizzle/0004_batch_jobs.sql` + `0005_rls_batch_jobs.sql` (2). **Modified files**: `lib/db/repositories/ai_generations.ts` (fill 3 methods), `lib/db/repositories/policies.ts` (add `listPublishedForOrg` + `updateSummary`), `lib/db/schema.ts` (add batchJobs table), `lib/policies/transitions.ts` (`publish` adds post-commit `generateSummaryForPolicy` call), `components/admin/AdminSidebar.tsx` (add "Consistency Check" item), `scripts/check-error-discipline.ts` (widen scan to `lib/stripe/`), `scripts/check-artifacts.ts` (extend with Phase 4 file-existence rows), `package.json` (+ `@anthropic-ai/sdk` exact-pin + `verify:phase-4` chain), `reference/API-SPEC.md` (citation-shape amendment), `reference/PROMPTS.md` (Q&A trailing citation-block instruction). **Next**: `/gsd-plan-phase 4` consumes `04-SPEC.md` + `04-CONTEXT.md` to produce the wave-grouped plans.

- **Resume 2026-05-22T~14:30Z (post Phase 4 ship-pending pause)**: Session resumed via `/gsd-resume-work` in fresh context after `/clear`. Primary resumption source: `.planning/HANDOFF.json` (one-shot, written 2026-05-22T14:21:38.422Z by prior pause commit `a9f68d6`) + companion `.planning/phases/04-ai-layer/.continue-here.md`. **Phase 4 status on resume**: SHIP-READY AWAITING OPERATOR SQUASH-MERGE OF PR #15. 14/14 plans executed cleanly (commits `c1eb22f` → `149a356`) + bug fix mid-run (`fb46eda` — `translateProcessingStatus` extracted to `lib/ai/batch-status.ts` after Next.js 15 route-typegen rejected named non-HTTP-verb exports); real-key UAT smoke (`f57b104`); gsd-secure-phase 60/60 CLOSED (`6887000`); ship orchestrator (`b327bfb` + `82c1732`); PR review hygiene 6-of-10 applied (`4fea328`); wip pause (`a9f68d6`). Branch `gsd/phase-4-ai-layer` @ `a9f68d6`; 81 commits ahead of origin/main. **PR #15 status divergence from HANDOFF.json**: HANDOFF.json snapshot said `mergeable: CLEAN, no blockers`; live state shows `state=OPEN, mergeable=MERGEABLE, mergeStateStatus=CLEAN, reviewDecision=CHANGES_REQUESTED`. CodeRabbit's CHANGES_REQUESTED review at 2026-05-22T07:39:56Z is **STALE** — it predates hygiene commit `4fea328` (which addressed 6 of CR's 10 findings + explicitly skipped 4 with rationale). Operator can either (a) push request-re-review to dismiss CR's stale verdict, (b) squash-merge anyway since branch protection is not blocking (mergeable=CLEAN), or (c) trigger a fresh CR re-review. **No code/test changes uncommitted; no blockers; no in-flight agents.** Untracked workspace items (`.agents/`, `.claude/`, `.mcp.json`, `.wiki/`, `audit-cache/`, `localhost-*.log`) all pre-existing per HANDOFF.json snapshot. STATE.md frontmatter + body-position-marker is stale (says Phase 4 "CONTEXT CAPTURED, next: /gsd-plan-phase 4") — full state-bookkeeping update for Phase 4 SHIPPED will land on the post-squash-merge state PR. **Next operator action**: review PR #15 → `gh pr merge 15 --squash --delete-branch` → `git checkout main && git pull --ff-only`. Pre-deploy: `pnpm db:migrate` against staging/prod DATABASE_URL (Plan 04-02 only migrated TEST_DATABASE_URL — surfaced during UAT). 4 non-blocking human actions queued in HANDOFF.json (decide on 4 SKIPPED CR findings; rename secrets/ API-key file to neutral name; optional re-UAT against live Anthropic — already done in-session; primary PR #15 review).

- **2026-05-22 Phase 4 ship + deploy-prep session (~5h, post-resume)**: Single end-to-end session that closed the Phase 4 ship loop AND built the staging/prod migration infrastructure. **Closed**: (a) rename of secrets/ key file to neutral `anthropic-dev.txt` via wildcard `mv` (no name echoed per `secrets-never-in-chat` memory); (b) `.gitignore` +7 lines locking `.mcp.json` + `.agents/` + `.claude/` + `.wiki/` + `audit-cache/` + `localhost-*.log` (PR #15 `3d9494d`); (c) 5-agent pr-review-toolkit 1st pass surfacing 2 truly load-bearing critical silent-failure findings (transitions.ts catch leaked PII + swallowed non-Anthropic bugs; consistency JSON.parse → empty findings silently marked 'completed'); (d) 6-fix commit `233a340` addressing both criticals + 4 easy follow-ups (require-admin.ts:18 stale line ref, scripts/check-schema.ts batch_jobs entry, AC-32b cache-read assertion, ConsistencyCheckRunner sanitized catch + dropped stale-closure guard); (e) 5-agent 2nd pass — unanimous APPROVE on `233a340`; (f) 1-line type fix `28d6f23` closing bounded `any` leak via `error.error?.type` → `error.type` (APIError.type direct field per `@anthropic-ai/sdk/core/error.d.ts:13`); (g) Phase 4.5 polish-PR backlog tracked at **GitHub issue #16** (~30 deferred items: 9 type tightening, 18 comment-rot, 6 test-coverage, 3 architecture); (h) **PR #15 squash-merged to `main` @ `f8207f4`** (125 files / +31,550 / -386 / Anthropic Phase 4 AI Layer SHIPPED); (i) post-merge one-shot artifact cleanup PR #17 squash-merged @ `eda7bb4` deleting HANDOFF.json + `.planning/phases/04-ai-layer/.continue-here.md` per resume-project workflow §94. **Deploy-prep PR (this entry)**: After ship, operator invoked deep ultrathink + wide-research mode for the absolute-best staging/prod migration discipline. Built env-agnostic `scripts/check-deploy-schema.ts` (validates migrations applied + 11 tenant tables RLS + GRANTs + Phase 4 ai_generations 5-column shape + idempotency partial-unique index + Phase 3 G3 + Phase 4 unique constraints; reads any DATABASE_URL/DIRECT_URL); `scripts/deploy-preflight.ts` (Vercel build-hook wrapper, graceful when DATABASE_URL absent); `docs/runbooks/deploy-migrations.md` (step-by-step manual procedure: pre-flight → staging migrate → verify → operator approval → prod migrate → verify; per-migration rollback notes flagging 0007's destructive DROP COLUMN as backup-restore-only; audit-log template); `docs/runbooks/README.md` (runbook index); `.github/workflows/migrate.yml` (manual-trigger CI with env selector + GitHub Environment approval gate for prod); `vercel.json` (buildCommand chains `pnpm deploy:preflight && pnpm build`); package.json +7 scripts (`db:migrate:staging`/`:prod`, `db:verify`/`:test`/`:staging`/`:prod`, `deploy:preflight`); `.env.local.example` +block pointing at secrets/<env>.env pattern; CLAUDE.md +Database Migration Discipline section between Git Workflow and AI API Rules; `scripts/check-artifacts.ts` +`checkDeployPrep()` block asserting all 13 new artifacts + script declarations + cross-references. **Next operator action**: (1) merge deploy-prep PR to main; (2) populate `secrets/staging.env` + `secrets/prod.env` (gitignored); (3) `pnpm db:migrate:staging` then `pnpm db:verify:staging` (apply drizzle/0005..0007 to staging); (4) operator-approval gate per CLAUDE.md ASK-FIRST (0007 is destructive); (5) `pnpm db:migrate:prod` then `pnpm db:verify:prod`; (6) record audit-log entry in STATE.md per runbook template; (7) enter Phase 5 (`/gsd-spec-phase 5` — Wave-1 parallel candidate per ADR-029). **Locked decisions count**: still 29 (no new ADRs from this session — Database Migration Discipline is policy-documentation, not an architectural lock). **Memory rules honored this session**: `secrets-never-in-chat` (key file renamed via wildcard `mv`; sentinel-verified via size+prefix without echoing name); `audit-before-security-changes` (verified `.mcp.json` ignore status before commit; sentinel-scanned UAT logs for sk-ant-api03- before gitignoring them); `context-warning discipline` (slowed and picked max-effort scope when context-warning hits during agent dispatching); `api-skill-routing` (no skill mis-route — enterprise-api-architect not needed for purely-internal migration tooling).

- **Resume 2026-05-22T~post-deploy-prep (post-PR-#18 + PR-#19 squash)**: Session resumed via `/gsd-resume-work` in fresh context. Reality outpaced STATE.md frontmatter: deploy-prep PR #18 (`bae9174`) and Vercel-prerender hotfix PR #19 (`a4968de` — `force-dynamic` on `(admin)/layout.tsx` + `(auth)/layout.tsx` to fix Phase 4 first-deploy ClerkAuthFailedError on `/dashboard/consistency` + `/post-sign-in`) both shipped to `main` between sessions. Working tree clean on `main` @ `a4968de`; no HANDOFF.json, no `.continue-here.md`, no incomplete plans, no in-flight agents. Operator picked next action: **staging migration** (deploy-prep step 2-3).

- **Pattern 3 credential-isolation architecture adopted (2026-05-22 session)**: Operator picked the "maximal isolation" credential-hygiene path mid-staging-migration after the deploy-prep PR-#18 Pattern 1 (file-based `secrets/staging.env`) revealed real-world failure modes — operator kept creating credential-bearing files at repo root (`blobby.txt` / `bub.txt` / `bubby.txt` / `project password <secret>.txt` / `boomboom.txt`), one `git add -A` away from a public commit each. New architecture: (a) password lives ONLY in PowerShell SecretStore (DPAPI-encrypted at rest, tied to Windows user account); (b) `scripts/with-deploy-creds.ps1` wrapper retrieves password at runtime via Get-Secret, constructs URLs in script scope with `[Uri]::EscapeDataString` defense against URL-special chars, materializes into env for spawned child only, clears env on exit via try/finally — no credential ever lands in a file or transits chat; (c) `scripts/deploy-config.json` holds non-secret per-env routing config (host / port / user with project_ref / secretName); (d) `scripts/store-deploy-password.ps1` uses Windows `Get-Credential` GUI dialog to avoid the Read-Host paste-truncation bug some ConHost configurations have; (e) `package.json` `db:migrate:staging` / `db:verify:staging` / `db:migrate:prod` / `db:verify:prod` switch from `--env-file=secrets/<env>.env` to `powershell -NonInteractive -ExecutionPolicy Bypass -File scripts/with-deploy-creds.ps1 <env> ...`. The `-NonInteractive` flag is required so SecretStore's master-password prompt throws rather than hangs; operator must `Set-SecretStoreConfiguration -Authentication None -Interaction None` once for the vault file to be openable without prompt (Windows DPAPI provides the encryption-at-rest). Pattern 3 supersedes Pattern 1 for staging+prod; dev still uses `.env.local` directly.

- **Deploy migration 2026-05-23T03:36Z (staging only — no-op confirmation)**: Pattern 3 wrapper end-to-end validated against `policypilot-staging` (project_ref `qwtbbbjbxffioeeazxrw`, region us-east-1, pooler `aws-1-us-east-1.pooler.supabase.com`). `pnpm db:verify:staging` exit 0: `OK — deploy schema audit passed: 8 migrations applied, 11 tenant-scoped tables (RLS + policy + 4 GRANTs each), 2 service-role tables (no RLS), Phase 4 column shape + partial-unique index present, Phase 3 G3 + Phase 4 unique constraints present.` `pnpm db:migrate:staging` exit 0 as no-op: `[✓] migrations applied successfully` with NOTICEs confirming `schema "drizzle" already exists, skipping` + `relation "__drizzle_migrations" already exists, skipping`. **Surprising discovery**: staging DB was ALREADY at journal entry 0007 before this session — neither this session nor PR #18 ran migrations against it. Either project pre-existed in migrated state OR operator applied migrations manually pre-session. Either way: schema invariants verified by `scripts/check-deploy-schema.ts`, including destructive 0007 (`DROP COLUMN tokens_used`) which is therefore already applied to staging. Operator: matthewutt. Migration types: 0005/0006 additive + 0007 destructive (operator-approved 2026-05-21 per pre-paying-customer status, ratified retroactively). Significant in-session debug burn (~6 pre-flight attempts): paste truncation in Read-Host (resolved via Get-Credential GUI dialog), SecretStore master-password locking the vault for non-interactive PS spawn (resolved via Authentication=None DPAPI), Supabase pooler password-propagation delay of ~3-5 min after rotation (resolved by waiting + re-test). 5 consecutive verifies succeeded after propagation window closed; Pattern 3 architecture proven stable. Prod migration ready when prod Supabase project is created and `Set-Secret -Name PolicyPilotProdDB` + `deploy-config.json prod.user` populated.

- **Resume 2026-05-23T~06:30Z (RLS perf + pooler-auth gate shipped to PR #20)**: Session resumed via `/gsd-resume-work` after the 2026-05-23 interstitial deploy-prep session left 9 files uncommitted on `chore/state-pattern-3-staging-deploy`. Operator picked "Commit + ship as PR off current branch". Applied the session report's suggested 2-commit split: **Commit A `b06f102`** `feat(ops): wait-pooler-auth gate + runbook rotation section` (`scripts/wait-pooler-auth.ts` +257 / `package.json` +3 scripts / `docs/runbooks/deploy-migrations.md` +~130 lines rotation section + 28P01 troubleshooting + 0008/0009 register rows / `.gitignore` +`.planning/reports/` for ephemeral session reports); **Commit B `cda4109`** `feat(db): 0008 RLS subquery-wrap + 0009 btree(org_id) indexes` (`drizzle/0008_rls_subquery_wrap.sql` 79 lines / `drizzle/0009_org_id_indexes.sql` 60 lines / `drizzle/meta/0008_snapshot.json` + `0009_snapshot.json` drizzle-kit-generated / `drizzle/meta/_journal.json` +2 entries / `lib/db/schema.ts` +index import + 9 `.index('<table>_org_id_idx')` calls). `tsc --noEmit` exit 0 on every commit boundary. Branch now 3 commits ahead of `main` (`884f6ed` Pattern 3 + `b06f102` ops + `cda4109` db); pushed to `origin/chore/state-pattern-3-staging-deploy`. **PR #20** title + body updated to capture the expanded scope (Pattern 3 + wait-pooler-auth + 0008/0009 RLS perf). **CR re-review in flight** against new HEAD `cda4109` (CodeRabbit status PENDING since 2026-05-23T06:28:28Z); prior CR CHANGES_REQUESTED on `884f6ed` (2026-05-23T03:50:55Z) raised 2 findings on Pattern 3 — still unaddressed: (1) `scripts/store-deploy-password.ps1` line 97 hardcodes `"staging"` instead of `$Environment`; (2) `scripts/with-deploy-creds.ps1` line 104-107 calls `Get-Secret` without `-Vault` parameter. Operator decides whether to address in this PR or split to follow-up. **No code/test changes uncommitted; no blockers; no in-flight agents.** Empirical validation from prior session preserved in commit bodies (0008: 11→0 unwrapped policies, EXPLAIN shows `InitPlan 1` node; 0009: 9 expected btree indexes present; wait-pooler-auth: cleared 28P01 in 2m1s during real staging migrate→verify gap). **Next**: monitor CR re-review verdict on PR #20; once green or accepted, squash-merge to `main`; then either Phase 5 entry (`/gsd-spec-phase 5` Employee Portal — Wave-1 parallel candidate per ADR-029) or close out the 2 CR findings as a follow-up PR.

- **2026-05-23 Phase 5 spec + discuss session (~1h35m, gsd/phase-5-employee-portal branch off `c50b317`)**: First session on the Phase 5 branch. Pipeline: `/gsd-spec-phase 5 --auto` → `/gsd-discuss-phase 5 --full --power` → ultrathink tightening pass → finalize. Six commits on the branch (HEAD `d03e2ae`, 6 ahead of `main`): (1) `1061a7a` SPEC.md initial (5 requirements, ambiguity 0.183, `--auto` skipped interview); (2) `afaf2f2` QUESTIONS.json + HTML generated (23 questions across 8 sections — Dashboard Query / Ack Server Action / Re-Ack UI / Admin Bulk-Assign / Append-Only Gate / Tests-Verify / Q&A Reconciliation / Schema Safety); (3) `86c2b82` HTML boot-path fix (bypass fetch fallback for file:// edge cases — JSDOM verified 8 sections + 23 cards render); (4) `af0fdf5` Operator answers applied — exported PDF (1.5MB, 14 pages), File System Access API save didn't land on disk, answers extracted via pymupdf page rendering + visual radio-button inspection, all 23 = recommended (a), coupling pairs Q-05↔Q-23 + Q-13↔Q-22 verified consistent; (5) `1254257` Q-21 corrected (a)→(c) per chat message "no 21 was c" + SPEC.md amended (R-6 Q&A surface added, In/Out-of-Scope rebalanced, ambiguity re-scored 0.183→0.162, AC count 12→13); (6) `d03e2ae` CONTEXT.md + DISCUSSION-LOG.md finalized — 30 locked decisions (23 power-mode + 7 ultrathink T-1..T-7 incl. operator OVERRIDE on T-2 picking 4c "server-tracked Q&A→citation grants" — most rigorous path, adds new qa_citation_grants table + repository + grant-issue step in askQuestion orchestrator + access-aware /my-policies/[id] rendering). Two ASK-FIRST schema migrations operator-approved (additive UNIQUE constraints via Q-22+Q-23, new table via T-2(4c)) — pre-paying-customer status basis; combined into drizzle/0010_phase5_uniques.sql + drizzle/0011_qa_citation_grants.sql per T-6 + T-2(4c). Locked-decisions count UNCHANGED at 29 (no new ADRs ratified — ADR-029 parallelism rationale moot post-Phase-4 ship 2026-05-22 but doesn't need amendment; R-6 is SPEC requirement not architectural lock). Two pause-work --report cycles run mid-session: (a) `.planning/reports/20260523-phase-5-discuss-session-report.md` (post-CONTEXT pre-DISCUSSION-LOG); (b) re-run after finalize for clean handoff. Outstanding: `.planning/phases/05-employee-portal/04-QUESTIONS.json` stray Phase-4 copy held open by external tool (Windows file lock; non-blocking for Phase 5 work — operator close + delete when convenient). **Next**: `/clear` then `/gsd-plan-phase 5` consumes CONTEXT.md (canonical input) + SPEC.md (locked requirements) to produce wave-grouped plans. Per CONTEXT, Phase 5 net-new files: 6 lib/, 4 app/(employee), 1 admin Server Action method extension, 2 components, 2 migrations, 1 repository, 1 CI gate (+self-test mode), 6 co-located vitest files, 1 integration script. Small touch to app/api/ai/qa/route.ts to extract askQuestion helper into lib/ai/qa.ts (T-3=A).

- **Resume 2026-05-23T14:30Z (post Phase 5 spec+discuss pause)**: Session resumed via `/gsd-resume-work` in fresh context after `/clear`. No HANDOFF.json, no `.continue-here.md`, no incomplete plans, no in-flight agents. Branch `gsd/phase-5-employee-portal` @ `8670a59` (7 ahead of `main` @ `c50b317`; base = `c50b317` so no rebase needed). Phase 5 artifacts all present: 05-SPEC.md (6 requirements R-1..R-6, ambiguity 0.162, 13 acceptance criteria), 05-CONTEXT.md (30 locked decisions D-01..D-30), 05-DISCUSSION-LOG.md, plus 05-QUESTIONS.json + .html scaffolding. **Inter-session activity on `main` since STATE last bookkept**: PR #20 `9f4d8eb` (deploy-prep-v2 Pattern 3 + RLS perf — MERGED), PR #22 `c50b317` (Phase 4 post-ship audit cascade re-targeted off main — MERGED). No open PRs. Working tree clean except for the same pre-existing `04-QUESTIONS.json` stray Phase-4 copy + the standard untracked sidecar dirs (`.agents/`, `.claude/`, `.mcp.json`, `.wiki/`, `audit-cache/`, `localhost-*.log`). **Next**: `/gsd-plan-phase 5` consumes CONTEXT.md + SPEC.md to produce wave-grouped plans. Per STATE planner notes: 6 lib/ files, 4 app/(employee) routes, 1 admin Server Action method extension, 2 components, 2 migrations (0010 + 0011), 1 repository, 1 CI gate, 6 co-located vitests, 1 integration script, plus the small `app/api/ai/qa/route.ts` extract into `lib/ai/qa.ts` per T-3=A.

- **2026-05-23 Phase 5 EAPI Critical Path refinement session (post-advisor pass)**: Operator chose Option A from the EAPI advisor pass conclusion — "Apply EAPI Critical Path findings (H-1/H-4/H-5/H-6), then execute in next session." Refinements committed @ `1d78487` `docs(phase-5): apply EAPI advisor Critical Path findings (H-1/H-4/H-5/H-6)`. **H-1 ts-morph raw-SQL bypass closure**: verified Phase 2 `0001_rls_policies.sql:69` GRANTs UPDATE+DELETE on acknowledgments to authenticated role (DB layer does NOT prevent raw SQL bypass); Plan 05-08 strengthened with Sub-pass 2 regex scan for `sql\`UPDATE/DELETE acknowledgments...\`` tagged template literals; negative-control fixture extended with second violation function; self-test now requires ≥2 violations with both hasDrizzle && hasRawSql flags. Deferred ASK-FIRST item: new `drizzle/0012_acknowledgments_revoke_mutation.sql` to REVOKE UPDATE+DELETE at DB layer for defense-in-depth — operator approval required (CLAUDE.md destructive migration discipline); documented in Plan 05-08 `<deferred>` block. **H-4 API-SPEC.md contract drift closure**: `reference/API-SPEC.md` POST /api/ai/qa response shape now documents the additive `accessibility: 'full' | 'tldr-only'` field per D-27a + backward-compatibility note; Plan 05-04 added `reference/API-SPEC.md` to `files_modified` + Task 3 acceptance criteria require grep checks for accessibility/additive/D-27a markers. **H-5 pure-hallucination negative test closure**: Plan 05-09 R-6 describe block adds 'H-5' test case asserting a syntactically-valid UUID-v4 not present in any policies row is (a) stripped from result.citations by parseQaResponse validIds filter and (b) writes zero rows to qa_citation_grants — dual-layer negative assertion closes RESEARCH gap-3 at runtime. **H-6 runtime cross-org real-UUID negative test closure**: Plan 05-09 R-6 describe block adds 'H-6' test case seeding REAL org-B policy UUID (P_B_real) and asserting U6 (in org A) submitting a question with that UUID in the mocked Anthropic response gets (a) zero citations referencing the foreign UUID, (b) zero qa_citation_grants rows for U6+P_B_real, (c) zero qa_citation_grants rows for P_B_real across the entire grants table — triple-assertion runtime complement to Plan 05-04's structural `grep -c "validIds = new Set"` check. **Carry-forward items**: H-2 (same-org grant-scope formalization → CONTEXT.md `<deferred>` + 05-04 threat_model edit during execute); H-3 (UUID-global-uniqueness ADR-030 OR add org_id to D-06 UNIQUE — operator decision during execute); M-1 (x-forwarded-for trust boundary ADR); M-2 (CSRF protection documented in threat models); M-3 (bulkAssignToDepartmentAction return shape); M-4 (shared `lib/actions/types.ts` ActionResult<T> discriminated union); M-5 (priorAck.policyVersionId <> pv.id NULL-safe form verification in Plan 05-03 SQL); M-6 (department-less user + assignment-revocation-mid-session fixtures in Plan 05-09); A-1 (askQuestion streaming-deferral NOTE comment); A-2 (scripts/check-qa-contract.ts API-SPEC schema validation). 13 LOW findings are CONFIRMED-CORRECT (no action). **Critical Path applied**: 4/6 HIGH closed in-plan; 2/6 carry forward; 6 MED + 2 ADV carry forward. **Branch state**: `gsd/phase-5-employee-portal` @ `1d78487` (9 ahead of `main` @ `c50b317`). No HANDOFF.json, no `.continue-here.md`, no incomplete plans. **Next session**: `/gsd-execute-phase 5` — Wave 1 (05-01 schema+migrations BLOCKING + 05-02 errors + 05-07 AckStatusBadge) executes first. Pre-execute checklist for operator: (1) consider whether to apply H-3 ADR-030 inline (UUID-global-uniqueness lock) or defer to a follow-up; (2) review Plan 05-08 `<deferred>` REVOKE migration recommendation and decide approve/defer; (3) verify SF-WHSEC-1 (Clerk webhook secret rotation) status before any webhook-touching plan executes — pre-existing carry-forward unchanged by Phase 5.

- **2026-05-24 Phase 5 Wave 1 closeout — Plan 05-07 AckStatusBadge (~4min, sequential executor)**: Final Wave 1 plan landed @ commit `a685d69` `feat(05-07): add AckStatusBadge component per D-11`. Single new file `components/policy/AckStatusBadge.tsx` (58 lines), Server Component, exhaustive switch on D-04 ackState enum. D-11 className-override pattern preserved — `components/ui/badge.tsx` UNCHANGED (no new CVA variant; verified via `git diff` returning empty). Three branches: `'none'` → null, `'stale'` → amber outline Badge with className override mirroring PolicyStatusBadge.tsx:35-44 `archived` branch's outline+override pattern, `'current'` → green ✓ inline-flex span with formatted ackedAt (en-US locale per CONTEXT discretion). `pnpm tsc --noEmit` exits 0. All 11 PLAN.md acceptance criteria pass. Zero deviations, zero new packages, zero `any` types. **Wave 1 COMPLETE 3/3**: 05-01 (schema migrations, c1d57f5 family) + 05-02 (PolicyDomainError hierarchy @ 2456b75) + 05-07 (AckStatusBadge @ a685d69). Branch `gsd/phase-5-employee-portal` now 10 ahead of `main` @ `c50b317`. **Next session**: orchestrator advances to Wave 2 — Plan 05-03 (repositories: Acknowledgments.record + PolicyAssignments.create fills + Policies.listAssignedAndPublishedForUser + qa_citation_grants new repo) and Plan 05-04 (orchestrators: lib/policies/acknowledgment.ts + lib/ai/qa.ts extraction). Both can run in parallel — zero file overlap. Downstream consumers ready: AckStatusBadge available for Plan 05-05 (employee routes) to render in `/my-policies/page.tsx` list + `/my-policies/[id]/page.tsx` detail pages; D-04 enum contract is canonical in Plan 05-03's `Policies.listAssignedAndPublishedForUser` return type — type-safe propagation from SQL → Server Component prop.

- **2026-05-24 Phase 5 Wave 3 partial — Plan 05-05 Employee Routes (~45min, sequential executor)**: Three task commits on `gsd/phase-5-employee-portal`: (1) `6883e84` `feat(05-05): add acknowledgePolicyAction + Client AcknowledgeButton` — `app/(employee)/my-policies/[id]/actions.ts` (~99 lines, Server Action with Zod policyId brand parse + headers().get('x-forwarded-for')?.split(',')[0]?.trim() ?? null first-hop IP capture per D-05 + typed-error mapping for PolicyArchivedError → POLICY_ARCHIVED / PolicyNotAssignedError → POLICY_NOT_ASSIGNED / PolicyNotFoundError → POLICY_NOT_FOUND + revalidatePath('/my-policies') + revalidatePath(`/my-policies/${parsed.data.policyId}`) OUTSIDE try/catch per D-10c) + `components/employee/AcknowledgeButton.tsx` (~62 lines, 'use client' + useActionState<AcknowledgeActionState | undefined, FormData> + renders success branch from `state.ackedAt` formState — NOT isPending — per RESEARCH Pitfall 5 / Next.js #82289 workaround; ackState prop union narrowed to 'none' | 'stale' since parent gates render on `!== 'current'`); (2) `0f9b6af` `feat(05-05): replace employee-portal stub with real dashboard + detail surface` — three Server Components: `app/(employee)/layout.tsx` (~48 lines, minimal force-dynamic shell, await getOrgContext() gate, NO admin role narrowing — admins can also be policy-assigned per SPEC In-Scope; mirror of admin layout MINUS sidebar+org-switcher; force-dynamic per Phase 4 PR #19 prerender lesson), `app/(employee)/my-policies/page.tsx` (~98 lines, wholesale replacement of 03-G3 T9 stub Card — calls Policies.listAssignedAndPublishedForUser(s, ctx.userId) inside withOrgScope per ADR-025, header with "My Policies" + "Ask the AI" Link to /my-policies/ask per D-24, empty-state Card with EXACT D-04a copy "No policies assigned yet — contact your administrator." Unicode U+2014, non-empty branch renders rows as Cards-inside-Links with AckStatusBadge per row), `app/(employee)/my-policies/[id]/page.tsx` (~163 lines, D-27 3-branch access-aware Server Component — PolicyIdSchema.safeParse at URL boundary returns notFound() on malformed UUID per D-10 "advertise nothing" / CR-PR3-#23 spirit; ALL 4 sub-queries inside ONE withOrgScope closure returning discriminated AccessResult union `full | tldr | notfound` for atomic RLS evaluation across sub-queries; Branch A: assignedRow from listAssignedAndPublishedForUser found → load full policy via Policies.findById defense-in-depth + render PolicyView + AckStatusBadge + AcknowledgeButton gated on ackState !== 'current'; Branch B: QaCitationGrants.hasGrant returns true → load policy + assert status='published' → render amber-banner Card with EXACT D-27 copy "This policy was cited in your AI answer but isn't assigned to you. Summary only — contact your admin for full access." Unicode U+2014 + tldr-only render with `policies.tldrSummary` text; Branch C: notFound(); contentJson cast `as JSONContent` at PolicyView boundary — safe per Phase 3 ADR-005 server-controlled jsonb); (3) `c4ddb01` `feat(05-05): add R-6 Ask the AI surface (page + Server Action + Client form)` — three new files: `app/(employee)/my-policies/ask/page.tsx` (~24 lines, R-6 RSC shell with defense-in-depth getOrgContext + AskQuestionForm + back-nav Link), `app/(employee)/my-policies/ask/actions.ts` (~80 lines, askQuestionAction Server Action wrapping lib/ai/qa.ts::askQuestion, Zod z.string().min(1).max(2000) char cap per Phase 4 D-42 attack-surface limit, Anthropic.APIError catch → semantic 503-equivalent inline error "AI service temporarily unavailable. Please try again." matching HTTP route /api/ai/qa contract intent; NO revalidatePath since Q&A is non-mutating), `components/employee/AskQuestionForm.tsx` (~108 lines, 'use client' + useActionState<AskActionState | undefined, FormData> + shadcn Textarea for question input + Cards rendering answer + Sources list with citation Links — italic+underline+text-muted-foreground className for `accessibility === 'tldr-only'` per D-27a visual hint, plain underline for `accessibility === 'full'`; "(summary only)" suffix span for tldr-only). `pnpm tsc --noEmit` exits 0 on every commit boundary per CLAUDE.md ALWAYS rule #1. All 8 plan-level grep-asserted acceptance criteria pass: layout has force-dynamic + getOrgContext + ZERO requireAdmin (after comment edit avoiding the literal token); page exact D-04a copy present + stub copy "Employee portal — coming soon" GONE (grep returns 0); detail PolicyIdSchema.safeParse + multiple notFound() calls + exact D-27 banner + PolicyView import + AckStatusBadge usage + AcknowledgeButton usage; action 'use server' + x-forwarded-for + split(',')[0] first-hop + 3 typed-error classes mapped to code discriminants + 2 revalidatePath calls outside try/catch (awk verified zero inside); button 'use client' + useActionState; ask actions 'use server' + askQuestion(ctx, ...) + Anthropic.APIError + z.string().min(1).max(2000); form 'use client' + useActionState + accessibility flag handling + italic className. **Task-commit ordering inversion** (declared Task 1 → Task 2 → Task 3 in plan; executed Task 2 @ 6883e84 → Task 1 @ 0f9b6af → Task 3 @ c4ddb01): Task 1's `[id]/page.tsx` imports `AcknowledgeButton` from `@/components/employee/AcknowledgeButton` which lives in Task 2's file set. Committing Task 1 first would fail `pnpm tsc --noEmit` with TS2307 (cannot find module). The reorder respects per-task atomicity intent of the plan while keeping every commit tsc-green per CLAUDE.md ALWAYS rule #1; plan logical task IDs are preserved in commit messages so SUMMARY.md traceability is intact. Zero deviations from plan content/scope/files. Zero new packages. Zero `any` types. **components/employee/ directory created** for employee-only Client Components (vs components/policy/ shared admin+employee namespace) — explicit "Claude's Discretion" call per CONTEXT.md, operator may reorganize later. **Wave 3 PARTIAL 1/2**: 05-05 ✓; 05-06 (admin bulk-assign + PolicyAssignmentsPanel + Departments.listAll listAll method) pending. Branch `gsd/phase-5-employee-portal` now 18 commits ahead of `main` @ `c50b317` (15 prior + 3 task commits; SUMMARY + STATE + ROADMAP bundle commit pending after this entry). Plan 05-05's downstream consumers are now unblocked: Plan 05-08 ci-gates (scripts/check-acknowledgment-immutability.ts will pass — actions.ts has zero .update/.delete on acknowledgments), Plan 05-09 integration-test (will create app/(employee)/my-policies/[id]/actions.test.ts + ask/actions.test.ts; AcknowledgeActionState + AskActionState types are exported from the action files for test-fixture imports). **Next session**: Plan 05-06 admin bulk-assign — Zero file overlap with 05-05 (app/(admin)/policies/[id]/actions.ts edit + new components/admin/PolicyAssignmentsPanel.tsx + lib/db/repositories/departments.ts listAll fill; bulkAssignToDepartmentAction extends the existing Phase 3 actions.ts file). Then Wave 4 (05-08 CI gates ‖ 05-09 integration test) then Wave 5 (05-10 verify chain + operator UAT).

- **2026-05-23 Phase 5 Wave 2 closeout — Plan 05-04 Orchestrators (~28min, sequential executor)**: Three task commits on `gsd/phase-5-employee-portal`: (1) `da235a5` `feat(05-04): add recordAcknowledgment orchestrator per D-10a + D-07 + D-08` — `lib/policies/acknowledgment.ts` (180 lines) single withOrgScope tx wrapping Policies.findById + cheap user-dept sub-query + PolicyAssignments.listForPolicy + PolicyVersions.findByVersionNumber + Acknowledgments.record; throws PolicyArchivedError per D-07 + PolicyNotAssignedError per D-08 + PolicyNotFoundError on missing/RLS-denied row; D-10 silent-success handling per RESEARCH Pitfall 8 (empty RETURNING returns existing-row timestamp); orchestrator receives ctx from caller so Server Action's outer try/catch can split BootstrapError vs PolicyDomainError per Phase 3 D-09 — NO internal context-bootstrap call. (2) `623f21e` `feat(05-04): extract askQuestion orchestrator + add D-26 grants + D-27a flag` — `lib/ai/qa.ts` (197 lines) extracted askQuestion(ctx, question) verbatim from app/api/ai/qa/route.ts:41-115 per D-25 / T-3=A; preserves Phase 4 invariants D-41 (same-closure validIds — SP-1 cross-org defense), D-33c (LONG_CACHE-first then EPHEMERAL — Anthropic 400 on inverse), D-40 (cold-miss observability), WARNING-4 (result: rawText audit row); ADD D-26 grant UPSERT loop iterating parsed.citations (POST-validIds-filter per RESEARCH gap-3 — NOT raw fence; iterating raw fence would pollute qa_citation_grants with hallucinated UUID rows); ADD D-27a accessibility annotation via single org-scoped Policies.listAssignedAndPublishedForUser query (UI hint only; security boundary at /my-policies/[id] page per Plan 05-05); cross-org isolation locked at 3 points inside same withOrgScope (validIds same-closure + grant UPSERT iterates filtered list + assignedIds query RLS-scoped). (3) `8faf5e6` `refactor(05-04): slim qa route to thin wrapper + API-SPEC.md H-4 closure` — `app/api/ai/qa/route.ts` 147→49 lines (67% reduction; ≤50 ceiling per Task 3 acceptance); HTTP contract UNCHANGED modulo additive accessibility field; preserved verbatim D-37 auth-outside-try + D-42 ZodError 400 + D-36 PII-safe log + SPEC R7 503+Retry-After:30. **H-4 EAPI Critical Path finding CLOSED** — `reference/API-SPEC.md` amended with additive accessibility field documentation + canonical Phase 4 citation-shape line preserved verbatim for `scripts/check-artifacts.ts:1909` regex anchor + explicit backward-compatibility note. `pnpm verify:phase-4` exits 0 (393/393 artifacts + 21/21 vitest test files + 1/1 integration test file); previously 392/393 due to Plan 05-03 carry-forward citation-shape check — now resolved by Task 3 API-SPEC.md update. **1 Rule-1 deviation per plan `<verification>` fallback**: extended `app/api/ai/qa/route.test.ts` (8 tests) + `scripts/check-ai-layer.test.ts` (SP-1 cross-org integration test) with mocks for the two new Phase 5 askQuestion dependencies (Policies.listAssignedAndPublishedForUser + QaCitationGrants.upsert/hasGrant/listForUser routed through outer postgres-js tx in integration test); citation-shape assertions updated to accept additive accessibility field; new SP-1 unit assertion that QaCitationGrants.upsert is called exactly once per validIds-filtered citation (regression-locks RESEARCH gap-3 — if grant-UPSERT ever iterates raw fence again, this assertion fails AND the integration test would surface a foreign-org garbage row). Zero new packages; zero `any` types; tsc clean. **Wave 2 COMPLETE 2/2** (05-03 ✓ + 05-04 ✓). Branch now 15 ahead of `main` @ `c50b317` (3 task commits + 1 SUMMARY commit pending; this STATE update + ROADMAP update will bundle with the SUMMARY commit). **Next session**: orchestrator advances to Wave 3 — Plan 05-05 (employee routes: app/(employee)/layout.tsx + my-policies/page.tsx + [id]/page.tsx D-27 3-branch access + acknowledgePolicyAction calling recordAcknowledgment + my-policies/ask + askQuestionAction calling askQuestion + employee components) ‖ Plan 05-06 (admin bulk-assign + PolicyAssignmentsPanel + Departments.listAll). Both can run in parallel — zero file overlap between `app/(employee)/**` and `app/(admin)/**`. Downstream consumers ready: recordAcknowledgment for Server Action wiring; askQuestion for direct Server Action call (no HTTP); QaCitationGrants.hasGrant for D-27 page handler 3-branch decision.

- **Resume 2026-05-24T07:30Z (post Phase 5 ship-pending pause)**: Session resumed via `/gsd-resume-work` in fresh context after `/clear`. No HANDOFF.json, no `.continue-here.md`, no incomplete plans, no in-flight agents. Branch `gsd/phase-5-employee-portal` @ `44d4b16` (54 ahead of `main` @ `c50b317`). PR #27 (Phase 5: Employee Portal) was opened 2026-05-24T07:24:40Z by ship commit `6bf720a` and is currently OPEN — `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE` (CI in flight), `reviewDecision=` empty (no reviews yet), `statusCheckRollup=PENDING`. Latest commit `44d4b16` `docs(codebase): map post-Phase-5 codebase via 4 parallel mapper agents`. Phase 5 status: 10/10 plans complete; `verify:phase-5` exits 0 (92s end-to-end, 4/4 chain steps green); UAT walked interactively via `/chrome` 18 PASS + 1 PASS-with-finding; 2 latent fast-follow bugs caught + fixed inline during UAT (`afb7693` Phase 3 `editPublished()` DUP-VN-2 + `6ac3e4e` Phase 4 qa-parser QA-PARSER-FENCE whitespace tolerance); 40/40 STRIDE threats CLOSED + Nyquist clean; post-Phase-5 codebase mapped via 4 parallel mapper agents. Operator chose **Run `/gsd-verify-work` for Phase 5** — goal-backward verification of Phase 5 SC #1-#6 belt-and-suspenders on top of UAT + verify:phase-5, before PR #27 squash-merge.

- **2026-05-23 Phase 5 plan-phase session (--research --auto, ultrathink)**: Operator invoked `/gsd-plan-phase 5 --research --auto` after the spec+discuss session, with two operator extensions: (a) `ultrathink` prefix for deep reasoning, (b) "run Enterprise API Architect Advisor after plan checker" as a post-pipeline rigor pass. Operator-acknowledged that research was "likely overkill — CONTEXT.md already locks 30 decisions and the per-file delta" but ran it anyway for belt-and-suspenders. **Pipeline executed end-to-end**: (1) `gsd-phase-researcher` (sonnet, ~10min, 247K tokens) → `05-RESEARCH.md` with 6 mechanical gaps surfaced (RLS subquery-wrap shape for 0011 vs D-29 baseline, `check-rls.ts` TENANT_TABLES missing `qa_citation_grants`, grant-iteration must use `parseQaResponse`-validated citations not raw fence JSON, `check-policy-id-brand.ts` REPO_TARGETS/OBJECT_FIELD_TARGETS needs Phase 5 extension, React 19 `useActionState` + `revalidatePath` `isPending` pitfall per Next.js #82289, `ON CONFLICT DO NOTHING` empty `RETURNING` handling); confirmed 3 operator-flagged hypotheticals already closed (R-6 cross-org leak by Phase 4 `qa-parser.ts:54` `validIds.has(c.id)`, dual-assignment by D-01 SELECT DISTINCT, D-04 two-join LEFT JOIN performance by D-06 auto-index). (2) `05-VALIDATION.md` written from template ($HOME/.claude/get-shit-done/templates/VALIDATION.md) — Nyquist Per-Task Verification Map skeleton + 15 Wave-0 requirements + 4 manual-only verifications. (3) UI-SPEC gate INTENTIONALLY SKIPPED — operator-intent override based on CONTEXT.md `<specifics>` locking all novel Phase 5 visual decisions (D-04a empty-state copy, D-11 AckStatusBadge variants, D-13 panel placement, D-27 TL;DR banner exact copy, D-27a citation accessibility flag) + Phase 5 reusing Phase 3 PolicyView + PolicyStatusBadge verbatim with zero novel UI primitives. Rationale committed inline to planner prompt. (4) `gsd-pattern-mapper` (sonnet, ~10min, 211K tokens) → `05-PATTERNS.md` classifying 36 files with 35/36 codebase analogs + concrete code excerpts; only `tests/fixtures/ack-mutation-attempt.ts` is novel (D-20 negative-control pattern). (5) `gsd-planner` (opus, ~35min, 398K tokens) → 10 PLAN.md files committed @ `48d5398` `docs(phase-5): create phase plan — 10 plans across 5 waves`. Wave structure: W1 (05-01 schema+migrations BLOCKING + 05-02 errors + 05-07 AckStatusBadge — planner hoisted 05-07 to W1 from suggested W3 since component has zero deps) / W2 (05-03 repositories + 05-04 orchestrators — parallel) / W3 (05-05 employee-routes + 05-06 admin-bulk-assign — parallel; planner collapsed suggested W3+W4 into single W3 since employee/admin namespaces have zero file overlap) / W4 (05-08 ci-gates + 05-09 integration-test — parallel) / W5 (05-10 verify-chain-uat — operator UAT checkpoint). (6) `gsd-plan-checker` (sonnet, ~2min, 253K tokens) → `## VERIFICATION PASSED` with 2 non-blocking warnings: Plan 05-05 scope borderline (8 files / 3 tasks but coherent grouping; splitting would create artificial deps), VALIDATION.md Per-Task Verification Map placeholder row (cosmetic; <automated> blocks in PLAN.md files intact). All 6 SPEC requirements (R-1..R-6) covered. All 30 CONTEXT decisions covered (including all sub-suffixes D-04a, D-10a/b/c, D-23a, D-27a). All 6 RESEARCH mechanical gaps closed. R-5 three-layer defense intact (type system D-07 + ts-morph D-18 + negative-control fixture D-20). 36+ STRIDE threats catalogued across plans. Schema migration [BLOCKING] task present in 05-01 per CLAUDE.md Database Migration Discipline (dev + test only — staging+prod is separate operator-gated step). `verify:phase-5` chain wired verbatim per D-23. Citation-shape additive `accessibility` field flagged for potential Phase 4 vitest adjustment (additive, non-breaking). **Coverage gates** (post-checker): `check.decision-coverage-plan` SDK call returned `skipped: no trackable decisions` — D-NN parser limitation; checker agent already validated all 30 covered. `state.planned-phase` + `roadmap.annotate-dependencies` SDK calls ran (ROADMAP already had wave annotations from planner commit). Locked-decisions count UNCHANGED at 29 (no new ADRs from plan-phase — UI-SPEC skip is operator-intent override, not architectural lock). Subagent token total: ~1.1M across 4 spawns (researcher 247K + pattern-mapper 211K + planner 398K + plan-checker 253K). **Next**: Enterprise API Architect Advisor pass on the 10 PLAN.md files (operator-requested step after plan-checker — focus: BOLA on /my-policies/[id], R-6 Q&A→citation grants AI-native pattern, idempotency on ack/assign Server Actions, append-only audit invariants, R-6 cross-org citation leak boundary verification, server-action signature design); then `/gsd-execute-phase 5`.

- **Resume 2026-05-24 (post-CR-rereview-fix, PR #27 wait state)**: Session resumed via `/gsd-resume-work` in fresh context. No HANDOFF.json, no `.continue-here.md`, no incomplete plans, no in-flight agents. All 10 Phase 5 plan SUMMARYs present (the heuristic flag on 4 plans is a false positive — short-form `05-04-SUMMARY.md` / `05-05-SUMMARY.md` / `05-09-SUMMARY.md` / `05-10-SUMMARY.md` vs long-form `05-NN-<slug>-SUMMARY.md`). Branch `gsd/phase-5-employee-portal` @ `e997e70` (57 ahead of `main` @ `c50b317`); clean tree except untracked `Designprototypes/ookingforsomethingmoreaggressiveandcutting....html` (operator design exploration, non-GSD, leave alone). **PR #27 live state on resume**: `state=OPEN`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`, `reviewDecision=CHANGES_REQUESTED` (CR verdict on prior HEAD `44d4b16`), `statusCheckRollup` CodeRabbit `PENDING` since 2026-05-24T15:11:07Z (re-reviewing `e997e70`). CR's 3 actionable findings from 07:57Z review all addressed by the latest commit: (1) `05-01-schema-migrations-SUMMARY.md` credential-incident operational details (env-file paths / password segments / run commands) redacted; (2) `05-10-SUMMARY.md` Task 2 contradiction (line 44 pending vs line 82 approved) reconciled to approved; (3) `05-QUESTIONS.html` save targets fixed from Phase-4 `04-QUESTIONS.json` to `05-QUESTIONS.json` at lines 1277/1298/1309. Wait state — next external signal is CR's re-review verdict on `e997e70`. **Operator chose**: start Phase 6 (Billing) spec in parallel — Wave 2 candidate per ADR-029 (Phase 6 depends on Phase 4 which SHIPPED to main, NOT on Phase 5). Phase 6 branch `gsd/phase-6-billing` to be cut off `main` @ `c50b317`, independent of PR #27 lifecycle. Phase 6 SC summary per ROADMAP §144-156: 6 Stripe products in Dashboard, 5-event webhook (`checkout.session.completed` + `invoice.paid` + `invoice.payment_failed` + `customer.subscription.deleted` + `customer.subscription.updated`) with raw-body signature verify + `stripe_events` idempotency, full checkout→webhook→DB-sync→tier-gate cycle surviving simulated `invoice.paid` renewal, `checkTierLimit(orgId, feature)` returning `{allowed, limit, current}` + Starter org getting `{error:'tier_limit_exceeded', upgradeUrl:'/pricing'}` 403 on Growth-only features, Customer Portal link from admin settings. Anchoring decisions ADR-013/017/020. **Next**: `/gsd-spec-phase 6` to clarify WHAT before discuss/plan. Carry-forward unchanged: SF-WHSEC-1 (Clerk webhook secret pasted in chat during Phase 2 — rotate via Svix Dashboard before any Phase 7 webhook live-smoke).

- **2026-05-29 Phase 6 plan-phase session (Claude Code, opus 4.8, xhigh; resume from paused `ready_for_planning`)**: Resumed the paused `gsd/phase-6-billing` branch (spec+discuss were already complete and operator-approved). **Rebased the branch onto `main` `af01f0a`** — conflict-free (the branch's 6 commits touched only `.planning/HANDOFF.json` + `06-billing/*`, disjoint from PR #30/#31); the branch now carries the PR #30 operating layer + PR #31 state refresh (new tips `3f3d9ea..49e2a1d`). Ran a read-only pre-plan adversarial critique first → PASS (no Critical/High; surfaced M1 checkout success/cancel URLs, M2 webhook HTTP-status matrix, M3 past_due grace, L1 null-subscription≠trialing, L2 multi-org checkout test, L3 SF-CASCADE-AUDIT stays deferred / no org-delete path, L4 ignore uncommitted session-report). **plan-phase chain**: `gsd-phase-researcher` (sonnet) → `06-RESEARCH.md` (HIGH; `stripe@22.2.0`, migration tag `0012`, all prereqs present) + `06-VALIDATION.md` (nyquist) committed `97ca202`; `gsd-planner` (opus) → 6 plans `06-01..06-06` in 5 waves; `gsd-plan-checker` (sonnet) → **VERIFICATION PASSED** (0 blockers / 0 warnings, all 12 dimensions, nyquist Dimension 8 green). Gates injected + verified present: a `<threat_model>` STRIDE block per plan, a `[BLOCKING] pnpm db:migrate:test` task, Wave-0 operator checkpoints (`pnpm add stripe` + 9 env vars + 6 Stripe products, all `autonomous:false`), and M1–M3/L1–L4. Coverage: REQ-tier-starter/growth/business in every plan; `check.decision-coverage-plan` returned `skipped — no trackable decisions` (the D-NN parser limitation seen in Phase 5; the checker already validated D-01..D-35 coverage). `roadmap.annotate-dependencies` reverted (it mangled `**Plans**` + churned whole-file whitespace) in favor of a surgical Phase-6-only ROADMAP edit. At that time Phase 6 was planned and ready to execute. Consultant delta `ops/deltas/2026-05-29-phase6-plan.md`. **Next**: operator `/clear` then `/gsd:execute-phase 6` (Wave 0 operator checkpoints first). No packages installed, no migration applied, no runtime/billing code written this session. Superseded by the later Phase 6 execution/verifier/UAT entries and the current top-of-file status.

- **2026-05-30 Phase 6 keep-current after verifier green + checkout UAT fix**: At that moment, local branch `gsd/phase-6-billing` was still unpublished. Starting state for this hygiene pass was `b818805` (`docs(ops): add historical forensic realignment brief`) on parent `b92a15f` (`fix(06-billing): allow first checkout for new orgs with trialing seed status`). `b92a15f` fixed the launch-blocking first-checkout bug by requiring a real `stripeCustomerId` before treating `trialing` as an existing subscription; regression coverage aligns the guard with the settings page. `0012_billing_state` is applied to the approved TEST/dev Supabase target; `pnpm db:verify` and `pnpm verify:phase-6` are green. At that snapshot, some test-clock UAT rows were still open; the later UAT completion entry supersedes that status. Ops risks recorded: Stripe app credentials and CLI/login/webhook-secret account differed and local `.env.local` webhook secret was realigned without reproducing secrets; reconcile those accounts before further live webhook testing. Clerk dev org provisioning without a webhook tunnel can still produce `OrgNotProvisionedError`; this is a dev ops/process gap, not a Phase 6 code blocker. Cleanup note: operator may delete the throwaway "Acme Test Co" Clerk org and canceled test subscription. Superseded for current execution guidance by the later UAT completion, PR #32 publication, and PR #32 shipped-state entries.

- **2026-05-30 Phase 6 Stripe test-clock UAT completion**: Live Stripe test-mode UAT rows 9 and 10 are now PASS with masked-only evidence. Row 9 used a true Stripe test-clock renewal and observed a fresh `invoice.paid` event with the app DB remaining Growth/active. Row 10 used a test-clock payment-failure path and observed `invoice.payment_failed` with the app DB remaining Growth/`past_due` at first failure, before any later terminal retry/deletion behavior. The Stripe CLI default profile still targets a different test account than the app test key; local UAT reconciled this by running Stripe CLI/listener commands with `STRIPE_API_KEY` sourced from the app test key and by injecting the active listener webhook secret into the local dev process without printing or committing it. Temporary listener logs that could contain the webhook secret were deleted. This entry captured the pre-publication UAT-complete state and is superseded for current PR guidance by the PR #32 shipped-state entry above.

- **Phase 7 context**: gathered 2026-06-05 via `/gsd-discuss-phase 7` — 13 implementation (HOW) decisions D-01..D-13 at `.planning/phases/07-crons-email/07-CONTEXT.md` (commit `cb1acfd` on `gsd/phase-7-crons-email`; `07-SPEC.md` owns the 10 requirements). Locks: loop-per-org via `withOrgScope` (RLS-enforced); record-then-send at-most-once via the additive `reminder_sends` ledger (migration `0013`, operator pre-approved authoring, dev/TEST-only, operator-signed header still required); cron-only dedup (event types gate on action idempotency); Railway native cron + dependency-free fetch script; shared React Email base layout + typed dispatch map; `review_due`→org admins; `ack_reminder` none+stale >7d; `next_review_date` forward-only on publish; per-org-isolation cron (200+counts, 5xx only on fatal); TEST-DB integration + stub-transport unit tests; bell UX deferred to `/gsd-ui-phase 7`. Resume: `/clear` → `/gsd-plan-phase 7`. NOTE: the top-of-file Current Position / Next action narrative still reads "Phase 7 not started" — left for the operator's deliberate STATE reconciliation (consistent with the s24/s25 practice; branch is unpushed).

---

## Phase Roster

| # | Phase | Requirements | Status |
|---|-------|--------------|--------|
| 1 | Foundation | REQ-product-vision | Complete — 5/5 plans shipped 2026-05-16 (PR #1) |
| 2 | Data Layer | REQ-user-roles, REQ-multi-tenancy | Complete — Plans 02-01..02-06 + 02-07 hotfix shipped 2026-05-17 → 2026-05-18 (7 / 7 plans complete); `pnpm verify:phase-2` exits 0 with 7/7 OK against live TEST DB. Code-review CR-01 (webhook publicMetadata.role mirror) + HI-01 (middleware narrowing parity) closed by Plan 02-07. Blockers: SF-DB-1 CLOSED, SF-DB-2 CLOSED, SF-M4 FULLY CLOSED, CR-01 CLOSED, HI-01 CLOSED. Carry-forward: CR-02 admin-matcher dead code → Phase 3. |
| 3 | Admin UI | REQ-policy-library, REQ-policy-lifecycle, REQ-access-control | Shipped — PR #3 @ `edebab7` (2026-05-20). Fast-follows: PR #5 ADR-026 typed errors @ `bf65712`, PR #7 ADR-027 lookup-scoping @ `c6dca6a`. Audit cascade (security/validation/verification) complete via PR #8/#10/#11 — PR #8 SHIPPED @ `5a57000` (SECURED 67/67 STRIDE); PR #10 + #11 OPEN. VALIDATION-2.7 closure via PR #9 OPEN (10-assertion `check:policies-list-filters`). PR 3.3 ADR-028 PolicyId brand still queued. |
| 4 | AI Layer | REQ-ai-policy-assistant, REQ-ai-usage-rules | **SHIPPED** — PR #15 squash-merged to `main` @ `f8207f4` (2026-05-22; 14/14 plans + UAT 5/5 + gsd-security-auditor 60/60 STRIDE CLOSED + real-key Anthropic smoke verifying SPEC R4 cache mechanics for ~$0.021 spend). Post-merge cleanup PR #17 @ `eda7bb4` (one-shot HANDOFF artifact deletions). Deploy-prep PR in flight (chore/phase-4-deploy-prep) — staging/prod migration infrastructure (verifier, runbook, CI workflow, Vercel hook). Phase 4.5 polish backlog tracked at GitHub issue #16 (~30 deferred items). |
| 5 | Employee Portal | REQ-acknowledgment-tracking, REQ-acknowledgment-rules | **SHIPPED** — PR #27 merged to `main` at `3344847` on 2026-05-27T22:06:16Z; 10/10 plans complete; audit remediation gates closed before merge. |
| 6 | Billing | REQ-tier-starter, REQ-tier-growth, REQ-tier-business | **SHIPPED** — PR #32 squash-merged to `main` at `243067e` on 2026-05-31T22:34:30Z; 6/6 plans committed; local `pnpm db:verify` and pre-merge `pnpm verify:phase-6` green; live Stripe test-mode UAT rows 1-11 PASS; hosted pre-merge PR #32 checks green/acceptable at `1abca44`; post-merge targeted checks PASS. |
| 7 | Crons + Email | REQ-notification-system | Not started |
| 8 | Validation | REQ-compliance-dashboard, REQ-integrations, REQ-acceptance-criteria | Not started |
