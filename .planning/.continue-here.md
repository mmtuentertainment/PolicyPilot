---
context: between-phases
phase: 03-admin-ui (complete)
next_phase: 04-ai-layer
task: phase-4-entry
status: paused_at_phase_boundary
last_updated: 2026-05-21T13:20:49.356Z
main_head: bd2257a
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through actual failure this session. Acknowledge each before proceeding.

- [ ] **CONSTRAINT: Comment-discipline anti-pattern re-fired (self-caught, severity demoted to advisory)** — A draft commit message for `fde9d1b` claimed "Mocked locally by renaming `findById` → `findByIdX`: gate correctly failed" before I'd actually run that experiment. Caught and amended the commit message (with actually-verified `findByIdXXX` output) before pushing. Severity: **advisory** (self-caught + corrected pre-push); the prior `.continue-here.md` (deleted in PR #8) marked the same pattern as **blocking**. The pattern continues to develop as a structural habit, not yet auto-suppressed. **How to apply:** Before writing ANY commit message line that claims "verified by X / tested by Y / mocked by Z", run the actual experiment first; paste the actual output into the message. Treat unverified claims as the same anti-pattern as confidently-wrong comments — they are.

- [ ] **CONSTRAINT: CR auto-fix scope-creep risk (PR #8 incident)** — CR's auto-apply feature on PR #8 commit `687b6d9` ran `pnpm install` as a side-effect, bumping ~30 unrelated `@unrs/resolver-binding-*` and `postcss` transitive deps AND resurrecting a deliberately-deleted `CardAction` component from Plan 01 IN-04. Severity: **blocking**. **How to apply:** Before merging ANY CR auto-fix commit, run `git show <hash> --stat` and inspect every changed file individually. If files outside the review-comment's stated scope are touched, refuse the merge and use Option B surgical revert pattern (additive commit + `git checkout HEAD~1 -- <files>`) instead of force-push reset. The CLAUDE.md ASK-FIRST rule on unauthorized dep bumps applies.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Confidently-wrong / unverified behavioral comment claims | Commit message or inline comment claims an experiment ran or a behavior holds without actually running the check | advisory (self-caught this session) | Before claiming "verified by X", actually run X. Paste the verbatim output into the message. Same anti-pattern that prior session flagged as blocking. |
| CR auto-fix scope creep via side-effect `pnpm install` | CR's auto-apply commits include unrelated lockfile bumps + resurrects deleted components | blocking | Inspect `git show <hash> --stat` before merging ANY auto-fix; Option B surgical revert if scope creep present. CLAUDE.md ASK-FIRST rule covers unauthorized dep bumps. |
| Repository signature drift on branded types | Method renames/removes silently slip past `check-policy-id-brand.ts` if per-method `found` flag absent | advisory (closed) | `scripts/check-policy-id-brand.ts` now tracks per-method presence in `checkObjectLiteralRepo`; CR PR #13 inline finding closed via fde9d1b/9e3d7cd. Pattern is locked. |
| Direct `policies` updates missing `org_id` predicate | Orchestrators called `.update(policies).where(eq(policies.id, ...))` — defense-in-depth held at runtime (RLS + repo) but app-layer predicate was absent in 6 sites | advisory (closed) | All 6 sites in `lib/policies/transitions.ts` now use `and(eq(policies.orgId, s.orgId), eq(policies.id, policyId))` per CR PR #13 outside-diff finding. Future `s.tx.update(<table>).where(...)` calls in orchestrators MUST include the `org_id` predicate per CLAUDE.md ALWAYS rule. |

<current_state>
**Phase 3 (Admin UI) FULLY CLOSED.** main is at `bd2257a` (PR #13 squash). All 3 fast-follows shipped (PR #5/#7/#13), all 3 audit cascade artifacts shipped (PR #8/#10/#11), VALIDATION-2.7 closure shipped (PR #9), state-bookkeeping for cascade shipped (PR #12).

Working tree clean. Zero uncommitted files. 6 untracked workspace files (`.agents/`, `.claude/`, `.mcp.json`, `.wiki/`, `audit-cache/`, `localhost-1779237331998.log`) — none in scope.

Locked decisions count is 27 in STATE.md (ADRs 001–027). ADR-028 ratified in `.planning/intel/decisions.md` but STATE.md count not yet bumped — defer to next state-bookkeeping PR per the bundle-with-Phase-4-start recommendation.

**Ready for Phase 4 (AI Layer)** — first non-bookkeeping phase work since Phase 3 shipped 2026-05-20.
</current_state>

<completed_work>

Massive single-session output (resume 2026-05-21T04:33Z → pause 2026-05-21T13:20Z, ~9h with deep work + 2 background-agent passes):

**7 PRs merged into main:**
- PR #7 (ADR-027 lookup-scoping fast-follow 2/3) — merged `c6dca6a` after CR comment-stripping fix
- PR #8 (Phase 3 security audit cascade 1/3) — merged `5a57000` after CR auto-fix over-scope incident + Option B surgical revert
- PR #9 (VALIDATION-2.7 closure test) — merged `d185efc` after CR Round-1 chain-inclusion assertion fix
- PR #10 (Phase 3 validation audit refresh cascade 2/3) — merged `85aebed` (APPROVED first review)
- PR #11 (Phase 3 UAT second pass cascade 3/3) — merged `28f646e` (APPROVED first review)
- PR #12 (state bookkeeping for cascade closure) — merged `6b95eaa` (APPROVED first review)
- PR #13 (ADR-028 PolicyId brand + UserNotProvisionedError subCode + tenant-isolation hardening) — merged `bd2257a` after CR Round-2 fixes (tenant-isolation predicates + check-policy-id-brand silent-pass closure)

**Major workflows executed:**
- Resumed from prior session's HANDOFF.json (`/gsd-resume-work`)
- Background-spawned `gsd-nyquist-auditor` (validation audit, ~5.7min, 208K tokens, stage-gated prompt with anti-pre-load rules)
- Live `claude-in-chrome` MCP spot-check on `/dashboard` for UAT second-pass (confirmed ADR-027 lookup-scoping transparent for single-org user; webhook user-context valid)
- ADR-028 implementation: 3-commit structure (foundation + threading + verify gate) + 2-commit CR-cycle (tenant-isolation + silent-pass) = 5 commits total, ~600 LOC across 17 files
- Branch cleanup: 5 stale local + 1 lingering remote

**Process meta-findings banked:**
- Option B surgical revert pattern for CR auto-fix scope creep (additive commit + `git checkout HEAD~1 -- <files>`)
- Stage-gated background-auditor pattern proven across two passes (security audit prior session + validation audit this session)
- `claude-in-chrome` MCP viable for live UAT spot-checks (faster than operator-in-loop browser clicks)
- ADR-028 reconsiders ADR-027's "wait for Phase 7+" punt on `UserNotProvisionedError` subCode — 4-agent review made it binding
</completed_work>

<remaining_work>

**Phase 4 (AI Layer) — not yet started.** Three workflow entry options:

1. **`/gsd-discuss-phase 4`** (standard / default) — HOW-decision gathering through adaptive questioning. Best for Phase 4 if the WHAT-deliverables feel concrete enough.
2. **`/gsd-spec-phase 4`** — clarify WHAT delivers + ambiguity scoring first. Best if Phase 4's scope feels fuzzy (e.g. exact AI workflow boundaries not yet locked).
3. **`/gsd-ai-integration-phase 4`** — formal AI-SPEC.md with eval planning + framework selection + domain research. Best if PolicyPilot wants the full AI-integration rigor (eval rubrics, golden datasets, etc.).

**My recommendation: `/gsd-discuss-phase 4`** — the AI Layer's scope is well-defined in CLAUDE.md (Sonnet 4.6 for draft/Q&A/consistency; Haiku 4.5 for TL;DR; Batch API for consistency check; prompt caching; ai_generations table). HOW-decisions are the real gap.

**Optional / deferred:**
- **State-bookkeeping PR for ADR-028 ship**: locked-decisions count `27 → 28`, fast-follow backlog cleared, new session-continuity bullet. Bundles cleanly with first Phase-4 state update — no need to ship standalone.
</remaining_work>

<decisions_made>

(See HANDOFF.json `decisions` array for full structured list with rationale + phase tags. Key ones:)

- **Audit cascade as 3 separate PRs** (#8/#10/#11) — operator's option-2 from prior context-warning pause. Lower review fatigue than bundling.
- **Option B surgical revert** for CR auto-fix scope creep — additive commit, no force-push, preserves history. Sets precedent for future CR auto-fix incidents.
- **ADR-028 bundle** — PolicyId brand + subCode discriminant + info-disclosure boundary in one PR per operator directive. Independently small + conceptually adjacent.
- **Slippery-slope policy in ADR-028** — PolicyId branded; UserId/OrgId stay unbranded (well-contained inside OrgContext/OrgScope; opportunistic adoption later).
- **UserNotProvisionedError.subCode = REQUIRED** parameter (not optional with default) — forces explicit discrimination at every throw site.
- **Signature-only ts-morph gate** — compiler catches caller violations; gate's job is signature-drift prevention only.
- **Tenant-isolation predicate** added to 6 direct `policies` updates in `transitions.ts` per CR PR #13 outside-diff finding. CLAUDE.md ALWAYS-rule compliance restored.
</decisions_made>

<blockers>

None. Clean state at phase boundary.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable handoff (primary; `/gsd-resume-work` uses this first)
2. This file (`.planning/.continue-here.md`) — human-readable handoff
3. `.planning/STATE.md` — current project state (locked decisions count still says 27; ADR-028 ratification recorded in `.planning/intel/decisions.md` lives there until next state PR bumps to 28)
4. `.planning/intel/decisions.md` § ADR-028 — full ratification record for the PolicyId brand + subCode + info-disclosure decisions
5. `.planning/PROJECT.md` — Phase 4 (AI Layer) scope reference. CLAUDE.md also has the AI rules (Sonnet 4.6 / Haiku 4.5 split, Batch API, prompt caching, ai_generations table).
6. `lib/policies/types.ts` + `scripts/check-policy-id-brand.ts` — new ADR-028 surface (familiarize before any Phase-4 work touching policies repository signatures)
7. CLAUDE.md `## Always` rules — particularly "Every DB query must include `org_id` in the WHERE clause. No exceptions." (CR PR #13 enforced this on transitions.ts)

## Critical Anti-Patterns (do NOT repeat these)

- **Unverified verification claims in commit messages** — re-fired in `fde9d1b` draft; self-caught + amended pre-push. Run the experiment first; paste verbatim output.
- **Merging CR auto-fix commits without `git show <hash> --stat` first** — PR #8 incident shipped 30+ unauthorized dep bumps via side-effect `pnpm install`. Always inspect; Option B surgical revert preferred over force-push.
- **Signature drift via method rename/removal** — closed by per-method `found` flag in `check-policy-id-brand.ts` `checkObjectLiteralRepo`. Pattern is locked.
- **Direct table updates missing `org_id` predicate** — 6 sites in `transitions.ts` previously violated CLAUDE.md ALWAYS rule. Now fixed. New orchestrator code MUST include `and(eq(<table>.orgId, s.orgId), ...)` in every `.where(...)`.

## Infrastructure State

- **Local branches**: only `main` (5 stale branches deleted earlier this session)
- **Remote branches**: only `origin/main` (PR #7's lingering `origin/refactor/lookup-scoping` deleted; all per-PR `--delete-branch` cleanups happened automatically)
- **main HEAD**: `bd2257a` (PR #13 squash — ADR-028 ship)
- **Background processes**: none. No `pnpm dev` running; no monitor or poll active.
- **Locked decisions count in STATE.md**: 27 (ADRs 001–027). ADR-028 ratified in `.planning/intel/decisions.md` but STATE.md not yet bumped — defer to next state PR per the bundle-with-Phase-4-start recommendation.
- **Test infrastructure signal**: `pnpm verify:phase-3` last ran 10/10 gates green · 91/91 vitest · 288/288 check-artifacts · 17/17 check-policy-id-brand · against `main @ bd2257a` pre-merge.

## Pre-Execution Critique Required

Not applicable — this pause is at phase-boundary, not between design and execution.

<context>
The session was deeply productive — 7 PRs merged with a single CR over-scope incident handled cleanly via the Option B surgical-revert pattern. The audit-cascade-and-fast-follows pattern is now well-rehearsed: 3-PR cascade for audits, separate PRs per fast-follow ADR, dedicated state-bookkeeping PRs at logical bundle boundaries.

The CR Round-1 → fix → Round-2 → APPROVED cascade pattern landed in <5min round-trips consistently. CR's auto-fix scope creep was the only friction — and the Option B revert is now banked as the response pattern for future incidents.

ADR-028's `UserNotProvisionedError.subCode` discriminant is the most architecturally interesting decision: it RECONSIDERS ADR-027's "wait for Phase 7+" punt because the 4-agent pre-merge review made the supplement binding. The pattern of "later ADR explicitly reconsiders a prior ADR's deferred decision" is healthy — ADRs aren't immutable; reconsideration is documented + ratified.

The PolicyId brand (ADR-028's primary deliverable) closes a real bug along the way: `app/(admin)/policies/[id]/page.tsx` previously had a latent 22P02 (non-UUID URLs → Postgres invalid_text_representation → 500). The brand surfaced it; `safeParse + notFound()` closes it per D-10 advertise-nothing. This is the value of branded types — they catch caller-side gaps the compiler couldn't otherwise prove.

Phase 4 (AI Layer) is the largest phase remaining. The CLAUDE.md AI rules give a strong baseline (Sonnet 4.6 / Haiku 4.5 split, prompt caching, ai_generations table, Batch API for consistency check). The HOW-decisions for `/gsd-discuss-phase 4` will likely include: prompt template authoring location, tier-gating mechanics, ai_generations row shape, error/retry behavior, Claude API rate-limit handling, prompt-cache hit-rate observability.
</context>

<next_action>

When resuming:

1. `/gsd-resume-work` will pick up this HANDOFF.json automatically.
2. Confirm `main @ bd2257a` is current. If yes, working tree clean → proceed.
3. Pick Phase 4 entry workflow per `<remaining_work>`:
   - **Default**: `/gsd-discuss-phase 4`
   - **If WHAT-deliverables feel fuzzy first**: `/gsd-spec-phase 4`
   - **If wanting formal AI-integration rigor**: `/gsd-ai-integration-phase 4`
4. (Optional) The next state-bookkeeping PR should bundle ADR-028 ship (locked-decisions 27→28) with the first Phase-4 state update — don't ship a standalone state PR for ADR-028 alone.

After successful resume, delete this `.continue-here.md` per the resume-project workflow line 94 (HANDOFF.json + .continue-here.md are one-shot artifacts).
</next_action>
