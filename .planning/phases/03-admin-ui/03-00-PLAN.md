---
phase: 03-admin-ui
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - .planning/phases/03-admin-ui/03-00-SUMMARY.md
autonomous: false
requirements:
  - REQ-access-control
tags: [clerk, webhook, operator-manual, onboarding]
user_setup:
  - service: clerk
    why: "Phase 3 ships <CreateOrganization /> live-smoke + post-sign-in redirect; signing secret rotation (L-04) and After-sign-in URL change (L-03) require Clerk/Svix dashboard interaction."
    env_vars:
      - name: CLERK_WEBHOOK_SECRET
        source: "Svix Dashboard → Clerk app → Endpoints → Signing Secret → Rotate (paste new whsec_… into .env.local; do NOT paste into chat)"
    dashboard_config:
      - task: "Change Clerk app 'After sign-in URL' from /sign-in-success to /post-sign-in"
        location: "Clerk Dashboard → app → Customization → Paths → After sign-in URL"
      - task: "Verify Clerk app Organizations are enabled (Phase 1 D-09)"
        location: "Clerk Dashboard → app → Organizations"
must_haves:
  truths:
    - "Clerk webhook signing secret has been rotated post-leak (closes SF-WHSEC-1)"
    - ".env.local CLERK_WEBHOOK_SECRET starts with `whsec_` and is non-empty after rotation"
    - "Clerk app After-sign-in URL is /post-sign-in (paves the way for L-03 route rename in Plan 03-02)"
    - "Clerk Organizations toggle is enabled (so <CreateOrganization /> in Plan 03-11 works)"
  artifacts:
    - path: ".env.local"
      provides: "Rotated whsec_ value for the Clerk webhook signature verification path"
      contains: "CLERK_WEBHOOK_SECRET=whsec_"
  key_links:
    - from: ".env.local CLERK_WEBHOOK_SECRET"
      to: "app/api/webhooks/clerk/route.ts svix.Webhook.verify"
      via: "process.env.CLERK_WEBHOOK_SECRET read inside POST handler"
      pattern: "CLERK_WEBHOOK_SECRET"
---

<objective>
Operator-manual configuration gate for Phase 3. Closes carry-forwards SF-WHSEC-1 (L-04) and REG-P1-01 prep (L-03 Clerk dashboard half) BEFORE the webhook live-smoke in Plan 03-11. No code changes — three Clerk/Svix dashboard tasks plus an env var paste.

Purpose: Rotate the leaked whsec_ signing secret so the Phase 2 webhook handler stops trusting a value that was pasted into chat transcript during Plan 02-02. Update Clerk's After-sign-in URL to the new /post-sign-in trampoline that Plan 03-02 will ship.
Output: Updated .env.local + Clerk dashboard state aligned with Phase 3 routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-ui/03-CONTEXT.md
@.planning/phases/03-admin-ui/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Rotate Clerk webhook signing secret (L-04 / SF-WHSEC-1)</name>
  <what-built>Nothing yet — pure operator-manual step. The previous whsec_ was leaked into chat transcript during Plan 02-02 checkpoint resolution. One-click rotation invalidates the leaked secret.</what-built>
  <how-to-verify>
    1. Open Svix Dashboard (https://dashboard.svix.com/) or Clerk Dashboard → Endpoints. The route is the Clerk Webhook integration that points at https://&lt;your dev tunnel or vercel preview&gt;/api/webhooks/clerk.
    2. Click the endpoint → Signing Secret → Rotate. Copy the new `whsec_…` value.
    3. Open `.env.local` (gitignored). Replace the value on the `CLERK_WEBHOOK_SECRET=whsec_…` line. Save.
    4. **CRITICAL — DO NOT paste the value back into chat or any tool input. The point of rotation is invalidating leaked values.**
    5. From a terminal in the repo root:
       ```pwsh
       Select-String -Path .env.local -Pattern '^CLERK_WEBHOOK_SECRET=whsec_' -Quiet
       ```
       Expected output: `True` (the var is present with a `whsec_` prefix).
    6. Run `pnpm verify:phase-2` once — it does not hit Clerk, but it confirms the env var is set and the existing webhook handler still typechecks. Expected: 7/7 OK (~22s).
  </how-to-verify>
  <resume-signal>Type "rotated" once `.env.local` has the new value AND `pnpm verify:phase-2` exits 0. If anything blocks (e.g., Svix permission), describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Update Clerk "After sign-in URL" + verify Organizations toggle (L-03 prep / D-08)</name>
  <what-built>Nothing yet — operator-manual Clerk dashboard config that prepares the way for Plan 03-02's L-03 route rename (`/sign-in-success` → `/post-sign-in`). The route itself is shipped in Plan 03-02; this task changes the Clerk-side redirect target so the new route receives traffic when Plan 03-02 lands.</what-built>
  <how-to-verify>
    1. Open Clerk Dashboard → your dev application → Customization → Paths.
    2. Find "After sign-in URL". Change from `/sign-in-success` (Phase 1 D-09 placeholder) to `/post-sign-in`.
    3. Save.
    4. In the same dashboard, navigate → Organizations. Confirm the Organizations feature is ENABLED (it should be — Phase 1 D-09 set this). If it shows disabled, enable it now. Without this, `<CreateOrganization />` in Plan 03-11 will not render.
    5. Confirm the change took effect: in Clerk Dashboard the "After sign-in URL" field reads `/post-sign-in`.
  </how-to-verify>
  <resume-signal>Type "approved" once both Clerk dashboard changes are saved. Note that traffic to `/post-sign-in` will 404 until Plan 03-02 ships — that is expected and is the closing handshake for L-03.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Clerk/Svix dashboards | Operator manually rotates secret + edits config; secret value crosses chat-untrusted → file-trusted boundary |
| Clerk webhook → app | Webhook payload crosses internet → Vercel runtime; signature verified by the existing Phase 2 handler using the (now-rotated) CLERK_WEBHOOK_SECRET |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Information Disclosure | Leaked Phase 2 whsec_ in chat transcript | mitigate | Rotate via Svix Dashboard (Task 1); old value becomes invalid for signature verification |
| T-03-00-02 | Spoofing | Webhook caller forging events with leaked secret | mitigate | Rotation invalidates leaked secret; subsequent forged requests fail svix.Webhook.verify |
| T-03-00-03 | Tampering | Operator pastes new secret into chat by mistake | accept | Mitigated by checklist language in Task 1 + chat-policy file `secrets-never-in-chat.md` in MEMORY.md |
| T-03-00-04 | Repudiation | No audit log of who rotated when | accept | Svix dashboard retains rotation timestamp + actor; low-frequency event, no app-layer logging warranted |
</threat_model>

<verification>
- `Select-String -Path .env.local -Pattern '^CLERK_WEBHOOK_SECRET=whsec_' -Quiet` returns `True`
- `pnpm verify:phase-2` exits 0 (7/7 OK)
- Clerk Dashboard "After sign-in URL" reads `/post-sign-in` (visual verification by operator)
- Clerk Dashboard Organizations toggle is ENABLED (visual verification by operator)
</verification>

<success_criteria>
- Old leaked whsec_ value is no longer accepted by the Clerk webhook signature verifier.
- New whsec_ value is in .env.local.
- Clerk app routes signed-in users to /post-sign-in (the route Plan 03-02 will ship).
- Organizations feature confirmed enabled in Clerk dashboard.
</success_criteria>

<output>
Create `.planning/phases/03-admin-ui/03-00-SUMMARY.md` after both checkpoints resolve. Note SF-WHSEC-1 CLOSED and L-03/Clerk-dashboard-half CLOSED.
</output>
