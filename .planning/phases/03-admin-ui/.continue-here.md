---
context: phase
phase: 03-admin-ui
task: validate-phase-3-interrupted
total_tasks: 3
status: paused_at_context_warning
last_updated: 2026-05-21T04:19:15.546Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through
> actual failure or explicit operator feedback this session. Acknowledge
> each before proceeding.

- [ ] **CONSTRAINT: Pace discipline under context warnings** — Operator's
  standing instruction in memory record `feedback-context-warnings`
  (operator's Claude Code per-project memory, not in this repo). When
  context-window warnings hit, do NOT panic-batch; slow down + scope-check
  + max-effort the remaining items. Surfaces in this session as the
  reason gsd-validate-phase 3 paused at 67% — operator chose option 2
  (clean PR boundary + /clear + fresh-context resume) rather than press
  on with degraded budget. Severity: blocking.
- [ ] **CONSTRAINT: Comment-discipline lesson is NOT internalized yet —
  re-fired on PR #7** — On PR #3 the v3 commit shipped a confidently-
  wrong behavioral claim about `updateDraft` ("runs an empty UPDATE that
  succeeds at the DB layer"). On PR #7 (this session, fast-follow #2)
  the ADR-027 short-form claimed downstream JOINs against `users`
  "silently returned empty results" — verifiable false by grep
  (`policies.ts`/`policy_versions.ts`/`workflow_stages.ts` don't join
  `users`; only `users.ts` does). Same imprecision flavor, caught by
  the 4-agent pre-merge review's comment-analyzer agent. Marker
  recorded in PR #7's v2 polish commit message. **When writing a
  behavioral claim about OTHER code, grep/read that other code at
  file:line BEFORE writing the claim. Do not trust mental model.**
  Severity: blocking.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Confidently-wrong behavioral comments | Writing a comment that makes a claim about other-file behavior (e.g. "X does Y in DB layer") without reading the cited code first. Re-fired on PR #7 short-form despite PR #3 v3 lesson. | blocking | Before writing any comment that makes a behavioral claim about code outside the current Edit's immediate scope, Read or Grep the cited file:line first. Treat comments as load-bearing contracts. |
| Context-warning panic-batching | When CONTEXT WARNING appears, the wrong reflex is "rush more tool calls into one message to save context." Communicates nervousness and produces sloppy work. Surfaced by operator 2026-05-20. | blocking | Treat warnings as a signal to REASSESS SCOPE, not as a deadline. Next user-facing message after a warning is status + scope-check, not "going hard and fast." Read the `feedback-context-warnings` memory before reactive work. |
| Skill-produced commits auto-stage unrelated session-local files | `gsd-sdk query commit` for the security audit auto-staged `.planning/config.json` (which had `_auto_chain_active: true` session-local flag). Soft-reset to clean is blocked by auto-mode classifier as destructive. | advisory | Workaround: second-commit pattern — make a new commit that removes the unwanted line (clean diff into main on PR merge). Don't fight the classifier on destructive history rewrites of skill-produced commits. |
| Pre-loading bird's-eye-view planning docs in audit agent prompts | First gsd-security-auditor attempt loaded `03-CONTEXT.md` + `03-PATTERNS.md` (~1300 lines) upfront and hit 65% context warning before reaching threat models. v2 prompt explicitly forbade reading those files. | advisory | For audit-class agent prompts: enumerate forbidden files explicitly. Provide pre-loaded facts in the prompt body so the agent doesn't burn budget rediscovering them. Stage-gated work plans with explicit checkpoints. |

<current_state>
**main** is at `db5ab77`. Phase 3 (Admin UI) shipped via PR #3. The first fast-follow (PR #5 ADR-026 typed errors) shipped @ `bf65712`. The second fast-follow (PR #7 ADR-027 lookup-scoping) is OPEN with v2 polish awaiting CR re-review. PR #8 (Phase 3 security audit) was just opened — pure docs (03-SECURITY.md, 263 lines, verdict SECURED 67/67).

I'm currently on branch `chore/phase-3-audit-trail` (PR #8's branch). The operator's full directive was "secure phase validate and verify" — 3 sequential GSD audit skills. Only `gsd-secure-phase 3` completed. `gsd-validate-phase 3` was interrupted at Step 1 (State A detected: `03-VALIDATION.md` already exists) when my context hit 67%. Operator chose option 2: ship the security audit as its own PR + /clear + fresh-context resume for validate+verify.
</current_state>

<completed_work>

**This session's fast-follow + audit cascade:**

- ✅ **PR #5 (ADR-026 typed errors)** — fast-follow 1 of 3. Shipped @ `bf65712`. Closed CR outside-diff finding + 4-agent pr-review-toolkit pre-merge pass + inline tsx-scope finding. ADR-026 ratified; locked decisions count 25 → 26.
- ✅ **PR #6 (state bookkeeping for PR #5)** — shipped @ `3c69beb`.
- ✅ **PR #7 (ADR-027 lookup-scoping)** — fast-follow 2 of 3. OPEN @ `e32173b`. 3 commits: ADR ratification (9e29e43), refactor + integration controls + artifact assertions (188f6b7), v2 polish from 4-agent review (e32173b). CR previously APPROVED at 188f6b7; v2 awaiting re-review. Closes Critical (ADR short-form joins-tense factual error — same PR #3 v3 flavor) + 2 MEDIUM (broad catch in pre-existing 22P02 negative control + scope-of-responsibility comment).
- ✅ **gsd-secure-phase 3** — full retroactive STRIDE audit. v1 auditor hit 65% context warning at Step 1; killed (already terminated). v2 auditor with stage-gated prompt + anti-pre-load rules completed in 419s with 120K tokens. **Verdict: SECURED — 67/67 threats closed** (Spoofing 5, Tampering 24, Repudiation 4, Info Disclosure 15, DoS 5, EoP 8). Wrote `03-SECURITY.md` (263 lines) to disk.
- ✅ **PR #8 (Phase 3 security audit)** — OPEN @ `4a444f8`. 2 commits: SECURITY.md added via gsd-sdk's commit helper (031a8a1; auto-staged `.planning/config.json` _auto_chain_active flag as a side-effect) + drop-session-local-flag follow-up (4a444f8). Net diff into main = 1 file (SECURITY.md, 263 lines).

</completed_work>

<remaining_work>

**Resume plan for fresh-context session:**

1. **Check PR #7 + PR #8 status** — `gh pr view 7` + `gh pr view 8`. Likely both APPROVED + CLEAN by then.
2. **Merge whichever's ready** — squash + delete-branch per CLAUDE.md Git Workflow. Same pattern as PR #5 / #6.
3. **`/gsd-validate-phase 3`** — State A audit-and-fill-gaps against existing `03-VALIDATION.md`. Apply same stage-gated background-auditor pattern as security audit:
   - Pre-load State A detection in prompt: VALIDATION.md exists, 14 PLAN files, 15 SUMMARY files
   - Explicit anti-pre-load rules: NO `03-CONTEXT.md` / `03-PATTERNS.md` / `03-RESEARCH.md` / `03-DISCUSSION-LOG.md`
   - 4-stage work plan: re-build requirement→task map → detect test infra → classify COVERED/PARTIAL/MISSING → update VALIDATION.md
   - Background-mode spawn so my context isn't burning
4. **`/gsd-verify-work 3`** — conversational UAT. Phase 3 already has `03-HUMAN-UAT.md` with 6/6 PASS from initial UAT cycle; this is a second pass against current shipped state at `main` post-PR-#5 ADR-026 typed-error changes. Likely fast.
5. **Each audit artifact gets its own short-lived PR + branch** — `chore/phase-3-validation-trail`, `chore/phase-3-verify-trail` (or similar). Same pattern as PR #8.
6. **PR 3.3 (ADR-028 PolicyId branded type)** — LAST queued fast-follow. Brand `PolicyIdSchema` + thread `PolicyId` type through `policyIdFrom` return + `Policies` repo signatures + `lib/policies/transitions.ts` orchestrators. ts-morph verify gate forbidding bare `string` in repo policyId params. Slippery-slope policy: don't brand UserId/OrgId yet. ~300-500 LOC. **Operator decision needed on resume:** bundle the 2 deferred MEDIUM findings from PR #7 review (`UserNotProvisionedError` subCode discriminant + `orgRow.id` in error message) with PR 3.3, OR give them their own ADR-029?

</remaining_work>

<decisions_made>

- **Audit cascade ships as 3 separate PRs**, not bundled. Operator's explicit choice when surfacing options at the 67% context-warning pause. Matches PR #4/#6 state-bookkeeping precedent + lower review fatigue.
- **ADR-027 multi-org Clerk lockout = product behavior, not bug.** Webhook handler at `app/api/webhooks/clerk/route.ts:296-303` unconditionally overwrites `users.org_id` on every `organizationMembership.created`. Documented in ADR-027 Open question section. Full multi-org support deferred to future ADR.
- **SQL-direct integration test > vitest-mocked unit test for ADR-027.** Mocking Drizzle's fluent chain would either test the mock or be implementation-coupled and miss SQL-level bugs. SQL-direct against TEST DB exercises the actual SQL invariant. Justified in PR #7 commit 2.
- **Stage-gated audit-agent prompt pattern works.** v2 gsd-security-auditor completed in 419s with explicit anti-pre-load rules + pre-loaded facts + 4-stage work plan + background-mode spawn. Apply same pattern to gsd-nyquist-auditor on resume.
- **Second-commit workaround for skill-produced commit pollution.** When `gsd-sdk query commit` auto-stages unwanted files, do NOT fight the classifier on soft-reset; second commit that removes the unwanted line yields same clean diff into main.

</decisions_made>

<blockers>

- **Context budget exhausted at 67% mid-validate-phase 3 Step 1.** Resolution: this handoff + /clear + fresh-context resume.
- **PR #7 awaiting CR re-review of v2 polish commit e32173b.** External; CR previously APPROVED on prior HEAD 188f6b7. Will likely be APPROVED + CLEAN by resume time.
- **PR #8 just opened — CR hasn't reviewed yet.** External; pure docs PR, expect quick clean review.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable handoff (primary; `/gsd-resume-work` uses this)
2. This file (`.continue-here.md`) — human-readable handoff
3. The operator's memory record `feedback-context-warnings` (per-project Claude Code memory; not in this repo) — applicable to next session's pace discipline
4. `.planning/phases/03-admin-ui/03-SECURITY.md` (263 lines) — the artifact PR #8 is shipping; full STRIDE register with file:line citations
5. `.planning/phases/03-admin-ui/03-VALIDATION.md` — existing State A document for the next gsd-validate-phase run
6. https://github.com/mmtuentertainment/PolicyPilot/pull/7 — PR #7 status
7. https://github.com/mmtuentertainment/PolicyPilot/pull/8 — PR #8 status
8. CLAUDE.md → Git Workflow + Post-PR-merge checklist

## Critical Anti-Patterns (do NOT repeat these)

- **Confidently-wrong behavioral comments** → Re-fired on PR #7 short-form despite PR #3 v3 lesson. Mitigation: grep/read cited code BEFORE writing claim.
- **Pre-loading bird's-eye-view planning docs in audit prompts** → First gsd-security-auditor failed at 65% from loading CONTEXT.md + PATTERNS.md. Mitigation: explicit forbidden-files list + pre-loaded facts in prompt body.
- **Context-warning panic-batching** → Operator's standing feedback from 2026-05-20. Mitigation: pace + scope-check, not "going hard and fast."
- **Direct push to main** → CLAUDE.md says `.planning/phases/**` files can't go direct; auto-mode classifier blocks. Mitigation: short-lived branch + PR per PR #4/#6/#8 pattern.

## Infrastructure State

- **Current branch:** `chore/phase-3-audit-trail` (PR #8 head; will switch on resume to verify state)
- **Local branches still around** (deleted on remote, harmless): `gsd/phase-3-admin-ui`, `chore/state-phase3-shipped`, `chore/state-adr-026-shipped`, `refactor/typed-errors`
- **Other open PR branch:** `refactor/lookup-scoping` (PR #7 head `e32173b`)
- **Background processes:** None. No `pnpm dev`, no `cloudflared`, no monitor (PR-#3 monitor died earlier this session per HANDOFF).
- **TEST DB:** Clean. `scripts/check-auth-context.ts` always TRUNCATEs at end (idempotent).
- **PR #3-era `.continue-here.md` content was about awaiting CR third re-review** — that pause was resolved when PR #3 merged earlier this session. This file replaces it.

## Pre-Execution Critique Required

Not applicable — this is a pause between audit skill invocations, not between design and execution.

<context>
The session's been LONG — 5 PRs shipped on this branch tree (PR #3 Phase 3 + PR #4 state + PR #5 ADR-026 + PR #6 state + PR #7 ADR-027 open + PR #8 audit just opened). The fast-follow cascade pattern worked well: ratify ADR → implement refactor → multi-agent pre-merge review → v2 polish → merge → state bookkeeping. Same pattern across PR #5 and PR #7.

The two BLOCKING constraints in the box above are the load-bearing context for the next session. The comment-discipline lesson re-fired on PR #7 (same flavor as PR #3 v3); I noted it in PR #7's v2 polish commit as a marker. The next-session agent needs to internalize "grep before claim" structurally, not just acknowledge it.

The mental model for the resuming agent: this isn't phase-task mid-flight (Phase 3 is shipped + its first fast-follow is shipped + the second's open in review + the security audit's open in review). This is "phase audit cascade between skills" mid-flight. Two more skills to go: gsd-validate-phase 3 (State A) and gsd-verify-work (conversational, operator-in-loop). Each generates its own artifact + own short-lived PR. After all 3 audits ship, PR 3.3 (ADR-028 PolicyId brand) is the LAST queued fast-follow before Phase 4 (AI Layer) can begin.

The deferred MEDIUM findings from PR #7's 4-agent review (UserNotProvisionedError subCode discriminant + orgRow.id in error message) are tracked in HANDOFF.json under human_actions_pending. Operator decision needed on resume: bundle with PR 3.3 or give them their own ADR-029. Don't make this call autonomously — the discipline pattern in this codebase has every architecture-class decision (modifies ADR-026 class hierarchy) go through an ADR with explicit operator ratification.
</context>

<next_action>
1. `/clear` to wipe context
2. `/gsd-resume-work` — this handoff JSON drives the resume
3. Check PR #7 + PR #8 state via `gh pr view 7` + `gh pr view 8`
4. If either ready, merge first (squash + delete-branch per CLAUDE.md)
5. `/gsd-validate-phase 3` — State A, audit-and-fill-gaps. Apply stage-gated background-auditor pattern from security audit.
6. After validate completes, `/gsd-verify-work 3` for the conversational UAT pass.
7. Then surface the operator-decision question about PR 3.3 + 2 deferred MEDIUM findings.
</next_action>
