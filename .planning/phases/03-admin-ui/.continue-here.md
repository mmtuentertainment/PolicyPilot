---
context: phase
phase: 03-admin-ui
task: awaiting_cr_third_re-review
total_tasks: 3
status: paused_waiting_external
last_updated: 2026-05-20T17:29:05.922Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through actual failure or
> explicit operator feedback this session. Acknowledge each before proceeding.

- [ ] **CONSTRAINT: Pace discipline under context warnings** — Operator gave
  explicit mid-session feedback: when context-window warnings hit (≥60%
  usage), do NOT panic-batch or rush. Slow down, pick max-effort scope,
  commit cleanly. Full feedback memorialized at
  `C:/Users/matth/.claude/projects/C--Users-matth-Desktop-PolicyPilot/memory/feedback-context-warnings.md`
  — **read it before doing anything reactive when you see the next
  CONTEXT WARNING**. Severity: blocking.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Next.js route groups silently change URLs | Moving page.tsx between route groups (e.g. `(admin)/onboarding/x/page.tsx` → `(onboarding)/x/page.tsx`) drops the `onboarding/` URL segment because parens-groups contribute nothing to URLs. Broke `/onboarding/create-org` in commit `395cf8c`; fixed in `a851e97` by re-adding the `onboarding/` directory inside `(onboarding)/`. | advisory | `app/(onboarding)/layout.tsx` now has an explicit directory diagram + "route groups don't add URL segments" callout. Future moves: derive the URL from the post-group path BEFORE moving. |
| String-includes error matching is a runtime contract pretending to be a type contract | `lib/auth/bootstrap-errors.ts` matches throw-site error messages via `String.includes(needle)`. If `lib/auth/context.ts` reflows a throw-string ("No active organization" → "no active organization"), the allow-list silently rots. Found by type-design-analyzer FLAG; partial mitigation shipped (extracted helper + 8 divergence-lock tests). | advisory | Fast-follow PR (deferred per operator) will replace string-includes with `instanceof` against typed error classes (OrgNotProvisionedError, UserNotProvisionedError, etc.) defined in lib/auth/context.ts. Until then, `lib/auth/bootstrap-errors.test.ts` divergence tests catch most silent regressions. |
| Context-warning panic-batching | When CONTEXT WARNING appears, the wrong reflex is "rush more tool calls into one message to save context." That communicates nervousness and produces sloppy work. Surfaced by operator on 2026-05-20. | blocking | Read `feedback-context-warnings.md` memory. Treat warnings as a signal to REASSESS SCOPE, not as a deadline. |

<current_state>
Phase 3 PR #3 is OPEN at https://github.com/mmtuentertainment/PolicyPilot/pull/3, HEAD at `bd25409`. Four post-review commits (`a851e97`, `b01c9f1`, `ab65944`, `bd25409`) were pushed at 17:27Z after the 5-agent pr-review-toolkit pre-merge review surfaced a critical BLOCK (URL regression from CR-PR3-#16) plus four FLAGs. All BLOCK + A/B/E-scoped FLAGs addressed. C + D (typed error classes, PolicyId branded type) intentionally deferred to a fast-follow PR per operator's explicit scoping decision.

`mergeState: CLEAN → UNSTABLE` fired on the push as CodeRabbit's status check transitioned to PENDING. `reviewDecision` is still `APPROVED` (carries over from CR's review at `b484bf9`; will flip when CR completes its third review on `bd25409`).

**Monitor `bsjmp75s9` was running but likely killed by the session pause.** Re-arm it on resume (the script is at `.tmp/monitor-pr3.mjs`).
</current_state>

<completed_work>

This session (Phase 3 PR review/merge loop):

- ✅ Full HUMAN-UAT walkthrough — 6/6 PASS (UAT-1 through UAT-6; record at `03-HUMAN-UAT.md`)
- ✅ 03-G3 hotfix shipped — DUP-VN + SF-W5 + MYPOL-STUB closed (8 commits; SUMMARY at `03-G3-SUMMARY.md`)
- ✅ Phase 3 PR #3 opened (`a297d8c`)
- ✅ CodeRabbit review #1 → 22 findings → addressed in 3 commits (`2f94cdd`, `395cf8c`, `ad169b1`)
- ✅ CodeRabbit re-review #2 → 1 finding (UUID validation) → addressed in `b484bf9` → APPROVED
- ✅ pr-review-toolkit 5-agent pre-merge review:
  - **BLOCK** (URL regression from #16) → fixed `a851e97`
  - **FLAG** comment accuracy → fixed in `b01c9f1`
  - **FLAG** test coverage → 8 bootstrap-errors tests + 5 UUID null-return tests in `b01c9f1` + `bd25409`
  - **FLAG** plan-doc auto-mode language + path sweep → fixed in `ab65944`
  - **FLAG** policyId rejection logging → added in `bd25409`
- ✅ 4 post-review commits pushed (`b484bf9..bd25409`)

Verify-gate state at HEAD `bd25409`:
- `pnpm tsc --noEmit` → 0
- `pnpm verify:phase-2` → 8/8 OK
- `pnpm verify:phase-3` → 8 gates + 270/270 artifacts + 67/67 vitest (was 53; +14 new tests)
</completed_work>

<remaining_work>

1. **Wait for CodeRabbit's third re-review at head `bd25409`** (~5-10 min typical turnaround)
2. **Decide merge** when CR re-approves:
   - `gh pr merge 3 --squash --delete-branch`
   - Post-merge: `git checkout main && git pull --ff-only` (CLAUDE.md Git Workflow checklist)
   - Verify squash commit landed on main with `git log -1`
3. **Begin Phase 4 (AI Layer)** — not yet planned; depends on Phase 3 admin shell
   - `/gsd-discuss-phase 4` (gather context) → `/gsd-plan-phase 4`
4. **Decide fast-follow PR scope** for typed error classes + PolicyId branded type
   - Both deferred from PR #3 per operator (architecture changes need BLUEPRINT.md update first)
   - Could be a small standalone PR before Phase 4 begins, or rolled into a "Phase 3.1 polish" branch
</remaining_work>

<decisions_made>

This session:
- **URL fix via Path B** — re-added `onboarding/` directory inside `(onboarding)/` route group instead of rewriting every redirect to `/create-org`. Smaller diff; preserves URL contract referenced in 8+ planning docs.
- **Skip middleware unit test** — `scripts/check-admin-routes.ts` already AST-parses `ADMIN_URL_PATTERNS` and fails on dead patterns. Exporting middleware internals purely for tests is a smell. pr-test-analyzer criticality-4 finding consciously accepted.
- **Defer typed error classes + PolicyId brand** — these are real architecture changes (typed error hierarchy in lib/auth/context.ts + branded type threading through repository signatures). Per CLAUDE.md ASK FIRST rule #2, need BLUEPRINT.md update first. CR APPROVED at `b484bf9` on the existing structure, so not blocking PR #3.
- **Path sweep is global, not narrative** — replaced all 20 stale `app/(admin)/onboarding/` references across 7 planning docs with `app/(onboarding)/onboarding/`. Future readers care about current location; git history preserves what was actually planned.
</decisions_made>

<blockers>

- **CR third re-review** (external, ~5-10 min) — auto-resolves when monitor fires
</blockers>

## Required Reading (in order)

1. `C:/Users/matth/.claude/projects/C--Users-matth-Desktop-PolicyPilot/memory/feedback-context-warnings.md` — operator feedback about pace, **read before any context-warning-reactive work**
2. `.planning/phases/03-admin-ui/03-G3-SUMMARY.md` — what 03-G3 closed (DUP-VN + SF-W5 + MYPOL-STUB)
3. `.planning/phases/03-admin-ui/03-HUMAN-UAT.md` — 6/6 PASS record
4. `https://github.com/mmtuentertainment/PolicyPilot/pull/3` — current PR state + review history
5. `.tmp/monitor-pr3.mjs` — monitor script that needs re-arming on resume
6. CLAUDE.md → Git Workflow + Post-PR-merge checklist

## Infrastructure State

- **Monitor `bsjmp75s9`** — was running (`node .tmp/monitor-pr3.mjs`, persistent) but likely killed at session pause. **Re-arm on resume** by reading `.tmp/monitor-pr3.mjs` + invoking Monitor with the same command.
- **PR #3** — OPEN, HEAD `bd25409`, `reviewDecision=APPROVED` (stale from prior review), `mergeState=UNSTABLE` (CR re-review PENDING)
- **pnpm dev** — STOPPED (was running during UAT-6 walkthrough; killed at session end)
- **cloudflared tunnel** — STOPPED (URL `pressing-boston-governmental-drives.trycloudflare.com` is now dead)
- **TEST DB (Supabase)** — has migration `0004_policy_versions_unique.sql` applied; UAT-3 duplicate v1 rows cleaned by self-healing DELETE pre-step
- **Dev DB (Supabase)** — same migration applied; original UAT-1 policy `41ab9db4-...` survived with 1 v1 row (oldest 10:55Z; 11:03 + 11:13 duplicates dropped)

## Pre-Execution Critique Required

Not applicable — no design artifact pending execution. Next non-passive work is post-merge (Phase 4 kickoff) or fast-follow PR (typed errors / PolicyId brand), both of which are clean-slate planning.

<context>
The PR review/merge loop dominated this session. Phase 3 ships the multi-tenant admin shell + policy lifecycle that CLAUDE.md gates as a prerequisite for Phase 4. The PR went through CR review #1 (22 findings) → CR re-review #2 (1 finding) → pr-review-toolkit pre-merge review (BLOCK + 4 FLAGs) → 4 post-review commits. CR's third re-review is the only remaining gate before merge.

Mental model for the resuming agent: this is a "wait state" not an "execute state." The work is paused waiting for an external signal (CR's review verdict). When the signal arrives, the decision tree is:
- APPROVED → ask operator about merge approach → execute merge + post-merge checklist
- CHANGES_REQUESTED with new findings → fetch comments, address per the established `cr-pr3` commit-message pattern, push, wait again
- CHANGES_REQUESTED but findings are already-addressed FLAGs the operator scoped out → reply on the PR thread with the scoping rationale + the deferred-PR plan
</context>

<next_action>
1. Re-arm the Monitor: read `.tmp/monitor-pr3.mjs` to confirm script is intact, then invoke Monitor tool with `node .tmp/monitor-pr3.mjs` (persistent, ~3600000 ms timeout).
2. Surface a brief status snapshot to the operator (the monitor state + a single-line PR summary).
3. Wait for the monitor event. Do not poll, do not preemptively re-fetch CR state.
</next_action>
