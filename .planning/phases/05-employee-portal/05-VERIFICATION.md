---
phase: 05-employee-portal
verified: 2026-05-24T07:00:00Z
status: gaps_found
score: 5/6 must-haves verified (5 SPEC SCs + ADR + REQ traceability all green; verify:phase-5 chain BLOCKED on Phase 3 test regression introduced by inline fast-follow fix)
overrides_applied: 0
gaps:
  - truth: "pnpm verify:phase-5 exits 0 end-to-end (CLAUDE.md ALWAYS rule #1 + ROADMAP Phase 5 ship gate)"
    status: failed
    reason: "verify:phase-5 chain step `pnpm verify:phase-4` → cascades through `pnpm test` → vitest reports 1 failed test in lib/policies/transitions.test.ts (test file: 1 failed / 27 passed; tests: 1 failed / 228 passed). Failing test: `editPublished (REQ-policy-lifecycle SC#3) > snapshots prior published content + resets status + bumps version` at lib/policies/transitions.test.ts:206. Root cause: the inline fast-follow fix `afb7693` (removing duplicate PolicyVersions.create from editPublished) updated production code in lib/policies/transitions.ts but did NOT update the corresponding test assertion. Verified by checkout of afb7693~1 -- lib/policies/transitions.ts and re-running vitest: 20/20 tests pass with pre-fix code (the editPublished test expected pvCreateMock.toHaveBeenCalledWith(...) which now correctly receives 0 calls because the snapshot moved into publish() exclusively). The Plan 05-10 SUMMARY's claim 'verify:phase-5 exits 0 in 92s' and 'AUTO-19 PASS' is FALSIFIED by current evidence — that claim was true at commit 81fa1ff (before the fast-follow fixes landed) but is no longer true at HEAD (commit 7c1c148)."
    artifacts:
      - path: "lib/policies/transitions.test.ts"
        issue: "Test at line 206 asserts pvCreateMock.toHaveBeenCalledWith(...) but production code no longer invokes PolicyVersions.create in editPublished() — the snapshot moved into publish() per fast-follow fix afb7693. Test was not updated alongside the production code change."
      - path: "lib/policies/transitions.ts:282-315"
        issue: "Comments at lines 293-303 document the new behavior (no snapshot in editPublished; publish writes it at the next publish call), confirming the intentional code change — but the corresponding test in transitions.test.ts:194-218 was not updated to match."
    missing:
      - "Update lib/policies/transitions.test.ts editPublished 'snapshots prior published content...' test (lines 194-218) to assert NEW behavior: pvCreateMock NOT called, tx.update called with currentVersion+1, status reset to draft. Suggested rename of test name from 'snapshots prior published content + resets status + bumps version' → 'resets status + bumps version (snapshot deferred to next publish() per 03-G3 UNIQUE)'."
      - "Re-run `pnpm verify:phase-5` end-to-end and confirm exit 0 (target ≤95s — current chain reports 92s when green)."
      - "Update Plan 05-10 SUMMARY (Task 2 row 'AUTO-19') to reflect re-verification timestamp + new green run hash."
deferred: []
human_verification: []
---

# Phase 5: Employee Portal Verification Report

**Phase Goal:** An employee can sign in, see only their assigned + published policies, read them, ask Q&A questions, and one-click acknowledge — with every acknowledgment captured append-only with timestamp and IP. Policy updates correctly require re-acknowledgment.

**Verified:** 2026-05-24T07:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification
**Branch:** `gsd/phase-5-employee-portal` @ HEAD (commit `7c1c148`, 46 commits ahead of `main`)

## Executive Summary

Phase 5 ships a substantively complete and correct Employee Portal. All 22 expected artifacts exist; the dashboard query, acknowledgment orchestrator, re-ack logic, bulk-assign Server Action, Q&A consumption, and append-only enforcement all match the SPEC and pass under `pnpm tsc --noEmit`. The 9/9 integration test (`check:employee-portal`) covering R-1, R-3, R-4, R-6, AC-10, H-5, H-6 passes against the live TEST DB. Operator UAT closed 2026-05-24T06:30Z with 18 PASS + 1 PASS-with-finding and live DB evidence captured for R-2 (ack row with `ip='::1'` + `policy_version_id`) and R-3 (2 ack rows preserved after re-ack, proving ADR-018 end-to-end).

**However, `pnpm verify:phase-5` exits 1** — one test failure (`lib/policies/transitions.test.ts:206`) introduced by the inline fast-follow fix `afb7693` whose production-code change was not mirrored in the test. This is a **BLOCKER** for the documented "tsc --noEmit AND verify:phase-N both exit 0 → squash to main" gate (CLAUDE.md Build Sequence). The fix is small (rewrite one test) but must land before PR squash. The Plan 05-10 SUMMARY's claim that `verify:phase-5` exits 0 was correct at the commit it was written (81fa1ff, pre-fast-follow) but is no longer correct at HEAD.

Goal-backward: every observable SPEC requirement is met by the codebase. The gap is a process-level regression in the test suite, not a missing feature.

## Goal Achievement

### Observable Truths (6 SPEC Success Criteria + 1 ship-gate truth)

| #   | Truth (Success Criterion)                                                                                                                                       | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SC-1 | Employee dashboard shows only assigned-or-dept + `status='published'` policies; Draft/Under Review never appear                                                | VERIFIED   | `Policies.listAssignedAndPublishedForUser` at `lib/db/repositories/policies.ts:134-206` enforces `eq(policies.status, 'published')` at line 203 + INNER JOIN policy_assignments at lines 158-174 with user OR dept match. Integration test `scripts/check-employee-portal.test.ts` R-1 block (3 tests) exercises 4-row seed (P1 published+user, P2 published+dept, P3 draft+user, P4 published+unassigned) → returns exactly P1+P2. |
| SC-2 | One-click "Acknowledge" inserts a row with full 5-field shape and UI updates without page reload                                                                | VERIFIED   | `Acknowledgments.record` at `lib/db/repositories/acknowledgments.ts:64-82` does INSERT with `{userId, policyId, policyVersionId, ipAddress}` + ON CONFLICT DO NOTHING. `recordAcknowledgment` orchestrator at `lib/policies/acknowledgment.ts:90-180` wraps it inside single `withOrgScope` tx. `acknowledgePolicyAction` at `app/(employee)/my-policies/[id]/actions.ts:61-107` reads `x-forwarded-for` first hop, captures IP, calls revalidatePath twice OUTSIDE try. UAT EMP-12 confirmed no page reload; live DB probe confirmed row with `ip='::1'`, `policy_version_id=ba667fd6...`, `acknowledged_at=2026-05-24T06:02:05`. |
| SC-3 | Re-publish surfaces "requires re-acknowledgment"; prior ack rows untouched                                                                                      | VERIFIED   | `listAssignedAndPublishedForUser` two-LEFT-JOIN pattern with `currentAck` (on `policyVersions.id`) and `priorAck` (on different version) at lines 183-199 + 3-state CASE projection. UAT EMP-14 confirmed amber "Requires re-acknowledgment" badge appears after admin re-publish; live DB probe confirmed 2 ack rows preserved (v1 + v2) — ADR-018 append-only proven end-to-end. Integration test `scripts/check-employee-portal.test.ts` R-3 block exercises full lifecycle. |
| SC-4 | Bulk assignment to dept creates ONE `policy_assignments` row visible to every dept member                                                                       | VERIFIED   | `bulkAssignToDepartmentAction` at `app/(admin)/policies/[id]/actions.ts` calls `PolicyAssignments.create(s, {assigneeType: 'department', assigneeId: departmentId, ...})` + ON CONFLICT DO NOTHING. UNIQUE(policy_id, assignee_type, assignee_id) from migration 0010 enforces single-row invariant. Integration test R-4 block asserts COUNT=1 + all 3 dept members see policy + duplicate bulk-assign idempotent. UAT ADMIN-4 verified D-14 empty-dept disabled UX. |
| SC-5 | No code path DELETEs or UPDATEs acknowledgments — verified by 3-layer enforcement                                                                               | VERIFIED   | Layer 1: `Acknowledgments` object at `lib/db/repositories/acknowledgments.ts:31-87` exports only `listForUser` + `record` (no `update`/`delete` keys) — `tests/types.ts` D-07 `@ts-expect-error` invariants pass under `pnpm tsc --noEmit`. Layer 2: `pnpm check:acknowledgment-immutability` exits 0 (53 lib/ files scanned, 0 violations). Layer 2b: `pnpm check:acknowledgment-immutability:self-test` exits 0 (2 violations in negative-control fixture → gate non-vacuous; both Drizzle-API + raw-SQL detection paths fire — H-1 EAPI advisor closed at CI layer). Layer 3: `drizzle/0001_rls_policies.sql:69-73` documents the DB GRANT-asymmetry (GRANT includes UPDATE/DELETE for RLS symmetry; app-layer + ts-morph CI gate are the operative defense). |
| SC-6 | Employee Q&A surface at `/my-policies/ask` consumes Phase 4 `askQuestion`; cited answers; D-27 access-aware page handler                                        | VERIFIED   | `app/(employee)/my-policies/ask/page.tsx` + `actions.ts` (askQuestionAction) wired. `askQuestion` extracted into `lib/ai/qa.ts` per D-25 (Phase 4 D-41 same-closure validIds preserved, D-26 grant UPSERT iterates POST-validIds-filter list, D-27a accessibility annotation added). `app/(employee)/my-policies/[id]/page.tsx:88-178` implements D-27 3-branch access logic (assigned → full PolicyView; has-grant → TL;DR-only with verbatim banner copy; else → notFound). Integration test R-6 + H-5 + H-6 (3 tests) confirms grants written iff validIds match + hallucinated UUIDs + foreign-org UUIDs stripped. UAT EMP-15 confirmed citations clickable; EMP-16 confirmed D-27 banner verbatim copy + tldr-only render; EMP-17 confirmed 404 for invalid UUID. |
| SG  | `pnpm verify:phase-5` exits 0 (CLAUDE.md Build Sequence ship gate: "tsc --noEmit AND verify:phase-N both exit 0")                                              | **FAILED** | Captured: `VERIFY_PHASE_5_EXITCODE=1` at 2026-05-24T07:00Z. Tail output: `Test Files 1 failed | 27 passed (28); Tests 1 failed | 228 passed (229); ELIFECYCLE Command failed with exit code 1`. Failing test: `lib/policies/transitions.test.ts:206` `editPublished (REQ-policy-lifecycle SC#3) > snapshots prior published content + resets status + bumps version` — `expect(pvCreateMock).toHaveBeenCalledWith(...)` received 0 calls. Confirmed regression-from-fast-follow: `git checkout afb7693~1 -- lib/policies/transitions.ts` → `pnpm vitest run lib/policies/transitions.test.ts` → 20/20 PASS; restoring HEAD → 19/20 (1 fail). Plan 05-10 SUMMARY claim FALSIFIED. |

**Score:** 6 / 6 SPEC observable truths VERIFIED (substantive goal achievement); **5/6 ship-ready truths VERIFIED** when counting SG (the ship-gate truth fails).

### Required Artifacts (22 files — all present on disk)

| Artifact                                                                        | Expected                                                       | Status     | Details                                                                                                                                                                                              |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `drizzle/0010_phase5_uniques.sql`                                               | 2 UNIQUE constraints (acknowledgments + policy_assignments)    | VERIFIED   | 1515 bytes, exists; applied to DEV; TEST DB unblocked per ec6b526. UNIQUE on `acknowledgments(user_id, policy_id, policy_version_id)` per D-06; UNIQUE on `policy_assignments(policy_id, assignee_type, assignee_id)` per D-15. |
| `drizzle/0011_qa_citation_grants.sql`                                           | New tenant table with RLS + GRANTs + composite UNIQUE + 2 idx  | VERIFIED   | 3256 bytes, wrapped-form `(SELECT auth.jwt()->>'org_id')` RLS predicate per RESEARCH gap-1; ON DELETE CASCADE on `org_id`/`policy_id`/`user_id` FKs.                                                  |
| `lib/db/schema.ts`                                                              | qaCitationGrants export + 2 UNIQUE constraint adds             | VERIFIED   | qaCitationGrants placed alphabetically at line 252; acknowledgments UNIQUE at line 68; policyAssignments UNIQUE in table-options array.                                                              |
| `lib/db/repositories/acknowledgments.ts`                                        | `record()` body filled; ADR-018 no update/delete keys          | VERIFIED   | 3467 bytes; `record()` at line 64-82 does INSERT + ON CONFLICT DO NOTHING + D-10 silent-success ops log; NO `update:` or `delete:` keys (grep verified).                                              |
| `lib/db/repositories/policy_assignments.ts`                                     | `create()` body filled                                         | VERIFIED   | 2208 bytes; `create()` returns inserted array with ON CONFLICT DO NOTHING.                                                                                                                           |
| `lib/db/repositories/policies.ts`                                               | `listAssignedAndPublishedForUser()` new method                 | VERIFIED   | 12964 bytes; new method at lines 134-206 with selectDistinct + 2x acknowledgments alias + 3-state CASE → ackState enum.                                                                              |
| `lib/db/repositories/qa_citation_grants.ts`                                     | New `QaCitationGrants` with listForUser/upsert/hasGrant         | VERIFIED   | 5645 bytes; 3 methods; `hasGrant(s, userId, policyId: PolicyId)` brand-typed per D-27.                                                                                                               |
| `lib/db/repositories/departments.ts`                                            | `listAll(s)` with asc(name) ordering                           | VERIFIED   | Modified for `asc(departments.name)` ordering per Plan 05-06.                                                                                                                                        |
| `lib/policies/errors.ts`                                                        | PolicyDomainError hierarchy (3 subclasses)                     | VERIFIED   | 5524 bytes; abstract PolicyDomainError + PolicyNotFoundError + PolicyArchivedError + PolicyNotAssignedError; ADR-026 shape (literal `code` union + explicit `this.name`).                            |
| `lib/policies/acknowledgment.ts`                                                | `recordAcknowledgment` atomic single-tx orchestrator           | VERIFIED   | 8762 bytes; 4 sub-ops inside single `withOrgScope` per D-10a; PolicyArchivedError + PolicyNotAssignedError + PolicyNotFoundError thrown per D-07/D-08.                                              |
| `lib/ai/qa.ts`                                                                  | Extracted `askQuestion` orchestrator                            | VERIFIED   | 9814 bytes; D-26 grant UPSERT (iterates post-validIds-filter) + D-27a accessibility annotation; Phase 4 D-41 same-closure defense preserved.                                                          |
| `app/api/ai/qa/route.ts`                                                        | Thin HTTP wrapper (≤50 lines)                                  | VERIFIED   | Refactored 147→49 lines per Plan 05-04; auth-outside-try, Zod parse, delegate, error mapping.                                                                                                        |
| `app/(employee)/layout.tsx`                                                     | Force-dynamic minimal employee shell                            | VERIFIED   | 1996 bytes; getOrgContext gate, no requireAdmin.                                                                                                                                                      |
| `app/(employee)/my-policies/page.tsx`                                           | Real dashboard (replaces 03-G3 T9 stub)                        | VERIFIED   | 3836 bytes; calls `listAssignedAndPublishedForUser`; D-04a empty-state copy verbatim with U+2014; "Ask the AI" header link.                                                                          |
| `app/(employee)/my-policies/[id]/page.tsx`                                      | D-27 3-branch access-aware detail page                         | VERIFIED   | 6619 bytes; full / TL;DR-only (verbatim D-27 banner) / notFound branches inside ONE withOrgScope closure.                                                                                            |
| `app/(employee)/my-policies/[id]/actions.ts`                                    | `acknowledgePolicyAction` Server Action                        | VERIFIED   | 4078 bytes; Zod brand parse, x-forwarded-for first hop, 3 typed-error mappings, 2 revalidatePath OUTSIDE try.                                                                                        |
| `app/(employee)/my-policies/ask/page.tsx`                                       | R-6 Q&A shell                                                  | VERIFIED   | 1113 bytes; renders AskQuestionForm.                                                                                                                                                                 |
| `app/(employee)/my-policies/ask/actions.ts`                                     | `askQuestionAction` Server Action                              | VERIFIED   | 3026 bytes; calls `askQuestion(ctx, q)` directly (no HTTP); Anthropic.APIError → semantic 503.                                                                                                       |
| `components/employee/AcknowledgeButton.tsx`                                     | React 19 useActionState ack form                               | VERIFIED   | 2921 bytes; formState-over-isPending per RESEARCH Pitfall 5.                                                                                                                                         |
| `components/employee/AskQuestionForm.tsx`                                       | React 19 useActionState Q&A form                               | VERIFIED   | 3918 bytes; clickable citation Links + D-27a italic hint for tldr-only.                                                                                                                              |
| `components/policy/AckStatusBadge.tsx`                                          | Server Component exhaustive switch on ackState enum            | VERIFIED   | 2541 bytes; 3 branches (none → null; stale → amber outline Badge; current → green ✓ inline span). `components/ui/badge.tsx` UNCHANGED — D-11 invariant satisfied.                                       |
| `components/admin/PolicyAssignmentsPanel.tsx`                                   | Server Component bulk-assign panel                             | VERIFIED   | 3663 bytes; renders at BOTTOM of /policies/[id] per D-13.                                                                                                                                            |
| `components/admin/PolicyAssignmentsPanelForm.tsx`                               | Client child with React 19 useActionState                      | VERIFIED   | 4231 bytes; D-14 empty-dept disabled button + tooltip + fallback `<p>`.                                                                                                                              |
| `scripts/check-acknowledgment-immutability.ts`                                  | ts-morph CI gate + raw-SQL bypass detection                    | VERIFIED   | 14243 bytes; default mode exits 0 (0 violations across 53 lib/ files); self-test mode exits 0 (2 violations in fixture, gate non-vacuous, H-1 EAPI closed).                                          |
| `tests/fixtures/ack-mutation-attempt.ts`                                        | Negative-control fixture (D-20)                                | VERIFIED   | 2291 bytes; 2 intentional violations (Drizzle-API + raw-SQL) exercising both detection paths.                                                                                                        |

**Score:** 25 / 25 artifacts VERIFIED.

### Key Link Verification (Wiring)

| From                                                | To                                                        | Via                                                   | Status   | Details                                                                                                                                                                                  |
| --------------------------------------------------- | --------------------------------------------------------- | ----------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/(employee)/my-policies/page.tsx`               | `Policies.listAssignedAndPublishedForUser`                | `await withOrgScope(ctx, s => Policies.list...)`      | WIRED    | Imported + called inside withOrgScope closure; result mapped to per-row AckStatusBadge.                                                                                                  |
| `acknowledgePolicyAction`                           | `recordAcknowledgment`                                    | Direct import + invocation                            | WIRED    | actions.ts:76 — `await recordAcknowledgment(ctx, parsed.data.policyId, ipAddress)`.                                                                                                      |
| `recordAcknowledgment`                              | `Acknowledgments.record` + `PolicyVersions.findByVersionNumber` | Inside `withOrgScope` tx                              | WIRED    | acknowledgment.ts:135-178 — 4 sub-ops in one tx (D-10a atomic).                                                                                                                          |
| `acknowledgePolicyAction`                           | `revalidatePath('/my-policies')` + `revalidatePath('/my-policies/[id]')` | Outside try/catch per D-10c                           | WIRED    | actions.ts:104-105.                                                                                                                                                                      |
| `AcknowledgeButton` Client                          | `acknowledgePolicyAction` Server                          | React 19 `useActionState` + formState                 | WIRED    | UAT EMP-12 confirmed inline state update without page reload (RESEARCH Pitfall 5 workaround verified).                                                                                  |
| `app/(employee)/my-policies/ask/actions.ts`         | `askQuestion(ctx, q)` orchestrator                        | Direct import (no HTTP)                               | WIRED    | actions.ts → askQuestion in lib/ai/qa.ts.                                                                                                                                                |
| `askQuestion`                                       | `QaCitationGrants.upsert` + `Policies.listAssignedAndPublishedForUser` | Inside same withOrgScope closure                      | WIRED    | qa.ts iterates `parsed.citations` (post-validIds-filter per RESEARCH gap-3) + derives accessibility flag.                                                                                |
| `/my-policies/[id]/page.tsx`                        | `QaCitationGrants.hasGrant` + `Policies.findById` + `Policies.listAssignedAndPublishedForUser` | Discriminated AccessResult union returned from ONE withOrgScope | WIRED    | page.tsx:88-135 — 3-branch logic with atomic RLS evaluation; assigned → full PolicyView; grant → TL;DR-only banner; else → notFound().                                                  |
| `bulkAssignToDepartmentAction`                      | `PolicyAssignments.create` + `revalidatePath`             | Inside withOrgScope; revalidates /policies/[id] + /my-policies | WIRED    | UAT ADMIN-4 verified D-14 empty-dept disabled-button UX.                                                                                                                                |
| `PolicyAssignmentsPanel` (admin)                    | `PolicyAssignments.listForPolicy` + `Departments.listAll` | Both queries inside ONE withOrgScope closure          | WIRED    | RSC shell reads both then projects dept subset to Client child form.                                                                                                                    |
| `tests/types.ts` D-07                               | `Acknowledgments.update` / `Acknowledgments.delete`       | `@ts-expect-error` invariants                          | WIRED    | `pnpm tsc --noEmit` exits 0 → keys absent → invariants hold.                                                                                                                            |
| `scripts/check-acknowledgment-immutability.ts`      | `lib/**/*.ts` (production)                                | ts-morph CallExpression walk + raw-SQL regex sub-pass | WIRED    | Default mode: 0 violations. Self-test mode: 2 violations in fixture; gate non-vacuous (H-1 closed at CI).                                                                              |
| `package.json` `verify:phase-5`                     | `verify:phase-4 && check:acknowledgment-immutability && :self-test && check:employee-portal` | Shell chain                                            | WIRED-BUT-FAILING | Chain step 1 (`verify:phase-4` → cascades through `pnpm test` → vitest) exits 1 due to transitions.test.ts regression. Subsequent chain steps not reached. See SG truth above. |

### Requirements Coverage

| Requirement                  | Source Plan(s)                                  | Description                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                       |
| ---------------------------- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ-acknowledgment-tracking  | 05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08, 05-09, 05-10 | Admin assigns policies to users or departments; employees see "Policies requiring acknowledgment" dashboard; one-click acknowledge with timestamp + IP; bulk assignment; ack rate computable per policy/dept/employee; ack append-only. | SATISFIED  | All 5 acceptance bullets covered: SC-1 (dashboard) + SC-2 (one-click + 5-field ack row + IP) + SC-3 (re-ack indicator) + SC-4 (bulk dept) + SC-5 (append-only). Ack rate per dept/employee deferred to Phase 8 compliance dashboard (REQ-compliance-dashboard) but the underlying append-only data model is in place. REQUIREMENTS.md traceability table marks this REQ "Complete". |
| REQ-acknowledgment-rules     | 05-01, 05-03, 05-04, 05-08                      | Acknowledgment shape `{user_id, policy_id, policy_version_id, acknowledged_at, ip_address}`; ACKs NEVER deleted; on policy update existing acks preserved + "requires re-acknowledgment" flag.            | SATISFIED  | 5-field shape verified via Drizzle schema at `lib/db/schema.ts` `acknowledgments` pgTable + integration test asserts row presence with all 5 fields. ADR-018 enforced at 3 layers (type-system + ts-morph CI + DB GRANT comment). Re-ack via 2-LEFT-JOIN pattern; UAT confirmed prior rows preserved + amber badge appears + DB COUNT preserved. REQUIREMENTS.md traceability table marks "Complete". |

**No ORPHANED requirements.** REQUIREMENTS.md lists exactly 2 REQs mapped to Phase 5 (REQ-acknowledgment-tracking + REQ-acknowledgment-rules); both appear in PLAN frontmatter `requirements` fields across the 10 plans + REVIEW.md and SUMMARY files; both marked "Complete" in the REQUIREMENTS.md traceability table.

### Anchoring Decisions Honored

| ADR | Decision | Status | Evidence |
| --- | -------- | ------ | -------- |
| ADR-018 | Acknowledgments are append-only (no UPDATE/DELETE app-side) | HONORED | 3-layer enforcement: type-system (tests/types.ts D-07 @ts-expect-error invariants pass under tsc --noEmit); ts-morph CI gate (`check:acknowledgment-immutability` exits 0); self-test mode proves non-vacuous (gate non-trivially detects both Drizzle-API + raw-SQL paths). |
| ADR-008 | Route group `(employee)` | HONORED | `app/(employee)/layout.tsx` exists with employee shell; routes under `/my-policies/**`. |
| ADR-009 | Employee gate | HONORED | `app/(employee)/layout.tsx` uses `getOrgContext` with NO admin role narrowing (admins can also be policy-assigned per spec — narrowing would be wrong). |
| ADR-023 | Per-aggregate repos | HONORED | All 4 new/extended repositories (acknowledgments, policy_assignments, policies, qa_citation_grants) take `OrgScope` first parameter; orchestrators use them. (Code review WR-04 flags one defense-in-depth gap in `lib/policies/acknowledgment.ts` where dept-id lookup runs `s.tx.select` directly on `users` rather than via `Users.findDeptId` — invariant still holds since `eq(users.orgId, s.orgId)` IS applied, but layering convention is slightly bent; documented in REVIEW.md as Warning, not Critical.) |
| ADR-025 | `withOrgScope` + RLS | HONORED | Every Phase 5 DB call flows through `withOrgScope(ctx, s => ...)`. RLS predicates (org_isolation policies) are the last-line defense; integration test AC-10 proves cross-org isolation under `SET LOCAL ROLE authenticated`. |
| ADR-026 | Typed errors | HONORED | New `lib/policies/errors.ts` mirrors ADR-026 `BootstrapError` shape (abstract base + literal `code` union + explicit `this.name`). `scripts/check-error-discipline.ts` widened to scan `lib/policies/**` per D-30 — exits 0. |
| ADR-028 | PolicyId brand | HONORED | Brand applied at trust boundaries: `acknowledgePolicyAction` lifts via `PolicyIdSchema.safeParse`; `recordAcknowledgment` accepts branded `PolicyId`; `QaCitationGrants.hasGrant` brand-typed signature. `scripts/check-policy-id-brand.ts` extended for Phase 5 surfaces (20/20 signatures verified). $inferInsert input types intentionally remain `string` per documented ADR-028 carve-out at `lib/policies/types.ts:23-31`. |

### Behavioral Spot-Checks

| Behavior                                                       | Command                                                  | Result                                                                        | Status   |
| -------------------------------------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------- | -------- |
| TypeScript compiles clean                                      | `pnpm tsc --noEmit`                                      | Exit 0 (no output)                                                            | PASS     |
| ADR-018 append-only CI gate (production scan)                  | `pnpm check:acknowledgment-immutability`                 | `OK — ADR-018 append-only: 0 .update/delete(acknowledgments) calls (53 files scanned)` (exit 0) | PASS     |
| ADR-018 self-test (gate non-vacuous proof)                     | `pnpm check:acknowledgment-immutability:self-test`       | `OK — 2 violation(s) detected; both Drizzle-API + raw-SQL paths exercised` (exit 0) | PASS     |
| Phase 5 integration test against live TEST DB                  | `pnpm check:employee-portal`                             | `9 passed (9)` in 11.02s — R-1 (3) + R-3 + R-4 + R-6 + H-5 + H-6 + AC-10 all green | PASS     |
| Full verify:phase-5 chain (CLAUDE.md ship gate)                | `pnpm verify:phase-5`                                    | `Test Files 1 failed | 27 passed (28); Tests 1 failed | 228 passed (229); ELIFECYCLE Command failed with exit code 1` — fails at `verify:phase-4 → pnpm test` step due to transitions.test.ts:206 regression introduced by `afb7693` | **FAIL** |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| (No `scripts/*/tests/probe-*.sh` convention in this project) | n/a | SKIPPED — phase uses npm/vitest gates documented above | SKIP |

### Anti-Patterns Found

Reviewed code review report `.planning/phases/05-employee-portal/05-REVIEW.md` (0 Critical / 7 Warning / 6 Info findings). All 7 warnings are non-blocking quality concerns properly classified by the code-reviewer:

| File                                                  | Line(s) | Pattern                                                                                                                   | Severity   | Impact                                                                                                                                                                                                                                       |
| ----------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/api/ai/qa/route.ts`                              | 38      | WR-01: Stale `err.error?.type` access (transitions.ts fix not propagated to qa/route.ts)                                  | ⚠ Warning  | Cosmetic / inconsistency; no runtime breakage today (SDK generic happens to keep `.error?.type` accessible). Low risk; minor tech debt.                                                                                                       |
| `components/policy/AckStatusBadge.tsx` + page.tsx     | 33-34, 91-92 | WR-02: `ackedAt` prop typed `Date | null` but functionally always non-null on 'current' branch                          | ⚠ Warning  | Defensive guard is functionally dead; if LEFT JOIN ever changed, badge would silently render "Acknowledged on " with trailing space. Discriminated-union refactor recommended; not a regression.                                              |
| `lib/db/repositories/policies.ts`                     | 138     | WR-03: Dept sub-query relies on PG `IN (NULL)` semantics + `assignee_id NOT NULL` schema constraint                       | ⚠ Warning  | Composes correctly today; defense-in-depth concern only. Integration test (R-1 D-02 case) explicitly covers it. Documented but fragile if future refactor adds OR-clause.                                                                    |
| `lib/policies/acknowledgment.ts`                      | 114-119, 161-172 | WR-04: Orchestrator runs raw `s.tx.select` on `users` + `acknowledgments` rather than via repository methods       | ⚠ Warning  | ADR-023 layering convention bent; security invariant (orgId scoping) preserved at each call site. Future contributors may copy the pattern. Refactor to add `Users.findDeptId` + `Acknowledgments.findExisting` recommended.                  |
| `lib/ai/qa.ts`                                        | 182-186 | WR-05: Calls expensive 5-table `listAssignedAndPublishedForUser` JOIN on every Q&A request just to derive accessibility flag | ⚠ Warning  | Performance concern at MVP scale (<100 assignments) acceptable; documented trade-off. Narrower `listAssignedIdsForUser` or per-citation `hasAssignment` recommended for Phase 7+.                                                              |
| `components/employee/AcknowledgeButton.tsx`           | 53-60   | WR-06: Success branch permanently masks subsequent failures within transient window                                       | ⚠ Warning  | Parent page gates render via `ackState !== 'current'`, so success branch is unreachable post-revalidation. Brief window between action-return + revalidation-completion is the only exposure. Cosmetic + accessibility (aria-live recommended). |
| `app/(employee)/my-policies/[id]/page.tsx`            | 88-107  | WR-07: Branch A omits `status='published'` re-check on freshly-fetched policy                                             | ⚠ Warning  | Race-window UI inconsistency only — `acknowledgePolicyAction` correctly catches via `PolicyArchivedError` at Server Action layer. No security boundary breached. Adding 1-line status check recommended for symmetry with Branch B.            |

All 6 Info-level findings are nits (locale hardcoding, parameter naming convention, dead discriminator codes, etc.) — none affect goal achievement.

**Debt markers (TBD/FIXME/XXX) scan:** None of the 22+ Phase 5 files contain unreferenced TBD/FIXME/XXX markers (verified via Grep tool against Phase 5 paths).

### Human Verification Required

None — Operator UAT completed 2026-05-24T06:30Z with 18 PASS + 1 PASS-with-finding (the 1 finding was fixed inline as `6ac3e4e` and re-verified live). All visual styling, copy verbatim, real-time React 19 useActionState UX, cross-org isolation, and acknowledgment-row persistence were operator-confirmed against live `pnpm dev` server + Clerk dev session + DEV DB. Phase-5-specific human checks are CLOSED.

### Gaps Summary

**One blocker gap:** `pnpm verify:phase-5` exits 1 due to a single test regression in `lib/policies/transitions.test.ts:206` (`editPublished` test). The regression was introduced by the inline fast-follow fix `afb7693` (Phase 3 DUP-VN-2 fix) — a production code change in `lib/policies/transitions.ts` that correctly removed a duplicate `PolicyVersions.create` call, but the corresponding test assertion in `transitions.test.ts` was not updated to match the new behavior.

**This is intentional code, broken test** — not a real Phase 5 feature gap.

The gap fix is mechanical:
1. Rewrite `lib/policies/transitions.test.ts` test at lines 194-218 (`snapshots prior published content + resets status + bumps version`) to assert NEW behavior: `pvCreateMock` NOT called; `txUpdateMock` called with `currentVersion + 1` and `status: 'draft'`.
2. Re-run `pnpm verify:phase-5` end-to-end and confirm exit 0.
3. Update Plan 05-10 SUMMARY (Task 2 AUTO-19 row) to record the re-verification.

The fix preserves the Phase 3 transitions production-code change (which was a real bug fix landed during Phase 5 UAT to unblock the re-ack flow), and aligns the test suite with the corrected behavior. No SPEC or REQ deviation.

**Suggested override pattern not applicable here** — the test failure is not an intentional Phase 5 deviation; it is a missed test update that must be closed before phase squash to keep `main` green per CLAUDE.md "End of each phase: run `tsc --noEmit` AND `verify:phase-N` (both exit 0) → squash to `main`" rule.

---

_Verified: 2026-05-24T07:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Files reviewed: 22 Phase 5 artifacts + 10 PLAN/SUMMARY files + REVIEW.md + ROADMAP.md + REQUIREMENTS.md_
_Commands executed: `pnpm tsc --noEmit` (exit 0), `pnpm verify:phase-5` (exit 1), `pnpm check:acknowledgment-immutability` (exit 0), `pnpm check:acknowledgment-immutability:self-test` (exit 0), `pnpm check:employee-portal` (exit 0, 9/9 tests), `pnpm vitest run lib/policies/transitions.test.ts` (1 fail / 19 pass at HEAD; 20/20 pass at afb7693~1 — regression confirmed)_
