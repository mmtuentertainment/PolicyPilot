---
context: phase
phase: 05-employee-portal
task: post-ship-review-complete
total_tasks: 10
status: paused-awaiting-operator-decisions
last_updated: 2026-05-24T16:47:40.532Z
---

# 🧠 REHYDRATE PROMPT — ULTRATHINK MODE REQUIRED

> **On resume, ULTRATHINK the Recommended Action queue below.**
>
> The 6-item list is not a checklist to execute mechanically. Each item carries trade-offs that must be reasoned about BEFORE acting. Specifically reason about:
>
> 1. **Migration-vs-merge ordering** — per CLAUDE.md Database Migration Discipline, "deploy code only AFTER `pnpm db:verify:<env>` exits 0". If `main` auto-deploys to Vercel, **migrate prod first then squash-merge**, NOT the order I synthesized in the review report. Operator knows the deploy topology — confirm before acting.
> 2. **Fast-follow bundling** — should ra-3 (error-handling fix) include the schema.ts count fix (comment-analyzer #1)? Should ra-3 + ra-4 collapse into one PR? Where does ra-6 (tests) fit? Scope discipline preserves the Phase 3 PR #5 precedent.
> 3. **Parametrize-now vs defer for ra-5** — extending the ts-morph immutability gate to `qa_citation_grants` is small and high-leverage, but it adds maintenance surface to a file that may not need it until cleanup-cron lands in Phase 7+.
>
> Once you've ultrathought the trade-offs, surface a recommended sequence + bundling plan to the operator with `AskUserQuestion` BEFORE executing any code changes.

---

<current_state>

**Phase 5 is shipped.** PR #27 (`gsd/phase-5-employee-portal` @ `b5fc134`) is OPEN · APPROVED · CLEAN · MERGEABLE. CodeRabbit closed 4 review rounds (commits `e997e70` → `4e171c5` → `3b2247b` → `b5fc134` — all doc-only polish). UAT walked 18 PASS + 1 PASS-with-finding via `/chrome` interactive walkthrough; 2 latent bugs surfaced + fixed inline (DUP-VN-2 `afb7693` + QA-PARSER-FENCE `6ac3e4e`). `verify:phase-5` chain exits 0 end-to-end in 92s (4/4 green).

This session ran a **4-agent human-level review** on top of CR's approval (pr-review-toolkit pattern: code-reviewer + silent-failure-hunter + pr-test-analyzer + comment-analyzer) + my own architectural deep-read on the load-bearing files. Full synthesis in `.planning/reports/20260524-phase-5-ship-review-report.md`.

**Verdict**: zero blocking issues. PR #27 is mergeable as-is. The review surfaced 3 Important + ~10 Suggestions worth of forward-looking polish — none gate ship.

Working tree: clean except `Designprototypes/` untracked (operator design exploration; pre-existing; non-GSD; leave alone).

</current_state>

<completed_work>

**Phase 5 Plans (10/10 complete)**:
- Wave 1: 05-01 (schema migrations 0010+0011) · 05-02 (PolicyDomainError hierarchy) · 05-07 (AckStatusBadge)
- Wave 2: 05-03 (repositories) · 05-04 (orchestrators recordAcknowledgment + askQuestion extract)
- Wave 3: 05-05 (employee routes + Server Actions) · 05-06 (admin bulk-assign + PolicyAssignmentsPanel)
- Wave 4: 05-08 (CI gates incl. check-acknowledgment-immutability ts-morph gate) · 05-09 (integration test scripts/check-employee-portal.test.ts — 1117 lines)
- Wave 5: 05-10 (verify chain + operator UAT)

**Post-ship**:
- UAT 18 PASS + 1 PASS-with-finding (operator-approved 2026-05-24T06:30Z)
- DUP-VN-2 surfaced + fixed inline (`afb7693` — Phase 3 editPublished dup snapshot)
- QA-PARSER-FENCE surfaced + fixed inline (`6ac3e4e` — Phase 4 parser whitespace tolerance)
- PR #27 opened + 4 CR review rounds closed (all doc-only)
- 4-agent multi-agent review complete — synthesis report at `.planning/reports/20260524-phase-5-ship-review-report.md`

</completed_work>

<remaining_work>

## Recommended Action queue (ULTRATHINK before executing)

```
1. Squash-merge PR #27 to main
   gh pr merge 27 --squash --delete-branch
   git checkout main && git pull --ff-only
   ⚠ ORDERING DECISION: ULTRATHINK whether to apply migrations to prod FIRST
   (per CLAUDE.md Database Migration Discipline) if main auto-deploys to Vercel.
   No merge-blockers. CR APPROVED. UAT 18/19 PASS. verify:phase-5 green.

2. Apply 0010 + 0011 to staging then prod via the runbook
   pnpm db:wait-pooler-auth:staging && pnpm db:migrate:staging && pnpm db:verify:staging
   (operator pause to soak)
   pnpm db:wait-pooler-auth:prod && pnpm db:migrate:prod && pnpm db:verify:prod
   Append audit log entry to STATE.md per docs/runbooks/deploy-migrations.md template
   Both migrations are ADDITIVE (no DROP) — no ASK-FIRST needed per CLAUDE.md.
   Operator-only execution (Pattern 3 PowerShell wrapper; credentials never transit chat).

3. Fast-follow #1 — single commit, high leverage (~15 lines):
   - Hoist getOrgContext() OUTSIDE try in both employee Server Actions:
       app/(employee)/my-policies/[id]/actions.ts:75 → above line 74
       app/(employee)/my-policies/ask/actions.ts:58 → above line 57
     (Both file headers claim "Phase 3 D-09 — auth-bootstrap-error split" but
     implementation contradicts the claim — BootstrapError currently surfaces as
     Next.js framework boundary instead of mapped error UX. The HTTP route
     app/api/ai/qa/route.ts:18 follows the convention correctly.)
   - Add console.error('[handler] unexpected', { ... }) breadcrumb before
     `throw err` in both Server Actions (mirrors the admin action pattern at
     app/(admin)/policies/[id]/actions.ts:454-459 + D-36 PII-safe shape from
     app/api/ai/qa/route.ts:33-47)
   - Add fallthrough `else if (err instanceof Error)` branch in ask action that
     returns the 503-equivalent error envelope (matches the comment claim at
     ask/actions.ts:17 — currently only Anthropic.APIError is caught)
   - Fix lib/db/schema.ts:1 file-header count: "13 tables: 11 tenant-scoped"
     → "14 tables: 12 tenant-scoped" (the qaCitationGrants add documented
     correctly two paragraphs lower; the leading count was missed)
   ⚠ BUNDLING DECISION: Should the schema.ts count fix land in #1 or wait
   for #4 (comment metadata sweep)? Recommend #1 — same correctness theme;
   tiny diff; lowers chance of forgetting.

4. Fast-follow #2 — comment metadata sweep (~6 file edits):
   - lib/ai/qa.ts:2 — "route.ts:41-115" → "route.ts" (post-extraction route
     is now 54 lines total; the historical anchor is dead)
   - lib/ai/qa.ts:25, 169 — "qa-parser.ts:54" → "qa-parser.ts:59" or drop
     line number (file grew 5 lines)
   - lib/policies/errors.ts:36 — "Plan 05-08 will widen" → "Plan 05-08 widened"
   - lib/db/repositories/qa_citation_grants.ts:110-112 — "WILL be added" →
     "Plan 05-08 added (line 67)"
   - lib/policies/acknowledgment.ts:79-80 — "Plan 05-08 widens" → "widened
     (line 91)"
   - lib/policies/transitions.ts:293 — "publish() (line 156-176)" → narrow to
     "publish() (lines 166-176, the PolicyVersions.create + status flip)"
   ⚠ BUNDLING DECISION: Can co-land with #3 if scope feels small enough,
   or split as a separate PR if #3 sprawls.

5. Phase 7+ obligation (track in carry-forward queue):
   - Parametrize scripts/check-acknowledgment-immutability.ts to also gate
     qa_citation_grants:
       const IMMUTABLE_TABLES = ['acknowledgments', 'qa_citation_grants'];
     (Currently only acknowledgments. qa_citation_grants is also DB-GRANT-permissive
     UPDATE+DELETE per drizzle/0011:56; the repo correctly exports no
     update/delete keys but that's enforced only by code review.)
   ⚠ PARAMETRIZE-NOW vs DEFER DECISION: ULTRATHINK.
       - Parametrize now: small change (~20 lines), forward-compatible, makes
         the gate cover both capability tables uniformly. Costs ~30min of work.
       - Defer to Phase 7+ cleanup-cron landing: smaller cognitive load this
         PR window. Costs nothing now but risks the gate falling out of sync
         with the second capability table.
       Recommend parametrize-now if Fast-follow #1 + #2 are already opening
       a polish-PR window; otherwise document the deferred decision in
       05-CONTEXT.md and queue for Phase 7+ entry.

6. Test polish (low urgency):
   - Add expect(QaCitationGrants.upsert).toHaveBeenCalledTimes(N) in the H-5
     block of scripts/check-employee-portal.test.ts:860 — locks the D-26
     raw-fence-iteration regression at the call site (not just the row count).
   - Add components/admin/PolicyAssignmentsPanelForm.test.tsx — React-tier
     test for D-14 empty-state UX (disabled selector + disabled button when no
     departments). Pattern reuse from components/policy/PolicyEditor.test.tsx.
   ⚠ COULD FILE AS GH ISSUES instead of executing — non-blocking, regression-
     lock-quality work that's a natural backlog item.
```

</remaining_work>

<decisions_made>

- **Multi-agent human-level review on top of CR approval** — operator-requested deeper second pass after CR's 4 rounds; surfaced 3 architecturally substantive findings (BootstrapError mapping, qa_citation_grants gate gap, error-handling asymmetry) that CR did not flag.
- **Review verdict: zero blocking; PR #27 mergeable as-is** — all Phase 5 invariants verified intact across both my deep-read and 4 agents.
- **All findings deferred to post-merge fast-follows** — quality improvements, not ship-blockers; bundling discipline preserves PR scope hygiene per Phase 3 PR #5 precedent (CR auto-fix scope-creep lesson).

</decisions_made>

<blockers>
- None. PR #27 is GH-greenlit. The only pending items are operator decisions (above) and operator-only infra actions (migration sequence).
</blockers>

## Required Reading (in order)

1. `.planning/reports/20260524-phase-5-ship-review-report.md` — full review synthesis (findings, strengths, recommended action) — written this session
2. `.planning/STATE.md` § Session Continuity — last 5 entries cover the Phase 5 ship loop + CR review cycles + this session's review pass
3. `CLAUDE.md` § Database Migration Discipline — load-bearing for ULTRATHINK on migration-vs-merge ordering (ra-1 + ra-2)
4. `docs/runbooks/deploy-migrations.md` — operator-facing procedure for the Phase 5 staging+prod migration sequence
5. `app/(employee)/my-policies/[id]/actions.ts` + `app/(employee)/my-policies/ask/actions.ts` — files modified by ra-3 (error-handling Fast-follow)
6. `lib/db/schema.ts:1` (count fix), `lib/ai/qa.ts:2, 25, 169` (line refs), `lib/policies/errors.ts:36`, `lib/db/repositories/qa_citation_grants.ts:110-112`, `lib/policies/acknowledgment.ts:79-80`, `lib/policies/transitions.ts:293` — files touched by ra-4 (comment sweep)
7. `scripts/check-acknowledgment-immutability.ts` — file to parametrize in ra-5 (qa_citation_grants gate)
8. `app/api/ai/qa/route.ts` — reference for the D-37 "auth OUTSIDE try" convention + D-36 PII-safe log shape that ra-3 mirrors

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| **CR auto-fix scope creep** | Phase 3 PR #8: CR's "Autofix" feature ran `pnpm install` as a side-effect and bumped unauthorized dependencies; required surgical revert via additive commit (`062f341`). | advisory | Always inspect the diff before merging a CR auto-fix; prefer surgical revert over `git reset --hard`. (No CR auto-fix on PR #27 — informational carry-forward.) |
| **Bundling-without-scope-check** | Phase 3 PR #5 precedent: stale `file:line` cross-references rot fast when bundling small fixes. | advisory | When sizing Fast-follow PRs, prefer self-locating snippets over line numbers; verify each cross-ref points where intended before committing. |

## Infrastructure State

- **Branch**: `gsd/phase-5-employee-portal` @ `b5fc134` (57 ahead of `main` @ `c50b317`)
- **PR**: #27 OPEN · APPROVED · CLEAN · MERGEABLE
- **CR status**: SUCCESS @ 2026-05-24T16:22:20Z on `b5fc134`
- **Verify chain**: green (92s, 4/4 against live DEV + TEST DB)
- **DB state**:
  - DEV @ HEAD (12 migrations + qa_citation_grants table + 3 new constraints + Wave 1 schema deltas applied 2026-05-24)
  - TEST @ HEAD (operator rotated credentials 2026-05-24; pooler-auth propagated; verify passes)
  - STAGING @ Phase 4 (0010 + 0011 NOT YET APPLIED)
  - PROD @ Phase 4 (0010 + 0011 NOT YET APPLIED)
- **Untracked**: `Designprototypes/ookingforsomethingmoreaggressiveandcutting....html` — single HTML file, operator design exploration, non-GSD, leave alone
- **Carry-forward blockers (pre-existing, unchanged)**:
  - SF-WHSEC-1: Clerk webhook signing secret pasted in chat during Phase 2 → rotate via Svix Dashboard before any Phase 7 webhook live-smoke (one-click, no code change)

<context>

**Mental model going into the next session**: PR #27 is mergeable as-is. The 4-agent review is forward-looking polish, not a gate. Architecture is sound at every load-bearing layer:
- D-27 BOLA boundary atomic (one withOrgScope closure wraps all 4 sub-queries)
- R-6 cross-org grant safety via same-closure validIds + filter-iterate (D-41 SP-1 defense)
- ADR-018 3-layer enforcement: type system (D-07 @ts-expect-error) + ts-morph gate (Sub-pass 1 AST + Sub-pass 2 raw-SQL regex) + future REVOKE migration
- D-30 typed-error hierarchy mirrors ADR-026 BootstrapError shape cleanly
- DUP-VN-2 + QA-PARSER-FENCE fixes are surgical

The 4 agents converged on zero critical findings. Three architecturally substantive Important findings (I-1/I-2/I-3) all map to fast-follow PRs. The ~10 Suggestions are polish — comment metadata drift (6 fixes), 1 test regression-lock, 1 component-tier test, assorted error-handling UX improvements.

**Why ultrathink the Recommended Action?** Three real trade-offs need reasoning:
1. Ordering: CLAUDE.md says deploy code AFTER schema verify. If main auto-deploys to Vercel, my synthesis got ra-1/ra-2 backwards.
2. Bundling: scope discipline for ra-3/ra-4 needs a deliberate call (combined PR vs split).
3. ra-5 (parametrize-now vs defer): small change with high safety leverage but adds to current PR window.

</context>

<next_action>

**FIRST**: Read `.planning/reports/20260524-phase-5-ship-review-report.md` to absorb the full review verdict.

**SECOND**: ULTRATHINK the Recommended Action queue above. Reason through the three trade-offs:
- Migration-vs-merge ordering (ra-1/ra-2)
- Bundling discipline for ra-3/ra-4
- Parametrize-now vs defer for ra-5

**THIRD**: Surface your recommended sequence + bundling plan to the operator via `AskUserQuestion` BEFORE executing any code/migration steps. Include rationale for each decision.

**FOURTH**: Once operator confirms, execute the agreed sequence. ra-1 + ra-2 are operator-gated infra actions; you facilitate but don't run prod commands. ra-3..ra-6 are code changes you can execute on a fresh branch off `main` (post-squash-merge).

</next_action>
