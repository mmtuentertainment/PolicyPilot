---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - middleware.ts
  - lib/db/index.ts
  - lib/db/schema.ts
  - drizzle.config.ts
  - scripts/check-db.ts
autonomous: true
requirements:
  - REQ-product-vision

must_haves:
  truths:
    - "`middleware.ts` enforces ADR-009 public-route policy: `/`, `/pricing`, `/sign-in`, `/sign-up` reachable unauthenticated; everything else redirects to `/sign-in`"
    - "Webhook routes `/api/webhooks/stripe` and `/api/webhooks/clerk` are matcher-exempt (don't break Phase 2 / Phase 6)"
    - "`/api/cron/*` is matcher-exempt (don't break Phase 7)"
    - "Drizzle client is connectable from server code via `lib/db/index.ts` using `DATABASE_URL`"
    - "`lib/db/schema.ts` exists as empty export (per D-07 — table definitions are Phase 2)"
    - "`pnpm check:db` (alias for `pnpm tsx scripts/check-db.ts`) prints `OK` and exits 0 after running `select 1`"
    - "`SUPABASE_SERVICE_ROLE_KEY` is NOT imported anywhere in Client Components (server-only)"
  artifacts:
    - path: "middleware.ts"
      provides: "Clerk auth + role-routing chokepoint (ADR-009 / D-10)"
      contains: "clerkMiddleware"
    - path: "lib/db/index.ts"
      provides: "Drizzle client over postgres-js, connects to Supabase pooler (D-06 / D-07)"
      contains: "drizzle"
    - path: "lib/db/schema.ts"
      provides: "Empty schema placeholder (D-07 — tables in Phase 2)"
      contains: "export {}"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit config pointing at lib/db/schema.ts + DATABASE_URL"
      contains: "schema:"
    - path: "scripts/check-db.ts"
      provides: "Tier-0 smoke check — `select 1` over Drizzle (success criterion 4)"
      contains: "select 1"
  key_links:
    - from: "lib/db/index.ts"
      to: "process.env.DATABASE_URL"
      via: "postgres-js connection string"
      pattern: "process\\.env\\.DATABASE_URL"
    - from: "middleware.ts"
      to: "@clerk/nextjs/server"
      via: "clerkMiddleware factory"
      pattern: "from \"@clerk/nextjs/server\""
    - from: "scripts/check-db.ts"
      to: "lib/db/index.ts"
      via: "import db; await db.execute(sql\\`select 1\\`)"
      pattern: "from \"@/lib/db\""
---

<objective>
Wire the Clerk middleware (the single auth chokepoint per ADR-009 / D-10) and the Drizzle skeleton (D-07). After this plan, success criteria 4 (Supabase `select 1` via Drizzle) and 5 (middleware public-route policy) are satisfied; Plan 05 verifies them end-to-end.

Purpose: This plan is Plan 03's parallel sibling in Wave 3. Plan 03 builds the rendered UI surface; this plan builds the auth + data-access plumbing. Zero file overlap — they run concurrently with no merge conflict.

Output: Working `middleware.ts` + `lib/db/index.ts` + empty `lib/db/schema.ts` + `drizzle.config.ts` + a `scripts/check-db.ts` smoke script. `pnpm check:db` exits 0 when run with valid `.env.local`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/intel/decisions.md
@CLAUDE.md
@BLUEPRINT.md
@reference/SCHEMA.md
@.env.local.example

<interfaces>
<!-- Key contracts the executor needs. -->

From `@clerk/nextjs/server` (v5+ middleware API):
- `clerkMiddleware(handler?, options?)` — factory returning a Next.js middleware function. Replaces the v4 `authMiddleware`.
- `createRouteMatcher(patterns: string[])` — helper that returns `(req) => boolean`. Use with route group glob patterns.

From `drizzle-orm/postgres-js`:
- `drizzle(client, config?)` — returns a `PostgresJsDatabase`. Pass a `postgres` client.
- `sql` — tagged template export from `drizzle-orm`. Used for raw SQL: `await db.execute(sql\`select 1\`)`.

From `postgres` (the `postgres-js` driver):
- `postgres(connectionString, options?)` — returns a query client. Per Supabase pooler docs, the `prepare: false` option must be set when using the transaction pooler (port 6543) — the pooler does not support prepared statements.

From `drizzle-kit` config schema (`drizzle.config.ts`):
- Type: `Config` from `drizzle-kit`.
- Required keys: `schema`, `out`, `dialect` (`'postgresql'`), `dbCredentials.url`.

From Next.js 15 middleware:
- File location: `middleware.ts` at repo root (sibling of `app/`).
- Export: `default` (the middleware function) AND `config` (an object with `matcher: string[]`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `middleware.ts` — Clerk auth + public-route policy (ADR-009 / D-10)</name>
  <files>middleware.ts</files>

  <read_first>
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-10 (Phase 1 middleware policy: public routes, webhook + cron exemption, role gating present but inert because admin/employee routes don't exist yet)
    - `BLUEPRINT.md` §4 (middleware architecture — verbatim public-route policy)
    - `.planning/intel/decisions.md` ADR-009 (single auth chokepoint, role gating rules)
    - `CLAUDE.md` — Multi-Tenancy Rules section (informational; multi-tenancy enforcement is Phase 2 RLS, but the middleware is the upstream gate)
  </read_first>

  <action>
    Create `middleware.ts` at the repo root (NOT under `app/` — Next.js middleware lives at the project root).

    The file uses the v5+ `clerkMiddleware` API from `@clerk/nextjs/server` (the v4 `authMiddleware` is deprecated; do not use it).

    ### Concrete file content for `middleware.ts`:

    Imports:
    - `import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";`
    - `import { NextResponse } from "next/server";`
    - `import type { NextRequest } from "next/server";`

    Define route matchers — keep them as module-scope constants (`createRouteMatcher` is hoisted by Next.js):

    ```
    const isPublicRoute = createRouteMatcher([
      "/",
      "/pricing",
      "/sign-in(.*)",
      "/sign-up(.*)",
    ]);

    const isWebhookRoute = createRouteMatcher([
      "/api/webhooks/stripe",
      "/api/webhooks/clerk",
    ]);

    const isCronRoute = createRouteMatcher([
      "/api/cron/(.*)",
    ]);

    const isAdminRoute = createRouteMatcher([
      "/(admin)/(.*)",
    ]);
    ```

    Note: Public route patterns include `(.*)` for `/sign-in` and `/sign-up` to match the Clerk catch-all child routes (e.g. `/sign-in/factor-one`). The bare `/` and `/pricing` need exact match — `createRouteMatcher` treats string entries as path globs, and a missing trailing `(.*)` means exact match.

    Default export — the middleware itself:

    ```
    export default clerkMiddleware(async (auth, req: NextRequest) => {
      // 1. Webhook routes — bypass auth entirely. Signature verification happens
      // inside the route handler (Phase 2 for Clerk, Phase 6 for Stripe).
      if (isWebhookRoute(req)) {
        return NextResponse.next();
      }

      // 2. Cron routes — auth via CRON_SECRET header, NOT Clerk session.
      // Phase 7 wires the actual route; in Phase 1 the matcher is exempted
      // so that Phase 7's route lands cleanly without needing to revisit
      // middleware.
      if (isCronRoute(req)) {
        return NextResponse.next();
      }

      // 3. Public routes — landing, pricing, sign-in, sign-up — no auth.
      if (isPublicRoute(req)) {
        return NextResponse.next();
      }

      // 4. Role-gated admin routes (ADR-009). Phase 1 has no /(admin)/* routes
      // yet — these route group folders don't exist. The matcher is wired so
      // that when Phase 3 adds them, the gate is already in place. Until then
      // this branch is unreachable. Note: route groups (parens) do NOT appear
      // in the URL — the matcher uses the conventional /(admin)/(.*) pattern
      // for clarity, but in practice Phase 3 admin routes will be at /dashboard,
      // /policies, etc., not /admin/*. Phase 3 PLAN will adjust this matcher
      // when those routes are actually defined.
      if (isAdminRoute(req)) {
        const { sessionClaims } = await auth();
        const role = (sessionClaims?.publicMetadata as { role?: string } | undefined)?.role;
        if (role !== "admin") {
          // D-10: return 404 in Phase 1 (admin routes don't exist; surfacing
          // a 403 would imply they do). Phase 3 may convert this to a redirect.
          return new NextResponse(null, { status: 404 });
        }
        return NextResponse.next();
      }

      // 5. Default: any other route requires authentication. Redirect to /sign-in.
      const { userId } = await auth();
      if (!userId) {
        const signInUrl = new URL("/sign-in", req.url);
        signInUrl.searchParams.set("redirect_url", req.url);
        return NextResponse.redirect(signInUrl);
      }

      return NextResponse.next();
    });
    ```

    Config export — Next.js matcher (excludes static + image assets so middleware doesn't run on them; canonical Clerk pattern):

    ```
    export const config = {
      matcher: [
        // Run on everything except static files and Next.js internals.
        "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp|ico)$).*)",
        // Always run on API routes (so the webhook/cron branches above can act).
        "/(api|trpc)(.*)",
      ],
    };
    ```

    ### Re-typecheck

    From repo root:
    ```
    pnpm tsc --noEmit
    ```
    Must exit 0. Specific failure modes to handle (no `any` per CLAUDE.md NEVER #4):
    - If `sessionClaims?.publicMetadata` is typed as `unknown` (Clerk v5 default), narrow with the `as { role?: string } | undefined` cast shown above. This is a type assertion against Clerk's untyped JWT-claim shape, not a `any` cast.
    - If `createRouteMatcher` accepts only specific overloads, the string-array form is supported in v5+; if compilation fails, run `pnpm add @clerk/nextjs@latest` to pull the latest API surface.
  </action>

  <verify>
    <automated>if (-not (Test-Path middleware.ts)) { Write-Error "middleware.ts missing"; exit 1 }; $m = Get-Content middleware.ts -Raw; if ($m -notmatch 'from "@clerk/nextjs/server"') { Write-Error "Clerk server import missing"; exit 1 }; if ($m -notmatch 'clerkMiddleware') { Write-Error "clerkMiddleware not used"; exit 1 }; if ($m -notmatch 'export const config') { Write-Error "matcher config not exported"; exit 1 }; if ($m -notmatch 'matcher:') { Write-Error "matcher: key missing"; exit 1 }; foreach ($p in @('"/"','/pricing','/sign-in','/sign-up','/api/webhooks/stripe','/api/webhooks/clerk','/api/cron/')) { if ($m -notmatch [regex]::Escape($p)) { Write-Error "Route policy missing: $p"; exit 1 } }; if ($m -match '\bany\b\s*[:,)]') { Write-Error "any type detected in middleware.ts — CLAUDE.md NEVER #4 violation"; exit 1 }; pnpm tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `middleware.ts` exists at repo root (NOT under `app/`)
    - `middleware.ts` imports `clerkMiddleware` AND `createRouteMatcher` from `@clerk/nextjs/server`
    - `middleware.ts` references all four public routes literally: `"/"`, `/pricing`, `/sign-in`, `/sign-up`
    - `middleware.ts` references both webhook exemptions: `/api/webhooks/stripe`, `/api/webhooks/clerk`
    - `middleware.ts` references the cron exemption: `/api/cron/`
    - `middleware.ts` exports `default` (the middleware) AND `config` with a `matcher:` array
    - `middleware.ts` contains NO `: any` type annotation (CLAUDE.md NEVER #4)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    Middleware enforces D-10 policy verbatim; matcher excludes webhooks/cron; admin role gate is wired but inert in Phase 1; tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Drizzle skeleton — empty schema, db client, drizzle.config.ts (D-07)</name>
  <files>lib/db/index.ts, lib/db/schema.ts, drizzle.config.ts</files>

  <read_first>
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-06 (postgres-js + Supabase pooler URI on 6543) and D-07 (empty schema, smoke check via script not route)
    - `BLUEPRINT.md` §2 — `lib/db/` directory layout
    - `.planning/intel/decisions.md` ADR-003 (Drizzle ORM), ADR-011 (Supabase + RLS)
    - `reference/SCHEMA.md` (informational — confirms `lib/db/schema.ts` is THE source of truth for the full Phase 2 schema)
    - `.env.local.example` — confirms `DATABASE_URL` key exists (Plan 01 Task 3 added it)
  </read_first>

  <action>
    Create three files. None of them touch business logic — pure plumbing per D-07.

    ### File 1: `lib/db/schema.ts`

    Per D-07 — empty export. Phase 2 fills this with the full table set from `reference/SCHEMA.md`.

    Exact contents:
    ```
    // Drizzle schema — populated in Phase 2 per reference/SCHEMA.md.
    // Phase 1 ships an empty export so the Drizzle client and drizzle-kit can resolve the import.
    export {};
    ```

    ### File 2: `lib/db/index.ts`

    Per D-06 — `postgres-js` driver over Supabase's transaction pooler. Server-only file (no `'use client'`; not imported from any Client Component).

    Concrete file content:

    ```
    // Server-only. Do NOT import from a Client Component — this module reads
    // server-only env vars (DATABASE_URL) and instantiates a Postgres connection.
    import "server-only";
    import { drizzle } from "drizzle-orm/postgres-js";
    import postgres from "postgres";
    import * as schema from "./schema";

    const connectionString = process.env.DATABASE_URL;
    if (!connectionString) {
      throw new Error(
        "DATABASE_URL is not set. Copy .env.local.example to .env.local and " +
          "paste the Supabase Transaction pooler URI (port 6543) per D-06.",
      );
    }

    // prepare: false is required by Supabase's Transaction pooler (port 6543).
    // The pooler routes each query through a fresh connection and does not
    // support prepared statement caching. See D-06.
    const client = postgres(connectionString, { prepare: false });

    export const db = drizzle(client, { schema });
    export type Database = typeof db;
    ```

    Note on `import "server-only"`: this is a Next.js convention that causes a build-time error if a Client Component tries to import this module. The `server-only` package is bundled with Next.js — no separate install needed.

    ### File 3: `drizzle.config.ts`

    Per D-06 — `drizzle-kit` config. Points at `lib/db/schema.ts`, uses `DATABASE_URL`, outputs migrations to `drizzle/`.

    Concrete file content:

    ```
    import type { Config } from "drizzle-kit";

    const databaseUrl = process.env.DATABASE_URL;
    if (!databaseUrl) {
      throw new Error(
        "DATABASE_URL is required for drizzle-kit. Set it in .env.local per D-11.",
      );
    }

    export default {
      schema: "./lib/db/schema.ts",
      out: "./drizzle",
      dialect: "postgresql",
      dbCredentials: { url: databaseUrl },
      verbose: true,
      strict: true,
    } satisfies Config;
    ```

    Note: `drizzle/` is the output directory for `drizzle-kit generate`. Plan 01 Task 1 step 7 added `drizzle/` to `.gitignore` to keep generated migrations out of version control until Phase 2 makes a deliberate choice about committing them. That's intentional — do not stage generated migration SQL in Phase 1.

    ### Re-typecheck

    From repo root:
    ```
    pnpm tsc --noEmit
    ```
    Must exit 0.
  </action>

  <verify>
    <automated>foreach ($f in @('lib/db/index.ts', 'lib/db/schema.ts', 'drizzle.config.ts')) { if (-not (Test-Path $f)) { Write-Error "Missing: $f"; exit 1 } }; $schema = Get-Content lib/db/schema.ts -Raw; if ($schema -notmatch 'export \{\}') { Write-Error "schema.ts must be empty export per D-07"; exit 1 }; $idx = Get-Content lib/db/index.ts -Raw; if ($idx -notmatch 'import "server-only"') { Write-Error "lib/db/index.ts must declare server-only"; exit 1 }; if ($idx -notmatch 'drizzle-orm/postgres-js') { Write-Error "Drizzle postgres-js driver not used"; exit 1 }; if ($idx -notmatch 'process\.env\.DATABASE_URL') { Write-Error "DATABASE_URL not read"; exit 1 }; if ($idx -notmatch 'prepare:\s*false') { Write-Error "Supabase pooler requires prepare: false (D-06)"; exit 1 }; if ($idx -match '\bany\b\s*[:,)]') { Write-Error "any type detected in lib/db/index.ts — CLAUDE.md NEVER #4 violation"; exit 1 }; $cfg = Get-Content drizzle.config.ts -Raw; if ($cfg -notmatch 'schema:\s*"./lib/db/schema.ts"') { Write-Error "drizzle.config schema path wrong"; exit 1 }; if ($cfg -notmatch 'dialect:\s*"postgresql"') { Write-Error "drizzle.config dialect missing"; exit 1 }; if ($cfg -notmatch 'satisfies Config') { Write-Error "drizzle.config not type-safe"; exit 1 }; pnpm tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `lib/db/schema.ts` exists and contains exactly `export {};` (per D-07)
    - `lib/db/index.ts` exists, contains `import "server-only";`, imports from `drizzle-orm/postgres-js`, reads `process.env.DATABASE_URL`, sets `prepare: false`, exports `db`
    - `drizzle.config.ts` exists, points `schema` at `./lib/db/schema.ts`, uses `"postgresql"` dialect, ends with `satisfies Config`
    - No `: any` annotations in any of the three files (CLAUDE.md NEVER #4)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    Drizzle client compiles, schema.ts is empty per D-07, drizzle.config is type-safe, tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: `scripts/check-db.ts` — `select 1` smoke check (success criterion 4)</name>
  <files>scripts/check-db.ts</files>

  <read_first>
    - `.planning/phases/01-foundation/01-CONTEXT.md` `<specifics>` block — script-not-route approach per D-07
    - Task 2 output — confirms `lib/db/index.ts` exports `db` and `lib/db/schema.ts` exists
    - `package.json` (from Plan 01 Task 3) — confirms `"check:db": "tsx scripts/check-db.ts"` script is reserved
  </read_first>

  <action>
    Create `scripts/check-db.ts`. The script:
    1. Imports the Drizzle client from `lib/db/index.ts`.
    2. Runs `await db.execute(sql\`select 1 as ok\`)`.
    3. Asserts the result contains `{ ok: 1 }`.
    4. Prints `OK` and exits 0 on success; prints a descriptive error and exits 1 on failure.

    **Note on env loading:** `tsx scripts/check-db.ts` does NOT automatically load `.env.local` (Next.js loads it, but a raw tsx invocation does not). The script must load env vars before importing `lib/db/index.ts`, because the import-time throw in `lib/db/index.ts` runs as soon as the module evaluates.

    Two acceptable approaches:
    1. Use `tsx --env-file=.env.local scripts/check-db.ts` (Node 20+ supports `--env-file`, and `tsx` passes through). **Preferred.**
    2. Use `dotenv` package inline at the top of the script.

    Use approach #1 — it requires no extra install. Update the `package.json` `check:db` script to:
    ```
    "check:db": "tsx --env-file=.env.local scripts/check-db.ts"
    ```
    (Edit `package.json` to swap the existing `"check:db": "tsx scripts/check-db.ts"` line for the `--env-file` version. The `verify:phase-1` slot reserved in Plan 01 Task 3 stays as-is; Plan 05 updates it.)

    ### Concrete file content for `scripts/check-db.ts`:

    ```
    // Phase 1 success criterion 4: Supabase client connects via Drizzle.
    // Runs `select 1` and prints OK on success. Invoked by `pnpm check:db`.
    import { sql } from "drizzle-orm";
    import { db } from "@/lib/db";

    async function main(): Promise<void> {
      try {
        const rows = await db.execute(sql`select 1 as ok`);
        // postgres-js result shape: array-like with column accessors.
        // Drizzle's execute returns a `Row[]`-compatible array; the row at [0]
        // exposes columns by name. With `noUncheckedIndexedAccess`, rows[0] is
        // possibly-undefined — narrow before reading.
        const first = rows[0];
        if (!first || (first as { ok?: number }).ok !== 1) {
          console.error("Unexpected result from `select 1`:", rows);
          process.exit(1);
        }
        console.log("OK");
        process.exit(0);
      } catch (err) {
        console.error("Drizzle smoke check failed:");
        console.error(err instanceof Error ? err.message : err);
        process.exit(1);
      }
    }

    void main();
    ```

    The `as { ok?: number }` cast is a narrowing assertion against postgres-js's untyped row shape (postgres-js returns dynamic objects; Drizzle's `execute` does not infer a column type for raw `sql` queries). This is NOT `any` — it's a structural type assertion, permitted by CLAUDE.md NEVER #4.

    ### Run the smoke check

    From repo root, with `.env.local` populated (Plan 02 Task 3 output):
    ```
    pnpm check:db
    ```
    Expected: prints `OK`, exits 0.

    Failure modes to recognize:
    - `DATABASE_URL is not set` → `.env.local` missing or `tsx --env-file=.env.local` not loading. Confirm Plan 02 Task 3 wrote `.env.local`.
    - `SASL: SCRAM-SERVER-FIRST-MESSAGE` errors → wrong DB password. Operator pasted a stale URI; ask operator to re-grab from Supabase dashboard.
    - `prepared statement cached plan` errors → `prepare: false` not set in `lib/db/index.ts`. Plan 04 Task 2 acceptance asserts this; if you see this error, Task 2's verify failed silently.
    - Connection timeout → Supabase project still provisioning. Wait, retry.
  </action>

  <verify>
    <automated>if (-not (Test-Path scripts/check-db.ts)) { Write-Error "scripts/check-db.ts missing"; exit 1 }; $s = Get-Content scripts/check-db.ts -Raw; if ($s -notmatch 'select 1') { Write-Error "select 1 not present"; exit 1 }; if ($s -notmatch 'from "@/lib/db"') { Write-Error "must import db from @/lib/db"; exit 1 }; if ($s -notmatch 'process\.exit\(0\)') { Write-Error "must exit 0 on success"; exit 1 }; $pkg = Get-Content package.json -Raw; if ($pkg -notmatch 'check:db.*--env-file=\.env\.local') { Write-Error "package.json check:db must pass --env-file=.env.local"; exit 1 }; pnpm tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit 1 }; if (Test-Path .env.local) { pnpm check:db; if ($LASTEXITCODE -ne 0) { Write-Error "pnpm check:db failed — Supabase connection broken"; exit 1 } } else { Write-Warning ".env.local not present — skipping live smoke check. Plan 02 must run first."; exit 1 }; Write-Output "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/check-db.ts` exists and contains the literal substring `select 1`
    - `scripts/check-db.ts` imports `db` from `@/lib/db`
    - `scripts/check-db.ts` calls `process.exit(0)` on success and `process.exit(1)` on failure
    - `package.json` `check:db` script reads `tsx --env-file=.env.local scripts/check-db.ts`
    - `pnpm tsc --noEmit` exits 0
    - With `.env.local` present (Plan 02 output), `pnpm check:db` exits 0 and prints `OK`
  </acceptance_criteria>

  <done>
    `pnpm check:db` round-trips `select 1` against Supabase via Drizzle and exits 0 — success criterion 4 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public web → middleware.ts | Every request crosses here. Misconfiguration = either public exposure of admin/employee routes OR locked-out users. |
| Server code → Drizzle client | `lib/db/index.ts` holds the DATABASE_URL (with embedded password). Must stay server-only. |
| `lib/db/index.ts` → Client Components | Must NOT cross. `import "server-only"` is the gate. |
| `scripts/check-db.ts` → Supabase | One-shot smoke connection; closes after `select 1`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of privilege | `middleware.ts` matcher misconfiguration exposes a private route as public | mitigate (HIGH severity) | Task 1's acceptance criteria assert all four public routes literally AND the webhook + cron exemptions. The default branch redirects to `/sign-in` — fail-closed posture. Plan 05 adds a live HTTP probe (GET `/sign-in-success` unauthenticated → must return 307). |
| T-04-02 | Information disclosure | `DATABASE_URL` (with embedded DB password) imported into a Client Component | mitigate (HIGH severity) | `lib/db/index.ts` starts with `import "server-only"` — any Client Component importing it triggers a build-time error. Verified by Task 2 acceptance. |
| T-04-03 | Tampering | A Client Component imports `db` from `@/lib/db` to run a tenant-scoped query, bypassing API-route validation | mitigate (cross-phase) | Phase 1 has zero data-bearing routes. Phase 2 establishes the `org_id`-in-every-query rule (ADR-019) via lint + code review. The `server-only` gate from Task 2 catches the most basic misuse. |
| T-04-04 | Denial of service | Misconfigured `prepare: false` causing connection pool exhaustion on Supabase pooler | mitigate | Task 2 acceptance asserts `prepare: false` literally — Supabase docs are explicit that the transaction pooler requires this. Without it, queries error after a few seconds; with it, the pool churns connections correctly. |
| T-04-05 | Spoofing | Webhook bypass through the matcher exemption — an attacker sends arbitrary JSON to `/api/webhooks/stripe` posing as Stripe | mitigate (cross-phase) | Phase 6 wires the actual webhook handler with `stripe.webhooks.constructEvent(rawBody, signature, secret)`. Phase 1 only carves out the routing exemption — no handler exists yet, so any request returns 404 from Next.js's default. ADR-020 locks signature verification. |
| T-04-06 | Information disclosure | Connection string with password leaks via error message in `scripts/check-db.ts` | mitigate | Task 3's error handler logs `err.message` only (which postgres-js never includes the full URL in, only SQL/host info). Stack traces include the connection string ONLY in node's default unhandled rejection output — `try/catch` around the `await` prevents that path. |
| T-04-07 | Tampering | Drizzle smoke check accidentally creates a row or schema change | mitigate | The script runs `select 1` only — a no-op SELECT. No `CREATE`, `INSERT`, `UPDATE`, `DELETE`. Task 3 acceptance asserts the literal `select 1` substring. |

**ASVS L1 severity assessment:** Two `high` severity threats. T-04-01 is mitigated by literal-string assertions on every required matcher entry + the live probe in Plan 05. T-04-02 is mitigated by the `server-only` import gate. Both gates are verified before this plan is considered complete.
</threat_model>

<verification>
After all three tasks:

```
ls middleware.ts lib/db/index.ts lib/db/schema.ts drizzle.config.ts scripts/check-db.ts
pnpm tsc --noEmit       # exit 0
pnpm check:db           # exit 0, prints "OK" (requires Plan 02 output)
```

Cross-plan dependency: This plan finishes Wave 3 alongside Plan 03. Plan 05 (Wave 4) does the integrated HTTP probe (`localhost:3000` + middleware redirects).
</verification>

<success_criteria>
- `middleware.ts` enforces ADR-009 / D-10 policy verbatim
- Webhook + cron route matchers are exempted (no Phase 2 / Phase 7 breakage later)
- Drizzle client compiles + connects to Supabase pooler
- `lib/db/schema.ts` is intentionally empty (D-07)
- `pnpm check:db` exits 0 and prints `OK`
- `pnpm tsc --noEmit` exits 0
- No `: any` annotations introduced
- `lib/db/index.ts` declared `server-only` — never importable from a Client Component
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done, recording:
- Resolved `@clerk/nextjs` version (must be v5+ for the `clerkMiddleware` API used)
- Resolved `drizzle-orm` + `postgres` + `drizzle-kit` versions
- `pnpm check:db` round-trip latency (informational — sanity-check on pooler health)
- Any deviation from the action text (e.g. if Clerk's matcher syntax changed since 2026-05-15)
</output>
