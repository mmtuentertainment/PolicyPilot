---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - components.json
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/ui/input.tsx
  - lib/utils.ts
  - .gitignore
  - .env.local.example
  - eslint.config.mjs
  - .git/
autonomous: true
requirements:
  - REQ-product-vision
user_setup: []

must_haves:
  truths:
    - "`pnpm install` succeeds on a clean clone"
    - "`pnpm tsc --noEmit` returns zero errors"
    - "`@clerk/nextjs`, `drizzle-orm`, `drizzle-kit`, `postgres`, `@supabase/supabase-js` are pinned in package.json dependencies"
    - "`tsconfig.json` enforces strict + noUncheckedIndexedAccess + noImplicitOverride (D-08)"
    - "Three shadcn primitives (Button, Card, Input) exist under `components/ui/`"
    - ".env.local.example contains DATABASE_URL (D-11) and is the single env template for all 8 phases"
  artifacts:
    - path: "package.json"
      provides: "pnpm-managed deps + Node 20 engine pin + scripts (`dev`, `build`, `start`, `lint`, `typecheck`)"
      contains: "\"next\": \"15."
    - path: "tsconfig.json"
      provides: "Strict TypeScript config (D-08)"
      contains: "\"noUncheckedIndexedAccess\": true"
    - path: "components/ui/button.tsx"
      provides: "shadcn Button primitive (D-05)"
    - path: "components/ui/card.tsx"
      provides: "shadcn Card primitive (D-05)"
    - path: "components/ui/input.tsx"
      provides: "shadcn Input primitive (D-05)"
    - path: "components.json"
      provides: "shadcn config — confirms components/ui path is wired"
    - path: ".env.local.example"
      provides: "Env template with DATABASE_URL added (D-11)"
      contains: "DATABASE_URL="
    - path: "lib/utils.ts"
      provides: "shadcn `cn()` helper used by all UI components"
    - path: ".gitignore"
      provides: "Existing .gitignore extended with Next.js / Drizzle patterns from create-next-app"
      contains: "/.next/"
  key_links:
    - from: "package.json"
      to: "all installed deps"
      via: "pnpm dependency declarations"
      pattern: "@clerk/nextjs"
    - from: "components/ui/button.tsx"
      to: "lib/utils.ts"
      via: "import { cn } from"
      pattern: "from \"@/lib/utils\""
---

<objective>
Scaffold the Next.js 15 App Router workspace with pnpm, install every Phase 1 dependency, harden tsconfig per D-08, initialize shadcn/ui with three primitives (Button, Card, Input) per D-05, and patch `.env.local.example` with `DATABASE_URL` per D-11. After this plan, the repo has a complete buildable shell — every subsequent plan in this phase consumes the output of this one.

Purpose: Establish the deterministic project skeleton so Wave 2 (operator config) and Wave 3 (app shell + middleware + Drizzle skeleton) can run in parallel without re-litigating tooling choices.

Output: A clean `pnpm install` + `pnpm tsc --noEmit` pair both exit 0 against the scaffolded project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/intel/decisions.md
@CLAUDE.md
@BLUEPRINT.md
@reference/STACK.md
@.env.local.example
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 + install all Phase 1 dependencies (pnpm)</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, app/globals.css, app/layout.tsx, app/page.tsx, eslint.config.mjs, .gitignore, .git/</files>

  <read_first>
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-01 (pnpm), D-02 (create-next-app flags), D-08 (tsconfig strictness), D-12 (git init + .gitignore), D-15 (Tailwind version = whatever create-next-app emits)
    - `BLUEPRINT.md` §2 (repo layout — no `src/` directory) and §5 Phase 1 step 1 (scaffold command)
    - `CLAUDE.md` Stack table (Next.js 15, TypeScript, Tailwind, App Router) and the `tsc --noEmit` rule
    - `.gitignore` (already exists at repo root — extend, do not overwrite)
    - `reference/STACK.md` (why Next.js 15 + pnpm — informational; do not introduce new deps not listed in CLAUDE.md Stack table)
  </read_first>

  <action>
    Execute from repo root `C:\Users\matth\Desktop\PolicyPilot` (which currently contains only docs + `.planning/` + `reference/` + `docs/` + an existing `.gitignore`).

    1. **Confirm pnpm is installed.** Run `pnpm --version`. If pnpm is missing, run `corepack enable && corepack prepare pnpm@latest --activate`. Do NOT use npm or npx anywhere.

    2. **Run the scaffold command verbatim (D-02), scaffolding INTO the current directory.** From the repo root:
       ```
       pnpm create next-app@latest . --typescript --tailwind --app --eslint --src-dir=false --import-alias='@/*' --use-pnpm --turbopack=false
       ```
       When create-next-app asks whether to overwrite the existing directory contents, answer YES (the docs are tracked in git or live alongside; we want the scaffold merged in). The CLI will preserve unrelated files (`BLUEPRINT.md`, `CLAUDE.md`, `REQUIREMENTS.md`, `STATE-FOUNDRY.md`, `docs/`, `reference/`, `.planning/`, existing `.gitignore`). If create-next-app refuses to scaffold into a non-empty directory, scaffold into a temp directory `..\policypilot-scaffold` with the same flags and then `Move-Item` every produced file/folder into the repo root — package.json, tsconfig.json, next.config.ts (or .mjs), eslint.config.mjs, postcss.config.mjs, tailwind.config.ts, app/, public/, .gitignore additions. **Merge** (do not overwrite) the existing `.gitignore`: keep its current content, then append any lines from the scaffold's `.gitignore` that are not already present.

    3. **Pin Node 20 LTS engine (D-01).** Edit `package.json` and add at top level:
       ```
       "engines": { "node": ">=20.0.0 <21.0.0" },
       "packageManager": "pnpm@9.0.0"
       ```
       Pick whatever pnpm version `pnpm --version` reports — use that exact version string.

    4. **Add scripts** so subsequent plans can call them. Ensure `package.json` `scripts` contains at minimum:
       ```
       "dev": "next dev",
       "build": "next build",
       "start": "next start",
       "lint": "next lint",
       "typecheck": "tsc --noEmit"
       ```
       (`dev`, `build`, `start`, `lint` come from create-next-app — only add `typecheck` if missing.)

    5. **Install Phase 1 runtime + tooling dependencies in one pnpm command.** Run:
       ```
       pnpm add @clerk/nextjs drizzle-orm postgres @supabase/supabase-js
       pnpm add -D drizzle-kit tsx @types/node
       ```
       Versions: accept whatever pnpm resolves as latest at the time of execution. Do NOT pin minor versions in this task — record the resolved versions in pnpm-lock.yaml.

       **Do NOT install:** Sentry, PostHog, Vitest, Jest, Playwright, TipTap, Anthropic SDK, Stripe SDK, Resend, React Email. These are deferred per D-13 / D-14 / phase boundary.

    6. **Harden `tsconfig.json` per D-08.** Read the create-next-app-generated `tsconfig.json`. In the `compilerOptions` block, ensure exactly these three flags are present alongside the create-next-app defaults:
       ```
       "strict": true,
       "noUncheckedIndexedAccess": true,
       "noImplicitOverride": true
       ```
       `strict: true` is the Next.js default; keep it. **Do not** add `exactOptionalPropertyTypes` (D-08 explicitly skips it — third-party type noise). Do not modify `target`, `module`, `moduleResolution`, `paths`, or `jsx` — keep create-next-app defaults.

    7. **Extend `.gitignore`.** Read the current `.gitignore` (already has `.env.local`, `node_modules/`, `.next/`, `out/`, `build/`, `*.tsbuildinfo`, `next-env.d.ts`, `.DS_Store`, `Thumbs.db`, `.idea/`, `.vscode/`, log patterns, `.vercel`, `.railway`, `.planning/intel/classifications/*.tmp`). Append these lines at the bottom under a new `# Drizzle / pnpm` header (skip any already present):
       ```
       # Drizzle / pnpm
       drizzle/
       .drizzle/
       *.local
       .pnpm-store/
       .next/cache/
       ```

    8. **Initialize git (D-12).** From repo root:
       ```
       git init
       git add -A
       git commit -m "phase 1: scaffold next.js + install dependencies"
       ```
       Skip `git init` if a `.git/` directory already exists. If `git init` succeeds but no user.email / user.name is configured, do NOT alter git config — instead set a per-commit author with `-c` flags if `git commit` fails, e.g. `git -c user.email=mmtuentertainment@gmail.com -c user.name="Matthew" commit -m "..."`. GitHub remote is deferred per D-12 — do not run `git remote add`.

    9. **Verify the scaffold compiles.** Run:
       ```
       pnpm install
       pnpm tsc --noEmit
       ```
       Both MUST exit 0. If `tsc` reports errors against unmodified create-next-app files, do NOT add `any` types (CLAUDE.md NEVER #4). If `noUncheckedIndexedAccess` exposes a generated-file bug (e.g. in `next-env.d.ts`), narrow the scope of strictness as a last resort — but first confirm the error is actually in framework code and not in your edits.
  </action>

  <verify>
    <automated>pnpm tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit 1 }; if (-not (Test-Path package.json)) { exit 1 }; if (-not (Test-Path tsconfig.json)) { exit 1 }; if (-not (Test-Path app/layout.tsx)) { exit 1 }; if (-not (Test-Path pnpm-lock.yaml)) { exit 1 }; $tsconfig = Get-Content tsconfig.json -Raw; if ($tsconfig -notmatch '"noUncheckedIndexedAccess":\s*true') { Write-Error "D-08 violation: noUncheckedIndexedAccess missing"; exit 1 }; if ($tsconfig -notmatch '"noImplicitOverride":\s*true') { Write-Error "D-08 violation: noImplicitOverride missing"; exit 1 }; $pkg = Get-Content package.json -Raw; foreach ($dep in @('@clerk/nextjs','drizzle-orm','drizzle-kit','postgres','@supabase/supabase-js','tsx')) { if ($pkg -notmatch [regex]::Escape($dep)) { Write-Error "Missing dependency: $dep"; exit 1 } }; if ($pkg -notmatch '"next":\s*"15\.') { Write-Error "Next.js 15 not pinned"; exit 1 }; if ($pkg -notmatch '"engines"') { Write-Error "Node engine not pinned"; exit 1 }; Write-Output "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` contains `"next": "15.` (Next.js 15 major)
    - `package.json` contains `"engines"` with a Node 20 spec
    - `package.json` `dependencies` includes `@clerk/nextjs`, `drizzle-orm`, `postgres`, `@supabase/supabase-js`
    - `package.json` `devDependencies` includes `drizzle-kit`, `tsx`
    - `tsconfig.json` contains the literal string `"noUncheckedIndexedAccess": true`
    - `tsconfig.json` contains the literal string `"noImplicitOverride": true`
    - `tsconfig.json` contains `"strict": true`
    - `app/layout.tsx` and `app/page.tsx` exist (create-next-app defaults)
    - `pnpm-lock.yaml` exists
    - `.gitignore` contains the literal string `/.next/` OR `.next/` (already present)
    - `pnpm tsc --noEmit` exits 0
    - No `src/` directory exists in the repo root (D-02 compliance)
    - `.git/` directory exists
  </acceptance_criteria>

  <done>
    `pnpm install` succeeds, `pnpm tsc --noEmit` exits 0, all five Phase 1 dependencies (@clerk/nextjs, drizzle-orm, drizzle-kit, postgres, @supabase/supabase-js) are pinned, tsconfig.json enforces D-08 strictness, and the initial git commit is created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui + add Button, Card, Input primitives (D-05)</name>
  <files>components.json, components/ui/button.tsx, components/ui/card.tsx, components/ui/input.tsx, lib/utils.ts, app/globals.css, tailwind.config.ts, package.json, pnpm-lock.yaml</files>

  <read_first>
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-05 (shadcn init + default zinc theme + Button/Card/Input only)
    - `BLUEPRINT.md` §2 — `components/ui/` path location
    - Output of Task 1 — confirm `app/globals.css` and `tailwind.config.ts` exist before running shadcn init
  </read_first>

  <action>
    1. **Initialize shadcn/ui with default theme (D-05).** From repo root:
       ```
       pnpm dlx shadcn@latest init --yes --base-color zinc --css-variables --no-src-dir
       ```
       When prompted (if `--yes` does not silence everything):
       - Style: `Default`
       - Base color: `Zinc`
       - Global CSS file: `app/globals.css`
       - CSS variables: yes
       - Tailwind config: `tailwind.config.ts` (whatever Task 1 produced)
       - Components alias: `@/components`
       - Utils alias: `@/lib/utils`
       - React Server Components: yes
       - `components/ui/` is the install path

       If `pnpm dlx shadcn@latest init` flags conflict with the create-next-app-generated Tailwind v4 setup (Tailwind v4 uses `@import "tailwindcss"` in CSS — not the v3 directive triplet), let shadcn detect and adapt automatically. Do NOT manually downgrade Tailwind. Per D-15, accept whatever Tailwind version create-next-app emitted.

    2. **Install the three components per D-05.** From repo root:
       ```
       pnpm dlx shadcn@latest add button card input --yes
       ```
       This creates `components/ui/button.tsx`, `components/ui/card.tsx`, `components/ui/input.tsx` and ensures `lib/utils.ts` (with the `cn()` helper) exists.

    3. **Do NOT install any other shadcn primitives in Phase 1.** D-05 explicitly limits this phase to Button, Card, Input. If shadcn's CLI pulls in additional internal dependencies (e.g. `@radix-ui/react-slot` for Button, `clsx` + `tailwind-merge` for `cn()`, `class-variance-authority` for variants), accept those — they're transitive deps of the three primitives, not extra primitives.

    4. **Re-run typecheck.** From repo root:
       ```
       pnpm tsc --noEmit
       ```
       Must exit 0. If it does not, the most likely culprit is an `unknown`-vs-`undefined` issue from `noUncheckedIndexedAccess` on a shadcn-generated file (rare — shadcn output is well-typed). Fix by narrowing types, never with `any`.

    5. **Commit progress.** From repo root:
       ```
       git add -A
       git commit -m "phase 1: init shadcn/ui + add button, card, input"
       ```
  </action>

  <verify>
    <automated>if (-not (Test-Path components/ui/button.tsx)) { exit 1 }; if (-not (Test-Path components/ui/card.tsx)) { exit 1 }; if (-not (Test-Path components/ui/input.tsx)) { exit 1 }; if (-not (Test-Path components.json)) { exit 1 }; if (-not (Test-Path lib/utils.ts)) { exit 1 }; $utils = Get-Content lib/utils.ts -Raw; if ($utils -notmatch 'export function cn') { Write-Error "lib/utils.ts missing cn() helper"; exit 1 }; $btn = Get-Content components/ui/button.tsx -Raw; if ($btn -notmatch 'from "@/lib/utils"') { Write-Error "Button does not import cn from @/lib/utils"; exit 1 }; pnpm tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `components.json` exists (shadcn init marker)
    - `components/ui/button.tsx`, `components/ui/card.tsx`, `components/ui/input.tsx` all exist
    - `lib/utils.ts` contains `export function cn`
    - `components/ui/button.tsx` contains `from "@/lib/utils"`
    - `pnpm tsc --noEmit` exits 0
    - No additional shadcn primitives beyond Button, Card, Input exist under `components/ui/` (D-05 enforcement)
  </acceptance_criteria>

  <done>
    Three shadcn primitives are installed at `components/ui/`, `lib/utils.ts` exports `cn`, and tsc remains clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch .env.local.example with DATABASE_URL (D-11) + add `verify:phase-1` script placeholder</name>
  <files>.env.local.example, package.json, pnpm-lock.yaml</files>

  <read_first>
    - `.env.local.example` (current contents — Supabase block has 3 keys, no `DATABASE_URL`)
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-11 (add DATABASE_URL for Drizzle migrations, separate from Supabase URLs) and the Phase 1 must-have keys list
    - `<specifics>` block in CONTEXT.md — confirms a `pnpm verify:phase-1` script is required (the actual script body is written by Plan 05; this task only reserves the npm-scripts slot)
  </read_first>

  <action>
    1. **Add `DATABASE_URL` to `.env.local.example` (D-11).** Read the existing file. The Supabase block currently has:
       ```
       # ─── Supabase ───────────────────────────────────────────────
       NEXT_PUBLIC_SUPABASE_URL=
       NEXT_PUBLIC_SUPABASE_ANON_KEY=
       SUPABASE_SERVICE_ROLE_KEY=
       ```
       Append one line at the end of the Supabase block (before the blank line that separates it from the Clerk block), making the full block read:
       ```
       # ─── Supabase ───────────────────────────────────────────────
       NEXT_PUBLIC_SUPABASE_URL=
       NEXT_PUBLIC_SUPABASE_ANON_KEY=
       SUPABASE_SERVICE_ROLE_KEY=
       # Drizzle migrations + runtime connection (use Supabase pooler URI on port 6543 for runtime,
       # direct URI on 5432 for migrations). See D-06 / D-07.
       DATABASE_URL=
       ```
       Do not touch any other block (Clerk, Stripe, Anthropic, Resend, App, Analytics). Leave Sentry / PostHog keys present but blank per D-13.

    2. **Reserve the `verify:phase-1` script slot in `package.json`.** Add to the `scripts` section a placeholder that calls a script written by Plan 05:
       ```
       "verify:phase-1": "tsx scripts/check-foundation.ts"
       ```
       Plan 05 writes `scripts/check-foundation.ts`. The script body does not exist yet — running `pnpm verify:phase-1` now will fail with "file not found", which is expected and correct: the gate becomes green only at the end of Wave 4. Do not invent a placeholder script that always exits 0.

       Also add a Drizzle-smoke-script slot (Plan 04 writes the body):
       ```
       "check:db": "tsx scripts/check-db.ts"
       ```

    3. **Re-run typecheck** (no-op against a JSON change but enforces the discipline):
       ```
       pnpm tsc --noEmit
       ```
       Must exit 0.

    4. **Commit progress.** From repo root:
       ```
       git add .env.local.example package.json
       git commit -m "phase 1: add DATABASE_URL to env template + reserve verify scripts"
       ```
  </action>

  <verify>
    <automated>$env = Get-Content .env.local.example -Raw; if ($env -notmatch '^DATABASE_URL=' -and $env -notmatch '\nDATABASE_URL=') { Write-Error "DATABASE_URL missing from .env.local.example"; exit 1 }; $pkg = Get-Content package.json -Raw; if ($pkg -notmatch '"verify:phase-1"') { Write-Error "verify:phase-1 script not registered"; exit 1 }; if ($pkg -notmatch '"check:db"') { Write-Error "check:db script not registered"; exit 1 }; pnpm tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `.env.local.example` contains a line starting with `DATABASE_URL=`
    - `.env.local.example` Supabase block still contains all three original keys (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`)
    - `.env.local.example` still contains the original Clerk + Stripe + Anthropic + Resend + App blocks unchanged (no deletions)
    - `package.json` `scripts` contains the key `verify:phase-1`
    - `package.json` `scripts` contains the key `check:db`
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    DATABASE_URL is part of the env template, the two verify-script slots are reserved, and tsc remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → public git remote | Code (but not `.env.local`) crosses this boundary on push. Phase 1 has no remote configured yet per D-12 — but `.gitignore` must already block secrets to prevent accidents. |
| pnpm registry → local node_modules | Untrusted third-party code installs locally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `.env.local` accidentally committed | mitigate | Existing `.gitignore` already ignores `.env.local` and `.env*.local`. Verify the file is still listed after Task 1's `.gitignore` merge (do not let create-next-app's `.gitignore` overwrite — Task 1's action explicitly preserves existing lines). |
| T-01-02 | Information disclosure | Secrets pasted into `.env.local.example` | mitigate | Task 3 leaves the `DATABASE_URL=` line empty (just the key + `=`). All other example keys are already blank. The example file is committed; the real `.env.local` is gitignored. |
| T-01-03 | Tampering | Supply-chain — installing a malicious or typosquatted npm package | mitigate | All installs use exact published package names from the locked stack (`@clerk/nextjs`, `drizzle-orm`, `drizzle-kit`, `postgres`, `@supabase/supabase-js`, `tsx`, `@types/node`). No new packages introduced without operator approval (CLAUDE.md ASK FIRST #1). pnpm-lock.yaml is committed so future installs are reproducible. |
| T-01-04 | Elevation of privilege | `npx`/`pnpm dlx` running attacker-controlled installer for shadcn | mitigate | shadcn/ui is an established Vercel-adjacent project; the `shadcn@latest` package on npm is the canonical CLI. Lockfile captures the resolved version. |
| T-01-SC | Tampering | npm install of stack packages | accept | Package legitimacy audit is not required by the planner_authority rules here: all six packages are explicit CLAUDE.md stack-table commitments (locked by ADR-010, ADR-011, ADR-012, ADR-003). No `[ASSUMED]` / `[SUS]` packages. |

**ASVS L1 severity assessment:** No `high` severity threats. T-01-01 would be high severity but is fully mitigated by the existing `.gitignore` rule; the task verifies the rule survives the merge.
</threat_model>

<verification>
After all three tasks complete, run:

```
pnpm install
pnpm tsc --noEmit
ls components/ui/
cat .env.local.example | grep DATABASE_URL
```

Expected:
- `pnpm install` exits 0, no peer-dep warnings that block resolution
- `pnpm tsc --noEmit` exits 0
- `components/ui/` lists exactly `button.tsx`, `card.tsx`, `input.tsx` (plus any shadcn-emitted shared files; see Task 2 acceptance for the no-extra-primitives rule)
- `.env.local.example` shows the `DATABASE_URL=` line
</verification>

<success_criteria>
- Repo is a valid pnpm-managed Next.js 15 App Router project (no `src/`, App Router only)
- All five Phase 1 stack dependencies are installed and pinned in `pnpm-lock.yaml`
- `tsconfig.json` enforces D-08 strictness flags
- shadcn/ui is initialized with the three D-05 primitives
- `.env.local.example` contains `DATABASE_URL` (D-11)
- `pnpm tsc --noEmit` exits 0
- Initial git commits exist (one per task is fine; a single "phase 1 scaffold" commit is also acceptable per D-12)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done, recording:
- pnpm version + Node version used
- Resolved versions of all installed packages (from `pnpm-lock.yaml`)
- Tailwind version emitted by create-next-app (v3 or v4 — D-15 informational)
- shadcn theme variant chosen (default zinc per D-05)
- Whether `.gitignore` needed merging (yes/no) and what was appended
- Any deviations from the action text (e.g. if pnpm version differed, if a peer-dep warning required attention)
</output>
