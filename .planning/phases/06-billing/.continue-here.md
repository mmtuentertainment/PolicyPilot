---
context: phase
phase: 06-billing
task: review-and-discuss
total_tasks: 6
status: ready_for_planning
last_updated: 2026-05-27T23:51:45-04:00
---

# Phase 6 Billing Handoff

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Codex OAuth through stdio wrapper | Stripe MCP was first configured through an `npx mcp-remote` stdio wrapper. That shape is not the Codex OAuth path and did not expose Stripe tools in the active session. | advisory | Keep Codex config as direct streamable HTTP: `[mcp_servers.stripe] url = "https://mcp.stripe.com"`. Use `codex mcp login stripe` for OAuth. |
| Secret echoing | Stripe webhook secrets and OAuth/login output may contain sensitive values. | advisory | Use sentinel-only checks. Do not paste keys, webhook secrets, OAuth URLs, customer data, or raw Stripe payloads into chat. |

<current_state>
Phase 6 Billing is specified, external review feedback has been recorded, Matthew approved the two previously gated decisions, and `/gsd discuss phase 6` captured implementation context in `06-CONTEXT.md` plus `06-DISCUSSION-LOG.md`. A post-discussion consultant pass confirmed plan-phase readiness and the live SPEC/CONTEXT now explicitly carry the remaining guardrails for duplicate subscriptions, scheduled cancellation entitlement, same-object/different-event webhook idempotency, metadata cross-checking, and non-destructive `maxUsers`. No Phase 6 implementation code exists yet.

Branch: `gsd/phase-6-billing`

Commits already made:
- `69432a6` - `docs(phase-6): add billing spec`
- `c24a3b8` - `docs(phase-6): add ChatGPT Pro review brief`
- `8a75570` - `docs(06): capture billing phase context`

Tracked Phase 6 artifacts:
- `.planning/phases/06-billing/06-SPEC.md`
- `.planning/phases/06-billing/06-CONTEXT.md`
- `.planning/phases/06-billing/06-DISCUSSION-LOG.md`
- `.planning/phases/06-billing/06-CHATGPT-PRO-REVIEW-BRIEF.md`
- `.planning/reports/2026-05-27-session-report.md`
- `.planning/HANDOFF.json`
- `.planning/phases/06-billing/.continue-here.md`
</current_state>

<completed_work>

Completed Tasks:
- Created Phase 6 Billing SPEC with six locked requirements and ambiguity score 0.170.
- Created ChatGPT Pro review brief with repo file map, review prompts, and Stripe/Next.js documentation anchors.
- Installed and smoke-tested Stripe CLI.
- Added Stripe webhook secret into `.env.local` via sentinel-only handling.
- Removed the source document after extracting the webhook secret.
- Confirmed `.mcp.json` is gitignored and contains a local Stripe entry.
- Corrected Codex global MCP config to use direct HTTP Stripe MCP:
  - `C:\Users\matth\.codex\config.toml`
  - `[mcp_servers.stripe]`
  - `url = "https://mcp.stripe.com"`
- Confirmed `codex mcp get stripe --json` reports the Stripe server as enabled.
- Triggered `codex mcp login stripe`; OAuth flow produced expected auth signals.
- Ran `/gsd discuss phase 6` non-interactively from Matthew's amended SPEC direction and approvals.
- Captured Phase 6 implementation decisions covering package/module shape, checkout/customer mapping, webhook correctness, admin billing UI, tier gates, verification, and UAT.
</completed_work>

<remaining_work>

- Open a fresh Codex session or fully restart Codex, then recheck whether Stripe MCP tools appear in the active tool registry.
- Run `/gsd-plan-phase 6` using the amended SPEC and the new CONTEXT. No remaining Matthew approval question blocks the Stripe SDK or the minimal billing-state migration.
- Carry these plan guardrails forward: reject/redirect checkout when the org already has active/trialing/past_due subscription state; keep active/trialing + cancel-at-period-end entitling until Stripe reports non-entitling status or a verified deletion; test different Stripe event IDs for the same underlying object/type; treat metadata as reconciliation hint, not sole authority; keep `maxUsers` enforcement non-destructive.
- Implement Phase 6 only after the implementation plan exists and keeps MVP scope tight: no tax, coupons, trials, custom dunning, invoice PDFs, revenue analytics, or custom billing identity.
</remaining_work>

<decisions_made>

- Use Codex direct streamable HTTP MCP config for Stripe, not `mcp-remote`, because Codex OAuth login is tied to streamable HTTP MCP servers.
- Keep Stripe CLI setup separate from Stripe MCP; CLI authentication works but does not make MCP tools appear.
- Treat public pricing plan intent as non-authoritative per Phase 6 SPEC.
- Matthew approved the official `stripe` package and the minimal additive organization billing-state migration for Phase 6.
- Webhook correctness must derive entitlement from the current Stripe Subscription for `checkout.session.completed`, `invoice.paid`, and `customer.subscription.updated`, not stale event snapshots.
- Checkout creation must prevent duplicate active subscriptions by redirecting or rejecting when an org already has a linked active/trialing/past_due subscription.
- Active/trialing subscriptions with `cancel_at_period_end = true` remain paid until Stripe reports a non-entitling status or a verified deletion event.
- Webhook tests must prove distinct Stripe event IDs for the same underlying customer/subscription and same event type are harmless because final org state is canonical current Subscription state.
- Keep secret handling sentinel-only.
</decisions_made>

<blockers>

- Active Codex chat session still does not expose `mcp__stripe...` tools. Codex CLI sees Stripe MCP as enabled, so the likely next step is a full restart or fresh thread.
- Stripe docs were refreshed with Context7 on 2026-05-27 during the discussion pass. Rerun Context7 before implementation only for any remaining SDK/API syntax ambiguity.
</blockers>

## Required Reading (in order)

1. `.planning/phases/06-billing/06-SPEC.md` - primary Phase 6 WHAT contract with Matthew's approvals recorded.
2. `.planning/phases/06-billing/06-CONTEXT.md` - implementation decisions captured by `/gsd discuss phase 6`.
3. `.planning/phases/06-billing/06-CHATGPT-PRO-REVIEW-BRIEF.md` - external review prompt, repo evidence map, and approval-gate update.
4. `.planning/reports/2026-05-27-session-report.md` - summary of this setup/pause session.
5. `AGENTS.md` - project rules, including Stripe, secret handling, Context7, and phase gates.
6. `.planning/ROADMAP.md` - Phase 6 success criteria and ADR-029 dependency context.
7. `.planning/PROJECT.md` - ADRs, especially ADR-013, ADR-017, ADR-020, ADR-023, ADR-024, ADR-029.
8. `reference/TIER-LIMITS.md`, `reference/API-SPEC.md`, `reference/SCHEMA.md` - billing contracts and existing schema.
9. `lib/stripe/products.ts`, `lib/stripe/errors.ts`, `lib/stripe/products.test.ts` - current tier-gating implementation.
10. `app/api/webhooks/clerk/route.ts` - existing raw-body webhook/idempotency analog.

## Infrastructure State

- Stripe CLI: authenticated by sentinel check.
- `.env.local`: contains `STRIPE_WEBHOOK_SECRET=whsec_...` sentinel. Do not print it.
- `.mcp.json`: valid local Stripe entry and gitignored.
- Codex global MCP config: direct Stripe URL configured and enabled.
- Active chat registry: no Stripe tools visible yet in this session.
- Background processes: none intentionally left running.
- Vercel CLI: not installed. Install later only when Vercel workflow needs it.

## Pre-Execution Critique Required

- Design artifacts: `.planning/phases/06-billing/06-SPEC.md`, `.planning/phases/06-billing/06-CONTEXT.md`
- Critique focus: Stripe lifecycle correctness, webhook idempotency/out-of-order events, exact org/customer mapping, tier-gate safety, no-client-trust, and whether existing schema is sufficient.
- Gate: Do not begin Phase 6 implementation until `06-SPEC.md` and `06-CONTEXT.md` are consumed by plan-phase and implementation plans are locked.

<context>
This pause happened after Phase 6 spec/review prep, Stripe local tooling setup, Matthew's SDK/schema approvals, and Phase 6 context capture. The work is intentionally pre-implementation. The next agent should resist jumping straight to code until plan-phase is complete.
</context>

<next_action>
Start with the amended `06-SPEC.md` and `06-CONTEXT.md`, then run `/gsd-plan-phase 6`. Do not re-ask Matthew whether the official `stripe` package or the minimal billing-state migration are approved.
</next_action>
