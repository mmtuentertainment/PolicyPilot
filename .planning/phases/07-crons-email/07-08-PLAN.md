---
phase: 07-crons-email
plan: 08
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-04"]
files_modified:
  - lib/db/repositories/notifications.ts
  - app/(employee)/notifications/actions.ts
  - components/notifications/NotificationBell.tsx
  - components/notifications/notification-href.ts
  - components/notifications/NotificationBellServer.tsx
  - app/(admin)/layout.tsx
  - components/admin/AdminTopbar.tsx
  - app/(employee)/layout.tsx
  - scripts/check-crons-email.ts
autonomous: true
requirements: [REQ-notification-system]
user_setup: []

must_haves:
  truths:
    # R7-6 + D1: mark-all-read backend — ONE org-scoped UPDATE, idempotent. notifications stays mutable (NOT in IMMUTABLE_TABLES — D-12).
    - "Notifications.markAllReadForUser(s, userId) flips ALL of (org_id=s.orgId, user_id=userId, read=false) rows to read=true in one org-scoped UPDATE; re-running it changes zero additional rows (idempotent); a second org's/user's rows are never touched"
    # D-12 + UI-SPEC §Backend: two Server Actions mirror acknowledgePolicyAction (typed ActionState, zod-validated, revalidatePath OUTSIDE try/catch), each going through withOrgScope.
    - "markNotificationReadAction(id) zod-validates the id, calls withOrgScope(ctx, s => Notifications.markRead(s, id)), and revalidatePath()s the persona layout path OUTSIDE try/catch; markAllNotificationsReadAction() calls withOrgScope(ctx, s => Notifications.markAllReadForUser(s, ctx.userId)) and revalidatePath()s OUTSIDE try/catch"
    # UI-SPEC §Architecture (server/client boundary): server wrapper fetches via the server-only repo; client <NotificationBell> never imports the repo.
    - "A Server Component wrapper calls getOrgContext() + withOrgScope(ctx, s => Notifications.listUnreadForUser(s, ctx.userId)) and passes unread + unread.length as PROPS to the client <NotificationBell> ('use client'); the client component never imports lib/db/repositories/notifications.ts"
    # UI-SPEC §Component Structure + D4/D6: base-ui wiring (render/onClick, not asChild/onSelect); desktop DropdownMenu + mobile Sheet; bell mounts admin + employee only (no reviewer header, D-06).
    - "<NotificationBell> renders a ghost icon Button trigger with an overlaid destructive Badge (9+ cap), a base-ui DropdownMenu panel (desktop) and Sheet (mobile <640px) with per-item mark-read + a 'Mark all read' footer disabled when count===0; it is mounted in AdminTopbar children (first child) and the employee layout header, and NOT on the reviewer header"
  artifacts:
    - path: "lib/db/repositories/notifications.ts"
      provides: "Notifications.markAllReadForUser org-scoped UPDATE (appended after 07-04's markRead)"
      contains: "markAllReadForUser"
    - path: "components/notifications/NotificationBell.tsx"
      provides: "Client notification-bell component (trigger + DropdownMenu + Sheet + states)"
      contains: "NotificationBell"
    - path: "app/(employee)/notifications/actions.ts"
      provides: "markNotificationReadAction + markAllNotificationsReadAction Server Actions"
      contains: "markAllNotificationsReadAction"
    - path: "scripts/check-crons-email.ts"
      provides: "TEST-DB assertion for markAllReadForUser (alongside the R7-6 markRead test)"
      contains: "markAllReadForUser"
  key_links:
    - from: "components/notifications/NotificationBellServer.tsx"
      to: "Notifications.listUnreadForUser"
      via: "getOrgContext + withOrgScope server fetch → props"
      pattern: "listUnreadForUser"
    - from: "app/(employee)/notifications/actions.ts markAllNotificationsReadAction"
      to: "Notifications.markAllReadForUser"
      via: "withOrgScope(ctx, s => Notifications.markAllReadForUser(s, ctx.userId))"
      pattern: "markAllReadForUser"
    - from: "components/notifications/NotificationBell.tsx"
      to: "app/(employee)/notifications/actions.ts"
      via: "useOptimistic + Server Action calls (markRead per item, markAll footer)"
      pattern: "markNotificationReadAction|markAllNotificationsReadAction"
---

<objective>
Close the Phase 7 bell plan-home gap (identified by `/gsd-ui-phase 7` in `07-UI-SPEC.md` §Plan-Home Gap). Plan 07-04 fills the bell BACKEND read path (`Notifications.markRead` + the already-live `listUnreadForUser`), but the in-app notification bell UI (R7-6) and the one operator-approved +1 backend method (`markAllReadForUser`, D1) have no plan home. This plan builds them: the `markAllReadForUser` org-scoped UPDATE, two Server Actions, the client `<NotificationBell>` component, the per-persona server wrappers, the two mount points, and the TEST-DB assertion for the new method.

Purpose: R7-6 ("In-app bell shows the correct unread count from `notifications.read = false`; mark-as-read flips `read = true` and decrements without a full reload") is unusable by a human until the bell UI exists. This plan makes R7-6 a real surface. It is built strictly from the LOCKED `07-UI-SPEC.md` + `07-BELL-UI-DESIGN.md` (D1–D8) + `07-CONTEXT.md` (D-01..D-13) — nothing is re-decided.
Output: `Notifications.markAllReadForUser` (appended to `notifications.ts`); `markNotificationReadAction` + `markAllNotificationsReadAction` (`app/(employee)/notifications/actions.ts`); `<NotificationBell>` + `notificationHref` helper + `<NotificationBellServer>` wrapper (`components/notifications/`); bell mounted in `AdminTopbar.tsx` + `app/(employee)/layout.tsx`; a `markAllReadForUser` assertion in `scripts/check-crons-email.ts`.

NO schema change: `markAllReadForUser` is a query against the existing `notifications.read` column (`lib/db/schema.ts:158`). No migration task, no schema-push task. `notifications` stays mutable — it is intentionally NOT in `IMMUTABLE_TABLES` (D-12 / ADR-018).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-crons-email/07-UI-SPEC.md
@.planning/phases/07-crons-email/07-BELL-UI-DESIGN.md
@.planning/phases/07-crons-email/07-CONTEXT.md
@.planning/phases/07-crons-email/07-SPEC.md
@.planning/phases/07-crons-email/07-PATTERNS.md
@.planning/phases/07-crons-email/07-04-PLAN.md
@lib/db/repositories/notifications.ts
@lib/db/scoped.ts
@lib/auth/context.ts
@app/(employee)/my-policies/[id]/actions.ts
@components/policy/PolicyTransitionMenu.tsx
@components/admin/AdminTopbar.tsx
@app/(admin)/layout.tsx
@app/(employee)/layout.tsx
@components/ui/dropdown-menu.tsx
@components/ui/sheet.tsx
@components/ui/button.tsx
@components/ui/badge.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Notifications.markAllReadForUser (D1) + TEST-DB assertion</name>
  <read_first>
    - `lib/db/repositories/notifications.ts` (full file — the live `listUnreadForUser` read shape at lines 22-32, the `markRead` method that 07-04 fills at lines 40-44, and the `import { and, eq }` already present at line 8). 07-04 runs in Wave 1 and replaces the `markRead` throw-stub; this Wave-2 task APPENDS `markAllReadForUser` after it. Do NOT touch `markRead` or `create` — they are 07-04's.
    - `07-UI-SPEC.md` §"Backend / API Contract" (the `markAllReadForUser` row: `UPDATE notifications SET read=true WHERE org_id=s.orgId AND user_id=userId AND read=false`, one statement, org-scoped, idempotent)
    - `07-BELL-UI-DESIGN.md` D1 (operator-approved 2026-06-05 — a conscious +1 to 07-04) + D-12 (`notifications` mutable, NOT in `IMMUTABLE_TABLES`)
    - `scripts/check-crons-email.ts` (created/extended by 07-04 — its R7-6 markRead TEST-DB block: the BYPASSRLS seed + `SET LOCAL ROLE authenticated` + `set_config('request.jwt.claims', …, true)` + ROLLBACK/TRUNCATE harness; mirror it for the new assertion). If the R7-6 block is not present when this task runs, the wave barrier ordering is wrong — STOP and surface it.
    - `07-PATTERNS.md` §notifications.ts (lines 596-606 — the `markRead` org-scoped UPDATE shape to mirror) + §check-crons-email (lines 505-565 — the TEST-DB harness analog)
  </read_first>
  <behavior>
    - markAllReadForUser(s, userId) sets read=true for every row matching (orgId=s.orgId, userId=userId, read=false) and returns the affected row ids. Already-read rows are not re-touched.
    - Re-invoking markAllReadForUser for the same (org, user) immediately after a first call affects zero additional rows (idempotent — the WHERE read=false predicate already excludes them).
    - A row belonging to another org OR another user in the same org is never updated (explicit orgId + userId predicate AND RLS org_isolation).
    - After markAllReadForUser, listUnreadForUser(s, userId) returns zero rows for that user.
  </behavior>
  <action>
    APPEND a new method `markAllReadForUser` to the `Notifications` object in `lib/db/repositories/notifications.ts`, after the `markRead` method that 07-04 fills (do not modify `markRead`/`create`/`listAll`/`listUnreadForUser`). Implement it as one org-scoped UPDATE: `async (s, userId) => s.tx.update(notifications).set({ read: true }).where(and(eq(notifications.orgId, s.orgId), eq(notifications.userId, userId), eq(notifications.read, false))).returning({ id: notifications.id })`. Reuse the already-present `import { and, eq }`. Do NOT add `notifications` to any `IMMUTABLE_TABLES` list (D-12 — it is intentionally mutable). No schema change — the `read` column already exists. No `any`.
    Add a TEST-DB assertion to `scripts/check-crons-email.ts` alongside the existing R7-6 `markRead` block (label it `R7-6 markAllReadForUser` or similar): seed two unread `notifications` rows for one user plus one unread row for a SECOND user in the same org plus one unread row in a SECOND org (BYPASSRLS seed), run `markAllReadForUser` under `withOrgScope` for the first (org, user), and assert: (a) both of the first user's rows are now read=true; (b) the second user's row and the second org's row remain read=false (no cross-user/cross-org write); (c) a second `markAllReadForUser` call returns zero affected ids (idempotent). Wrap in the existing ROLLBACK/TRUNCATE harness. Use `s.tx` only — zero raw `db` in the assertion's scoped path (the BYPASSRLS seed uses the harness's raw `postgres` client exactly as the existing R7-6 block does).
  </action>
  <verify>
    <automated>pnpm vitest run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts -t "markAllReadForUser"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `lib/db/repositories/notifications.ts` `markAllReadForUser` contains `update(notifications).set({ read: true })` with all three predicates `eq(notifications.orgId, s.orgId)`, `eq(notifications.userId, userId)`, AND `eq(notifications.read, false)`.
    - SOURCE: `markRead`, `create`, `listAll`, `listUnreadForUser` are UNCHANGED by this task (07-04 owns `markRead`/`create`).
    - SOURCE: `notifications` is NOT present in any `IMMUTABLE_TABLES` list (filtered grep `grep -v '^#' | grep -c "notifications" <IMMUTABLE_TABLES file>` returns 0).
    - SOURCE: `scripts/check-crons-email.ts` contains a `markAllReadForUser` assertion that seeds a second user AND a second org and asserts no cross-user/cross-org write plus idempotency.
    - TEST: `pnpm vitest run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts -t "markAllReadForUser"` is GREEN.
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`.
  </acceptance_criteria>
  <done>`Notifications.markAllReadForUser` is implemented as one idempotent org-scoped UPDATE, `notifications` stays out of `IMMUTABLE_TABLES`, and the TEST-DB assertion (cross-user + cross-org isolation + idempotency) is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Two Server Actions (markNotificationReadAction + markAllNotificationsReadAction)</name>
  <read_first>
    - `app/(employee)/my-policies/[id]/actions.ts` (full file — the EXACT shape to mirror: `"use server"`, typed `ActionState` union (`{ ok: true; … } | { ok: false; error: string }`), `z.object` zod schema with `safeParse`, `getOrgContext()`, the try/catch around the business call, and `revalidatePath` called OUTSIDE the try/catch at the end per D-10c / Next.js 15)
    - `lib/db/scoped.ts` (`withOrgScope(ctx, fn)` signature) + `lib/auth/context.ts` (`getOrgContext(): Promise<OrgContext>`, `OrgContext.userId` is the internal users.id UUID)
    - `lib/db/repositories/notifications.ts` (`Notifications.markRead(s, id)` from 07-04 + `Notifications.markAllReadForUser(s, userId)` from Task 1)
    - `07-UI-SPEC.md` §"Server Actions" + §"`revalidatePath` scope" (revalidate `/dashboard` for admin persona, `/my-policies` for employee persona; pass the current path or persona indicator into the action; NO `revalidateTag`/`updateTag` — D7)
    - `07-BELL-UI-DESIGN.md` D7 (Server Action + `revalidatePath`; no polling/SSE; `revalidateTag` only if a tag scheme is ever added — never `updateTag`)
  </read_first>
  <behavior>
    - markNotificationReadAction(prev, formData) reads `id` from formData, zod-validates it (non-empty string id), calls `withOrgScope(ctx, s => Notifications.markRead(s, id))`, then revalidates the persona layout path; returns `{ ok: true }` on success and `{ ok: false, error }` on invalid payload / failure.
    - markAllNotificationsReadAction(prev, formData) reads the persona indicator from formData, calls `withOrgScope(ctx, s => Notifications.markAllReadForUser(s, ctx.userId))`, then revalidates the persona layout path; returns `{ ok: true }` / `{ ok: false, error }`.
    - revalidatePath is invoked OUTSIDE the try/catch (Next.js 15 throws specially for it; catching breaks revalidation).
  </behavior>
  <action>
    Create `app/(employee)/notifications/actions.ts` with `"use server"` at the top (this is a route-group-neutral Server Action module the bell imports from both personas; co-locate here mirroring the `my-policies` actions file). Define a `NotificationActionState = { ok: true } | { ok: false; error: string }` type. Define two zod schemas: one `z.object({ id: z.string().min(1) })` for mark-read and one `z.object({ persona: z.enum(['admin', 'employee']) })` for the path selection (the bell passes the persona via the FormData so the action revalidates the right tree — UI-SPEC §revalidatePath scope). Implement a small `revalidateForPersona(persona)` helper that calls `revalidatePath('/dashboard')` for `'admin'` and `revalidatePath('/my-policies')` for `'employee'`.
    - `markNotificationReadAction(prev, formData)`: `safeParse` the id + persona; on failure return `{ ok: false, error: 'Invalid action payload.' }`. Call `const ctx = await getOrgContext();` then inside a try/catch `await withOrgScope(ctx, (s) => Notifications.markRead(s, parsed.id));` returning `{ ok: false, error }` (masked log via `console.error` object-literal, never the raw id/email) on a caught Error. AFTER the try/catch (outside it), call `revalidateForPersona(parsed.persona)` and return `{ ok: true }`.
    - `markAllNotificationsReadAction(prev, formData)`: `safeParse` the persona; `getOrgContext()`; try/catch `await withOrgScope(ctx, (s) => Notifications.markAllReadForUser(s, ctx.userId));`; then OUTSIDE the try/catch `revalidateForPersona(parsed.persona)` and return `{ ok: true }`.
    Use `withOrgScope` for BOTH actions (never raw `db`). Do NOT use `revalidateTag`/`updateTag` (D7). No `any`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `app/(employee)/notifications/actions.ts` starts with `"use server"`, exports `markNotificationReadAction` and `markAllNotificationsReadAction`, defines a typed `NotificationActionState` union, and zod-validates input via `safeParse`.
    - SOURCE: `markNotificationReadAction` calls `withOrgScope(ctx, (s) => Notifications.markRead(s, …))`; `markAllNotificationsReadAction` calls `withOrgScope(ctx, (s) => Notifications.markAllReadForUser(s, ctx.userId))`.
    - SOURCE: `revalidatePath` is called OUTSIDE the try/catch in both actions; the file contains no `revalidateTag` or `updateTag`.
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`.
  </acceptance_criteria>
  <done>Both Server Actions mirror the `acknowledgePolicyAction` contract (typed state, zod, `withOrgScope`, `revalidatePath` outside try/catch), go through `Notifications.markRead` / `markAllReadForUser`, and `tsc` is clean.</done>
</task>

<task type="auto">
  <name>Task 3: NotificationBell client component + server wrapper + mount in admin + employee headers</name>
  <read_first>
    - `07-UI-SPEC.md` §"Bell-Specific Contract" in full — esp. §Component Structure (Trigger / Desktop DropdownMenu / Mobile Sheet / Mount Points), §Per-Type Rendering (the `notificationHref` helper + the per-type title/secondary table), §Interaction Contract and Refresh (`useOptimistic` count, NO polling/SSE), §States (empty/loading/error/overflow), §Accessibility (aria-label/aria-controls, sr-only live region, ≥44px mobile touch target, never auto-mark-read on open), §Icons (BellIcon/CheckIcon/BellOffIcon required; per-type icons optional with BellIcon fallback)
    - `07-BELL-UI-DESIGN.md` §4 (base-ui-correct component structure) + D4 (base-ui `render`/`onClick`, NOT Radix `asChild`/`onSelect`) + D6 (bell mounts admin + employee ONLY — no reviewer header)
    - `components/policy/PolicyTransitionMenu.tsx` (the WORKING base-ui wiring to mirror EXACTLY: `<DropdownMenuTrigger render={<Button …/>} />`, `<DropdownMenuItem onClick={…} closeOnClick={…}>`, controlled `<DropdownMenu>` via `useState`/`useTransition`)
    - `components/ui/dropdown-menu.tsx` + `components/ui/sheet.tsx` + `components/ui/button.tsx` (sizes `icon`=32px, `icon-xs`=24px; variants `ghost`, `destructive`) + `components/ui/badge.tsx` + `components/ui/skeleton.tsx` + `components/ui/card.tsx` (exact export names + prop surfaces)
    - `app/(employee)/notifications/actions.ts` (Task 2 — the two Server Actions + `NotificationActionState`) and `lib/db/schema.ts:152-162` (the `notifications` row shape: `id`, `userId`, `type` is plain `text`, `payloadJson` is `jsonb`, `read`, `createdAt` — there is NO exported `NotificationType` union, so the bell defines its own local union + a typed view of the row)
    - `components/admin/AdminTopbar.tsx:67` (the `<div className="flex items-center gap-2">{children}</div>` slot; the bell goes FIRST among `children`, supplied by `app/(admin)/layout.tsx:61-64`) + `app/(admin)/layout.tsx` (Server Component; `<AdminTopbar><OrganizationSwitcher/><UserButton/></AdminTopbar>`) + `app/(employee)/layout.tsx:40-45` (Server Component header; bell goes between the "My Policies" `<span>` and `<UserButton/>`)
  </read_first>
  <behavior>
    - Given an `unread` prop array of length N, the bell trigger shows a destructive Badge reading `N` (or `9+` when N>9) with an sr-only "{N} unread notifications"; when N===0 no badge renders and the trigger shows BellIcon only.
    - Clicking a list item calls `markNotificationReadAction(id)` AND navigates to `notificationHref(type, policyId)` (`review_due`→`/policies/[id]`; all other types→`/my-policies/[id]`).
    - Clicking the per-item secondary CheckIcon button marks the item read WITHOUT navigating (`e.stopPropagation()` so the desktop menu does not close; the item's `closeOnClick={false}`).
    - The footer "Mark all read" button calls `markAllNotificationsReadAction()` and is `disabled` when count===0.
    - The optimistic count (via `useOptimistic`) decrements by one on a per-item mark-read and drops to zero on mark-all; on Server Action failure the optimistic value reverts to the real prop-derived count.
    - Empty state (unread.length===0) shows BellOffIcon + "You're all caught up"; loading shows Skeleton rows; a fetch error shows a `Card` with `border-destructive` + "Unable to load notifications. Please try again." + retry (no Alert primitive). Opening the panel NEVER auto-marks-read.
    - Below 640px the panel renders as a `Sheet` (side="right") with the same list + footer; at ≥640px it renders as the `DropdownMenu`.
  </behavior>
  <action>
    Create `components/notifications/notification-href.ts` — a plain (non-`"use client"`) helper exporting a local `NotificationType` union (`'policy_assigned' | 'policy_updated' | 'review_due' | 'ack_reminder'`) and `notificationHref(type: NotificationType, policyId: string): string` returning `` `/policies/${policyId}` `` for `'review_due'` and `` `/my-policies/${policyId}` `` otherwise (UI-SPEC §Per-Type Rendering, D8). Also export a `NotificationRow` type (the minimal view the bell consumes: `id: string; type: NotificationType; payloadJson: unknown; read: boolean`) and small pure helpers `titleFor(type)` + `secondaryFor(row)` per the UI-SPEC §Per-Type Rendering table (title strings verbatim; secondary metadata: `policy_updated`→`v{n}` if `payload_json.versionNumber`; `ack_reminder`→`{n} days overdue` if `daysOverdue`; `review_due`→`Due {Mon DD}` from `dueDate`). Narrow `payloadJson` defensively (it is `jsonb`/`unknown`) — no `any`.
    Create `components/notifications/NotificationBell.tsx` (`"use client"`). Props: `{ unread: NotificationRow[]; count: number; persona: 'admin' | 'employee' }`. Mirror the base-ui wiring from `PolicyTransitionMenu.tsx`:
    - Trigger: `<Button variant="ghost" size="icon" className="relative" aria-label="Notifications" aria-controls={panelId}>` containing `<BellIcon className="size-5" aria-hidden />` and, when `count>0`, an overlaid `<Badge variant="destructive" className="absolute -top-1 -right-1 …">{count>9?'9+':count}<span className="sr-only">{count} unread notifications</span></Badge>`. Add a mobile touch-target expansion (`min-h-11 min-w-11` transparent hit area or equivalent) per UI-SPEC §Accessibility.
    - Desktop ≥640px: controlled `<DropdownMenu open onOpenChange>` → `<DropdownMenuTrigger render={<the trigger button/>} />` (render, NOT asChild) → `<DropdownMenuContent align="end" sideOffset={4} className="w-80 p-0">` with: a header (`Notifications` + `{count} unread` when count>0); a scrollable list `<div className="max-h-96 overflow-y-auto divide-y" role="list">` mapping each row to `<DropdownMenuItem onClick={() => markReadAndNavigate(row)} closeOnClick={false} className="px-1.5 py-2 …">` (onClick, NOT onSelect) containing `titleFor`/`secondaryFor` and a secondary `<Button variant="ghost" size="icon-xs" onClick={(e)=>{e.stopPropagation(); markRead(row.id);}} aria-label="Mark as read"><CheckIcon aria-hidden /></Button>`; a footer `<Button variant="ghost" size="sm" disabled={count===0} onClick={markAllRead} className="w-full">Mark all read</Button>`.
    - Mobile <640px: a `<Sheet open onOpenChange>` → `<SheetContent side="right">` → `<SheetHeader><SheetTitle>Notifications</SheetTitle>{count>0 && <SheetDescription>{count} unread</SheetDescription>}</SheetHeader>` → same scrollable list → `<SheetFooter>` with the same Mark-all-read button. Use a viewport breakpoint hook or a `sm:`/`max-sm:` render split (planner discretion — keep both panels driven by the SAME `open` state + the SAME item/footer render helpers).
    - Use `useOptimistic` for the displayed count (decrement on per-item mark-read, zero on mark-all; revert on action failure) — do NOT mirror the count into plain `useState` (D7 / UI-SPEC §Interaction). Wrap the Server Action calls in `useTransition` (mirror PolicyTransitionMenu's `startTransition`). Build a FormData with `id` (for mark-read) and always `persona` (the prop) and call the imported `markNotificationReadAction` / `markAllNotificationsReadAction`. Navigation uses `useRouter().push(notificationHref(...))` from `next/navigation`.
    - States: empty (BellOffIcon + "You're all caught up", centered, `text-muted-foreground`); the loading Skeleton variant is rendered when a `loading` prop branch is exercised (the server wrapper passes real data so the steady state is non-loading — keep the Skeleton branch present for the Suspense/pending case); error (a `Card` with `border-destructive` + the copy + a retry button — no Alert primitive). NEVER auto-mark-read on open.
    - A11y: `aria-label="Notifications"` + `aria-controls={panelId}` on the trigger and a matching `id` on the panel; an sr-only `<div role="status" aria-live="polite" aria-atomic="true" className="sr-only">{count} unread notification{count===1?'':'s'}</div>` live region updated on count change; the per-item mark-read button keyboard-reachable with `aria-label="Mark as read"`.
    - Icons from `lucide-react`: `BellIcon`, `CheckIcon`, `BellOffIcon` (required). Optional per-type icons are NOT required — do not block on them; `BellIcon` is the correct fallback.
    Create `components/notifications/NotificationBellServer.tsx` — a Server Component (NO `"use client"`, NO `import 'server-only'` needed because it is a layout-rendered server component, but it MUST be the only thing that touches the repo). Signature: `async function NotificationBellServer({ persona }: { persona: 'admin' | 'employee' })`. Body: `const ctx = await getOrgContext(); const unread = await withOrgScope(ctx, (s) => Notifications.listUnreadForUser(s, ctx.userId));` then map each row to the minimal `NotificationRow` view and `return <NotificationBell unread={rows} count={rows.length} persona={persona} />`. The client `<NotificationBell>` must NOT import `lib/db/repositories/notifications.ts` (the repo is `server-only`).
    Mount it:
    - Admin: in `app/(admin)/layout.tsx`, render `<NotificationBellServer persona="admin" />` as the FIRST child passed into `<AdminTopbar>` (before `<OrganizationSwitcher/>` and `<UserButton/>`), so the topbar's `children` slot at `AdminTopbar.tsx:67` renders `[Bell][OrgSwitcher][UserButton]`. Do NOT change `AdminTopbar.tsx`'s structure beyond what's needed (it already renders `{children}` in the right slot — preferred: add the bell in the layout, leaving `AdminTopbar.tsx` untouched; if a wrapper is needed list `components/admin/AdminTopbar.tsx` in files_modified — it is listed defensively).
    - Employee: in `app/(employee)/layout.tsx`, render `<NotificationBellServer persona="employee" />` in the header `<div>` between the `<span>My Policies</span>` and `<UserButton />` (right-aligned group).
    - Do NOT add the bell to any reviewer header (D-06 — reviewers receive no Phase 7 notifications).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `components/notifications/NotificationBell.tsx` starts with `"use client"`, uses `useOptimistic` for the count, wires the base-ui `DropdownMenu` via `render=` on the trigger and `onClick=` + `closeOnClick={false}` on items (NO `asChild`, NO `onSelect`), renders a `Sheet` for the mobile panel, a destructive `Badge` with a `9+` cap + sr-only count, and a "Mark all read" footer `disabled={count===0}`; it does NOT import `lib/db/repositories/notifications.ts`.
    - SOURCE: `components/notifications/notification-href.ts` exports `notificationHref` returning `/policies/${policyId}` for `review_due` and `/my-policies/${policyId}` otherwise.
    - SOURCE: `components/notifications/NotificationBellServer.tsx` calls `getOrgContext()` + `withOrgScope(ctx, (s) => Notifications.listUnreadForUser(s, ctx.userId))` and passes `unread`/`count`/`persona` as props.
    - SOURCE: `<NotificationBellServer persona="admin" />` is rendered as the first child of `<AdminTopbar>` in `app/(admin)/layout.tsx`; `<NotificationBellServer persona="employee" />` is rendered in `app/(employee)/layout.tsx`'s header; no reviewer-header mount exists.
    - SOURCE: an sr-only `role="status" aria-live="polite"` live region is present; the trigger has `aria-label="Notifications"` + `aria-controls`; the per-item mark-read button has `aria-label="Mark as read"`.
    - CLI: `pnpm tsc --noEmit` exits 0 (no `any`); `pnpm build` succeeds (the bell renders + types are sound in both layouts).
  </acceptance_criteria>
  <done>The client `<NotificationBell>` (base-ui DropdownMenu + Sheet, optimistic count, per-item + mark-all read, full states + a11y) is built, fed by per-persona server wrappers, mounted in the admin + employee headers (not the reviewer header), and the build + `tsc` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client `<NotificationBell>` → Server Action → repository | The client supplies a notification `id` + a `persona`; the action zod-validates and runs every DB op through `withOrgScope` (RLS), never trusting the client for org/user scope. |
| any org's scope → notifications rows | Every read (`listUnreadForUser`) and write (`markRead`, `markAllReadForUser`) includes `org_id` (and, for the mark-all, `user_id`) in the WHERE; RLS `org_isolation` is the last line. |
| server-only repository ↔ client component | The repository is `server-only`; the client bell receives data only as props via the server wrapper — it never imports the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-21 | Information Disclosure (BOLA / cross-org) | `Notifications.markRead` via `markNotificationReadAction` | mitigate | The UPDATE predicate is `(orgId = s.orgId AND id = id)` under `withOrgScope` RLS; a guessed/forged id from another org cannot be flipped (RLS `org_isolation` blocks the row even if the id is valid elsewhere). The action zod-validates the id and never derives org/user from the client. |
| T-7-22 | Information Disclosure (cross-user) | `Notifications.markAllReadForUser` | mitigate | `userId` comes from `ctx.userId` (server-resolved `getOrgContext`), NOT from the client FormData; the UPDATE predicate is `(orgId = s.orgId AND user_id = ctx.userId AND read = false)`, so one user cannot mark another user's notifications read. The TEST-DB assertion (Task 1) proves the second-user + second-org rows stay unread. |
| T-7-23 | Elevation of Privilege | server/client boundary leak | mitigate | The interactive bell is `"use client"` and receives only a minimal `NotificationRow[]` view as props; the `server-only` repo is reached solely from the `<NotificationBellServer>` Server Component. The client never imports `lib/db/*`; `tsc` + `build` would fail if a `server-only` module were pulled client-side. |
| T-7-24 | Tampering | bell marking the wrong persona's tree stale / over-revalidation | accept | The `persona` is a low-trust hint used ONLY to choose which already-public layout segment to `revalidatePath`; the actual read/write are RLS-scoped, so a forged persona at worst revalidates the wrong (still org-scoped) cache tree — no data crosses tenants. A user is one persona per session (D7). |
| T-7-SC | Tampering | npm/pnpm installs | mitigate | This plan adds ZERO packages: all deps — `@base-ui/react`, `lucide-react` — are already installed and vetted through prior phases (`07-UI-SPEC.md` §Registry Safety: `"registries": {}`). No install task; no schema migration. |
</threat_model>

<verification>
- `pnpm vitest run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts -t "markAllReadForUser"` GREEN (idempotent + cross-user + cross-org isolation).
- `pnpm tsc --noEmit` exits 0; no `any`.
- `pnpm build` succeeds (the bell renders in both the admin + employee layouts; types are sound).
- `notifications` absent from `IMMUTABLE_TABLES`; the client bell does not import the `server-only` repo.
- The cumulative `pnpm verify:phase-7` (wired by 07-07, Wave 3) runs AFTER this Wave-2 plan via the wave barrier and stays green with the bell in place.
</verification>

<success_criteria>
- `Notifications.markAllReadForUser` implemented (D1) — one idempotent org-scoped UPDATE; `notifications` stays mutable.
- Two Server Actions (`markNotificationReadAction`, `markAllNotificationsReadAction`) mirror the `acknowledgePolicyAction` contract through `withOrgScope`.
- `<NotificationBell>` (R7-6) — base-ui DropdownMenu (desktop) + Sheet (mobile), per-item + mark-all read, optimistic count, empty/loading/error states, full a11y — fed by per-persona server wrappers and mounted in the admin + employee headers (not reviewer; D-06).
- TEST-DB assertion for `markAllReadForUser` is GREEN; `tsc` + `build` pass.
</success_criteria>

<output>
Create `.planning/phases/07-crons-email/07-08-SUMMARY.md` when done.
</output>
