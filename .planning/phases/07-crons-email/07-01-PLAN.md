---
phase: 07-crons-email
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/check-crons-email.ts
  - scripts/check-crons-email.vitest.config.ts
  - app/api/cron/reminders/route.test.ts
  - lib/email/send.test.ts
autonomous: true
requirements: [REQ-notification-system]
user_setup: []

must_haves:
  truths:
    # D-13: TEST-DB integration harness + co-located stub-Resend vitests exist (RED) before any implementation runs.
    - "scripts/check-crons-email.ts runs against the real TEST DB and asserts R7-2 idempotency, R7-3 two-org isolation, R7-5 windows, R7-6 markRead, R7-8 writer (RED until impl exists)"
    - "app/api/cron/reminders/route.test.ts asserts R7-1 401-on-missing-Bearer / 200-on-correct-Bearer (RED)"
    - "lib/email/send.test.ts asserts R7-4 one Resend send + correct subject/template per type via stub transport (RED)"
    # D-13: the vitest config mirrors check-employee-portal so the TEST-DB harness has node env + single-fork.
    - "scripts/check-crons-email.vitest.config.ts mirrors check-employee-portal.vitest.config.ts (node env, single-fork, TEST DB env passthrough)"
  artifacts:
    - path: "scripts/check-crons-email.ts"
      provides: "TEST-DB integration harness covering R7-2/R7-3/R7-5/R7-6/R7-8"
      min_lines: 80
    - path: "scripts/check-crons-email.vitest.config.ts"
      provides: "vitest config for the TEST-DB harness"
    - path: "app/api/cron/reminders/route.test.ts"
      provides: "R7-1 cron auth-gate unit test"
    - path: "lib/email/send.test.ts"
      provides: "R7-4 dispatch + template selection unit test (stub Resend)"
  key_links:
    - from: "scripts/check-crons-email.ts"
      to: "TEST DB (DATABASE_URL_TEST)"
      via: "raw postgres-js + SET LOCAL ROLE authenticated + set_config jwt claims + ROLLBACK/TRUNCATE"
      pattern: "SET LOCAL ROLE authenticated"
---

<objective>
Wave 0 (Nyquist): create the four RED test files that pin every Phase 7 behavior BEFORE any implementation. Per `07-VALIDATION.md` Wave 0 Requirements, these files must exist first so each downstream implementation task has an automated `<verify>` to turn GREEN. This plan creates the test scaffolding only — it does NOT implement the cron, email layer, repo fills, or migration (those are Waves 1-2).

Purpose: Establish the validation contract (D-13) so feedback latency stays < 120s and no implementation merges without a falsifiable check.
Output: `scripts/check-crons-email.ts` (+ its vitest config), `app/api/cron/reminders/route.test.ts`, `lib/email/send.test.ts` — all RED (failing/skipped because the modules under test do not exist yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-crons-email/07-SPEC.md
@.planning/phases/07-crons-email/07-CONTEXT.md
@.planning/phases/07-crons-email/07-RESEARCH.md
@.planning/phases/07-crons-email/07-PATTERNS.md
@.planning/phases/07-crons-email/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEST-DB integration harness scaffold (check-crons-email.ts + vitest config)</name>
  <read_first>
    - `scripts/check-employee-portal.test.ts` (full file — the canonical TEST-DB harness: raw postgres-js + BYPASSRLS seed + `SET LOCAL ROLE authenticated` + `set_config('request.jwt.claims', …, true)` + intentional ROLLBACK + final TRUNCATE) — primary analog per 07-PATTERNS.md §check-crons-email lines 505-566
    - `scripts/check-employee-portal.vitest.config.ts` (full file — node env, `pool: 'forks'`, `singleFork: true`, TEST DB env passthrough) — config analog per 07-PATTERNS.md lines 543-565
    - `scripts/check-rls.ts` lines 36-50 (TENANT_TABLES + cross-org ROLLBACK block pattern)
    - `07-VALIDATION.md` Wave 0 Requirements (lines 70-77) and Per-Task Verification Map (lines 50-66)
    - `07-RESEARCH.md` §Validation Architecture (lines 818-882) for the DB-truth-vs-mockable classification
  </read_first>
  <action>
    Create `scripts/check-crons-email.ts` as a vitest `describe` suite mirroring `check-employee-portal.test.ts`. Build fixture seeders (BYPASSRLS connection) and assertion blocks for the five DB-truth requirements, each as an `it(...)` that is RED until its implementation lands:
    - R7-2 idempotency: insert a candidate, run the `reminder_sends` claim twice for the same `(orgId, userId, policyId, type, windowDate)` → assert exactly one row exists (UNIQUE + onConflictDoNothing). Reference the real `reminderSends` export and `Notifications.create` once they exist.
    - R7-3 two-org isolation: seed Org A + Org B; run the org-wide ack query inside `withOrgScope({ orgId: A })` under `SET LOCAL ROLE authenticated` → assert zero Org B rows are returned/written.
    - R7-5 windows: seed a policy due in 13d (incl) vs 15d (excl); an assignment unacked 8d (incl) vs 5d (excl); a `stale` user → assert correct candidate counts.
    - R7-6 markRead: seed an unread notification → call `Notifications.markRead` → assert `read=true` and `listUnreadForUser` count decremented.
    - R7-8 writer: publish a policy with `review_interval_months=12` → assert `next_review_date ≈ published_at + 12 months`.
    Use `vi.mock('server-only', () => ({}))` and `vi.mock('@/lib/email/client', ...)` stub Resend (per 07-PATTERNS.md lines 525-533) so the harness never sends real email. End the suite with a `TRUNCATE` cleanup of all seeded tables (filtered to exclude `'^#'` comment lines is N/A here — this is TS). Mark each `it` so the suite is collectible and RED (failing assertions or `it` referencing not-yet-existing exports), NOT erroring at import time — guard not-yet-existing imports behind dynamic `await import()` inside the `it` body so collection succeeds.
    Create `scripts/check-crons-email.vitest.config.ts` by copying `check-employee-portal.vitest.config.ts` verbatim and changing only `test.include` to `['scripts/check-crons-email.ts']` and the `'server-only'` alias path identical to the analog.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `scripts/check-crons-email.ts` exists and contains the strings `SET LOCAL ROLE authenticated`, `set_config`, `request.jwt.claims`, `TRUNCATE`, and one `it(` per requirement R7-2, R7-3, R7-5, R7-6, R7-8.
    - SOURCE: `scripts/check-crons-email.vitest.config.ts` exists with `include: ['scripts/check-crons-email.ts']` and `pool: 'forks'` + `singleFork: true`.
    - CLI: `pnpm tsc --noEmit` exits 0 (the harness compiles; not-yet-existing imports are inside dynamic `await import()` so they do not break type-check) — no `any`.
    - BEHAVIOR: `pnpm vitest run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts` COLLECTS the suite (does not error at import) and reports the DB-truth `it`s as failing/red (RED state expected — implementation lands in Waves 1-2).
  </acceptance_criteria>
  <done>The TEST-DB integration harness and its vitest config exist, compile clean, collect successfully, and are RED pending implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Unit RED tests — cron auth gate (R7-1) + email dispatch (R7-4)</name>
  <read_first>
    - `app/api/webhooks/stripe/route.test.ts` lines 1-100 (module-boundary mock + FakeTx pattern) — analog per 07-PATTERNS.md §clerk T8 lines 692-713 and §send.test
    - `07-RESEARCH.md` §Pattern 7 (lines 418-436) — stub Resend transport via `vi.mock('@/lib/email/client', ...)`
    - `07-RESEARCH.md` §Cron Route Patterns R7-1 (lines 711-727) — `Bearer ${cronSecret}` gate; 401 on mismatch
    - `07-RESEARCH.md` §Typed Dispatch Map (lines 631-676) — `sendNotificationEmail(type, to, ctx)` signature; `DISPATCH_MAP` subject/component per `NotificationType`
    - `07-VALIDATION.md` rows R7-1 and R7-4 (lines 52, 55)
  </read_first>
  <action>
    Create `app/api/cron/reminders/route.test.ts`: `vi.mock('server-only', () => ({}))`; mock `@/lib/db` so org enumeration returns an empty array; set `process.env.CRON_SECRET` to a test sentinel. Assert: (a) `GET` with no `Authorization` header → 401; (b) `GET` with `Authorization: 'Bearer wrong'` → 401; (c) `GET` with `Authorization: 'Bearer <sentinel>'` → 200 and JSON body shaped `{ reviewReminders: number, ackReminders: number }`. Import the `GET` handler behind a dynamic `await import('@/app/api/cron/reminders/route')` inside each `it` so the suite collects before the route exists (RED).
    Create `lib/email/send.test.ts`: `vi.mock('server-only', () => ({}))` + the stub Resend `vi.mock('@/lib/email/client', () => ({ getResendClient: vi.fn(() => ({ emails: { send: vi.fn().mockResolvedValue({ data: { id: 'test-email-id' }, error: null }) } })) }))`. For each of the 4 `NotificationType` values (`policy_assigned`, `policy_updated`, `review_due`, `ack_reminder`) assert that calling `sendNotificationEmail(type, 'a@b.test', ctx)` calls `emails.send` exactly once with `expect.objectContaining({ react: expect.anything(), subject: expect.stringContaining(<type-specific token>) })` and `from` defaulting to `noreply@policypilot.com`. Import `sendNotificationEmail` behind dynamic `await import('@/lib/email/send')` so the suite collects RED.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `app/api/cron/reminders/route.test.ts` contains three assertions for 401 (no header), 401 (wrong bearer), 200 (correct bearer) and asserts the `{reviewReminders, ackReminders}` body shape.
    - SOURCE: `lib/email/send.test.ts` contains one `it` per `NotificationType` (4 total) asserting `emails.send` called once with `react` + a type-specific `subject` substring, plus the stub Resend mock.
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`.
    - BEHAVIOR: `pnpm vitest run app/api/cron/reminders/route.test.ts` and `pnpm vitest run lib/email/send.test.ts` both COLLECT (no import-time error) and report RED pending implementation.
  </acceptance_criteria>
  <done>Both unit RED test files exist, compile clean, collect successfully, and are RED pending the route + send-dispatch implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → TEST DB | The harness opens a privileged (BYPASSRLS) connection to seed, then drops to `authenticated` role via `SET LOCAL ROLE` to exercise RLS. The TEST DB is the dev/test Supabase target — never staging/prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Information Disclosure | `scripts/check-crons-email.ts` connection string | mitigate | Read `DATABASE_URL_TEST`/`DIRECT_URL_TEST` from env only (mirror check-employee-portal.vitest.config.ts); never echo/print secret values; harness exits 1 on missing var. |
| T-7-02 | Tampering | TEST DB residual rows leaking across `it`s | mitigate | Intentional ROLLBACK per cross-org block + final `TRUNCATE` cleanup (mirrors check-rls.ts); single-fork pool prevents parallel-run contamination. |
| T-7-SC | Tampering | npm installs | mitigate | This plan adds ZERO packages (vitest already installed). No install task here — package adds are gated in Plan 07-02 with a blocking-human checkpoint. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0 with all four test files present.
- All four files COLLECT under vitest (no import-time errors) and are RED — confirming they pin behavior not yet implemented.
- No production code changed in this plan (test scaffolding only).
</verification>

<success_criteria>
- Four Wave 0 test files exist per `07-VALIDATION.md` (lines 72-76).
- `scripts/check-crons-email.ts` exercises the five DB-truth requirements against the real TEST DB.
- All tests compile, collect, and are RED.
</success_criteria>

<output>
Create `.planning/phases/07-crons-email/07-01-SUMMARY.md` when done.
</output>
