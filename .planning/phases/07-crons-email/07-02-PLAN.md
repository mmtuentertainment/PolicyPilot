---
phase: 07-crons-email
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - lib/db/schema.ts
  - drizzle/0014_reminder_sends.sql
  - drizzle/meta/_journal.json
autonomous: false
requirements: [REQ-notification-system]
user_setup:
  - service: resend
    why: "Email send transport (ADR-016); reminders + event notifications"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys (already present blank in .env.local.example)"
      - name: RESEND_FROM_EMAIL
        source: "Resend verified domain; defaults to noreply@policypilot.com"

must_haves:
  truths:
    # D-05: additive forward-only migration 0014 with operator-signed header; reminder_sends shape + UNIQUE + RLS + GRANT + org_id index.
    - "drizzle/0014_reminder_sends.sql creates reminder_sends(org_id,user_id,policy_id,type,window_date,sent_at) with natural-key UNIQUE on (org_id,user_id,policy_id,type,window_date), RLS org_isolation (wrapped form), GRANT to authenticated, and org_id btree index"
    # D-05: window_date is a UTC calendar date; the Drizzle export uses date(..., { mode: 'string' }).
    - "lib/db/schema.ts exports reminderSends with windowDate as date('window_date', { mode: 'string' }) and the same UNIQUE + index"
    # phase constraint #1: operator-signed header is required before commit (ASK-FIRST checkpoint).
    - "The 0014 migration header carries rationale + operator-approval timestamp + decision ID D-05 before commit (operator sign-off checkpoint)"
    # phase constraint #2 + #3: migration applied to dev/TEST only via repo runner; packages pinned >=14d old, installed ASK-FIRST.
    - "reminder_sends is applied to the TEST/dev DB via pnpm db:migrate + pnpm db:migrate:test (NOT drizzle-kit push) and db:verify exits 0"
    - "resend@6.12.3 + react-email@6.1.5 (both >=14 days old) are installed after an ASK-FIRST checkpoint with a re-run supply-chain sweep; pnpm ls svix shows one version"
  artifacts:
    - path: "drizzle/0014_reminder_sends.sql"
      provides: "additive reminder_sends migration with operator-signed header"
      contains: "reminder_sends_dedup_key"
    - path: "lib/db/schema.ts"
      provides: "reminderSends Drizzle export"
      contains: "reminderSends"
  key_links:
    - from: "lib/db/schema.ts reminderSends"
      to: "drizzle/0014_reminder_sends.sql"
      via: "matching table/column/constraint names"
      pattern: "reminder_sends"
    - from: "package.json dependencies"
      to: "resend + react-email"
      via: "exact pins (no caret)"
      pattern: "\"resend\": \"6.12.3\""
---

<objective>
Wave 0 foundation: install the two ASK-FIRST packages (`resend@6.12.3`, `react-email@6.1.5`), author the additive `reminder_sends` migration `0014` with an operator-signed header, add the `reminderSends` Drizzle export, and apply the migration to the dev/TEST DB via the repo runner so the Wave 0 DB-truth harness (Plan 07-01) and `db:verify` have a real schema to assert against.

Purpose: R7-2's at-most-once dedup lives in the `reminder_sends` UNIQUE constraint (D-05). The TEST-DB integration check and every downstream cron task fail against a schema the live TEST DB does not have — so the migration MUST land (and be applied) before Waves 1-2.
Output: `resend` + `react-email` in `package.json`; `lib/db/schema.ts` `reminderSends` export; `drizzle/0014_reminder_sends.sql`; the migration applied to dev + TEST DBs.

This plan has TWO blocking checkpoints (package install; migration-header sign-off) → `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-crons-email/07-SPEC.md
@.planning/phases/07-crons-email/07-CONTEXT.md
@.planning/phases/07-crons-email/07-RESEARCH.md
@.planning/phases/07-crons-email/07-PATTERNS.md
@drizzle/0011_qa_citation_grants.sql
@drizzle/0013_review_decisions.sql
@docs/runbooks/deploy-migrations.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Package-install gate (ASK-FIRST, supply-chain). The two packages are stack-sanctioned (ADR-016) but unlisted in package.json, so install requires operator approval and a fresh supply-chain sweep. Both pins are independently date-verified ≥14 days old per the project supply-chain rule: `resend@6.12.3` (published 2026-05-06, 30 days) and `react-email@6.1.5` (published 2026-05-18, 18 days). NOTE the latest versions are UNSAFE (`react-email@6.5.0` = 9 days; `resend@6.12.4` = 11 days) — do NOT use them.
  </what-built>
  <how-to-verify>
    1. Re-run the current-month supply-chain sweep per `MEMORY.md` audit-before-security-changes: WebSearch current-month IOCs for `resend` / `react-email`; sweep `.mcp.json` + postinstall ledger; confirm no malicious/yanked advisory for the two pinned versions.
    2. Confirm `resend@6.12.3` on `npmjs.com/package/resend` and `react-email@6.1.5` on `npmjs.com/package/react-email` are ≥14 days old and have no postinstall scripts.
    3. On approval, run: `pnpm add resend@6.12.3 react-email@6.1.5` (exact pins, no caret).
    4. Verify dedup: `pnpm ls svix` shows exactly ONE version (resend@6.12.3 bundles svix@1.92.2; project has svix@1.93.0 — pnpm should dedup to one). If two appear, add a pnpm override per 07-RESEARCH.md Pitfall 7.
    5. `pnpm tsc --noEmit` exits 0.
  </how-to-verify>
  <resume-signal>Type "approved — installed" once both packages are pinned, the sweep is clean, and `pnpm ls svix` shows one version. Or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: reminderSends Drizzle export + 0014 migration (additive, RLS + GRANT + UNIQUE)</name>
  <read_first>
    - `lib/db/schema.ts` lines 152-162 (`notifications` table) + the `qaCitationGrants` export — exact analog per 07-PATTERNS.md §schema.ts lines 419-454
    - `drizzle/0011_qa_citation_grants.sql` (RLS + GRANT + org_id index shape; wrapped-form `(SELECT auth.jwt()->>'org_id')`) — primary migration analog
    - `drizzle/0013_review_decisions.sql` lines 1-14 (operator-approval header format with ASK-FIRST clearance) — header analog
    - `07-PATTERNS.md` §0014 migration (lines 361-415) and §schema export (lines 419-454)
    - `07-RESEARCH.md` §Pattern 8 (lines 438-486) — full SQL shape; CRITICAL wrapped-RLS form
    - `07-CONTEXT.md` D-05 + phase constraint #1 (migration index 0014, columns, UNIQUE key, date type)
  </read_first>
  <action>
    Add a `reminderSends` export to `lib/db/schema.ts` using `pgTable('reminder_sends', …)` with: `id` uuid PK defaultRandom; `orgId` uuid notNull references organizations.id onDelete cascade; `userId` uuid notNull references users.id; `policyId` uuid notNull references policies.id; `type` text notNull; `windowDate` via `date('window_date', { mode: 'string' })` notNull (UTC `YYYY-MM-DD` string — per 07-PATTERNS.md CRITICAL note + RESEARCH Open Question 2, NOT `date()` without mode); `sentAt` via `timestamp('sent_at', { withTimezone: true })` notNull defaultNow. Table extras: `index('reminder_sends_org_id_idx').on(table.orgId)` and `unique('reminder_sends_dedup_key').on(table.orgId, table.userId, table.policyId, table.type, table.windowDate)`. Import `date`/`unique` from `drizzle-orm/pg-core` if not already imported.
    Author `drizzle/0014_reminder_sends.sql` by hand (next free immutable index after `0013_review_decisions`; do NOT run `drizzle-kit generate` to avoid renumbering churn — write the file directly). Mirror the `0011_qa_citation_grants.sql` body: CREATE TABLE with the six columns above; three FK constraints (org_id cascade, user_id no-action, policy_id no-action); the natural-key UNIQUE constraint named `reminder_sends_dedup_key` on `(org_id,user_id,policy_id,type,window_date)`; `CREATE INDEX reminder_sends_org_id_idx`; `ENABLE ROW LEVEL SECURITY`; `CREATE POLICY "org_isolation" … FOR ALL USING (org_id::text = (SELECT auth.jwt()->>'org_id'))` (WRAPPED form per 0008 convention — bare form fails the check-rls gate); `GRANT SELECT, INSERT, UPDATE, DELETE ON "reminder_sends" TO authenticated`. Use `--> statement-breakpoint` between statements. Add the new migration tag to `drizzle/meta/_journal.json` following the existing entry format (idx, tag `0014_reminder_sends`, breakpoints true). Write the header per the 0013 format with a placeholder approval line: rationale (at-most-once send-ledger for cron dedup, R7-2/D-05, additive only), `Decision ID: D-05`, and an `Operator-approval: <PENDING — fill at sign-off checkpoint>` line.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `lib/db/schema.ts` exports `reminderSends` with `date('window_date', { mode: 'string' })`, `unique('reminder_sends_dedup_key')` on the 5-column natural key, and `index('reminder_sends_org_id_idx')`.
    - SOURCE: `drizzle/0014_reminder_sends.sql` contains `CREATE TABLE "reminder_sends"`, `reminder_sends_dedup_key` UNIQUE on the 5 columns, `ENABLE ROW LEVEL SECURITY`, `(SELECT auth.jwt()->>'org_id')` (wrapped form), and `GRANT SELECT, INSERT, UPDATE, DELETE ON "reminder_sends" TO authenticated`.
    - SOURCE: `drizzle/meta/_journal.json` has a `0014_reminder_sends` entry as the highest idx.
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`.
  </acceptance_criteria>
  <done>The `reminderSends` export and `0014_reminder_sends.sql` exist with additive RLS+GRANT+UNIQUE+index shape; the migration is registered in the journal; the header carries rationale + D-05 + a PENDING approval line.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Migration-header operator sign-off (ASK-FIRST per CLAUDE.md "schema change after Phase 2" + Database Migration Discipline). The 0014 migration body is authored; the operator-signed header (rationale + approval timestamp + decision ID) is required before the migration is committed. The authoring approval is banked (s27) but the header sign-off is a separate, required gate.
  </what-built>
  <how-to-verify>
    1. Review `drizzle/0014_reminder_sends.sql` — confirm it is ADDITIVE ONLY (new table; no DROP/REVOKE/NOT-NULL-on-existing; no existing schema objects mutated).
    2. Confirm the header rationale references R7-2/D-05 and the pre-paying-customer status (STATE.md) basis.
    3. Operator provides the approval timestamp + decision ID; replace the `Operator-approval: <PENDING …>` line with the signed value.
  </how-to-verify>
  <resume-signal>Type "approved — header signed" with the timestamp/decision ID once the header is signed. Or describe required changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply 0014 to dev + TEST DB via the repo runner; db:verify green</name>
  <read_first>
    - `package.json` scripts `db:migrate` (uses `.env.local`), `db:migrate:test` (uses `.env.local.test`), `db:verify` (runs `scripts/check-deploy-schema.ts` against `.env.local`) — lines 20-24
    - `docs/runbooks/deploy-migrations.md` (additive-migration apply procedure)
    - `07-CONTEXT.md` phase constraint #2 ([BLOCKING] schema apply to TEST/dev only; NOT drizzle-kit push; staging/prod operator-gated)
    - Plan 07-04 (this phase) extends `check-schema.ts`/`check-deploy-schema.ts` for `reminder_sends` — if that extension has not run yet, `db:verify` asserts only the pre-existing tables; this task confirms the migration APPLIES cleanly and existing assertions still pass. The full `reminder_sends` shape assertion in `db:verify` becomes green after Plan 07-04.
  </read_first>
  <action>
    [BLOCKING] Apply the additive `0014_reminder_sends.sql` to BOTH dev and TEST DBs via the repo's immutable forward-only runner — do NOT use `drizzle-kit push`:
    - `pnpm db:migrate` (dev, reads `.env.local`)
    - `pnpm db:migrate:test` (TEST, reads `.env.local.test`)
    Then run `pnpm db:verify` and confirm it exits 0 (existing-table assertions + RLS + GRANTs still pass; the `reminder_sends`-specific shape assertion is added in Plan 07-04 and goes green there). Do NOT apply to staging or prod (operator-gated, out of scope this phase). This task is the hard gate: the Wave 0 `check:crons-email` harness (Plan 07-01) and every downstream cron task require the live TEST DB to actually have `reminder_sends`.
  </action>
  <verify>
    <automated>pnpm db:verify</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `pnpm db:migrate` and `pnpm db:migrate:test` both complete without error (migration `0014_reminder_sends` applied to dev + TEST).
    - CLI: `pnpm db:verify` exits 0.
    - BEHAVIOR: querying the TEST DB shows the `reminder_sends` table exists with RLS enabled and the `reminder_sends_dedup_key` UNIQUE constraint present (confirmable via the Plan 07-01 harness collecting against a real table rather than erroring on a missing relation).
    - CONSTRAINT: no `drizzle-kit push` was used; staging/prod were NOT touched.
  </acceptance_criteria>
  <done>`reminder_sends` is live on the dev + TEST DBs via the forward-only runner; `db:verify` exits 0; staging/prod untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → npm registry | New packages cross the supply-chain boundary; install is gated on operator approval + a current-month IOC sweep. |
| migration runner → dev/TEST DB | Schema mutation; restricted to dev/TEST via `.env.local`/`.env.local.test`; staging/prod operator-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-SC | Tampering | `pnpm add resend react-email` | mitigate | Blocking-human checkpoint BEFORE install; both pins independently verified ≥14 days old; current-month IOC sweep of `.mcp.json` + postinstall ledger; latest (younger) versions explicitly rejected; `pnpm ls svix` dedup verification. |
| T-7-03 | Tampering | `0014_reminder_sends.sql` (immutable migration) | mitigate | Additive-only (no DROP/REVOKE/NOT-NULL-on-existing); operator-signed header gate before commit; applied to dev/TEST only; forward-only runner (no drizzle-kit push). |
| T-7-04 | Elevation of Privilege | `reminder_sends` table access | mitigate | RLS `org_isolation` (wrapped form) + GRANT only to `authenticated`; org_id NOT NULL; FK to organizations onDelete cascade — RLS is the last line for the cron path. |
| T-7-05 | Information Disclosure | RESEND_API_KEY in env | mitigate | Read from local env files only; never echoed/printed/committed; checkpoint instructions forbid printing secret values. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0 with the `reminderSends` export present.
- `pnpm db:migrate` + `pnpm db:migrate:test` apply `0014` cleanly; `pnpm db:verify` exits 0.
- `pnpm ls svix` shows one version after install.
- Migration is additive-only with a signed operator header before commit.
</verification>

<success_criteria>
- `resend@6.12.3` + `react-email@6.1.5` installed (exact pins, ≥14 days old, ASK-FIRST approved).
- `reminder_sends` table live on dev + TEST DBs with UNIQUE + RLS + GRANT + index (R7-2 foundation).
- `lib/db/schema.ts` exports `reminderSends`.
- Operator-signed migration header present before commit.
</success_criteria>

<output>
Create `.planning/phases/07-crons-email/07-02-SUMMARY.md` when done.
</output>
