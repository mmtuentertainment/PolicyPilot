---
phase: 07-crons-email
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - app/api/cron/reminders/route.ts
autonomous: true
requirements: [REQ-notification-system]
user_setup: []

must_haves:
  truths:
    # R7-1 / D-02: in-route Bearer CRON_SECRET self-gate (middleware bypasses Clerk); 401 on bad/missing; 200 with typed counts; 503 only on pre-loop DB fatal.
    - "GET /api/cron/reminders returns 401 on missing/incorrect Bearer CRON_SECRET, 200 + {reviewReminders:number, ackReminders:number} on success, and 5xx only on a pre-loop fatal (DB-connect → 503)"
    # D-01: loop-per-org via withOrgScope; org enumeration is the only raw db usage (allow-listed); RLS is the last line.
    - "The route enumerates orgs via the allow-listed raw SELECT id FROM organizations, then runs all per-org reads/writes inside withOrgScope({ orgId }) so RLS enforces isolation"
    # D-03: record-then-send — claim reminder_sends via onConflictDoNothing().returning(), insert notifications for winners, COMMIT, then send Resend AFTER commit with per-user try/catch.
    - "Inside the per-org tx: reminder_sends is claimed via onConflictDoNothing().returning() and notifications inserted for winners, then COMMIT; Resend sends happen AFTER commit with per-user try/catch (email I/O never held in the DB tx)"
    # D-02 + D-04: counts = committed rows; review_due → admins, ack_reminder → none+stale; reminder_sends gates ONLY cron types; window_date is server-UTC.
    - "Counts equal committed reminder_sends/notifications rows (post-commit send failures logged not counted); review_due routes to admins and ack_reminder to ackState none+stale; window_date is computed server-side as new Date().toISOString().slice(0,10) (UTC)"
  artifacts:
    - path: "app/api/cron/reminders/route.ts"
      provides: "GET cron handler: auth gate + per-org loop + record-then-send"
      contains: "withOrgScope"
  key_links:
    - from: "app/api/cron/reminders/route.ts"
      to: "lib/db/repositories/reminders.ts + lib/email/send.ts"
      via: "candidate queries → reminder_sends claim → post-commit sendNotificationEmail"
      pattern: "sendNotificationEmail"
    - from: "app/api/cron/reminders/route.ts"
      to: "reminderSends (onConflictDoNothing)"
      via: "in-tx claim short-circuit"
      pattern: "onConflictDoNothing"
---

<objective>
Wave 2: build `GET /api/cron/reminders` — the in-route `Bearer {CRON_SECRET}` self-gate (R7-1), org enumeration via the allow-listed raw `db` (D-01), and the per-org `withOrgScope` loop with the record-then-send idempotency pattern (D-03): claim `reminder_sends` → insert `notifications` for winners → COMMIT → send Resend after commit with per-user `try/catch`. This is the orchestration that wires the Wave 1 building blocks (email layer + reminders repo + notifications repo) into the running cron.

Purpose: R7-1/R7-2/R7-3/R7-5 converge here — the authenticated, org-scoped, idempotent daily reminder engine for the two cron types (`review_due`, `ack_reminder`).
Output: `app/api/cron/reminders/route.ts`. Turns `app/api/cron/reminders/route.test.ts` (R7-1) and the R7-2/R7-3 DB-truth steps of `check:crons-email` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-crons-email/07-SPEC.md
@.planning/phases/07-crons-email/07-CONTEXT.md
@.planning/phases/07-crons-email/07-RESEARCH.md
@.planning/phases/07-crons-email/07-PATTERNS.md
@app/api/webhooks/stripe/route.ts
@middleware.ts
@scripts/check-db-imports.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cron route — auth gate + org enumeration + per-org record-then-send</name>
  <read_first>
    - `app/api/webhooks/stripe/route.ts` lines 1-15 (imports, `runtime='nodejs'`, `dynamic='force-dynamic'`) + lines 224-254 (`onConflictDoNothing().returning()` in-tx short-circuit) — primary analog per 07-PATTERNS.md §route lines 38-131
    - `07-PATTERNS.md` §cron route (lines 38-131) — auth gate, org enumeration, idempotency-in-tx, post-commit dispatch, CRITICAL synthesized OrgContext `userId=''` note, window_date UTC derivation
    - `07-RESEARCH.md` §Cron Route Patterns (lines 711-755) — auth gate (R7-1), org enumeration (R7-3), synthesized OrgContext shape + `role:'admin'` for JWT injection only
    - `07-RESEARCH.md` §Pattern 3 (lines 322-353) — record-then-send tx body; D-03 commit-before-send
    - `middleware.ts` lines 28-30, 118-120 (Clerk bypass for `/api/cron/(.*)` → route MUST self-gate)
    - `scripts/check-db-imports.ts` line 42 (cron route pre-allow-listed for raw `db`)
    - `lib/db/repositories/reminders.ts` + `lib/email/send.ts` + `lib/db/repositories/notifications.ts` (Plans 07-03/07-04 — the methods this route calls)
    - `app/api/cron/reminders/route.test.ts` (Plan 07-01 RED test — the R7-1 contract this turns GREEN)
  </read_first>
  <behavior>
    - GET with no Authorization → 401; GET with wrong Bearer → 401; GET with `Bearer ${CRON_SECRET}` → 200 + `{reviewReminders:number, ackReminders:number}`.
    - Two GETs in the same UTC window produce exactly one reminder_sends row + one notifications row per (org,user,policy,type); the second run's counts for already-sent tuples are zero.
    - Org A's run never reads or writes Org B's rows (per-org withOrgScope + RLS).
    - A per-org failure is caught and logged (masked) and does not abort the remaining orgs; counts reflect committed rows only.
  </behavior>
  <action>
    Create `app/api/cron/reminders/route.ts` with `export const runtime = 'nodejs'` and `export const dynamic = 'force-dynamic'`. Implement `GET(req: Request): Promise<Response>`:
    1. Auth gate: read `authHeader = req.headers.get('authorization')` and `cronSecret = process.env.CRON_SECRET?.trim()`; if `!cronSecret || authHeader !== \`Bearer ${cronSecret}\`` → `Response.json({ error: 'Unauthorized' }, { status: 401 })`.
    2. Org enumeration (ONLY raw `db` usage — allow-listed): `const orgIds = await db.select({ id: organizations.id }).from(organizations)`. Wrap the DB connect in a try/catch that returns 503 on a pre-loop fatal (D-02).
    3. Compute `const windowDate = new Date().toISOString().slice(0, 10)` (UTC `YYYY-MM-DD` — D-05/Pitfall 3). Init `reviewReminders = 0`, `ackReminders = 0`.
    4. For each org, wrap in `try/catch` (D-02 — one org's failure must not block the rest; log `console.error({ event: 'cron_org_error', orgId: orgId.slice(-4), error: String(err) })`). Inside the loop call `withOrgScope({ orgId, userId: '', clerkOrgId: '', clerkUserId: '', role: 'admin' } satisfies OrgContext, async (s) => { … })`:
       - Read candidates via `Reminders.listReviewDueCandidatesForOrg(s)` and `Reminders.listAckReminderCandidatesForOrg(s)`.
       - For each candidate, claim its slot: `s.tx.insert(reminderSends).values({ orgId: s.orgId, userId: candidate.userId, policyId: candidate.policyId, type: candidate.type, windowDate, sentAt: new Date() }).onConflictDoNothing().returning({ id: reminderSends.id })`. If `inserted.length === 0` → `continue` (already sent this window — short-circuit; reminder_sends gates ONLY the two cron types per D-04).
       - For winners, `Notifications.create(s, { userId, type: candidate.type, payloadJson: { policyId, policyTitle }, read: false })`; collect winners (with email + title) into an array returned from the callback.
       - The tx auto-commits when the callback returns. Return the winners + per-type counts from the callback.
    5. AFTER `withOrgScope` resolves (committed), send Resend per winner with per-user try/catch: `await sendNotificationEmail(winner.type, winner.email, { policyTitle, orgName, acknowledgeUrl|reviewUrl })`; on failure log `console.error({ event: 'cron_email_error', type, error: String(err) })` and do NOT decrement counts (D-02 — counts = committed rows, post-commit send failures self-heal next day).
    6. Accumulate `reviewReminders`/`ackReminders` from committed winners per type; return `Response.json({ reviewReminders, ackReminders })`.
    CRITICAL: the synthesized OrgContext `userId=''` is for JWT injection only (RLS checks `org_id`); `role:'admin'` satisfies `asRole()` type narrowing, NOT a privilege grant — authority is the CRON_SECRET gate. Never write `s.userId` into a FK (audit confirmed in Plan 07-04). No `any`.
  </action>
  <verify>
    <automated>pnpm vitest run app/api/cron/reminders/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `route.ts` contains the `Bearer ${cronSecret}` gate returning 401, `runtime='nodejs'`, `dynamic='force-dynamic'`, the raw `db.select({ id: organizations.id })` enumeration (only raw `db` usage), `withOrgScope`, `onConflictDoNothing().returning(`, `Notifications.create`, and post-commit `sendNotificationEmail` inside per-user try/catch.
    - SOURCE: `windowDate` is `new Date().toISOString().slice(0, 10)`; the response body is `{ reviewReminders, ackReminders }`.
    - SOURCE: the per-org loop is wrapped in try/catch with masked `cron_org_error` logging; a pre-loop DB fatal returns 503.
    - TEST: `pnpm vitest run app/api/cron/reminders/route.test.ts` GREEN (R7-1 401/401/200).
    - TEST: the R7-2 (double-run → 1 row) and R7-3 (two-org isolation) steps of `pnpm check:crons-email` GREEN.
    - CLI: `pnpm check:db-imports` exits 0 (raw `db` only in the allow-listed cron route); `pnpm tsc --noEmit` exits 0; no `any`.
  </acceptance_criteria>
  <done>The cron route authenticates, enumerates orgs, runs the record-then-send loop per org, sends after commit, returns typed counts, and passes R7-1/R7-2/R7-3 + the db-imports allow-list gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway worker (machine caller) → cron route | Untrusted HTTP request; middleware bypasses Clerk for `/api/cron/*`, so the route is the sole auth point via `Bearer {CRON_SECRET}`. |
| cron route → per-org DB tx | Single machine caller fans out across all orgs; isolation must hold per org via RLS. |
| cron route → Resend | Post-commit external send; failure must not corrupt committed state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-11 | Spoofing | unauthenticated cron trigger | mitigate | In-route `Bearer {CRON_SECRET}` gate; middleware bypasses Clerk (existing) so the route self-gates; `cronSecret` read from env, never committed/logged; mismatch → 401. |
| T-7-12 | Information Disclosure | cross-org reads/writes in the loop | mitigate | Per-org `withOrgScope` + RLS; raw `db` used ONLY for the allow-listed org-enumeration SELECT; `check-db-imports` gate enforces the allow-list; two-org TEST-DB fixture (R7-3) proves isolation. |
| T-7-13 | Denial of Service | duplicate emails/notifications on retry | mitigate | `reminder_sends` UNIQUE + `onConflictDoNothing().returning()` in-tx short-circuit (D-03); same-window re-run sends zero duplicates. |
| T-7-14 | Denial of Service | email I/O holding the DB tx / one org aborting all | mitigate | Resend sends happen AFTER commit; per-org and per-user try/catch isolate failures; a pre-loop DB fatal returns 503 so Railway surfaces a hard failure. |
| T-7-05 | Information Disclosure | CRON_SECRET / recipient in logs | mitigate | Secret never logged; masked structured logging (`orgId.slice(-4)`, masked recipient); recipient masked in `ResendSendError`. |
| T-7-SC | Tampering | npm installs | mitigate | This plan adds ZERO packages; no install task. |
</threat_model>

<verification>
- `pnpm vitest run app/api/cron/reminders/route.test.ts` GREEN (R7-1).
- `pnpm check:crons-email` R7-2 + R7-3 steps GREEN.
- `pnpm check:db-imports` exits 0 (allow-list respected).
- `pnpm tsc --noEmit` exits 0; no `any`; no secret or full recipient in logs.
</verification>

<success_criteria>
- Authenticated cron endpoint (R7-1) returning typed counts.
- Org-scoped per-org loop via withOrgScope (R7-3, D-01).
- Idempotent record-then-send (R7-2, D-03) for the two cron types (D-04).
- review_due → admins, ack_reminder → none+stale, server-UTC window_date (R7-5, D-02/D-06/D-07).
</success_criteria>

<output>
Create `.planning/phases/07-crons-email/07-05-SUMMARY.md` when done.
</output>
