---
phase: 07-crons-email
plan: 04
type: execute
wave: 1
depends_on: ["07-01", "07-02"]
files_modified:
  - lib/db/repositories/notifications.ts
  - lib/db/repositories/reminders.ts
  - lib/policies/transitions.ts
autonomous: true
requirements: [REQ-notification-system]
user_setup: []

must_haves:
  truths:
    # D-12: bell backend only — markRead UPDATE (notifications mutable, NOT in IMMUTABLE_TABLES) + create() fill; listUnreadForUser already live.
    - "Notifications.create(s, input) inserts an org-scoped notifications row; Notifications.markRead(s, id) flips read=true via org-scoped UPDATE (notifications is intentionally mutable, NOT in IMMUTABLE_TABLES)"
    # D-07 + D-09: org-wide ack_reminder candidate query reusing 05 D-01 JOIN/ackState WITHOUT the per-user filter, expanding department assignments; ackState in {none, stale}, unacked >7d.
    - "listAckReminderCandidatesForOrg(s) returns org-wide (userId, userEmail, policyId, policyTitle, ackState) tuples for ackState in {none, stale} unacked >7d, expanding department assignments to member users via users.department_id (one query per org, no N+1)"
    # D-08: forward-only next_review_date writer on publish; no backfill.
    - "publish() persists next_review_date = published_at + review_interval_months (default 12) inside the existing withOrgScope tx; existing published policies stay NULL (no backfill)"
    # phase constraint #6: cron OrgContext userId='' FK audit — no method writes s.userId into a createdBy/assignedBy FK.
    - "No cron-called repository method writes s.userId into a createdBy/assignedBy UUID FK (Notifications.create input omits createdBy; reminderSends.user_id comes from the candidate row) — audit documented"
  artifacts:
    - path: "lib/db/repositories/notifications.ts"
      provides: "Notifications.create + markRead implementations"
      contains: "markRead"
    - path: "lib/db/repositories/reminders.ts"
      provides: "org-wide ack_reminder + review_due candidate queries"
      contains: "listAckReminderCandidatesForOrg"
    - path: "lib/policies/transitions.ts"
      provides: "next_review_date forward-only writer in publish()"
      contains: "nextReviewDate"
  key_links:
    - from: "lib/db/repositories/notifications.ts markRead"
      to: "notifications.read"
      via: "org-scoped UPDATE set read=true"
      pattern: "update\\(notifications\\)"
    - from: "lib/db/repositories/reminders.ts"
      to: "policy_assignments + acknowledgments + users.department_id"
      via: "JOIN reusing 05 D-01 ackState logic, org-wide (no userId filter)"
      pattern: "ackState"
---

<objective>
Wave 1 data layer: fill the two `Notifications` throw-stubs (`create`, `markRead` — D-12 bell backend), add the org-wide `ack_reminder`/`review_due` candidate queries (D-07/D-09), wire the forward-only `next_review_date` writer into `publish()` (D-08), and perform the cron `userId=''` FK audit (phase constraint #6). These are the org-scoped DB primitives the cron route (Plan 07-05) consumes.

Purpose: R7-6 (bell backend), R7-5/R7-8 (reminder semantics + writer) need real org-scoped repository methods before the cron can call them. The org-wide ack query (D-09) is explicitly NOT a reuse of the per-user `listAssignedAndPublishedForUser` — it is a new method with no userId filter and department fan-out.
Output: `Notifications.create`/`markRead` filled; new `lib/db/repositories/reminders.ts`; `next_review_date` writer in `publish()`. Turns the R7-6 / R7-8 DB-truth steps of `check:crons-email` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-crons-email/07-SPEC.md
@.planning/phases/07-crons-email/07-CONTEXT.md
@.planning/phases/07-crons-email/07-RESEARCH.md
@.planning/phases/07-crons-email/07-PATTERNS.md
@lib/db/repositories/notifications.ts
@lib/db/repositories/policies.ts
@lib/policies/transitions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill Notifications.create + markRead; document cron userId='' FK audit</name>
  <read_first>
    - `lib/db/repositories/notifications.ts` (full file — the two throw-stubs at lines 34-44 + the live `listAll`/`listUnreadForUser` read shape) per 07-PATTERNS.md §notifications.ts lines 570-606
    - `lib/db/repositories/policy_assignments.ts` `create()` (the `{ ...input, orgId: s.orgId }.returning()` org-scoped insert pattern)
    - `07-PATTERNS.md` §notifications.ts (lines 587-606) — `create()` injects `orgId: s.orgId`; `markRead()` org-scoped UPDATE; CRITICAL `NotificationCreateInput` omits `createdBy`
    - `07-RESEARCH.md` Open Question 1 (lines 911-914) + Pitfall 5 (lines 782-785) — the cron `userId=''` audit
    - `07-CONTEXT.md` phase constraint #6 (FK audit) + D-12 (markRead UPDATE; notifications mutable)
  </read_first>
  <behavior>
    - Notifications.create(s, { userId, type, payloadJson, read }) inserts exactly one row with orgId = s.orgId and returns it.
    - Notifications.markRead(s, id) sets read=true for the row matching (orgId, id) and returns its id; a row from another org is never updated (RLS + explicit orgId predicate).
    - listUnreadForUser count decrements by one after a markRead of an unread row.
  </behavior>
  <action>
    Replace the `create` throw-stub with `async (s, input) => s.tx.insert(notifications).values({ ...input, orgId: s.orgId }).returning()` (inject `orgId` from scope; `NotificationCreateInput` already omits `orgId`/`id`/`createdAt` — confirmed no `createdBy` field, so the cron's synthesized `userId=''` is never written into a FK). Replace the `markRead` throw-stub with `async (s, id) => s.tx.update(notifications).set({ read: true }).where(and(eq(notifications.orgId, s.orgId), eq(notifications.id, id))).returning({ id: notifications.id })`. Keep `import { and, eq }` (already present). Do NOT add `notifications` to `IMMUTABLE_TABLES` — it is intentionally mutable (D-12).
    Add a short header comment block to `notifications.ts` documenting the cron `userId=''` FK audit finding (phase constraint #6 / RESEARCH Open Question 1): `Notifications.create` input omits `createdBy`; the only column written from scope is `orgId`; the synthesized cron OrgContext's empty `userId` is therefore never written into a UUID FK. State that the `reminder_sends.user_id` value comes from the candidate row (the target user), not `s.userId`.
  </action>
  <verify>
    <automated>pnpm vitest run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts -t "R7-6"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `notifications.ts` `create` contains `insert(notifications).values({ ...input, orgId: s.orgId }).returning()`; `markRead` contains `update(notifications).set({ read: true })` with `eq(notifications.orgId, s.orgId)` AND `eq(notifications.id, id)`.
    - SOURCE: a header comment documents the cron `userId=''` FK audit (no `createdBy` write; `reminder_sends.user_id` from candidate).
    - SOURCE: `notifications` is NOT present in any `IMMUTABLE_TABLES` list (grep returns 0).
    - TEST: the R7-6 step of `pnpm check:crons-email` is GREEN (markRead flips read=true; listUnreadForUser decrements).
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`.
  </acceptance_criteria>
  <done>`Notifications.create`/`markRead` are implemented org-scoped, the FK audit is documented, and the R7-6 DB-truth step is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Org-wide ack_reminder + review_due candidate queries (lib/db/repositories/reminders.ts)</name>
  <read_first>
    - `lib/db/repositories/policies.ts` lines 135-209 (`listAssignedAndPublishedForUser`: dept sub-select, 3-state `ackState` CASE, JOIN chain) per 07-PATTERNS.md §reminders lines 458-501
    - `07-PATTERNS.md` §listAckReminderCandidatesForOrg (lines 458-501) — adaptation: remove userId param, org-wide dept-membership JOIN, filter ackState IN ('none','stale'), >7d date filter, return tuples, wrap in withOrgScope (s.tx)
    - `07-RESEARCH.md` §Pattern 6 (lines 401-416) — org-wide query; D-09 one-query-per-org, no N+1
    - `07-CONTEXT.md` D-06 (review_due → admins), D-07 (ack_reminder ackState none+stale >7d, dept fan-out), D-09 (org-wide query, not per-user reuse)
    - `lib/db/schema.ts` — `users.role` (admin filter for review_due), `users.departmentId`, `policies.nextReviewDate`, `policy_assignments.assigneeType`
  </read_first>
  <behavior>
    - listReviewDueCandidatesForOrg(s) returns one (adminUserId, adminEmail, policyId, policyTitle) tuple per org admin (users.role='admin') per policy whose next_review_date <= now+14d. A policy due in 13d is included; 15d excluded.
    - listAckReminderCandidatesForOrg(s) returns org-wide (userId, userEmail, policyId, policyTitle, ackState) for ackState IN ('none','stale') unacked >7d, expanding department assignments to member users. An assignment unacked 8d is included; 5d excluded; a 'stale' user is included; a 'current' user is excluded.
    - Both queries are org-scoped (use s.tx, every WHERE includes s.orgId) and run as a single query each (no per-user loop).
  </behavior>
  <action>
    Create `lib/db/repositories/reminders.ts` (`import 'server-only'`; `OrgScope` from `@/lib/db/scoped`; never raw `db`). Export a `Reminders` object (or named functions — planner discretion) with two methods:
    - `listReviewDueCandidatesForOrg(s)`: SELECT policies where `policies.orgId = s.orgId AND policies.status = 'published' AND policies.nextReviewDate IS NOT NULL AND policies.nextReviewDate <= now() + interval '14 days'`, cross-joined to org admin users (`users.orgId = s.orgId AND users.role = 'admin'`), returning `(userId, userEmail, policyId, policyTitle)` — one row per admin per due policy (D-06).
    - `listAckReminderCandidatesForOrg(s)`: adapt `listAssignedAndPublishedForUser` (policies.ts 135-209) — keep the 3-state `ackState` CASE + the JOIN to `policy_assignments` + `acknowledgments`, but REMOVE the per-user filter and expand department assignments org-wide via `users.departmentId = policyAssignments.assigneeId` (for `assigneeType='department'`) OR `users.id = policyAssignments.assigneeId` (for `assigneeType='user'`). Filter to `ackState IN ('none','stale')` and the >7d window (none: assignment/publish >7d ago; stale: the current version's publish >7d ago). Return `(userId, userEmail, policyId, policyTitle, ackState)`. One query, no N+1 (D-09). All WHEREs include `s.orgId`; use `s.tx` only.
    SQL formatting / JOIN order is Claude's discretion (reuse 05 D-01's shape). No `any`.
  </action>
  <verify>
    <automated>pnpm vitest run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts -t "R7-5"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `lib/db/repositories/reminders.ts` exports `listReviewDueCandidatesForOrg` and `listAckReminderCandidatesForOrg`, imports `OrgScope`, uses `s.tx` (zero raw `db` imports), and every query includes `eq(... .orgId, s.orgId)` / `... .orgId = s.orgId`.
    - SOURCE: `listAckReminderCandidatesForOrg` filters `ackState IN ('none','stale')` and contains the `users.departmentId` dept-fan-out join; `listReviewDueCandidatesForOrg` filters `users.role = 'admin'` and `next_review_date <= now() + 14 days`.
    - TEST: the R7-5 window step of `pnpm check:crons-email` is GREEN (13d incl / 15d excl; 8d incl / 5d excl; none+stale incl; dept fan-out per member).
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`.
  </acceptance_criteria>
  <done>Both org-wide candidate queries exist, are org-scoped, expand department assignments, and the R7-5 DB-truth step is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Forward-only next_review_date writer in publish() (D-08)</name>
  <read_first>
    - `lib/policies/transitions.ts` lines 260-340 (`publish()` body: the `withOrgScope` callback, `PolicyVersions.create`, the existing `.update(policies).set({ status: 'published', updatedAt: sql\`now()\` })`) per 07-PATTERNS.md §transitions lines 610-651
    - `07-PATTERNS.md` §transitions (lines 610-651) — where to insert the writer (extend the existing `.set(...)`); D-08 forward-only, no backfill
    - `lib/db/schema.ts` — `policies.reviewIntervalMonths` (default 12), `policies.nextReviewDate`
    - `07-CONTEXT.md` D-08 (forward-only; `next_review_date = published_at + review_interval_months`; existing rows stay NULL)
  </read_first>
  <behavior>
    - Publishing a policy with review_interval_months=12 sets next_review_date ≈ published_at + 12 months inside the same publish tx.
    - Existing already-published policies are NOT backfilled (only the policy being published this call is written).
  </behavior>
  <action>
    Inside the existing `publish()` `withOrgScope` callback, after `PolicyVersions.create(...)`, compute `const months = policy.reviewIntervalMonths ?? 12;` and a forward-only `nextReviewDate` from the publish timestamp (`new Date()` + `months` via `setMonth`, OR a SQL interval expression — planner discretion; keep it UTC-consistent). EXTEND the existing `.update(policies).set({ status: 'published', updatedAt: sql\`now()\` })` call to ALSO set `nextReviewDate`. Do NOT add any backfill loop over other policies (D-08 — existing rows stay NULL). Add a comment citing D-08 forward-only. Keep the write inside the same org-scoped tx (no schema change — the column exists).
  </action>
  <verify>
    <automated>pnpm vitest run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts -t "R7-8"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `publish()` sets `nextReviewDate` in the same `.update(policies).set(...)` as `status: 'published'`, computed from `reviewIntervalMonths ?? 12`; a D-08 forward-only comment is present.
    - SOURCE: no loop/UPDATE touches policies other than the one being published (no backfill).
    - TEST: the R7-8 step of `pnpm check:crons-email` is GREEN (publish with interval 12 → next_review_date ≈ 12mo out).
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`. `pnpm vitest run lib/policies/transitions.test.ts` still GREEN (no regression to existing publish behavior).
  </acceptance_criteria>
  <done>`publish()` writes `next_review_date` forward-only inside its existing tx; no backfill; R7-8 step GREEN; existing transition tests unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron (synthesized OrgContext) → repository methods | The cron calls these methods with `userId=''`; methods must not write that into a FK. |
| any org's scope → notifications/reminders rows | Every read/write must include `org_id`; RLS is the last line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-08 | Information Disclosure | org-wide ack/review queries | mitigate | Every query includes `eq(... .orgId, s.orgId)` and runs via `s.tx` under `withOrgScope` RLS; zero raw `db`; the two-org TEST-DB fixture (R7-3) confirms no cross-org rows. |
| T-7-09 | Tampering | `Notifications.markRead` UPDATE crossing orgs | mitigate | UPDATE predicate is `(orgId = s.orgId AND id = id)`; RLS `org_isolation` blocks any cross-org row even if the id is guessed. |
| T-7-10 | Elevation of Privilege | cron `userId=''` written into a FK | mitigate | FK audit documented: `Notifications.create` input omits `createdBy`; `reminder_sends.user_id` sourced from candidate row, not `s.userId`; no method writes `s.userId` into a UUID FK. |
| T-7-SC | Tampering | npm installs | mitigate | This plan adds ZERO packages; no install task. |
</threat_model>

<verification>
- `pnpm check:crons-email` R7-5, R7-6, R7-8 steps GREEN.
- `pnpm vitest run lib/policies/transitions.test.ts` GREEN (no publish regression).
- `pnpm tsc --noEmit` exits 0; no `any`.
- `notifications` absent from `IMMUTABLE_TABLES`; FK audit comment present; zero raw `db` in `reminders.ts`.
</verification>

<success_criteria>
- Bell backend: `create`/`markRead` implemented (R7-6, D-12).
- Org-wide ack/review candidate queries with dept fan-out (R7-5, D-07/D-09).
- Forward-only `next_review_date` writer (R7-8, D-08).
- Cron `userId=''` FK audit documented (phase constraint #6).
</success_criteria>

<output>
Create `.planning/phases/07-crons-email/07-04-SUMMARY.md` when done.
</output>
