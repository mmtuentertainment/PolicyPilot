---
phase: 07-crons-email
plan: 07
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-08"]
files_modified:
  - app/api/webhooks/clerk/route.test.ts
  - scripts/check-rls.ts
  - scripts/check-schema.ts
  - scripts/check-deploy-schema.ts
  - scripts/check-artifacts.ts
  - package.json
  - .github/workflows/verify-phase-7.yml
autonomous: true
requirements: [REQ-notification-system]
user_setup: []

must_haves:
  truths:
    # R7-10: Clerk-webhook 409/catch vitest scaffold (T8 carry-forward); SF-W5 behavior fix stays deferred.
    - "app/api/webhooks/clerk/route.test.ts exercises the Clerk webhook 409 (duplicate) and catch (error) branches and passes; SF-W5 idempotency-reorder remains deferred"
    # D-13: extend schema gates for reminder_sends (shape + RLS + GRANT) and check-artifacts for the new route/lib/email/worker/templates/T8.
    - "reminder_sends is added to the TENANT_TABLES arrays in check-rls.ts, check-schema.ts, and check-deploy-schema.ts (shape + RLS + GRANT asserted); check-artifacts.ts asserts the cron route, lib/email, worker, railway.json, and templates exist"
    # D-13: cumulative verify:phase-7 wraps verify:phase-6 and adds the Phase 7 gates; CI job mirrors verify-phase-6.yml.
    - "package.json defines check:crons-email and a cumulative verify:phase-7 (tsc → verify:phase-6 → check:crons-email → lib/email + app/api/cron + app/api/webhooks/clerk vitests → db:verify → check:artifacts); a required Phase 7 CI job mirrors verify-phase-6.yml"
  artifacts:
    - path: "app/api/webhooks/clerk/route.test.ts"
      provides: "T8 Clerk-webhook 409/catch vitest scaffold"
      contains: "409"
    - path: "package.json"
      provides: "check:crons-email + verify:phase-7 scripts"
      contains: "verify:phase-7"
    - path: ".github/workflows/verify-phase-7.yml"
      provides: "required Phase 7 CI verification job"
      contains: "verify:phase-7"
  key_links:
    - from: "package.json verify:phase-7"
      to: "verify:phase-6 + check:crons-email + db:verify + check:artifacts"
      via: "cumulative chain"
      pattern: "verify:phase-7"
    - from: "scripts/check-deploy-schema.ts TENANT_TABLES"
      to: "reminder_sends"
      via: "shape + RLS + GRANT assertion"
      pattern: "reminder_sends"
---

<objective>
Wave 3: close out verification. Add the T8 Clerk-webhook 409/catch vitest (R7-10), extend the schema gates (`check-rls.ts`/`check-schema.ts`/`check-deploy-schema.ts`) and `check-artifacts.ts` for the Phase 7 surfaces, wire the cumulative `verify:phase-7` + `check:crons-email` scripts (D-13), and add a required Phase 7 CI job mirroring `verify-phase-6.yml`. This is the final wave — it consumes every prior plan's output.

Purpose: D-13 — make the whole phase falsifiable in one cumulative gate (`verify:phase-7` exits 0) and enforce the `reminder_sends` shape/RLS/GRANT + the new artifacts in CI. The live Railway 08:00 UTC run is manual operator evidence (R7-7), NOT part of this gate.
Output: `app/api/webhooks/clerk/route.test.ts`; extended schema/artifact gates; `verify:phase-7` + `check:crons-email` scripts; `.github/workflows/verify-phase-7.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-crons-email/07-SPEC.md
@.planning/phases/07-crons-email/07-CONTEXT.md
@.planning/phases/07-crons-email/07-RESEARCH.md
@.planning/phases/07-crons-email/07-PATTERNS.md
@.planning/phases/07-crons-email/07-VALIDATION.md
@app/api/webhooks/clerk/route.ts
@app/api/webhooks/stripe/route.test.ts
@.github/workflows/verify-phase-6.yml
@scripts/check-rls.ts
@scripts/check-artifacts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: T8 Clerk-webhook 409/catch vitest scaffold (R7-10)</name>
  <read_first>
    - `app/api/webhooks/clerk/route.ts` lines 274-314 (409 paths: org-not-found / user-not-found on membership event) and 383-406 (catch path) per 07-PATTERNS.md §clerk T8 lines 688-713
    - `app/api/webhooks/stripe/route.test.ts` lines 1-60 (module-mock + FakeTx pattern) — analog
    - `07-PATTERNS.md` §clerk T8 (lines 688-713) — branches to cover; stub Resend mock
    - `07-SPEC.md` R7-10 (lines 73-76) — T8 vitest IN; SF-W5 behavior fix DEFERRED
  </read_first>
  <behavior>
    - The 409 (duplicate / not-found) branch and the catch (error) branch of the Clerk webhook handler are each exercised and asserted.
    - The SF-W5 invert-idempotency-before-dispatch behavior is NOT changed — this is a test scaffold only.
  </behavior>
  <action>
    Create `app/api/webhooks/clerk/route.test.ts`: `vi.mock('server-only', () => ({}))` + the svix/verify + db mocks per the stripe test pattern (FakeTx). Cover the branches identified at `clerk/route.ts:274-314` (org-not-found / user-not-found → 409 on a membership event) and `:383-406` (error thrown inside the handler → catch path). Assert the HTTP status / response for each branch. Add the stub Resend mock (`vi.mock('@/lib/email/client', …)`) since the handler graph may touch email-adjacent code. Do NOT modify `route.ts` (SF-W5 fix stays deferred — test scaffold only). No `any`.
  </action>
  <verify>
    <automated>pnpm vitest run app/api/webhooks/clerk</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `app/api/webhooks/clerk/route.test.ts` exists and asserts both the 409 branch and the catch branch; contains the string `409`.
    - SOURCE: `app/api/webhooks/clerk/route.ts` is UNCHANGED (SF-W5 deferred).
    - TEST: `pnpm vitest run app/api/webhooks/clerk` GREEN.
    - CLI: `pnpm tsc --noEmit` exits 0; no `any`.
  </acceptance_criteria>
  <done>The T8 Clerk-webhook 409/catch vitest exists and passes; the handler behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend schema gates + check-artifacts for reminder_sends + Phase 7 surfaces</name>
  <read_first>
    - `scripts/check-rls.ts` lines 36-50 (TENANT_TABLES array) per 07-PATTERNS.md §check-schema lines 733-748
    - `scripts/check-schema.ts` and `scripts/check-deploy-schema.ts` (their TENANT_TABLES arrays + per-table shape/RLS/GRANT assertions) per 07-PATTERNS.md lines 733-748
    - `scripts/check-artifacts.ts` lines 20-51 (exists/assert helpers + per-phase blocks) per 07-PATTERNS.md §check-artifacts lines 752-782
    - `07-PATTERNS.md` §check-schema (lines 733-748) + §check-artifacts (lines 752-782) — the exact additions
    - `07-CONTEXT.md` D-13 (extend check-schema/check-deploy-schema for reminder_sends shape+RLS+GRANT; check-artifacts for route/lib/email/worker/templates/T8)
  </read_first>
  <action>
    Add `'reminder_sends'` to the `TENANT_TABLES` array in all three of `scripts/check-rls.ts`, `scripts/check-schema.ts`, and `scripts/check-deploy-schema.ts` (with a `// Phase 7 D-05` comment) so the per-table loop asserts RLS `org_isolation` (wrapped form) + the 4 GRANTs. If `check-schema.ts`/`check-deploy-schema.ts` carry explicit per-table column-shape assertions (as they did for Phase 4/5 tables), add a Phase 7 assertion block verifying the `reminder_sends` columns (`org_id`, `user_id`, `policy_id`, `type`, `window_date` date, `sent_at` timestamptz), the `reminder_sends_dedup_key` UNIQUE, and the `reminder_sends_org_id_idx` index.
    Extend `scripts/check-artifacts.ts` with a Phase 7 block asserting existence of: `app/api/cron/reminders/route.ts`, `lib/email/client.ts`, `lib/email/send.ts`, `lib/email/errors.ts`, `lib/email/templates/base-layout.tsx` (+ the 4 per-type templates), `worker/trigger-reminders.mjs`, `railway.json`, and `app/api/webhooks/clerk/route.test.ts` (T8). Add two field assertions: `railway.json` `deploy.cronSchedule === '0 8 * * *'` and `typeof deploy.startCommand === 'string'`. Use the existing `exists`/`assert` helpers; when counting tokens in any grep-style gate use `grep -v '^#' | grep -c` (never a bare `== 0` on an unfiltered file).
  </action>
  <verify>
    <automated>pnpm check:rls; pnpm db:verify; pnpm check:artifacts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `reminder_sends` appears in the TENANT_TABLES array of `check-rls.ts`, `check-schema.ts`, and `check-deploy-schema.ts`.
    - SOURCE: `check-artifacts.ts` asserts all Phase 7 files (cron route, `lib/email/*`, 5 templates, worker, `railway.json`, T8 test) and the `railway.json` `cronSchedule`/`startCommand` fields.
    - CLI: `pnpm check:rls` exits 0 (RLS + GRANT for `reminder_sends` pass), `pnpm db:verify` exits 0 (shape assertions pass against the applied TEST DB), `pnpm check:artifacts` exits 0.
    - CLI: `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All three schema gates assert `reminder_sends` shape/RLS/GRANT and `check-artifacts` enforces the Phase 7 file surface; the gates pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire check:crons-email + cumulative verify:phase-7 + Phase 7 CI job</name>
  <read_first>
    - `package.json` line 53 (`verify:phase-6` cumulative definition) + line 44 (`check:employee-portal` invocation form: `tsx --env-file=.env.local node_modules/vitest/vitest.mjs run … --config …`) per 07-PATTERNS.md §package.json lines 718-729
    - `07-PATTERNS.md` §package.json (lines 718-729) — `check:crons-email` + `verify:phase-7` entries
    - `07-RESEARCH.md` §verify:phase-7 Definition (lines 867-871) + §Validation Architecture (lines 862-871)
    - `07-VALIDATION.md` Test Infrastructure (lines 27-33) — exact cumulative chain
    - `.github/workflows/verify-phase-6.yml` (full file) — CI job analog; the env block already lists `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`CRON_SECRET`
    - `07-CONTEXT.md` D-13 (cumulative chain; live Railway run is manual evidence not CI)
  </read_first>
  <action>
    Add two scripts to `package.json`:
    - `"check:crons-email": "tsx --env-file=.env.local node_modules/vitest/vitest.mjs run scripts/check-crons-email.ts --config scripts/check-crons-email.vitest.config.ts"` (mirror the `check:employee-portal` invocation form so the TEST-DB harness gets the env file).
    - `"verify:phase-7": "pnpm tsc --noEmit && pnpm verify:phase-6 && pnpm check:crons-email && pnpm run test -- --run lib/email && pnpm run test -- --run app/api/cron && pnpm run test -- --run app/api/webhooks/clerk && pnpm db:verify && pnpm check:artifacts"` (cumulative — wraps `verify:phase-6`, never removes a prior gate, per 07-VALIDATION.md lines 30-32).
    Create `.github/workflows/verify-phase-7.yml` by copying `verify-phase-6.yml` and changing: `name: Verify Phase 7`; the job/concurrency identifiers to `phase-7`/`verify-phase-7-${{ github.ref }}`; and the final step to `run: pnpm verify:phase-7`. Keep the same `on:` triggers (pull_request + push:main + workflow_dispatch — single-verifier-DB concurrency discipline), pinned action SHAs, the env block (already includes Resend/CRON secrets), and the `.env.local` + `.env.local.test` writer step. Do NOT add a live-Railway step (operator evidence, not CI — D-11/R7-7).
  </action>
  <verify>
    <automated>pnpm verify:phase-7</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `package.json` contains `check:crons-email` (with `--env-file=.env.local` + the vitest config) and `verify:phase-7` (cumulative chain wrapping `verify:phase-6`, ending in `db:verify && check:artifacts`).
    - SOURCE: `.github/workflows/verify-phase-7.yml` exists, has `name: Verify Phase 7`, `pull_request` + `push: branches: [main]` + `workflow_dispatch` triggers, and a final `pnpm verify:phase-7` step; no live-Railway step.
    - CLI: `pnpm verify:phase-7` exits 0 (full cumulative gate green — every prior phase + all Phase 7 gates).
  </acceptance_criteria>
  <done>`check:crons-email` + cumulative `verify:phase-7` are wired, the Phase 7 CI job mirrors Phase 6, and `pnpm verify:phase-7` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → verification Supabase DB | The Phase 7 job mutates the shared dev/test verifier DB via TRUNCATE/seed; never staging/prod. |
| CI secrets → env files | Repository secrets are written to ephemeral `.env.local`/`.env.local.test`; never printed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-18 | Tampering | weakened verify chain (dropping a prior gate) | mitigate | `verify:phase-7` cumulatively wraps `verify:phase-6` and appends; acceptance criteria assert the chain ends in `db:verify && check:artifacts` and includes `check:crons-email`. |
| T-7-19 | Information Disclosure | CI secrets in logs | mitigate | Secrets passed via `${{ secrets.* }}` env; the env-writer step uses `printf` to files, never `echo`-to-stdout; mirrors the audited verify-phase-6.yml. |
| T-7-20 | Denial of Service | concurrent CI jobs deadlocking the verifier DB on TRUNCATE | mitigate | Same `concurrency` group + `cancel-in-progress` + push restricted to `main` (the verify-phase-6.yml discipline) prevents dual jobs on the shared DB. |
| T-7-02 | Tampering | reminder_sends RLS/GRANT regression slipping past CI | mitigate | `reminder_sends` added to all three TENANT_TABLES gates; `check-rls` + `db:verify` assert RLS `org_isolation` (wrapped) + GRANTs in CI. |
| T-7-SC | Tampering | npm installs | mitigate | This plan adds ZERO packages; `pnpm install --frozen-lockfile` in CI prevents drift from the Plan 07-02 pinned lockfile. |
</threat_model>

<verification>
- `pnpm vitest run app/api/webhooks/clerk` GREEN (T8).
- `pnpm check:rls`, `pnpm db:verify`, `pnpm check:artifacts` all exit 0 (reminder_sends + Phase 7 artifacts).
- `pnpm verify:phase-7` exits 0 (full cumulative gate).
- `.github/workflows/verify-phase-7.yml` mirrors the Phase 6 job; no live-Railway CI step.
</verification>

<success_criteria>
- T8 Clerk 409/catch vitest passes (R7-10); SF-W5 deferred.
- reminder_sends shape/RLS/GRANT enforced in all three schema gates; Phase 7 artifacts enforced (D-13).
- Cumulative verify:phase-7 + Phase 7 CI job green (D-13); live Railway run = operator evidence (R7-7).
</success_criteria>

<output>
Create `.planning/phases/07-crons-email/07-07-SUMMARY.md` when done.
</output>
