# secrets
.env.local
.env*.local
# Phase 2 — env file for test-DB-targeted commands (Plan 02-03)
.env.local.test
# Phase 4 UAT — local secrets folder for SDK keys, OAuth tokens, etc.
# NEVER commit anything from secrets/ — files here are operator-private.
secrets/

# Next.js / Node
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel / Railway
.vercel
.railway

# GSD ephemeral
.planning/intel/classifications/*.tmp
.planning/reports/

# Next.js / create-next-app additions
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
*.pem
.pnpm-debug.log*

# Drizzle / pnpm
# (drizzle/ removed in Plan 02-03 — migrations + meta MUST be tracked so
# the team / deployment can reproduce schema state. The Plan-01-01 entry
# was a mistake-in-advance; this is the fix.)
.drizzle/
*.local
.pnpm-store/
.next/cache/

.tmp/

# Operator-side / generator artifacts — NOT FOR REPO
# .mcp.json contains MCP server tokens; .agents/.claude/.wiki/audit-cache/
# are generator/operator-side directories that shouldn't be tracked.
# .wiki/ is the Karpathy wiki experiment — aspirational, not tracked yet.
.mcp.json
.agents/
.claude/
.wiki/
audit-cache/
skills-lock.json

# Dev server logs (UAT smoke runs, real-key probes)
localhost-*.log
