# Consultant Delta — 2026-06-05 — Phase 7 (Crons + Email) PLANNED

**Trigger:** `/gsd-plan-phase 7` completed (this session, GSD chain: research → plan → check). 7 `PLAN.md` files + `07-RESEARCH.md` + `07-PATTERNS.md` + `07-VALIDATION.md` authored and committed on `gsd/phase-7-crons-email`. Not pushed; no PR (operator owns push/PR). Extends the discuss delta `2026-06-05-phase7-context.md` (`cb1acfd`) and spec delta `2026-06-05-phase7-spec.md` (`cc3cf48`).

**Commits (this session, on `gsd/phase-7-crons-email`, NOT pushed):**
- `2492e32` — CONTEXT migration-index correction 0013→0014 (see below)
- `749f27e` — 07-RESEARCH.md (Resend/React Email/Railway + verified ≥14-day version pins)
- `3be5492` — 07-VALIDATION.md (Nyquist contract)
- `6a09a83` — 07-PATTERNS.md (20 files → analogs)
- `83f3459` — 7 PLAN.md files across 4 waves + ROADMAP plan list
- `819adc2` — plan-checker warning fixes (0 blockers)
- `fd941e9` — STATE PLANNED record + ROADMAP wave/constraint annotation

**What changed (planning artifacts only — STILL no runtime/schema change):**
- **7 plans across 4 waves** (plan-checker PASSED, 0 blockers, 4 warnings addressed): W0 `07-01` RED test scaffolding ‖ `07-02` ASK-FIRST package install + additive `0014_reminder_sends` migration; W1 `07-03` `lib/email` layer ‖ `07-04` notifications backend + org-wide ack/review queries + `next_review_date` writer + cron FK audit; W2 `07-05` cron route (record-then-send) ‖ `07-06` event emission + Railway worker; W3 `07-07` T8 + schema/artifact gates + cumulative `verify:phase-7` + CI.
- **Research:** verified, date-checked supply-chain pins — `resend@6.12.3` (2026-05-06, 30d, no postinstall) + `react-email@6.1.5` (2026-05-18, 18d, no postinstall); both current "latest" (`6.12.4`/`6.5.0`) correctly excluded as <14 days. Railway native cron config confirmed (`railway.json` `cronSchedule "0 8 * * *"`, no Dockerfile, worker must `process.exit()`). Install remains ASK-FIRST.

**⚠ Migration-index correction (the load-bearing plan-stage finding):** the discuss delta + `07-CONTEXT.md` D-05 named the send-ledger migration **`0013_reminder_sends`**. But `0013_review_decisions.sql` already shipped (Phase 9, on `main` → this branch); `_journal.json` highest index is **13** and migrations are immutable/forward-only. **Corrected to `0014_reminder_sends`** (5 stale refs in `07-CONTEXT.md` fixed, `2492e32`). This supersedes the discuss delta's "additive migration `0013`" wording. Caught before the planner could author a colliding immutable tag.

**Risk register touch:**
- **R-006** (reminder idempotency / backlog rank-6) — design now fully translated into executable plans (record-then-send + `reminder_sends 0014` natural-key UNIQUE + `onConflictDoNothing`-in-tx + post-commit Resend). Build pending Phase 7 execute → **still not mitigated** (no code yet); flips to Mitigated on ship.
- **New, resolved-in-planning:** migration-index immutable-tag collision (0013) — caught + corrected to 0014 at plan stage; no register entry needed (planning-doc fix, not a system risk), recorded here for the audit trail.
- R-013 / SF-W5 / SF-WHSEC-1 unchanged (T8 vitest in scope; SF-W5 idempotency-reorder fix + `CLERK_WEBHOOK_SECRET` rotation remain deferred per SPEC out-of-scope).

**Other consultant files (`system_map`, `feature_inventory`, `working_context`, `backlog`): no material change at plan stage** — Phase 7 still has zero runtime code. Substantive updates land at ship per keep-current: `system_map` gains the cron route + `lib/email` + worker + `reminder_sends` table; `feature_inventory` gains the notification/reminder feature; `risk_register` flips R-006 → Mitigated; `reference/SCHEMA.md` notifications-`org_id` reconcile.

**Doc debt (carried, unchanged):** `reference/SCHEMA.md` notifications block STALE (omits live `org_id`) — reconcile at Phase 7 ship.

**Next:** `/gsd-ui-phase 7` for the bell UI-SPEC (D-12 deferred the bell UX to after planning), then `/gsd-execute-phase 7`. Build routes ASK-FIRST (operator's call: read-mostly Claude → Codex, unless operator directs Claude to build, per the s14/s17 Phase-9 precedent). Migration `0014` is authored-pending the operator-signed header, dev/TEST apply only; `resend`/`react-email` install is ASK-FIRST.

---

## Addendum (same day) — bell design round-trip with ChatGPT

- **ChatGPT returned the bell-UI design** (`Downloads/report.html`, Part A bell UI + Part B plan review) off the brief `%TEMP%\policypilot-phase7-bell-ui-CHATGPT-design-brief-2026-06-05.md`. **Claude reviewed it read-only against the live working tree** → verdict **GO with a fix-list** (much cleaner than the Phase-9 sketch; correct mounts/tokens/routes/refresh-convention, no fabricated data-layer symbols). Findings (all verified file:line): C1 no `markAllRead` backend; C2 `/notifications` "View all" out of scope; H1 Part-B misread of cron `userId` (notifications.user_id = real recipient, never empty); H2 pseudo-JSX used Radix `asChild`/`onSelect` but repo is `@base-ui/react`; H3 preferences/unsubscribe note contradicts the locked transactional-MVP scope; + M1–M5 polish.
- **Tightened design-decisions doc authored** → `.planning/phases/07-crons-email/07-BELL-UI-DESIGN.md` (clean input for `/gsd-ui-phase 7`; decisions D1–D8 locked). **Operator decision D1 (this session): Mark-all-read = IN** via a new one-query `markAllReadForUser(s, userId)` (+ `markAllNotificationsReadAction`) — a conscious **+1 to plan 07-04's backend** beyond D-12's `markRead(id)`.
- **Plan-home gap flagged:** the bell *UI component* is not in any current plan (D-12 deferred it); after `07-UI-SPEC.md` it needs a plan home (`/gsd-plan-phase 7 --gaps` → `07-08`, or amend `07-04`) — together with the D1 `markAllReadForUser` method.
- **Consultant files (`system_map`, `feature_inventory`, `risk_register`, `working_context`, `backlog`): no-change at design-input stage** — Phase 7 still has zero runtime code. The D1 `markAllReadForUser` +1 and the notification/reminder feature land in `feature_inventory`/`system_map` at ship (per the existing keep-current plan).
