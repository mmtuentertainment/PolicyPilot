# Consultant Delta — 2026-06-05 — Phase 7 (Crons + Email) SPEC authored

**Trigger:** Operator authorized Phase 7 planning (session 27). `SPEC.md` authored + committed (`5e9e8b4`) on `gsd/phase-7-crons-email` (off `main` `c90dd44`). Not pushed; no PR (operator owns push/PR).

**What changed (planning artifact only — NO runtime/schema change yet):**
- New `.planning/phases/07-crons-email/07-SPEC.md` — **10 falsifiable requirements**, ambiguity **0.125** (gate ≤0.20). Grounded by a 5-agent scout (`wf_708557e5-baf`) + 1 Socratic interview round.
- 4 operator decisions locked:
  1. **Idempotency → new additive `reminder_sends` send-ledger table** (`org_id, user_id, policy_id, type, window_date, sent_at`; natural-key UNIQUE; `onConflictDoNothing` in-tx — mirrors `stripe_events`/`clerk_events`). **ASK-FIRST additive migration; operator pre-approved Claude AUTHORING it for header sign-off; dev/TEST apply only, staging/prod operator-gated.**
  2. **Full 4-type coverage** — adds a `next_review_date`-on-publish writer (so `review_due` can fire) + `policy_assigned`/`policy_updated` event emission on the assign/republish paths.
  3. **T8** Clerk-webhook 409/catch vitest scaffold IN; **SF-W5** invert-idempotency-before-dispatch DEFERRED.
  4. **Daily-nag cadence**, uniform across all tiers, deduped per `(user,policy,type)`/day.

**Risk register touch:** R-006 (reminder idempotency / backlog rank-6) — design now **DECIDED** (send-ledger). Build pending Phase 7 execute → still **not mitigated** (no code yet).

**Other consultant files (`system_map`, `feature_inventory`, `working_context`):** **no material change at spec stage** — Phase 7 has zero runtime code. To be updated at discuss/plan/ship per keep-current.

**Doc debt surfaced:** `reference/SCHEMA.md` notifications block is **STALE** (omits the live `org_id NOT NULL` from `lib/db/schema.ts:152-162`) — reconcile at Phase 7 ship or sooner.

**Next:** `/gsd-discuss-phase 7` (auto-loads `SPEC.md`). Build routes ASK-FIRST → Codex unless operator directs Claude.
