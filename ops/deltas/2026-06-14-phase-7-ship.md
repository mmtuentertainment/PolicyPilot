# Delta — 2026-06-14 — Phase 7 (Crons + Email) published as draft PR #44

**Trigger:** Phase 7 published — pushed `gsd/phase-7-crons-email`, opened draft PR #44, folded ship-review follow-ups FU-2/FU-4 (FU-1 false positive), re-ran the local gate green.

**Publication status:** Published, **not merged** (operator-owned). Pushed to `origin` at `aa6d8ab`. Draft PR #44 → `main`. `main` unchanged at `c90dd44`.

## What changed
- **Push + draft PR #44** opened with the drafted body (Summary / What shipped / Test status / follow-ups / NOT-in-PR).
- **FU-2** (`6fd033a`, `vitest.config.ts`): default test env `node`, jsdom scoped to `**/*.test.tsx` via `environmentMatchGlobs`. No `.test.ts` file broke on the switch.
- **FU-4** (`aa6d8ab`): (a) completed the `@/lib/db/schema` mock in `lib/policies/transitions.test.ts` (added `notifications/acknowledgments/organizations/policyAssignments/policyVersions/users` — pulled in via the `transitions.ts` → notifications/reminders module graph) — 24 vitest "No export defined" warnings → 0; (b) contained the `RESEND_FROM_EMAIL` env leak in `lib/email/send.test.ts` via `vi.stubEnv` + `afterEach vi.unstubAllEnvs`.
- **FU-1**: confirmed false positive (timer tests already restore real timers via per-test `try/finally`). No commit.

## Verification
- Local `verify:phase-7` **green at `aa6d8ab`**: `tsc --noEmit` 0 errors; vitest **39 files / 332 tests** pass; `check:rls` OK (positive control = 1 row against TEST project `qwtbbbjbxffioeeazxrw`); `db:verify` OK (dev project `kdoahaxhmaftxaiwbtdw`, resumed this session); `check:artifacts` 543/543.
- **CI (FU-3) is RED — environmental, not a Phase 7 defect.** Hosted `verify-phase-6`/`verify-phase-7` fail at `check:rls` on the CI runner: `POSITIVE CONTROL FAILED: orgA cannot see its own policy row (0 rows)` → GRANT/migration state on the DB behind the CI `DATABASE_URL` secret (fails on Phase 2 `0001_rls_policies.sql`, which Phase 7 never touches). CI `RESEND_API_KEY`/`CRON_SECRET` secrets are empty. The same `check:rls` gated green for Phase 6 (PR #32), so the CI Supabase DB/secret degraded since (free-tier auto-pause class — see [[gate-needs-two-supabase-projects]]). Operator must reconcile the CI Supabase secret/project before un-drafting on green CI.

## Current recommendation
1. Operator: resume / re-point the CI Supabase `DATABASE_URL` (and optionally set `RESEND_API_KEY`/`CRON_SECRET`), re-run CI on PR #44 head, confirm green.
2. Then un-draft + squash-merge PR #44; post-merge `git checkout main && git pull --ff-only`.
3. No deploy / no staging-prod migration / no live email until those gates are opened.

## Consultant keep-current — DONE (2026-06-14)
The deferred bookkeeping is closed. The 5 consultant files + `README.md` were **updated** (not `no-change`) for the Phase 7 ship:
- `working_context.md`, `system_map.md`, `feature_inventory.md`, `backlog.md`: folded in the Phase 7 EXECUTED / verify-green / draft-PR-#44 state (backlog rank-6/7 flip Pending → **Shipped (PR #44) / pending-merge**).
- `risk_register.md`: **R-006 → Controlled / draft PR #44** (idempotent `reminder_sends` claim-in-tx ledger); **new R-018** (Ops/Deliverability, P2·I3 = 6, Open / accepted-for-MVP) — cron email is at-least-once-*attempt* while the claim row is at-most-once, so a post-commit Resend failure silently drops the email (the in-app bell stays the durable channel). Pre-launch control: verify the Resend sending domain (SPF/DKIM/DMARC) + set `RESEND_FROM_EMAIL` off a real domain (currently defaults to `noreply@policypilot.com` per `lib/email/send.ts`).
- `README.md` "Current Phase State" § refreshed to 2026-06-14.
- **Method:** edits authored then adversarially fact-checked against the live repo (workflow `wf_e9de7b80-459`). Verifier findings applied: the stale `c90dd44` SHA pin (decoupled — `main` has since advanced to `7410700` via the unrelated PR #43 CLAUDE.md context-diet merge; `c90dd44` is now only the branch's merge-base, and no Phase 7 commit is on `main`); and two file-attribution overclaims (the `onConflictDoNothing` claim-before-send INSERT lives in `app/api/cron/reminders/route.ts`, not `lib/db/repositories/reminders.ts`; the `policy_assigned` emission lives in the admin bulk-assign action `app/(admin)/policies/[id]/actions.ts`, not `lib/policies/transitions.ts` — which emits only `policy_updated`).
- **Still carried doc-debt (NOT edited — frozen contract, ASK-FIRST):** `reference/SCHEMA.md` notifications block omits the live `org_id` column. Tracked in `backlog.md` Next-Micro-Batch to reconcile at Phase 7 ship / Phase 8; not touched here.
