# Consultant Delta — 2026-06-15 — Phase 8 (Validation, CSV-first slice) PLANNED

**Trigger:** Operator authorized Phase 8 scoped to the CSV-first slice (acceptance criterion #5) and directed the Claude/Opus GSD planning chain (ULTRATHINK + ULTRACODE). This session ran: pr-branch → spec → discuss(--power) → UAT intent → research → plan, plus a still-running adversarial red-team Workflow. Codex owns execution next (per `AGENTS.md`). **Not pushed; no PR; operator owns push/PR/merge.**

**Branch:** `gsd/phase-8-validation` cut off `main` `3df5223` (clean base). All Phase 8 planning docs + (later) code live here; `.planning/phases/**` never committed to `main`.

**Planning artifacts authored (on-branch):**
- `08-SPEC.md` — 7 locked requirements (R8-1..R8-7), ambiguity 0.10. WHAT = `GET /api/reports/acknowledgments?format=json|csv` (admin-only, org-scoped) + read-only ack-compliance query + hand-rolled RFC-4180 CSV + Clerk identity enrichment + cumulative `verify:phase-8`. Maps to acceptance criterion **#5** only.
- `08-CONTEXT.md` + `08-DISCUSSION-LOG.md` — 13 HOW decisions (D-01..D-13). **Two operator-locked (2026-06-15):** CSV identity = **Clerk-enriched name+email** (`clerkClient` already in-stack → no new package); **fold the ASK-FIRST `reference/SCHEMA.md` notifications `org_id` doc-fix** into this phase.
- `08-UAT-INTENT.md` — operator hand-verify scenarios (valid CSV download + expected columns; org isolation; non-admin 403; bad params 4xx; CSV formula-injection safety).
- `08-RESEARCH.md` — confirmed (Context7 + repo): Clerk `getUserList({userId})` caps at **100 ids/call** (chunk 100); v7 returns `{data,totalCount}`; `await clerkClient()`; `primaryEmailAddress?.emailAddress`; CSV idiom = `new Response(str,{headers})` + `export const dynamic='force-dynamic'`.
- `08-PLAN.md` — **the Codex deliverable.** 11-file touch-list, 6 TDD tasks (CSV serializer → Clerk enrich → report query → route → TEST-DB integration gate → verify/CI wiring), a full **STRIDE `<threat_model>`** (T-8-01 cross-org leak … T-8-08 DoS … T-8-SC supply-chain), and the **R-007 accepted-risk note**.

**Scope (DEFERRED, not abandoned):** compliance dashboard donut / **recharts** (ASK-FIRST + ≥14-day rule when picked up), aggregate widgets, populated-org seed harness, **Stripe test-clock renewal run** (AC#6, code-complete), **beat-manual benchmark** (SC#5), evidence-capture for the 6 already-shipped/CI-gated criteria. No scope expansion without re-authorization.

**Guardrails honored:** no schema/migration change (journal stays **15 / 0014**); no new runtime package (CSV hand-rolled); `org_id`+RLS in every query + a two-org negative test (Org A export = 0 Org B rows, incl. Org-B-`policyId`-under-Org-A = 0); admin-only (`requireAdminFromCtx` → 403); `withOrgScope` not raw `db` (no `check-db-imports` allow-list change); `tsc --noEmit` no `any`; `verify:phase-8` strictly **chains `verify:phase-7`** (never weaker) + `verify-phase-8.yml` mirrors `verify-phase-7.yml`. Planning docs hand-authored (gsd-sdk STATE/ROADMAP finalizers mangle on this install per [[gsd-tooling-quirks]]).

**Risk register touch:**
- **R-007** (beat-manual, Open, score 15 ≥ escalation threshold): per the Escalation Rule, carried as an **accepted-risk note in `08-PLAN.md`** — the CSV ack-export is a *partial enabler* of the audit-trail value prop; the full side-by-side benchmark (SC#5) is DEFERRED to the evidence-capture leg. Not silently dropped.
- **R-009** (consultant staleness) — exercised this session: the 5 consultant files + README reconciled (Phase 7 SHIPPED `8b7019d`, main `3df5223`, R-006 → Shipped, R-018 → shipped-but-open, Phase 8 in progress, backlog rank-6/7 → Shipped, rank-8 CSV → In progress / dashboard Deferred).
- R-006 → Shipped/monitor; R-018 → Open/accepted (live-send SPF/DKIM/DMARC + RESEND_FROM_EMAIL + real send still operator-gated). No new system risk introduced by this planning.

**Keep-current doc folds done this session:**
- `reference/SCHEMA.md` — notifications block `org_id` added (operator-approved ASK-FIRST frozen-contract fix). **→ closes the long-standing notifications-`org_id` doc-debt** (backlog rank-7 / micro-batch note that still cite this as open are now superseded — flip to resolved on next consultant touch).
- `docs/runbooks/deploy-migrations.md` — journal count corrected 13 → **15** (`0000..0014`, adding `0013_review_decisions` [Phase 9] + `0014_reminder_sends` [Phase 7]); prose only (deploy verifier code already dynamic).
- `.planning/consultant/{working_context,risk_register,feature_inventory,backlog,system_map}.md` + `README.md` — status-flipped to current truth (see R-009 above).

**⚠ PENDING (must complete before Codex):** the adversarial red-team Workflow **`wf_1f196a0e-e8e`** (task id `w1v0ffwwq`) — 6 lenses (goal-backward, cross-org-leak, csv-injection, gate-weakening, coverage/auth, guardrail/scope) + independent verification — was still running at session-context-critical handoff. **Resume must collect it (`TaskOutput w1v0ffwwq`), fold any confirmed must-fixes into `08-PLAN.md`, then append a red-team addendum to this delta.** No findings folded yet.

**Next:** (resume session) fold the red-team must-fixes below into `08-PLAN.md`/`08-SPEC.md`/`08-CONTEXT.md` → final consultant micro-touch (flip the resolved SCHEMA.md doc-debt notes) → **hand `08-PLAN.md` to Codex** for execute → verify → validate → secure → ship (per `AGENTS.md`). Operator owns push/PR/merge. CI Phase-6 deadlock flake on the PR = re-run (merge signal = umbrella `Verify` + nested `Verify Phase 7` + new `Verify Phase 8`). Resume BOTH Supabase projects (TEST `qwtbbbjbxffioeeazxrw` + dev `kdoahaxhmaftxaiwbtdw`) before any verify run.

---

## Addendum — Red-team Workflow `wf_1f196a0e-e8e` COMPLETE (2026-06-15)

13 agents / 6 lenses + independent verification. **Full detail (108k chars):** `C:\Users\matth\AppData\Local\Temp\claude\C--Users-matth-Desktop-PolicyPilot\45e9daee-f54f-4f91-84b1-1a62ecab44d7\tasks\w1v0ffwwq.output`. **Verdict: the report QUERY design, org-scoping, scope boundaries, and guardrails (no schema change, no new package) are SOUND; defects are concentrated in the auth status mechanism, the integration-gate fidelity, and CSV test completeness — all fixable in-plan, NO scope/design change.** `08-PLAN.md` carries a PENDING-FOLD banner until these are applied.

**MUST-FIX before Codex executes:**
1. **AUTH 401/403 mechanism (HIGH — GBC-1 / F1).** The plan's "auth gates OUTSIDE the try (D-37) → 401/403 via Next error boundary" is WRONG for Route Handlers: an uncaught throw in a Next.js route handler → **500**, not 401/403 (no API error boundary exists). FIX: read the GROUND TRUTH of how shipped routes actually return their status (`app/api/ai/consistency/route.ts` + its test) — likely an in-route `try/catch` discriminating `NotAuthenticatedError`→401 / `ForbiddenError`→403. Correct D-07/D-11 + Task 4, and make the route test assert real status codes (NOT `rejects.toThrow`).
2. **Integration gate fidelity (HIGH — GBC-2 / XORG-1 / F2 / F3 / XORG-2).** Task 5 is incoherent: it cites a nonexistent `scripts/check-employee-portal.ts` (real = `check-employee-portal.test.ts`, a **vitest**) and can't both "mirror `check-rls.ts` (raw-SQL/BYPASSRLS)" AND "drive the real `Reports.listAckComplianceForOrg` via `withOrgScope`." As written it risks proving isolation by app-predicates alone (BYPASSRLS) → false RLS confidence. FIX: model `check-reports` on the **vitest** `check-employee-portal.test.ts` — drive the REAL `Reports` query through a REAL `SET LOCAL ROLE authenticated` + `set_config` jwt-claims session (NOT BYPASSRLS), add a positive control + RLS-firing assertion. Wire `check:reports` via the vitest runner with `--env-file=.env.local` (DATABASE_URL_TEST), NOT bare `tsx`.
3. **CSV guard+quote COMBINATION untested (HIGH — CSV-1).** Task 1 tests the formula guard and RFC-4180 quoting only in ISOLATION; add a RED test asserting the COMBINATION/ordering: `=a,b` → `"'=a,b"` (guard-then-quote). Primary T-8-04 vector.
4. **Test-glob mismatch (HIGH — VG-1).** SPEC R8-7 text names a `lib/db/repositories/reports` test glob (no planned test there → RED build / unplanned file); PLAN uses `lib/reports`. PIN both to `pnpm run test -- --run lib/reports` + `--run app/api/reports`. (`reports.ts` repo has NO unit test — DB-truth via `check:reports`; that's correct.)

**FOLD (MED/LOW):**
5. **CSV-2/3/4 (MED):** leading-whitespace-before-trigger bypass of the first-char guard; define the null/number → string cell-coercion contract (unacked rows have null `acknowledgedAt`/`ipAddress` — must not throw or emit `'null'`); prepend a UTF-8 **BOM** so Excel-on-Windows doesn't mojibake non-ASCII names/titles. Tighten D-09 + Task 1.
6. **check-artifacts wiring (MED — VG-2):** `check-artifacts.ts` has no auto-discovery — the new Phase-8 assertions MUST be hand-spread into `main()`'s `all` array or they silently never run. State this in Task 6.
7. **check:reports env (MED — VG-3):** the `check:reports` script MUST pass `--env-file=.env.local` and MUST NOT skip-on-missing-env (else vacuous pass on the isolation gate).
8. **Dedup/assignedAt (MED):** naive `selectDistinct` + `assignedAt` double-counts a dual-assigned user. Make dedup concrete (MIN(assignedAt) grouped, or drop `assignedAt` from the distinct set); the integration test already asserts exactly one row.
9. **departments JOIN org_id (LOW — XORG-3):** add `and(eq(departments.orgId, s.orgId), eq(departments.id, users.departmentId))` to the departments LEFT JOIN.

**REFUTED / clean (no action):** the "empty test glob → vacuous green" worry — empirically REFUTED (vitest 1.6.1 exits 1 on zero-match `--run`; fails CLOSED). The cross-org QUERY org-scoping (mirrors proven `reminders.ts`/`policies.ts` JOINs), the D-06 Clerk-id-set scoping at the helper boundary, middleware non-bypass (T-8-07), and the no-schema/no-new-package guardrails — all SOUND.
